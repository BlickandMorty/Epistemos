# PROGRESS.md — enterprise hardening run (non-Goose scope)

> LOOP ITERATION GUIDE — READ FIRST ON EVERY /loop FIRE: this run is in progress. Do NOT restart Phase 0. Read HARDENING_AUDIT.md, this file, LESSONS.md, DECISIONS.md (esp. D-009); continue from "Next actions". Background builds/subagents wake the session — fold results in first.
>
> **SCOPE (updated 2026-07-02, owner interrupt → D-009):** OUT OF SCOPE / being REMOVED: (a) Goose lane (Epistemos/Goose/, ActGoose, goosed, ACP — another lane owns it), (b) **Omega/computer-use** (Epistemos/Omega/**, omega-ax, omega-mcp — REMOVAL TARGET), (c) **local models / MLX-LLM** (LocalModel*, MLXInferenceService, LocalAgent*, ConfidenceRouter, SSMStateService, Mamba2*, mlx-swift pkgs, GGUF/llama research — REMOVAL TARGET). Do NOT harden (b)/(c) — they're being deleted; a scoping agent is mapping a phased removal plan for owner sign-off BEFORE any deletion. KEEP + harden: meetings (Apple SpeechAnalyzer), Kokoro voice/TTS (kept), arXiv, browser, notes, editor, graph, CLOUD agent + cloud LLM transport. Never `git add -A`; never touch dirty Goose files; the shared tree has a concurrent lane that reverts edits to agent/Omega files (it reverted my CONC-2/CONC-3) — sequence around it.

## Phase status (updated 2026-07-02, all 7 sweeps in)

- Phase 0 (Ground truth): DOCS COMPLETE — ARCHITECTURE_BASELINE.md ✅, THREAT_MODEL.md (B1–B8) ✅, PLAN_AUDIT.md ✅, HARDENING_AUDIT.md holds all findings (SEC/WEB/RUST/FEAT/PERF/CONC/MEET/PLAN). Debug build GREEN (10 warnings). Release/AppStore rebuild after Float16 fix STILL RUNNING (background bz5tykyb3 — universal -O build, zero errors so far). Full Swift test suite + Rust baseline + manual smoke = PENDING (blocked on toolchain being free). Phase-0 CHECKPOINT presented in-session.
- Phase 1 (forensic Plan 1–3): DONE → PLAN_AUDIT.md. Plan 1 = Goose (out of scope, retired by owner OpenChamber pivot). Plan 2 = editor/HTMLWorkspace/notes (code VERIFIED, in-app proof layer MISSING — 5 isLive caps false). Plan 3 = Kokoro+browser-use (most defensible; 1 REGRESSION = Float16, fixed F-0003).
- Phase 2 (threat model): DONE → THREAT_MODEL.md, boundary IDs B1–B8 in use across the findings table.
### ACTIVE TASK (2026-07-02): REMOVE Omega + local-models + Work lane (owner signed off — proceed in stages, build-green). Plan = REMOVAL_PLAN.md. Work/OpenCode lane = REMOVE too. Stages: (1) unwind references in KEPT files [AppBootstrap wiring, StreamingDelegate computer-use/SSM, bridges, InferenceState→cloud-only, Settings/RootView entry points] keeping build green; (2) delete Omega/** + Work/** + MLX files + computer-use bridges; (3) remove vmlx-swift+GGUF packages + omega crate links + build scripts from project.yml [both targets] + xcodegen; (4) drop entitlements apple-events/accessibility/unsigned-exec-mem; (5) delete orphaned tests + full verify + strings sweep. NEVER git add -A. Watch concurrent lane. Full triangulated plan + phases + KEEP carve-outs (omega-mcp/MCPBridge tool-bus, allow-jit deferred, Goose shares the tool protocol → stub not protocol-delete) in REMOVAL_PLAN.md. EXECUTION STATUS (updated 2026-07-03): **REMOVAL COMPLETE FOR APP TARGET — `xcodebuild build -scheme Epistemos` = BUILD SUCCEEDED, 0 errors.** All phases executed + verified: Phase 1 Omega/computer-use GONE (Swift + omega-ax crate + AXorcist + apple-events/accessibility entitlements + Info.plist strings). Phase 2 MLX/local-models GONE (vmlx-swift+GGUFRuntimeBridge pkgs removed from project.yml + LocalPackages ~360K lines; unsigned-executable-memory entitlement dropped; SSM/ConstrainedDecoding/Phase5Bridge deleted; InferenceState ~6000→3433, TriageService 2529→1437, LLMService/PipelineService/AppBootstrap/ACC/OverseerProtocol stripped cloud-only via 5-mapper plan + 3 implementer subagents + parent). **SOURCE GATE PASSES**: 0 `.localMLX` producers survive (cloud can't silent-fail to no-model). Tombstones KEPT for exhaustiveness: ChatModelSelection.localMLX, LLMProviderType.localGGUF/.localMLX, BackendRuntimeKind{.gguf,.mlx}(UniFFI mirror—Rust cut deferred). **REMAINING:** (1) TEST TARGET reconcile — subagent a4fb71f5 iterating build-for-testing (deleted KIVIKVCacheRuntimeTests[MLX import]; ~16 files ref deleted symbols); (2) RUNTIME GATE test (effectiveChatSurfaceSelection(.fast)→.cloud w/ configured provider) — add after test target green; (3) optional dead-code cleanup (TriageService appleIntelligenceOnlyStream/CloudRoutingError/steeringHintsJSON). AUTHORITATIVE detail: **SURGERY_PLAN.md** (repo root) + REMOVAL_PLAN.md. Work/OpenCode lane removal = still deferred (separate large lane, not started; app green without touching it).

## ✅ DEEP HARDENING WAVE (2026-07-03) — removal DONE, Work KEPT, hardening kept surfaces
**Cloud-only removal: COMPLETE + FULLY VERIFIED** (app + test targets green; runtime gate CloudOnlyRuntimeGateTests 3/3 pass; CloudProviderAuthServiceTests 27/27; source gate 0 `.localMLX`). **Work/OpenCode lane: KEEP** — owner decision 2026-07-03 "dont remove work anymore" (Goose transitively depends on 16 of its 48 files via WorkSPA web-surface; can't remove without breaking off-limits Goose). See memory work-lane-is-goose-entangled-keep.
**Then pivoted to DEEP HARDENING of kept surfaces (owner: "deeply harden beyond deleting dead code"). Findings fixed+build-verified this wave (all in HARDENING_AUDIT.md w/ evidence):**
- MEET-1 (CRITICAL) meeting transcript 10k silent-truncation → 2M cap + visible marker + 2 regression tests. VERIFIED (build+tests).
- MEET-4 (HIGH) ←Home destroyed unsaved transcript → HomeEmbeddedLeaveGuardKey confirm dialog. VERIFIED (build).
- SEC-4 12 user-query logs `.public`→`.private`. VERIFIED (build).
- BUP-1 (HIGH) browser-use orphan procs + 0600 keyed env leak → SIGKILL escalation + app-quit registry + launch/stale env purge + regression test. VERIFIED (build + 25/25 suite).
- WEB-2 (MED) MarkEdit isInspectable → `#if DEBUG`. WEB-5 KaTeX escaping → jsStringLiteral. VERIFIED (build).
- BRW-1 (MED) downloads dead-ended → WKDownloadDelegate + save panel. BRW-2 mailto/tel → NSWorkspace handoff (tight allowlist). VERIFIED (build).
- CONC-2 (HIGH) unbounded agent stream → StreamingBufferPolicy + onTermination cancel. CONC-15 activeEmbeddingLookup existential race → NSLock accessor. VERIFIED (build).
- SEC-3 (MED) CloudLLMClient urlSession `.shared` no-timeout → dedicated 120s/1800s session. VERIFIED (build).
**Re-classified by reading code:** SEC-7 (dev paths already `#if DEBUG`), MEET-5 (NSPanel concern moot — meeting is home PAGE not panel; residual = feature-switch loss documented), MEET-6 (MOOT — meeting panel unreachable, 0 `show(.meetingNote)` callers), CONC-15 storage layer (already NSLock-serialized).
**MORE fixed+verified (later in wave):** CONC-13 (SyntaxCoreService FFI unlocked ptr → NSLock on all 7 accessors), MEET-7 (meeting title in trace → SHA256 hash, + regression test 52/52), BUP-2 (per-view browser-use supervisor → `.shared` singleton, one server on :7788), WEB-1 (Epdoc bridge webview no nav policy → decidePolicyFor allowlist about:+editor scheme), WEB-3 (MarkEdit CORS disabled by default → enforce CORS). **TOTAL: 17 findings fixed+build-verified this wave** (2 with regression tests: BUP-1, MEET-7).
**Re-classified/documented (read the code):** SEC-5 (accessibility/apple-events entitlements already removed; residual inert), SEC-6 (widgets = future-ship task), SEC-7 (dev paths already #if DEBUG), MEET-5 (NSPanel concern moot), MEET-6 (MOOT — panel unreachable), CONC-15 storage (already NSLock-serialized).
**Deferred w/ written analysis:** ARX-1 (needs deterministic path or indexed arxiv_id), WEB-4 (CSP — needs validated npm rebuild + editor load-test; blind CSP risks breaking the editor).
**Remaining OPEN (lower-value / needs owner or hardware / larger):** MEET-8 (audio-route restart — needs hardware verify), A11Y-1 (graph VoiceOver — large build-out), BRW-3 (drawsBackground private KVC — no clean public API), BRW-4 (in-session cookie clear — moderate UI), ARX-2 (arXiv etiquette — serial queue), SEC-1 (disable-library-validation — risky, needs dylib-signing audit; memory says KEEP), SEC-8 (MAS app group TEAMID — owner/App-Store-Connect), CONC-4 (approval continuation), RUST-6/7/8 (scope-ambiguous "Rust core"), PERF-* (owner-deferred). High-value tractable Swift findings are DONE.

### Regression sweep (2026-07-03, post-wave)
Ran behavioral test suites for every touched fix area to confirm no regressions:
- **1 REGRESSION CAUGHT + FIXED:** BRW-1's `download(_:didFailWithError:)` used `error.localizedDescription` (in code AND its comment) → tripped BrowserPlan3Tests:79 (browser must use privacy-safe `domain=code=` errors, not raw descriptions). Fixed → BrowserPlan3Tests now 6/7 (only pre-existing stale-mirror :201 remains).
- **CLEAN (no regressions):** AgentQueryEngine (CONC-2) + BrowserUseWebUIView (BUP-2) + EpdocEditorToolbar (WEB-1) 18/18; BlockEmbeddings (CONC-15) + QueryRuntime (SEC-4) 55/55; EpdocCopilotSurface + EpdocSlashMenuView pass; MeetingNoteCaptureService — only the documented F-STALE-1 (:629/635 stale-mirror, unchanged count) fails, my MEET-1/4 tests pass. Earlier: MEET-7 TextCapturePipeline 52/52, BUP-1 BrowserUse 25/25, CloudProviderAuth 27/27.
- Net: **all documented failures are the known pre-existing stale-mirror set; ZERO new behavioral regressions remain after the BRW-1 fix.** Lesson: source-guard tests do naive substring matching — even a COMMENT containing a forbidden string trips them.

### Fixes this session (verified unless noted)
- F-0003 (Float16 x86_64 Release blocker, LocalPackages/KokoroPipeline) — **VERIFIED** (AppStore Release build GREEN).
- MEET-2 (meeting false-"Saved" data-loss, MeetingNoteCaptureService.finalize + regression test) — **VERIFIED** (test passed; meeting suite 19/20, the 1 fail = pre-existing stale mirror F-STALE-1).
- MEET-3/SEC-2 (mic entitlement device.audio-input added to Epistemos-AppStore.entitlements + Epistemos.entitlements) — **APPLIED** (codesign -d verify pending on an AppStore build).
- RUST-4 (epistemos-core provider_client.rs connect_timeout(10s)+read_timeout(120s)) — **VERIFIED** (cargo check exit 0).
- RUST-5 (agent_core claude.rs ClaudeAuth + credential_pool.rs manual redacting Debug) — **VERIFIED** (cargo check exit 0).
- CONC-5 (EpistemosSpeechAnalyzer.swift resultsCont.onTermination→stopInternal, mic-stops-on-cancel) — APPLIED; compile-verify build detached (scratchpad/conc5-build.log).

### Full-suite ground truth (Phase 0 gate DONE)
2044 pass / 27 fail (non-Goose). ALL 27 (+F-STALE-1) = stale source-mirror/doc-parity tests + 2 out-of-scope Goose. ZERO session regressions, ZERO real defects. Forensic "real suspects" (HTML-Workspace, NoteSavingStress) now PASS. Classification in HARDENING_AUDIT.md. Rule-6: red = brittle-test debt, not stop-the-line.

### NEXT (toolchain-aware; ~5-6min bg-task kill limit — use nohup for full builds, cargo check per-crate is fast)
1. Confirm CONC-5 build (conc5-build.log) BUILD SUCCEEDED.
2. RUST-8 (vault symlink canonicalize) — careful pass: read agent_core file_ops.rs:184-199 pattern, apply nearest-existing-ancestor canonicalize + symlink test, cargo test.
3. Phase 3: add SWIFT_APPROACHABLE_CONCURRENCY per target in project.yml (xcodegen regen); fix CONC-1 (Screen2AXFusion double-resume crash), CONC-2/3 (unbounded agent streams), CONC-4 (approval continuation hang), CONC-13/15 (data races); TSan run. CHECKPOINT at Phase 3 end.
4. Phase 5: WEB-1 (Epdoc decidePolicyFor), WEB-2 (isInspectable #if DEBUG at our MarkEdit embedding, not vendored line), WEB-3 (CORS), WEB-4 (CSP on 3 bundled UI docs — fix at js-editor/CoreEditor SOURCE), WEB-5 (KaTeX jsStringLiteral).
5. Phase 4 remainder: SEC-1/3/4/5, MEET-7; Phase 9: MEET-1 (transcript cap — product-magnitude call, update 4 coupled tests w/ justification), MEET-4/5/6, browser downloads/handoff. Phase 7: F-STALE-1 + stale-mirror batch reconcile + dead .meetingNote window removal.
- Phases 3–12: NOT STARTED. Phase 6 SUSPENDED for goose-owned code (non-goose Rust audited: RUST findings; safe fixes RUST-4/5/8 queued for cargo-verify once toolchain free). Phase 3 baseline: project.yml is Swift 6.0 + MainActor-default + complete concurrency but LACKS SWIFT_APPROACHABLE_CONCURRENCY (the 6.2 target).

## Next actions (in order — most are toolchain-gated)

1. WHEN release build frees DerivedData: record Release gate (exit + warnings) → HARDENING_AUDIT. Then `xcodebuild test` the meeting suite (MeetingNoteCaptureServiceTests) to VERIFY MEET-2 + full app link to verify F-0003. Do NOT run a competing build while bz5tykyb3 holds the lock (TreeSitter/DerivedData race — LESSONS).
2. Full Swift test suite → get ground truth on the reported 62-failure red state. Classify: Goose-live = out-of-scope/environment; stale window-regime mirrors (MeetingNoteCaptureServiceTests:573-575 etc.) = fix-with-justification; NoteSavingStressTests/multiplePageConcurrentStress = REAL, investigate (stop-the-line).
3. Rust: cargo fmt/clippy/test for non-goose crates + apply RUST-4 (epistemos-core timeout — app-hang candidate), RUST-8 (vault symlink canonicalize), RUST-5 (secret Debug redaction); cargo-verify each.
4. Manual smoke pass once a runnable build exists (launch, graph, meeting, arXiv, browser).
5. Phase 3: add SWIFT_APPROACHABLE_CONCURRENCY per target in project.yml + re-verify zero diagnostics; fix CONC-1..5 (HIGH: double-resume crash, 2 unbounded agent streams, approval-continuation hang, mic-stays-on) + CONC-13/15 data races; TSan run. CHECKPOINT at end of Phase 3.
6. Phase 4: entitlements (SEC-1/2/5/8 + MEET-3 audio-input) + APP_REVIEW_NOTES.md + secret sweep (done) + network table. Phase 5: WEB-1..5. Phase 9: MEET-1 (transcript cap — product-decision on magnitude, update the 4 tests w/ justification), MEET-4/5/6, browser downloads/handoff.

## Evidence pointers

- HARDENING_AUDIT.md — findings table + commands log.
- scratchpad (session): /private/tmp/claude-501/-Users-jojo/6594a750-07ca-4662-a2b6-cf70567a762e/scratchpad/ — build logs live here before being summarized into repo docs.
- Prior lane evidence: /tmp/epi_*_green_20260702.xcresult (9 targeted suites, per NON_GOOSE_UI_DEEP_CLEAN handoff).
