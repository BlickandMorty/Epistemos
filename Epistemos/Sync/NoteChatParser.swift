import Foundation

// MARK: - NoteChatParser
// Stub — the v2 Note Chat uses simple --- dividers appended to NSTextStorage
// instead of HTML comment markers. No separate parser needed.

enum NoteChatParser {}
