import SwiftUI

// MARK: - Agent Runtime Settings (Retired)
// Agent settings now managed via main chat provider selection.

struct OmegaSettingsDetailView: View {
    var body: some View {
        Text("Agent settings are now part of the main chat configuration.")
            .foregroundStyle(.secondary)
            .padding()
    }
}
