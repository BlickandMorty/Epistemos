import SwiftUI

// MARK: - Confirmation Sheet (Stub)
// The Omega confirmation gate has been retired. Approvals now go through
// the Rust agent_core's permission flow via StreamingDelegate.

struct ConfirmationSheet: View {
    var body: some View {
        EmptyView()
    }
}
