import SwiftUI

// MARK: - Confirmation Sheet
// Retired compatibility view. Approvals now go through
// the Rust agent_core's permission flow via StreamingDelegate.

struct ConfirmationSheet: View {
    var body: some View {
        EmptyView()
    }
}
