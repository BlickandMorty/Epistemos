# REMOVAL_PLAN.md — delete Omega/computer-use + local-model (MLX) lanes → cloud-only

> Owner signed off (D-009/D-010): remove Omega/computer-use + local-model MLX + Work-lane's Omega coupling; app is CLOUD-ONLY inference. Proceed in build-green stages. Triangulated by 4 map passes (2026-07-02). KEEP: Apple FoundationModels, SpeechAnalyzer voice, NL embeddings, Kokoro TTS, arXiv, browser, notes, editor, graph, cloud agent.

## Definitive KEEP carve-outs (do NOT delete — they live under Omega/ but power kept features)
- **omega-mcp crate + MCPBridge.swift + MCPRegistryClient.swift + MCPUrlServerDirectory.swift + BestOfPreset.swift** = the agent tool-dispatch bus. Linked into BOTH targets (mas-sandbox), consumed by KEPT VaultMCPHost.swift, Work/OpenCode, Settings→Skills (ExtensionsDetailView), AgentCommandCenterState, StreamingDelegate cloud-agent tool calls. KEEP (optionally rename off "omega" later).
- **ReasoningLoopService.swift** (Omega/Inference) = wraps CLOUD TriageService (THINK→CRITIQUE→ACT→REFINE); KEEP (re-key its omega.enableReasoningLoop toggle). Actively edited by concurrent lane — sequence around it.
- **ToolCallParser.swift** (used by kept Engine/Extensions.swift), **ToolSchemaGrammar/ReasoningTraceLogger** — KEEP unless proven orphan.
- **allow-jit entitlement: KEEP for now** — the two maps CONFLICT (one says WKWebView needs it, one says only MLX did). WKWebView JS runs out-of-process (WebContent has its own entitlements), so it's *likely* MLX-only and removable — but DEFER until verified with a real build test; not worth breaking JIT on a guess. **disable-library-validation: KEEP** (kept Rust dylibs). Only **allow-unsigned-executable-memory** is confidently MLX-removable.

## GOOSE-PROTOCOL nuance (critical)
manageSsmState/generateConstrained/perceiveApp/etc are on a shared tool PROTOCOL (StreamingDelegate.swift:28-42) that GooseInProcessACPServer.swift ALSO implements (as canned "unavailable" stubs). Goose is OFF-LIMITS. So: DO NOT change the protocol. Instead GUT StreamingDelegate's implementations to return the same "unavailable" JSON stub (matching Goose's pattern) and delete the backing MLX/Omega services. Protocol unchanged → Goose untouched.

## Blast radius (measured)
- Vendored LOC removed: ~368K Swift (vmlx-swift 302K + mlx-swift-lm 56K orphan + LocalLLMClient 9.5K orphan + GGUFRuntimeBridge) + omega-ax 1,158 Rust.
- App files deleted: ~18-22 (Omega computer-use ~14 + non-Omega computer-use bridges 4 + SSM/local ~4). ~5-7K app LOC.
- App files surgically EDITED (kept): InferenceState.swift (6021), TriageService.swift (2598), LLMService, AgentCommandCenterState, PipelineService, Phase5Bridge, StreamingDelegate, AppBootstrap (14 Omega sites).
- SPM deps removed: AXorcist, vmlx-swift (9 products), GGUFRuntimeBridge; frameworks: libomega_ax.dylib, AXorcist.framework, llama.framework.
- Entitlements: apple-events, accessibility mach-lookup, unsigned-executable-memory (+ allow-jit deferred) + 3 Info.plist strings.
- Tests to fix: ~36 files referencing Omega/MLX symbols + gate-asserting hardening tests.

## PHASES (each ends build-green; ▸=current)

### ▸ Phase 0 — Dead-code sweep (near-zero risk, not in Xcode build)
Delete on-disk orphans NOT in project.yml + zero live consumers: `LocalPackages/mlx-swift-lm`, `LocalPackages/LocalLLMClient` (neither declared in project.yml), `Epistemos/Engine/Mamba2ForwardPass.swift` + `EpistemosTests/Mamba2MetalRuntimeTests.swift` (test-only consumer). VERIFY-BEFORE-DELETE (agent conflict): `Engine/LocalModelInfrastructure.swift` — one map says orphan, one says its PreparedModel*/PreparedRetrieval* feeds graph/search; grep consumers first. Verify: build unaffected (nothing referenced them).

### Phase 1 — Omega computer-use removal (Pro target; MAS already ships without it → low-med risk)
Delete: Omega/Vision/* (7), Omega/Agents/OmegaAgent, Omega/Inference/DeviceAgentService, Omega/OmegaPermissions, Omega/OmegaDurationExtension, Omega/Inference/{DualBrainRouter,HybridRouter,HardwareTierManager}, Bridge/ComputerUseBridge.swift, Bridge/Phase4Bridge.swift, AppStore/AppStoreComputerUseStubs.swift, State/AmbientCaptureService.swift, Intents/Custom/OmegaIntent (already stub), omega-ax crate + build-omega-ax.sh. Rewire: remove #if EPISTEMOS_APP_STORE computer-use conditionals wholesale, sever 14 AppBootstrap sites + StreamingDelegate:736 (gut perceiveApp/interactWithApp/startScreenWatch to stub), drop -lomega_ax + omega_axFFI + AXorcist package + Scrub-phase omega/AXorcist lines; remove OMEGA entitlements + 3 plist strings; xcodegen. Fix gate tests.

### Phase 2 — Local-models → cloud-only (delicate; own revert point)
Delete: Vault/SSMStateService.swift, Engine/SSMMemorySidecar.swift, Engine/SSMRuntimeProfile.swift, Omega/Inference/ConstrainedDecodingService.swift; LocalPackages/vmlx-swift, LocalPackages/GGUFRuntimeBridge. Rewire cloud-only: strip LocalTextModelID enum + LocalRoutingMode + local gates from InferenceState (keep CloudModelProvider/CloudTextModelID/AIProviderSelection/hasConfiguredCloudAccess); flip TriageService always-cloud; clean local branches in LLMService/AgentCommandCenterState/PipelineService; gut SSM/ConstrainedDecoding routes in Phase5Bridge + StreamingDelegate:788,804 to "unavailable" stubs; drop 9 MLX products both targets + vmlx/GGUF packages + Patch-MLX-Metal preAction; remove allow-unsigned-executable-memory (+ allow-jit iff build-verified); xcodegen. Fix ~36 MLX/local test files.

### Phase 3 — namespace cleanup (optional): relocate kept MCP/Reasoning files out of Omega/; rename omega-mcp→epistemos-mcp.

## Open questions already answered by owner: Work lane = REMOVE (but its omega_mcp dep = KEEP the crate; remove only Work's own files + Omega-tool coupling). omega-mcp = KEEP (confirmed load-bearing). Deferred to owner if hit: Omega/Knowledge/* + Omega/Safety/* + Omega/Research* (zero-consumer, don't block Phases 0-2 — default DELETE unless a kept feature uses them; verify DeepResearch vs Omega/Research* before deleting).

## Shared-tree hazard: concurrent cursor-work worktree + this session both touch the 2 entitlements files + my meeting/cloud/Kokoro fixes are the "uncommitted concurrent edits" the maps saw. Rebase entitlement edits; land Phase 0-1 before AppBootstrap churns. NEVER git add -A.

## EXECUTION STATUS (2026-07-02)
- Phase 0: DONE (build-verify in flight, phase0-build.log). Deleted: LocalPackages/mlx-swift-lm + LocalPackages/LocalLLMClient (1985 files, not in project.yml — MLX vendored orphans), Epistemos/Engine/Mamba2ForwardPass.swift + EpistemosTests/Mamba2MetalRuntimeTests.swift (test-only). CORRECTION: LocalModelInfrastructure.swift is NOT orphan — PreparedModel*/PreparedRetrieval* used by 7 kept graph/search files (GraphState/EmbeddingService/QueryEngine/QueryRuntime/AdvertisedModelStore/InferenceState/AppBootstrap) → Phase 2 surgical reduce, NOT delete.
- Phase 1 (Omega computer-use): IN PROGRESS. DONE: gutted StreamingDelegate perceive/interact/startScreenWatch to stubs (Goose-shared protocol intact); deleted Omega/Vision/* (7), Bridge/ComputerUseBridge, Bridge/Phase4Bridge, AppStore/AppStoreComputerUseStubs, State/AmbientCaptureService, Omega/OmegaPermissions, Omega/OmegaDurationExtension, Intents/Custom/OmegaIntent, Omega/Inference/{DeviceAgentService,HardwareTierManager,DualBrainRouter,HybridRouter}, Omega/Agents/OmegaAgent; removed AppBootstrap computer-use wiring (props+init+ambientCapture.start); stubbed Work/WorkNativeToolExecutor.executeComputerAction. Omega/Inference left = 5 KEPT (ConstrainedDecoding[Phase2], ReasoningLoop, ReasoningTraceLogger, ToolCallParser, ToolSchemaGrammar). Build phase1b-build.log verifying (1st build had only 1 ref error: WorkNativeToolExecutor ComputerUseBridge — fixed). REMAINING Phase 1: project.yml drop -lomega_ax + omega_axFFI + build-omega-ax.sh + AXorcist package + Scrub-phase omega/AXorcist lines (both targets); delete omega-ax crate + build-omega-ax.sh; remove OMEGA entitlements (apple-events, accessibility mach-lookup) + 3 Info.plist strings; xcodegen; fix gate tests (AppStoreHardeningTests/ProductionHardeningTests/RuntimeValidationTests/ProjectInclusionTests + delete ComputerUse*/Phase4Bridge* tests). UPDATE: Omega SWIFT removal COMPLETE (all computer-use + Knowledge/Orchestrator/Safety/Research/Views-Omega deleted; refs unwound in StreamingDelegate[5 tool methods stubbed], AppBootstrap[props+init+registerAgents+Knowledge], AppEnvironment[3 injections], LandingView[env], UtilityWindowManager[.omega case+arms+routing], WorkNativeToolExecutor[computer-action stub], OrchestratorState[dead props]). Omega/ = 9 KEPT files (MCPBridge/MCPRegistryClient/MCPUrlServerDirectory/BestOfPreset + Inference/{ConstrainedDecoding[Phase2],ReasoningLoop,ReasoningTraceLogger,ToolCallParser,ToolSchemaGrammar}). Build phase1e-build.log verifying. 2 benign stale strings left (WorkNativeToolExecutor.swift:15 comment, StructureRegistry.swift:272 summary) — Phase 3 cosmetic. REMAINING Phase 1: project.yml drop -lomega_ax+omega_axFFI+build-omega-ax.sh+AXorcist pkg+Scrub-phase omega/AXorcist (both targets), delete omega-ax crate+build-omega-ax.sh, remove OMEGA entitlements(apple-events, accessibility mach-lookup)+3 Info.plist strings, xcodegen, fix gate tests. Omega SWIFT removal VERIFIED GREEN (phase1e BUILD SUCCEEDED). project.yml config DONE: removed -lomega_ax + omega_axFFI includes (both targets) + build-omega-ax.sh + AXorcist package dep+def + AppStore omega_ax.swift exclude + Scrub-Pro libomega_ax/AXorcist.framework lines + test copy_tree/copy_file; deleted omega-ax crate + build-omega-ax.sh + stale build-rust/swift-bindings/omega_ax* bindings; xcodegen regenerated OK. AXorcist verified used by ZERO non-Goose code (false matches were maxPackage*). Kept llama.framework scrub for Phase 2. Build phase1f verifying (project.yml + regen). NEXT TICK: read phase1f-build.log; if green → OMEGA entitlements cleanup (Epistemos.entitlements: drop automation.apple-events + temporary-exception.mach-lookup accessibility.api; Epistemos-Info.plist: drop NSAccessibility/NSScreenCapture/NSAppleEvents usage strings — CAREFUL: entitlements also hold my MEET-3 mic add + concurrent-lane edits) + gate tests (AppStoreHardeningTests/ProductionHardeningTests/RuntimeValidationTests/ProjectInclusionTests + delete ComputerUse*/Phase4Bridge*/VisualVerifyLoop*/ReasoningLoop* tests). OMEGA ENTITLEMENT/PLIST CLEANUP DONE (build phase1g verifying bundle): Epistemos.entitlements removed automation.apple-events + temporary-exception.mach-lookup(accessibility.api) — KEPT mic(device.audio-input)/allow-jit/disable-library-validation/network/files/app-groups + allow-unsigned-executable-memory(Phase2). Epistemos-Info.plist removed NSAccessibility/NSScreenCapture/NSAppleEvents usage strings + fixed mic string ("...transcribe meetings on-device", was "using local models"); plutil OK. MAS plist already clean. Used sed (Edit failed on plist tabs). PHASE 1 ~COMPLETE pending phase1g + gate-test reconciliation (delete ComputerUse*/Phase4Bridge*/VisualVerifyLoop*/ReasoningLoop*/AmbientCapture* tests + fix AppStoreHardeningTests/ProductionHardeningTests/RuntimeValidationTests/ProjectInclusionTests that assert omega_ax/AXorcist presence — these are the ~gate tests; do in the test-build pass).
PHASE 2 IN PROGRESS. DONE (build phase2a verifying): gutted StreamingDelegate manageSsmState+generateConstrained to stubs; deleted Vault/SSMStateService.swift (LAST MLX importer) + Engine/SSMMemorySidecar.swift + Engine/SSMRuntimeProfile.swift + Bridge/Phase5Bridge.swift + Omega/Inference/ConstrainedDecodingService.swift; unwound AppBootstrap(constrainedDecoding decl + ssmStateService prop+init + PipelineService arg) + AppEnvironment(.environment constrainedDecoding) + PipelineService(property+param+assignment). **ZERO import MLX remain in app** (verified). REMAINING PHASE 2: (1) project.yml remove vmlx-swift pkg + 9 MLX products [both targets 122-139/272-289 approx] + GGUFRuntimeBridge pkg + Patch-MLX-Metal preAction + llama.framework scrub line; (2) delete LocalPackages/vmlx-swift + LocalPackages/GGUFRuntimeBridge; (3) InferenceState.swift cloud-only surgery: delete LocalTextModelID enum + Local* enums + local branches, KEEP CloudModelProvider/CloudTextModelID/AIProviderSelection/hasConfiguredCloudAccess; (4) TriageService cloud-only; clean LLMService/AgentCommandCenterState/PipelineService local branches (localModelClient/LocalConfigurableLLMClient/LocalInferenceRoutingError); (5) State/LocalModelResolution.swift delete; LocalModelInfrastructure reduce (keep PreparedModel* for graph/search); BackendRuntimeContract/RuntimeExecutor/TimeoutUtility strip .mlx/.gguf; (6) remove allow-unsigned-executable-memory entitlement (Epistemos+Debug); xcodegen; (7) reconcile ALL tests (Phase 1 gate + Phase 2 MLX). phase2a GREEN. vmlx-swift/GGUF PACKAGE REMOVAL DONE (build phase2b verifying): project.yml removed all 9 vmlx-swift MLX products (both targets, via sed) + GGUFRuntimeBridge dep + both package defs + 2 Patch-MLX-Metal scheme preActions + llama.framework scrub; deleted LocalPackages/vmlx-swift + LocalPackages/GGUFRuntimeBridge (~360K LOC) + scripts/patch_mlx_metal_warnings.sh; xcodegen regenerated OK. project.yml functionally clean (1 benign SwiftTerm comment mentions vmlx-swift). REMAINING PHASE 2 (app-internal cloud-only surgery): InferenceState.swift delete LocalTextModelID enum + Local* enums + local branches (KEEP CloudModelProvider/CloudTextModelID/AIProviderSelection/hasConfiguredCloudAccess); TriageService cloud-only (delete LocalInferenceRoutingError + LocalConfigurableLLMClient local routing, keep cloud); LLMService (LocalConfigurableLLMClient protocol@40 + local branches); PipelineService localModelClient; State/LocalModelResolution.swift delete; LocalModelInfrastructure reduce (keep PreparedModel*); BackendRuntimeContract/RuntimeExecutor/TimeoutUtility strip .mlx/.gguf; NodeInspectorState LocalInferenceRoutingError catch. Then remove allow-unsigned-executable-memory entitlement (Epistemos+Debug); xcodegen; reconcile ALL tests (Phase 1 gate + Phase 2 MLX). phase2b GREEN (vmlx-swift/GGUF removal verified). UNSIGNED-EXEC-MEM ENTITLEMENT REMOVED: dropped com.apple.security.cs.allow-unsigned-executable-memory from Epistemos.entitlements + Epistemos-Debug.entitlements (both plutil OK); GONE from all entitlement files; MAS never had it (proof safe); also fixed stale omega-ax comment in Debug entitlements. KEPT allow-jit (deferred/Metal). 
IN-FLIGHT: 5 read-only mappers producing the InferenceState/local-model cloud-only surgery plan (a46fdca=InferenceState core, a6e63=AppBootstrap/Overseer/AnswerPacket, abf66=AgentCommandCenter/NodeInspector/Pipeline, a95e7=BackendRuntimeContract/RuntimeExecutor/Timeout, a492c=LocalModelInfrastructure). DO NOT edit local-model files until maps land (blind edit = cloud-inference break risk). 
NOTE: repo CLAUDE.md is STALE (describes MLX/Omega/LocalAgent as live) — owner overrode it to cloud-only; ignore its local-model guidance (see memory epistemos-cloud-only-no-local-models).
MAPPERS: 4/5 DONE + consolidated into **SURGERY_PLAN.md** (repo root) — the authoritative Phase-2 surgery plan with locked cross-cutting decisions + map-file pointers (scratchpad/map-*.txt). Keystone InferenceState-core map (a46fdca) still finalizing.
LOCKED DECISIONS (see SURGERY_PLAN.md): (D-A) KEEP BackendRuntimeKind 3 cases — it's a UniFFI mirror of Rust RuntimeKind; dropping breaks FFI bridge; local already neutralized at policy layer; only delete init(localReasoningMode:); Rust cut DEFERRED. (D-B) RENAME LocalInferenceRoutingError→InferenceRoutingError (NOT delete — cloud throws it as generic fallback @TriageService:2204). (D-C) KEEP ChatModelSelection.localMLX (~30 sites, vestigial arms). (D-D) OverseerProtocol Option A (behavior-preserving, matches current cloud-only runtime). unsigned-exec-mem entitlement + StructureRegistry screen_element already removed+green (phase2c).
NEXT TICK: get keystone a46fdca map → append to SURGERY_PLAN.md → execute coordinated single sweep per SURGERY_PLAN.md EXECUTION STRATEGY (rename first, then delete catalog/enum/local-funcs, strip shared-func local branches keeping cloud fallbacks, strip RuntimeLane/BreakerDomain .mlx, fix tests, build compiler-guided). VERIFY cloud inference intact after green.
- Phase 2 (local→cloud): InferenceState/TriageService surgery + vmlx-swift removal + SSM. Delicate; own revert point.
- NEVER git add -A (stage only removal files; my meeting/cloud/Kokoro fixes + Goose lane also dirty).
