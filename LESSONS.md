# LESSONS.md — enterprise hardening run (one lesson per entry)

- The real repo is /Users/jojo/Downloads/Epistemos; /Users/jojo (cwd) and epistemos-site are decoys with similarly-named files. Always operate on absolute paths into Downloads/Epistemos.
- The owner's directive lives at "EPISTEMOS_HARDENING_DIRECTIVE (2).md" (browser duplicate-name artifact); the canonical-path copy was created 2026-07-02 by this run. Check both before assuming a doc is missing.
- Never edit Epistemos.xcodeproj directly — project.yml + xcodegen is the source of truth (repo CLAUDE.md). Per-target build-setting changes (Phase 3) go through project.yml.
- Two schemes matter: `Epistemos` (dev) and `Epistemos-AppStore` (MAS). Entitlements differ per config (3 .entitlements files).
- The worktree is shared with an active Goose lane (9 dirty files incl. RootView.swift) + a sibling cursor worktree. NEVER `git add -A`; stage explicit paths only; never revert working-tree changes you didn't make.
- Shared-DerivedData TreeSitter build race exists (prior memory): if a build fails with a TreeSitter/DerivedData race and no other build is running, retry in place before investigating.
- App-hosted live test suites have a known iso-DD test-host degradation mode (prior memory): classify such red runs as environment, prove features via targeted suites/scripts instead, and say so explicitly.
- Note-open freeze rule (prior handoff, commit 3e33eb271): body hydrate must stay synchronous at mount or notes blank; fix the freeze by measuring (spindump during the freeze), not by re-async-ing the seed.
- The app shell is SwiftUI-lifecycle (RootView, @Observable, SwiftData), not the AppKit shell the directive assumes — verify framework reality before applying AppKit-specific prescriptions; "no framework migration" cuts both directions.
- OWNER CORRECTION 2026-07-02 (D-009): OMEGA + LOCAL MODELS are OLD/unwanted — off-limits like Goose. The repo CLAUDE.md is STALE: it calls MLX "the current live local inference lane" and treats Omega as live; the owner says both are dead. Do NOT harden/revive/add code to Epistemos/Omega/**, MLX/mlx-swift, Epistemos/LocalAgent/**, or on-device LLM paths. App is CLOUD-ONLY for inference. I got burned editing Omega/Inference/ReasoningLoopService.swift (a CONC-3 concurrency fix) — reverted. Before touching ANY file, check its namespace/purpose against the exclusion set, not just against "Goose". AgentHarness/ status unclear (cloud agent vs old local agent) — confirm before touching. This unlocks a Phase-4 security win: drop MLX-justified allow-jit/unsigned-executable-memory entitlements.
- docs/APP_ISSUES_AUTO_FIX.md is the living runtime-issue tracker (3k lines) and the repo CLAUDE.md session protocol expects it read at session start; several "known" issues there are already Patched — check Status before re-fixing.
- CLAUDE.md hard rules that bite here: AsyncStream must use .bufferingNewest(256) never .unbounded; DispatchQueue.main.async (never .sync) in UniFFI callbacks; API keys via Keychain only; no print() in production paths; Swift Testing (@Test/#expect) for new tests.
- SOURCE-MIRROR TESTS are pervasive and brittle: many suites use loadMirroredSourceTextFile(...) + #expect(source.contains("literal string")) to pin implementation details (e.g. MeetingNoteCaptureServiceTests.swift:566-591 `sourceBoundaries`, :527-528, VoiceCodepackPlan3Tests, MeetingSTTCodepackPlan3Tests, BrowserPlan3Tests, +HTMLWorkspace/Landing parity mirrors). They break on legitimate refactors and encode the very strings a fix must change. Rule: when a real change breaks one, UPDATE the assertion with a written justification (operating rule 5) — do NOT rip the mechanism out wholesale (not this run's mandate), and do NOT contort code to satisfy a stale literal. The thermo-nuclear skill would flag these as brittle; note but don't crusade.
- MEET-1 (10k transcript cap) is a COUPLED change: behavioral test MeetingNoteCaptureServiceTests.swift:58-71 (transcriptBufferIsCappedToCapturePipelineEnvelope) + source-mirror :577-578 both encode maxTranscriptCharacters == TextCapturePipeline.maxCleanedTextCharacters. Also LiveVoiceInputService.maxTranscriptCharacters + TextCapturePipeline.cleanText re-truncate at persist. A real fix must: give meetings their own larger cap, make the pipeline honor it at persist (parameterize cleanText's max), make hitting it VISIBLE not silent, and update those tests + add one, all with DECISIONS.md justification. The magnitude is a product call → treat as a deliberate Phase 9 change, not a loop-tick rush.
- MEET-2 fix is verified-safe against the source-mirror test: it preserved `let finalizeGeneration = UUID()` + the isCurrentCapture guards asserted at :589-591 and only added a new guard the mirror doesn't check. Behavioral verification still pending a test run.
- Release/AppStore build is universal (x86_64+arm64) + release-optimized incl. the full Rust core → genuinely 30-45+ min cold. Debug is arm64-only (ONLY_ACTIVE_ARCH) so far faster. Don't run a competing xcodebuild/cargo while it holds DerivedData (TreeSitter/DD race). A green AppStore Release build is itself the F-0003 (Float16 x86_64) verification.
- ENVIRONMENT CONSTRAINT (Rule-6 documented): background tasks here get KILLED around ~5-6 min wall-clock (the full-suite `xcodebuild test` = compile ~500 test files + run 2679 tests died "BUILD INTERRUPTED" mid-compile, 0 tests run). Adaptation: (1) can't run the whole suite in one shot — split into `build-for-testing` (build the .xctest bundle; incremental across retries) THEN `test-without-building -only-testing:<Suite>` (no compile, runs fast, fits budget); (2) verify targeted suites, not the monolith; (3) editing an app-module .swift cascades a ~5-min app-module recompile, so batch source edits before a single verify build. macOS has NO `timeout` cmd (that's GNU; use `gtimeout` if coreutils, else none) — a `timeout` wrapper exits 127 and the real command never runs.
- Full-suite ground-truth plan under the kill limit: build-for-testing once (retry until the bundle exists), then `xcodebuild test-without-building -scheme Epistemos -destination 'platform=macOS'` for the whole run (skips compile → should fit), or `-only-testing:EpistemosTests/<Suite>` for the real-suspect suites (NoteSavingStressTests, *ConcurrentStress). If even test-without-building of the full set exceeds budget, run suites in named batches.
- Per-lane ISOLATED DerivedData is the real fix for shared-DerivedData contention (the @Model macro race + mutual incremental-state corruption + cargo-lock stalls): build with `-derivedDataPath ~/.cache/epistemos-dd-<lane>`. First build is a cold full recompile; every build after is warm + race-free (no waiting for or pkilling siblings). The Rust cargo target dir is repo-relative (shared), so concurrent builds still serialize briefly on the cargo lock, but that self-resolves. A plain `xcodebuild build` also compiles the TEST target (scheme builds tests), so another lane's broken test file fails your build — verify via the `Ld .../Epistemos.app/.../Epistemos` link line + only-foreign errors.
- Shared `.git` index SWEEP hazard: if you `git add` your files then pause (checks/edits) before committing, another lane's `git add -A && git commit` will SWEEP your staged files into THEIR commit (attribution lost). Commit ATOMICALLY in one shot with explicit pathspecs — `git commit -o -F - -- <path...>` — which takes those paths' working-tree content and ignores the shared index. Shared append-only docs (HARDENING_AUDIT.md, LESSONS.md, DECISIONS.md) get concurrent appends: write via `cat >>` (O_APPEND) and `git diff <doc>` to confirm no foreign lines rode in before staging.
- A DEEP adversarial audit of ONE live subsystem finds real per-keystroke / @Observable / main-hop bugs that broad scouts skim past: e.g. HW-DOM-1 (a tag-scan NSRegularExpression rebuilt on every keystroke on the main thread) and HC-1 (an @Observable property reassigned every keystroke with no equality guard, re-publishing to every observer). When a broad scout calls a subsystem "clean," a focused deep pass is still worth it before declaring a hardening plateau.
