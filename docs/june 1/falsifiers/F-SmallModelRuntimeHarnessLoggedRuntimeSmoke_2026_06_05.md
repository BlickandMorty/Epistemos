# F-SmallModelRuntimeHarnessLoggedRuntimeSmoke - 2026-06-05

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Verdict

- Status: PASS, metadata-only primary witness.
- Command: `Tools/falsifiers/f_small_model_runtime_harness_logged_runtime_smoke.sh`
- Artifact: `artifacts/falsifiers/small_model_runtime_harness_logged_runtime_smoke/result.json`
- Current L1 cursor: `small_model_runtime_harness_fresh_product_runtime_answer_packet_probe` after downstream `F-SmallModelRuntimeHarnessFreshProductRuntimeLiveProbe`
- Scope: L1 logged missing-snapshot proof only; not an MLX inference success, not a local-agent product route, not a 70B route, and not MAS Live capability.

## What It Proves

This witness consumes the abortable runtime probe artifact and proves the owner-approved small-model smoke path reaches the runtime harness logging boundary without opening runtime, model, or transport bytes. The positive fixture binds the Swift `MLXInferenceService` surface, `LocalInferenceSerialController`, local model catalog refs, explicit missing local snapshot refs, prompt envelopes, SCOPE-Rex/SovereignGate admission, compatibility, cancellation, rollback, RunEventLog, AnswerPacket, privacy, budget, and missing-snapshot reasons.

The positive fixture records 3 logged smoke attempts, 3 missing-snapshot failures, 3 unavailable snapshots, 11 phases, max observed elapsed `34` ms, zero first tokens, zero output tokens, zero runtime starts, zero runtime completions, zero model-open attempts, zero route/policy mutation, and zero runtime/model/transport bytes.

## Required Rejections

The primitive rejects missing or duplicate lanes/runs, missing phases, missing abortable artifact refs, missing Swift runtime or serial-controller refs, missing catalog/snapshot/prompt/admission/SCOPE-Rex/SovereignGate/compatibility/cancellation/rollback/RunEventLog/AnswerPacket/privacy/budget/failure-reason refs, unlogged attempts, unlogged missing snapshots, local-snapshot availability overclaims, model-open attempts, runtime start/completion, first-token or output-token claims, committed mutation, route-policy mutation, gate bypass, AnswerPacket suppression, hidden route authority, hidden chain/cloud, subprocess spawn, autogenous-kernel attempts, 70B probes, budget overflow, MAS overclaim, false L2/L3 green claims, runtime/model/transport bytes, metadata-budget overflow, and nondeterministic addresses.

## Three-Layer Truth

- L1: Advanced. `F-SmallModelRuntimeHarnessLoggedRuntimeSmoke` passes metadata-only and the regenerated pending-work guard reports `next_existing_work=small_model_runtime_harness_product_answer_packet_live_probe` with duplicate risk `0`.
- L2: Not advanced. The capability kernel remains `overall_pass=false`, route status `vault_research_route_with_packetized_mitigation`, with `next_bottleneck=small_model_runtime_harness_fresh_product_runtime_answer_packet_probe`.
- L3: Not advanced. User-facing/product runtime is unchanged; no live model route, first token, 70B route, or MAS agent mode promotes.

## Caveat

This is not a live MLX run. It is the logged failure-path proof that must exist before a tightly bounded first-token runtime probe can be attempted under explicit owner-approved conditions and local snapshot availability.
