# Chat-Surface Deletion Inventory & New-Chat Route Plan

**Minimum-proof item 0 — deletion inventory for old Chat routes/views/state/backend wiring, plus the seam map for the new Epistemos-native Swarm-backed Chat route.**

This ledger merges 7 read-only recon lanes into a single authority for the Chat surface refactor. It draws the four-way ownership line (chat-delete / act-delete / work-untouchable / shared-preserve) so the CHAT agent knows exactly what it owns, what it must hand to the ACT agent, what it must never touch (WORK), and what it reuses verbatim. It then specifies the smallest new Chat route that compiles against the in-process engine via Swarm's provider seam — **no hidden sidecar, in-process inference only, landing as the only top-level shell, no "tap anywhere to start a conversation" default, Swarm-first MIT.** Prune means **REAL removal**: old destinations are detached then deleted once the replacement compiles, not hidden behind flags.

> **VERIFIED AGAINST `main` ON 2026-06-24 — READ-ONLY.** HEAD `8af17c841` (2026-06-24 08:03 -0500), branch `main`, no worktree. Every file:line below was confirmed present at this snapshot. The shared-in-flux set (uncommitted Work-agent edits) was re-confirmed via `git status` / `git ls-files --others`: `ChatTypes.swift`, `ThreadState.swift`, `ChatInputBar.swift`, `ComposerCurrentAccessPlan.swift`, `VaultSyncService.swift`, `EpistemosApp.swift` are ` M`. The Work surface has **THREE additional ` M` (tracked-but-modified) files actively edited by the Work agent** — `Epistemos/Work/WorkOpenCodeRuntime.swift`, `Epistemos/Work/WorkOpenCodeShell.swift`, `Epistemos/Work/WorkTerminalView.swift` (+ `EpistemosTests/WorkOpenCodeRuntimeTests.swift`) — plus `WorkCloneSettingsView.swift` (` M`) and **23 untracked `Epistemos/Work/*.swift`** (`git ls-files --others --exclude-standard 'Epistemos/Work/' | wc -l` ⇒ **23**, `WorkEngineTranscript.swift` included). This recon writes nothing.

---

## 1. Ownership legend

| Tag | Meaning | Who acts |
|---|---|---|
| **chat-delete** | Old regular-chat route/view/state/backend wiring. CHAT agent detaches then deletes after the new route compiles. | CHAT |
| **act-delete** | Act-as-chat / Osaurus / agent-tier execution path. Flagged only — CHAT must NOT delete; ACT agent owns removal. | ACT |
| **work-untouchable** | Work surface files (`Epistemos/Work/*`, Work settings). Never edit, never call, never `git clean`. | WORK |
| **shared-preserve** | Mode-agnostic primitive (picker, theme, recents identity, permission/tool cards, motion). New Chat reuses; do not delete. | CHAT (reuse) |
| **shared-in-flux** | Shared file with LIVE uncommitted Work-agent edits. Reuse READ-ONLY; do not edit until Work commits; coordinate. | CHAT (coordinate) |
| **new-chat-target** | The seam the new Chat route attaches to (Epistemos router/state hooks) or the Swarm donor API it plugs into. | CHAT (build) |

---

## 2. Deletion inventory

Every chat-delete and act-delete item from recon, de-duplicated (richest evidence kept), grouped by subsystem. `Recommendation`: **detach** = unwire the edge now; **retire** = remove the ontology/copy; **investigate/flag** = ACT-owned, hand off; **do-not-touch** = WORK/ACT-owned engine.

### 2a. Landing / Routing

| Path | Symbol | Kind | Ownership | Role today | Recommendation | Evidence |
|---|---|---|---|---|---|---|
| `Epistemos/App/RootView.swift` | `HomeRouter.body` | route | new-chat-target | THE central surface switch; two-mode only (`.act`/`.work`). Add a `.chat` branch here. | adapter-boundary | RootView.swift:3814 `struct HomeRouter`; body 3867–3924 switches on workspaceMode / showChat; toggle at 3920 |
| `Epistemos/App/RootView.swift` | bare `ChatView()` sites (3876, 3909, 3998) | route | **chat-delete** | The 3 old regular-chat mounts: MAS showChat, non-MAS showChat, OsaurusCore-absent fallback. `showChat = !chat.messages.isEmpty && !chat.showLanding` (:3840). | detach | RootView.swift:3876 `ChatView().transition(.blurFade())`; :3909 same; :3998 `ChatView()` — all 3 confirmed via grep |
| `Epistemos/App/RootView.swift` | `ChatSidebarView()` mounts (1535, 3861) | route/view | **chat-delete** | The two TOP-LEVEL old-chat recents-sidebar mounts (a 3rd embed lives at ChatView.swift:579). This is the surface min-proof item 8 ("own recents rail") REPLACES. | detach | RootView.swift:1535 `ChatSidebarView()`; :3861 `ChatSidebarView()` (both confirmed via grep) |
| `Epistemos/Views/Chat/ChatSidebarView.swift` | `struct ChatSidebarView` | view | **chat-delete** | The old-chat recents sidebar VIEW; fetches `SDChat` via `recentChatsDescriptor`, lists/opens/deletes threads. New Chat must build its own native recents rail (do NOT reuse this view; reuse the underlying `SDChat`/`recentChatsDescriptor` persistence — see §3). | detach (delete after new rail) | ChatSidebarView.swift:31 `struct ChatSidebarView`; mounted RootView.swift:1535/:3861; embedded ChatView.swift:579 |
| `Epistemos/App/RootView.swift` | `HomeRouter` act branch (`shouldRouteActThroughOsaurus`, `LandingView(onSubmitActPrompt:)`, `actEpistemosChatSurface`) | route | **act-delete** | Act-as-chat entry: Landing as Act search page → ActEpistemosChatSurface. | flag (ACT) | RootView.swift:3893–3907; 3981–4000 |
| `Epistemos/App/RootView.swift` | `ActEpistemosChatSurface` | view | **act-delete** | Wraps OLD ChatView w/ `actUsesOsaurus:true, composerMode:.osaurusAct`. | flag (ACT) | RootView.swift:1547 struct; :1565 `actUsesOsaurus: true` (confirmed) |
| `Epistemos/App/RootView.swift` | `.submitActOsaurusPrompt` onReceive (single receiver) | state | **act-delete** | The ONE receiver of the escalation channel → selects `.act`, mounts ActEpistemosChatSurface. All portal posters funnel here. | flag (ACT) | RootView.swift:3959 (confirmed); routes via WorkspaceModeSelection.select(.act) |
| `Epistemos/App/RootView.swift` | `showingActChatSurface` / `actLandingSurfaceVisible` / `activeHomeChat` toolbar gating | state | **act-delete** (act parts) / coordinate (shared chrome) | Toolbar/back-button chrome gating; Act parts ACT-owned, landing chrome reused by new Chat. | coordinate | RootView.swift:1061–1137; toolbar 1182–1226 |
| `Epistemos/Views/Landing/LandingView.swift` | Background Tap Layer (`Color.clear.onTapGesture → activateLandingSearch`) | view | **chat-delete** | The "click anywhere → open search → old chat" default ontology. **Must be removed** for a deliberate chooser. | retire | LandingView.swift:362–390 |
| `Epistemos/Views/Landing/LandingView.swift` | `submitLandingSearch()` → `chat.startNewChat()` + `MainChatSubmissionRouter.submit(...)` | backend | **chat-delete** | Regular-chat submit from landing (when `onSubmitActPrompt==nil`). Shares one function with the Act path. | detach | LandingView.swift:2008 func; :2030 startNewChat; :2040 MainChatSubmissionRouter.submit (confirmed) |
| `Epistemos/Views/Landing/LandingView.swift` | `onSubmitActPrompt` closure / `isActSearchPage` | backend | **act-delete** | Act-as-chat escalation from landing; do NOT delete — same `submitLandingSearch` function. | flag (ACT) | LandingView.swift:88 decl; :208 isActSearchPage; :2020–2022 consumed (confirmed) |
| `Epistemos/State/UIState.swift` | LandingGreetingPhrase "click anywhere to start a conversation" | state | **chat-delete** | Rotating greeting asserting the wrong ontology. | retire | UIState.swift:91 |
| `Epistemos/Views/Landing/LiquidGreeting.swift` | `promptPair = HeroPair("Click anywhere","to start a conversation")` | view | **chat-delete** (copy) / shared-preserve (typewriter) | Typewriter motion is reusable IP; the hero pair text encodes the wrong ontology. | retire (copy only) | LiquidGreeting.swift:82 |

### 2b. Main Chat View + State

| Path | Symbol | Kind | Ownership | Role today | Recommendation | Evidence |
|---|---|---|---|---|---|---|
| `Epistemos/Views/Chat/ChatView.swift` | `struct ChatView` | view | **chat-delete** (regular) — blocked on ACT migration | The regular-chat product VIEW when `ChatView()` (actUsesOsaurus defaults false). Same struct doubles as Act-as-chat. Also embeds `ChatSidebarView()` at :579. **Cannot be deleted until ACT migrates off it.** | detach the 3 regular sites + `!actUsesOsaurus` branches | ChatView.swift:184 `var actUsesOsaurus: Bool = false`; body 269–517; sidebar embed :579 |
| `Epistemos/State/ChatState.swift` | `final class ChatState` (@Observable) | state | **chat-delete** (regular wiring) — co-resident w/ Act + shared-in-flux | In-memory session/streaming/message state. Hosts BOTH regular path (submitQuery) AND Act path (runActOsaurusTurn). | detach regular only; coordinate | ChatState.swift:228 class; submitQuery :685; co-edited file |
| `Epistemos/State/ChatState.swift` | `submitQuery` / `completeProcessing` / `completeCancelledProcessing` / `startStreaming` / `appendStreamingText` / think-tag routers | backend | **chat-delete** | Regular-chat streaming/turn-completion over EventBus `.querySubmitted` → ChatCoordinator. | detach (after new route streams) | ChatState.swift:685, 935–1093, 1095–1186, 1278–1366; routers 1250–1253 |
| `Epistemos/State/ChatState.swift` | `enum MainChatSubmissionRouter` (submit / autoPromotedMode) | backend | **chat-delete** | Regular-chat submission dispatch → `chat.submitQuery`. New adapter supersedes. | detach | ChatState.swift:1782 (confirmed); called LandingView:2040 + ChatView submitMainChatQuery |
| `Epistemos/State/ChatState.swift` | `runActOsaurusTurn(_:modelID:attachments:contextAttachments:)` | state | **act-delete** | Direct Act-Osaurus turn; bypasses ChatCoordinator; streams via SharedActInference/ActTurnStreamCore. | flag (ACT) | ChatState.swift:740 (confirmed); :802 actEventStreamIfArmed; :819 ActTurnStreamCore.consume |
| `Epistemos/State/ChatState.swift` | `actTurnTask` / `persistActTurn` / `ActTurnStatsStore` usage | state | **act-delete** | Act-execution support state inside ChatState. | flag (ACT) | ChatState.swift:237, 604, 770, 832/844, 1294–1295 |
| `Epistemos/Views/Chat/ChatView.swift` | `actUsesOsaurus` act-branches (submitMainChatQuery, ActGenerationStatsChip, actConfigurationToolbarButton, forced `.agent`) | state | **act-delete** | Act conditional path threaded through ChatView (~18 sites). | flag (ACT) | ChatView.swift:185, 237/243/250, 295–302, 601–613, 631–634 |
| `Epistemos/Views/Chat/ChatView.swift` | `ActComputerUseApprovalOverlay`, `ActNativeSecretPromptSheet`, `ActNativeClarifyPromptSheet`, `ActNativePrivacyReviewSheet` (+states) | view | **act-delete** | ~920 lines of Act-only native permission/prompt UI under `#if !EPISTEMOS_APP_STORE && canImport(OsaurusCore)`. Compile inside the same file → must be relocated by ACT before ChatView.swift can be removed. | flag (ACT) | ChatView.swift:783–1023, 1025–1370, 1372–1705; gated `guard actUsesOsaurus` 729/750 |
| `Epistemos/App/RootView.swift` | `.submitActOsaurusPrompt` onReceive handler | route | **act-delete** | Escalation receiver from portals; flips into Act-as-chat. (Same item as 2a receiver.) | flag (ACT) | RootView.swift:3959 |

### 2c. Mini / Graph / Note Portals + Extra State

| Path | Symbol | Kind | Ownership | Role today | Recommendation | Evidence |
|---|---|---|---|---|---|---|
| `Epistemos/Views/MiniChat/MiniChatView.swift` | `MiniChatView` / `MiniChatThread` / `MiniChatBubble` / `MiniChatInputBar` | view | **chat-delete** | 3219-line PRIVATE floating-window chat (own renderer/composer/local-inference via `triage.streamGeneral`). Must become a PORTAL into the new Chat session. | detach | MiniChatView.swift:33 struct; :64 MiniChatThread; :2364 streamGeneral |
| `Epistemos/Views/MiniChat/MiniChatView.swift` | `runActPromptInMiniChat` / `submitActPromptInMainChat` / `loadActOsaurusTranscriptIfAvailable` | backend | **act-delete** | In-panel Osaurus turn + escalation post + Osaurus transcript loader. | flag (ACT) / coordinate | MiniChatView.swift:222, 2452 (confirmed post), 2456 |
| `Epistemos/Views/MiniChat/MiniChatWindowController.swift` | `MiniChatWindowController` | view | **chat-delete** (content host) / shared-preserve (window shell) | Floating-window lifecycle; the SHELL is reusable but content host (MiniChatView) + Osaurus default-mode wiring must re-point at the new Chat route. | preserve-reuse (shell) / re-point | MiniChatWindowController.swift:127; defaultPreferredOperatingMode 159–165 |
| `Epistemos/State/DialogueChatState.swift` | `DialogueChatState` | state | **chat-delete** | Private graph "node creature" dialogue chat (own messages/typewriter/streaming + SDChat `chatType:"dialogue"` + persona engine + FFI mouth-anim hook). | detach (decide persona-box fate) | DialogueChatState.swift:624 class; :860 stream; :771 SDChat dialogue; :946 startTypewriter |
| `Epistemos/Views/Graph/MetalGraphView.swift` | `DialogueChatState` env binding (+ HologramController/HologramOverlay/HomeGraphEmbeddedView) | view | **chat-delete** (re-point) | Render/host bindings + `onStreamingChanged` FFI hook. Re-point or remove with persona box; coordinate w/ graph owner. | investigate | MetalGraphView.swift:570, 748; HologramController 32/47; HologramOverlay 256/392 |
| `Epistemos/State/NoteChatState.swift` | `NoteChatState` | state | **chat-delete** | Per-open-note chat that streams INLINE into NSTextStorage (accept/discard divider) + SDChat `chatType:"notes"`. Inline-edit ergonomics are note-specific — preserve UX while sharing session identity. | detach | NoteChatState.swift:44 class; :400 submitQuery; :838 chatType notes; instantiated NoteDetailWorkspaceView:671 |
| `Epistemos/Views/Graph/PinnedInspector.swift` | `PinnedInspector` (chatMessages/chatInput/isChatStreaming/sendMessage) | state | **chat-delete** | Graph-inspector chat (SEPARATE from DialogueChatState) via `triage.streamGeneral(localSurface:.graph)`. Conversational part → portal. | detach | PinnedInspector.swift:19–21, 301 sendMessage, 329 streamGeneral |
| `Epistemos/Views/Graph/HologramSearchSidebar.swift` | `sendGraphChatMessage` / `graphNodeContextAttachment` | view | **chat-delete** (inline) / coordinate (escalation) | Inline-send → PinnedInspector (re-point); agent/research intents → `.submitActOsaurusPrompt` (ACT seam). | detach inline | HologramSearchSidebar.swift:1254–1260 post (confirmed); :1265 sendMessage |
| `Epistemos/Views/Notes/NoteDetailWorkspaceView.swift` | `submitToolbarAskInline` / `routeToolbarAskToMainChat` / `openMiniChatForCurrentNote` | view | **chat-delete** (inline) / coordinate (escalation) | Inline → NoteChatState (re-point); agent intents → `.submitActOsaurusPrompt` (ACT). | detach inline | NoteDetailWorkspaceView.swift:2565–2571 post (confirmed); :2551 submitToolbarQuery; :2599 MiniChat |
| `Epistemos/App/AppBootstrap.swift` | `routeGraphChatRequestIntoMainChat` (`.submitActOsaurusPrompt` poster + `primeGraphChatRequest` fallback) | backend | **act-delete** (act branch) / coordinate (old-chat fallback) | Graph "Tell me about X" → Act when Osaurus active, else primes OLD main chat. | coordinate | AppBootstrap.swift:1242–1248 post (confirmed); :1252 primeGraphChatRequest |

### 2d. Backend / Session / Persistence

| Path | Symbol | Kind | Ownership | Role today | Recommendation | Evidence |
|---|---|---|---|---|---|---|
| `Epistemos/App/ChatCoordinator.swift` | `handleQuery(_:pipeline:chatState:operatingMode:)` | backend | **chat-delete** | THE old regular-chat backend entry: runs `PipelineService.run`, consumes `.thinkingDelta/.textDelta/.completed/.error` into ChatState, persists. | detach (after new route streams) | ChatCoordinator.swift:1785 (confirmed); :2277 pipeline.run; :2324 stream loop |
| `Epistemos/App/ChatCoordinator.swift` | `handleCommandCenterSubmission` / `runCommandCenterRustAgentPath` / `runCommandCenterLocalAgentPath` | backend | **act-delete** | Agent Command Center submission → cloud agent FFI or LocalAgentLoop. Writes AgentChatState, NOT main ChatState. | flag (ACT) | ChatCoordinator.swift:369, 650, 724 runAgentSession, 1127/1174 liveLoop |
| `Epistemos/App/ChatCoordinator.swift` | `runRustAgentPath` / `persistCompletedAgentTurn` | backend | **act-delete** | Agent-tier execution for main ChatState (Agent mode); records lineage + graph gen. | flag (ACT) | ChatCoordinator.swift:2964; :2984 AgentSessionLineageStore.recordCompletedSession |
| `Epistemos/State/ChatState.swift` | `runActOsaurusTurn` + `persistActTurn` (dup of 2b) | state | **act-delete** | Act-as-chat turn driver on main ChatState. | flag (ACT) | ChatState.swift:740, 604 |
| `Epistemos/LocalAgent/SharedActInference.swift` | `actStreamIfArmed` / `actEventStreamIfArmed` / `actTextIfArmed` | backend | **act-delete** | The single act=Osaurus injection chokepoint; returns Osaurus stream when armed, else nil (MLX fallback). | do-not-touch | SharedActInference.swift:10, 23, 29; always nil on MAS |
| `Epistemos/LocalAgent/ActTurnStreamCore.swift` | `ActTurnStreamCore.consume` + `ActTurnStreamSinks` | backend | **act-delete** | The ONE shared Act stream consumer (main act + MiniChat + graph/note). | do-not-touch | ActTurnStreamCore.swift:3 header, :48 consume |
| `Epistemos/ActOsaurus/ActTurnStatsStore.swift` | `ActTurnStatsStore` + `ActGenerationStatsChip` | backend | **act-delete** | Act-only TTFT/tokens telemetry + chip. | do-not-touch | defined ActTurnStatsStore.swift; used ChatView:296–302, ChatState:770 |
| `Epistemos/Vault/AgentSessionLineageStore.swift` | `recordCompletedSession` / `parentSessionID(forChatThread:)` | persistence | **act-delete** (agent caller) / shared-preserve (identity anchor) | Maps chatThreadID → Rust sessionID lineage. Only agent path calls it today; Work's in-flux `ChatThread.parentSessionID` foreshadows a mini-session bridge. **Anchor for unified Chat session identity.** | flag (ACT caller) / preserve-reuse (anchor) | AgentSessionLineageStore.swift:18, 27; ChatCoordinator:2618/2984; ChatTypes.swift:457 mirror |
| `Epistemos/App/AppBootstrap.swift` | `chatState.persistActTurn` wiring + `bindSSMStatePath` | backend | **shared-in-flux** | Bootstrap wires `persistActTurn → persistChatCompletion(mode:.local)`; constructs ChatCoordinator. EpistemosApp shell is ` M`. | coordinate | AppBootstrap.swift:2234, 2139 |

---

## 3. Preserve & reuse (shared-preserve primitives)

The OpenCode-TUI-minimalism visual target is **already built** in Epistemos IP — reuse, do not re-skin. New Chat colors ONLY via theme tokens; flat `theme.card` + 1.5px `theme.border`, never `.glassEffect` default, never bare SwiftUI chrome.

| Primitive | Path | Symbol | How new Chat reuses |
|---|---|---|---|
| **Model/engine picker (flat TUI)** | `Views/Chat/InlineRuntimePickerPanel.swift` | `InlineRuntimePickerPanel` | Mount directly as the primary picker — the flat pixel-art look is the visual target. **Set `showsOsaurusModelSection:false`** (default); surface only Fast/Think/Code tiers, leave Act rows to ACT. (InlineRuntimePickerPanel.swift:21–33, 283–317) |
| **Picker data model (honest gating)** | `Engine/EpistemosRuntimePicker.swift` | `EpistemosRuntimePicker` (Option/Environment) | Feed live `InferenceState` into `Environment`; render `options()`. Honest install+memory gating with one-line `blockedReason` — never silent omission. (EpistemosRuntimePicker.swift:16, 21–32, 37–49) |
| **Mode/effort controls** | `Views/Chat/ChatBrainPickerMenu.swift` | `ChatBrainPickerMenu` / `MainChatOperatingModePreference.sanitize` | Reuse `sanitize()` to clamp modes to availability; prefer InlineRuntimePickerPanel as primary control. (ChatBrainPickerMenu.swift:15–26, 34–58) |
| **Model-details sheet** | `Views/Chat/ModelAboutSheet.swift` | `ModelAboutSheet` | Present from picker's "details" affordance (320pt caps sheet). (ModelAboutSheet.swift:6–21) |
| **Theme tokens** | `Theme/EpistemosTheme.swift` | `ResolvedTheme` (textPrimary/Secondary/Tertiary, card, chatSurface, border, accent) + `surfaceVariant(.mainChat)` | All Chat subviews read `ui.theme`. 12-theme aware via semantic tokens only. (EpistemosTheme.swift:100–209, 1275–1294, 359–377) |
| **Theme injection** | `State/UIState.swift` | `@Observable UIState.theme` | Mount new route inside existing UIState environment; `@Environment(UIState.self) var ui; var theme { ui.theme }`. No parallel theme channel. (UIState.swift:249, 276) |
| **Fonts** | `Theme/EpistemosTheme.swift` + `Theme/EpistemosFont.swift` | `ClaudeAppTypography.monoFont/assistantFont` + `EpistemosFont.registerFonts()` | monoFont (JetBrainsMono) for TUI labels/code, assistantFont (Anthropic Sans) for prose. Already registered at launch — verify the new surface's build target runs registration (unit-test hosts fall back silently). (EpistemosTheme.swift:2430–2498, 1832; EpistemosFont.swift:37–75) |
| **Provider row icon** | `Views/Shared/ProviderLogoView.swift` | `ProviderLogoView` | Per-row brand logo (template-tinted) in picker rows. (ProviderLogoView.swift:7–26) |
| **Message render stack** | `Views/Chat/MessageBubble.swift` | `MessageBubble` | Mode-agnostic row renderer on `ChatMessage` + theme. Already standalone & safe. Point new transcript at it. (MessageBubble.swift:130–201) |
| **Inline streaming renderer** | `Views/Chat/AssistantInlineTranscriptView.swift` | `AssistantInlineTranscriptView` | Live answer + thinking trail + tool/content blocks. Standalone. (consumed by MessageBubble + ChatView StreamingIndicator) |
| **Streaming/render transforms** | `State/ChatState.swift` | `DisplayPacedTextBuffer` + `StreamingReasoningTraceBuffer` + `makeChatTranscriptRows`/`ChatPresentationFormatter` | Pure transforms — **adopt into the new adapter**, do not re-implement (and extract before ChatState.swift is deleted). (ChatState.swift:1848–1897, 132–222; ChatView.swift:12–106, 120–177) |
| **Transparency panel + model** | `Views/Chat/ChatView.swift` (private) + `State/ChatState.swift` | `ChatBrainPanelView` / `ChatBrainSnapshot` / `CapturedModelInput` | Native transparency card stack. **Private inside ChatView.swift / ChatState.swift — MUST be extracted to standalone files BEFORE those files are deleted.** (ChatView.swift:1802–2199; ChatState.swift:5–130, 396–519) |
| **Streaming status surface** | `Views/Chat/ChatView.swift` (private) + `LiveActivityStrip.swift` | `StreamingIndicator` + `LiveActivityStrip` | Extract regular-chat behaviour (`showsActivityStrip:!actUsesOsaurus`) before deleting ChatView.swift. (ChatView.swift:1709–1800) |
| **Context meters** | `Views/Chat/ContextWindowIndicator.swift` + `ContextWindowCompactBadge.swift` | `ContextWindowIndicator` / `ContextWindowCompactBadge` | Live token-budget bar + compact "x / y (z%)" badge on composer control row. (ContextWindowCompactBadge.swift:10–30) |
| **Tool/evidence cards** | `Views/Chat/ProcessDisclosureViews.swift` | `ProcessDisclosureHeader/Divider/DetailBlock` | Canonical flat `[TITLE]` mono tone-tinted cards. Render tool/thinking/evidence with these. (ProcessDisclosureViews.swift:32–94, 127–168) |
| **Tool narrator** | `Views/Chat/ToolActivityNarrator.swift` | `ToolActivityNarrator` | Pure nonisolated tool-name→phrase+Surface (WEB/FILE/RUN/THINK). Feed the in-process tool stream through it. (ToolActivityNarrator.swift:14–89) |
| **Tool config panel** | `Views/Chat/AgentToolTogglePanel.swift` | `AgentToolTogglePanel` | Reuse VIEW + badges + honest Pro/MAS footers; **pass the new Chat's own tool source**, not the old AgentCommandCenterState (coordinate). (AgentToolTogglePanel.swift:16–21, 268–293) |
| **Provenance badge** | `Views/Chat/AnswerPacketBadge.swift` | `AnswerPacketBadge` | Attach to assistant messages (claim-kind+confidence chips, reads `LatestAnswerPacketSink`). Reuse as-is. (AnswerPacketBadge.swift:3–34) |
| **Approval modal (in-process)** | `Views/Approval/ApprovalModalView.swift` | `ApprovalModalView` / `PendingApproval` | `.sheet(item:)` for any in-process tool that pauses. **SEAM NOTE: the `RustAgentBridge.resolveApproval(...)` referenced in ApprovalModalView.swift:25 is a CODE COMMENT only — that symbol does NOT exist in the codebase.** The new route must wire approval resolution to the REAL in-process resolver it builds against `PendingApproval`/`ApprovalDecision` (the `ApprovalModalView` resolves through its own `onResolve` closure) — **NO HIDDEN SIDECAR**; verify the concrete resolver before wiring item 7. (ApprovalModalView.swift:6–58; comment-only ref :25) |
| **Motion triad** | `Views/Shared/MotionReveal.swift`, `Views/Landing/BlurFade.swift`, `Views/Shared/TypewriterASCIIRippleText.swift`, `Theme/PhysicsModifiers.swift`, `Views/Shared/TypewriterMarkdown.swift`, `Views/Shared/MotionTitle.swift` | `.motionReveal` / `.blurFade()` / `TypewriterASCIIRippleText` / `ASCIIRippleText` / `TypewriterPlainText` / `MotionTitle` | Blur-reveal on titles/empty-state; blurFade for Landing↔Chat swap; ASCII-typewriter for hero lines; ASCII-morph toolbar title. DISPLAY-ONLY (never the composer). Live streaming forwards tokens immediately (STREAM-EVERYTHING). (per-file headers cited in Lane 1/5) |
| **Button styles** | `Theme/NativeButtonStyles.swift` | theme-aware ButtonStyles | Per-theme beveled (Platinum) / flat treatment; not bare `.bordered`. (NativeButtonStyles.swift) |
| **Recents / session identity** | `Models/SDChat.swift`, `Models/SDMessage.swift`, `Models/SDPage+Queries.swift`, `Vault/ChatTranscriptVaultWriter.swift`, `App/ChatCoordinator.swift` | `SDChat` (chatType `"chat"`) / `SDMessage` / `recentChatsDescriptor` (fetchLimit 200) / `ChatTranscriptVaultWriter` / **`persistChatCompletion`** | **THE canonical writer + recents query the new Chat MUST reuse** so recents/reopen + vault markdown keep working — Act path already routes through it. New Chat upserts `SDChat(id==chatId)`, inserts SDMessage rows, inherits vault transcript for free; the new native recents rail reads `recentChatsDescriptor` (the same query `ChatSidebarView` used). (ChatCoordinator.swift:6142 (confirmed), 6155, 6182, 6266; SDPage+Queries.swift:104–119) |
| **Session lineage anchor** | `Vault/AgentSessionLineageStore.swift` | `AgentSessionLineageStore` | Derive parent/child lineage for new Chat + Mini/Graph/Note portals from here (`ChatThread.parentSessionID` already mirrors it). (AgentSessionLineageStore.swift:18, 27) |
| **Citation gate** | `App/ChatCoordinator+EidosCitationGate.swift` | `runEidosCitationGate` | Stateless Rust-authoritative validator; new emit path can call it before committing a row. (ChatCoordinator+EidosCitationGate.swift:62) |
| **SSM replay store** | `Vault/ConversationPersistence.swift` | `ConversationPersistence.shared` | Reuse the actor for SSM-state binding ONLY (`bindSSMStatePath`); recents identity stays in SDChat. **Do not retire** — MLX in-process KV-state depends on it. (ConversationPersistence.swift:46, 167; AppBootstrap:2139) |
| **Value-type primitives** | `Models/BrandedTypes.swift`, `Models/EngineTypes.swift` | `MessageRole`, `InferenceMode` | Reusable role/tier enums for the new transcript model + adapter. (BrandedTypes.swift:34; EngineTypes.swift:296) |
| **ACC own surface (NOT a portal)** | `State/AgentChatState.swift` | `AgentChatState` | **Out of Chat-portal scope** — Agent Command Center's own surface (PLAN_V2 line 169). Do NOT fold into the chat-delete sweep; fate is an ACT/ACC decision. (AgentChatState.swift:11, header 7–9) |

---

## 4. Coordination hazards

**Concurrent editors on shared `main`, no worktree, all uncommitted.** Every in-flight non-doc edit is the WORK agent's (Work-surface code) — plus a handful of unrelated non-Work edits (see below). **KEY FINDING (Lane 7, re-verified):** running `grep actUsesOsaurus|runActOsaurusTurn|submitActOsaurusPrompt` over the six shared-in-flux files yields exactly ONE match — **`EpistemosApp.swift:1510` `static let submitActOsaurusPrompt = Notification.Name("epistemos.submitActOsaurusPrompt")`, the channel-name DECLARATION** (the other five files are clean). That single match is a constant decl OUTSIDE the Work-edited `⌘4` hunk, so the conclusion still holds — **there is NO delete-vs-edit collision on old Act-Osaurus chat wiring**; every hazard is a same-file shared-primitive overlap. (The earlier "grep is empty" phrasing was wrong; the match exists but is benign.)

### Shared-in-flux files (reuse READ-ONLY; do NOT edit until Work commits)

| Path | Live Work edit | Why new Chat cares | Safe handling |
|---|---|---|---|
| `Epistemos/App/EpistemosApp.swift` | +7: new `⌘4 Open Work Surface` Button after the `⌘3` block (~1570) → `WorkWebSurfaceWindowController.shared.open()`; also hosts ChatState/notification wiring; declares `submitActOsaurusPrompt` channel name (:1510) | **HIGHEST RISK**: new Chat needs its own `⌘N` command in the SAME `EpistemosCommands` block; the `.submitActOsaurusPrompt` NAME also lives here (ACT retires it after the receiver dies) | Do NOT edit now. Add the Chat command in a **separate, well-separated hunk AFTER Work commits**; avoid reusing ⌘4. The channel name (:1510) is ACT-owned — do not remove. |
| `Epistemos/Models/ChatTypes.swift` | +7: `ChatThread.parentSessionID: String?` (member + init param + assignment) | New Chat reuses `ChatMessage`/`ChatThread` transcript model | Treat ChatThread as **frozen**. Do NOT add/reorder stored props or init params. Prefer a NEW session type or adapter until Work commits. |
| `Epistemos/State/ThreadState.swift` | +9: `parentSessionID:` threaded through `ensureMiniChatSession`/`upsertMiniChatSession` + 2 ChatThread build sites | New Chat session identity should flow THROUGH ThreadState (already carries parentSessionID) | Pin to CURRENT call signatures; do NOT change these signatures; coordinate before adding params. |
| `Epistemos/Views/Chat/ChatInputBar.swift` | +11: `@Environment(NotesUIState.self)` + `vault:none` "Connect…" grants-row branch (~1402) | The composer the new Chat reuses (shared-preserve) | Reuse READ-ONLY. Do not edit grants-row rendering until Work commits. Treat as adapter boundary. |
| `Epistemos/Views/Chat/ComposerCurrentAccessPlan.swift` | +16: synthetic `vault:none` `ComposerResourceGrantRow` when `vaultURL==nil` | Grant-rows model the composer reads | Reuse READ-ONLY; do not edit the grant-row assembly. |
| `Epistemos/Sync/VaultSyncService.swift` | +9/−2: `recoveryVaultURL` now returns `nil` (was `defaultRecoveryVaultURL`) | New Chat consumes `vaultURL` through this service | Treat the result as **Optional/nil-safe**; do not assume a non-nil fallback vault. |
| `EpistemosTests/ThreadStateTests.swift`, `EpistemosTests/CurrentAccessParityTests.swift` | Work-added `@Test`s for parentSessionID / vault:none row | New Chat may add tests in the same files | Add Chat tests in separate hunks; coordinate. |
| `EpistemosTests/OntologyRefactorRegressionGuardTests.swift` | ` M`: encodes CURRENT two-mode (act/work) ontology + recent-chats split | **Will fail when `.chat` case lands** — asserts the old two-surface reality | Update these guards as part of the `.chat` change; expect breakage until updated. |

### Additional uncommitted NON-Work edits (coordinate; not Work-surface, not chat-delete)

These ` M`/untracked files are unrelated to the Chat refactor but are live on shared `main` — do not stomp them, and watch the string surface for collisions:

| Path | State | Why it matters to new Chat |
|---|---|---|
| `Epistemos/Resources/Localizable.xcstrings` | ` M` | **Likely collision surface for any new Chat UI strings.** If the new route adds localized strings, add them in a separate, minimal hunk and re-verify `git diff` after; coordinate before touching. |
| `Epistemos/Engine/HTMLWorkspacePDFExporter.swift` | ` M` | Unrelated live edit; do not touch. |
| `EpistemosTests/HTMLWorkspaceSourceGuardTests.swift` | ` M` | Paired source-guard test for the exporter; do not touch. |
| `agent_core/src/lib.rs` | ` M` | Live Rust edit (agent core); out of CHAT scope; do not touch. |
| `agent_core/src/act_goose.rs` | untracked | New Rust file on disk; leave in place, do not delete (build-ordering). |

### Work-untouchable (never edit / call / `git clean`)

`Epistemos/Views/Settings/WorkCloneSettingsView.swift` (` M`, +12 Work-surface preview section) **PLUS three TRACKED-but-MODIFIED Work files the Work agent is actively editing** — `Epistemos/Work/WorkOpenCodeRuntime.swift` (` M`), `Epistemos/Work/WorkOpenCodeShell.swift` (` M`), `Epistemos/Work/WorkTerminalView.swift` (` M`) (+ `EpistemosTests/WorkOpenCodeRuntimeTests.swift` ` M`) — **PLUS 23 untracked `Epistemos/Work/*.swift`** (incl. `WorkWebSurfaceWindowController.swift`, `WorkNativeMCPHost.swift`, `WorkNativeMCPServer.swift`, `WorkNativeToolExecutor.swift`, `WorkSession*`, `WorkSessionRailView.swift`, `WorkSPA*`, `WorkOpenGUI*`, `WorkOpenWork*`, `WorkRuntimeSupervisor.swift`, `WorkSkillsProvisioner.swift`, `WorkToolMCPCore.swift`, `WorkEngineSurface*`, **`WorkEngineTranscript.swift`**) + ~14 untracked `EpistemosTests/Work*Tests.swift`. (`git ls-files --others --exclude-standard 'Epistemos/Work/' | wc -l` ⇒ **23**.) The three ` M` Work files are LIVE-EDIT COLLISION HAZARDS in the same class as the shared-in-flux set — never edit them. New Chat must NOT call `WorkWebSurfaceWindowController`, must NOT confuse `WorkSession`/`WorkSessionStore`/`WorkSessionRailView` with chat session identity, and must build its own recents rail.

### Collisions with deletion targets + safe ordering

1. **No deletion target is also a Work-edit target.** The new Chat's *reuse* of shared primitives (ChatInputBar, ComposerCurrentAccessPlan, ThreadState, ChatThread) overlaps Work edits — handle by reusing read-only and waiting for Work to commit, NOT by editing.
2. **Escalation channel is co-owned.** The **4** `.submitActOsaurusPrompt` posters (AppBootstrap.swift:1243, HologramSearchSidebar.swift:1255, NoteDetailWorkspaceView.swift:2566, MiniChatView.swift:2452) are CHAT/portal seams; the **single** RootView receiver (:3959) + ActEpistemosChatSurface + the channel NAME (EpistemosApp.swift:1510) are ACT. **Removing the channel must wait until ACT retires the receiver**, else portal escalations silently no-op.
3. **ChatView/ChatState cannot be deleted outright.** ACT still mounts `ChatView(actUsesOsaurus:true)` (RootView.swift:1565) and runs `runActOsaurusTurn` on ChatState. CHAT detaches the 3 bare `ChatView()` sites + the 2 `ChatSidebarView()` mounts + the regular `submitQuery`/EventBus path; the files die only after ACT migrates AND the private shared-preserve primitives (ChatBrainPanelView, StreamingIndicator, ChatBrainSnapshot) are extracted.
4. **Build-ordering hazard.** `EpistemosApp.swift:1576` and `WorkCloneSettingsView.swift:51` hard-reference the UNTRACKED `WorkWebSurfaceWindowController`. The 23 untracked Work files + ~14 untracked test files MUST stay on disk; a `git clean`/stash-include-untracked breaks the shared build. Synchronized-folder project: any incomplete new file on disk breaks the build for the Work agent too — keep new-Chat files compiling at every save.
5. **No commits.** All edits are uncommitted on shared `main` with no branch isolation. This recon is READ-ONLY. If the CHAT agent later commits, scope `git add` to ONLY its own new files and verify `git diff --cached` excludes the six shared-in-flux files, the three ` M` Work files, `WorkCloneSettingsView.swift`, `Localizable.xcstrings`, and the unrelated non-Work edits above.

---

## 5. Adapter boundary (in-process engine seam — NO HIDDEN SIDECAR)

The new Chat's ONLY inference path is an **in-process Epistemos provider**. Two facts anchor the seam:

- **Epistemos side:** `LocalAgentLoop.liveLoop(using:...)` (`LocalAgent/LocalAgentLoop.swift:232`, `.run` at :319) is the single in-process MLX chokepoint (`mlxStreamingGenerator` over `bootstrap.localMLXClient` + `constrainedDecoding`). Concrete construction pattern to mirror lives at `ChatCoordinator.runCommandCenterLocalAgentPath` (ChatCoordinator.swift:1174 → loop.run :1310). **NO subprocess** — MLX/Rust-FFI in-process. CRITICAL: the act=Osaurus swap is injected INSIDE liveLoop (LocalAgentLoop.swift:250–271 via `SharedActInference.actStreamIfArmed`) and is ACT-owned. The new Chat adapter targets the **MLX generator path WITHOUT re-triggering `shouldRouteActThroughOsaurus()`**, or regular Chat would silently route through the Act-owned Osaurus engine. Engine reuse and Act removal are entangled here — coordinate the boundary with ACT.

- **Swarm side (MIT, verified `LICENSE:3` "Copyright (c) 2025 Christopher Karani"; Swift 6.2; `.macOS(.v26)`):** Swarm owns tool orchestration; Epistemos supplies inference. The plug point is **`protocol InferenceProvider`** (`Sources/Swarm/Core/AgentRuntime.swift:221`, confirmed) declaring **exactly 3 required methods**, all with an `options:` parameter:
  - `generate(prompt: String, options: InferenceOptions) async throws -> String` (:228)
  - `stream(prompt: String, options: InferenceOptions) -> AsyncThrowingStream<String, Error>` (:235)
  - `generateWithToolCalls(...)` (:244) — defaulted via prompt-emulation, so an adapter need not implement it.

  Mirror **`TextOnlyConversationInferenceProviderAdapter`** (`Sources/Swarm/Providers/TextOnlyConversationInferenceProviderAdapter.swift:9–59`): an `EpistemosInProcessProvider: InferenceProvider` that implements **`generate(prompt:options:)` + `stream(prompt:options:)`** (note: the full signatures carry `options: InferenceOptions` — honor the parameter, do not drop it) over `MLXInferenceService`/liveLoop's MLX path, wrapped so Swarm owns orchestration. Report **`.privateInference`** capability (`ConversationInferenceProvider.swift:32`, `1<<5`) so `Agent.resolvedPrivateInferenceProvider` (Agent.swift:1063–1073) accepts it.

**How Swarm's provider abstraction plugs in:** `Agent` (`Sources/Swarm/Agents/Agent.swift:46`) resolves providers in order — **explicit `inferenceProvider:` injection wins (step 1, Agent.swift:986)**, so the Chat passes its in-process provider directly and **never reaches Conduit/cloud**. Use per-Agent injection (`Agent(_:inferenceProvider:tools:)`, Agent.swift:425–449) over the global `Swarm.configure(provider:)` (SwarmConfiguration.swift:78) to avoid global mutable state. **`Swarm.configure(cloudProvider:)` must NEVER be set.**

**TRAP — must exclude (Swarm `Providers/Conduit/*`, retire):** `ConduitInferenceProvider.swift` (`import Conduit`) and `MLXPromptProvider.swift:14` (`Conduit(Provider.mlx())` — LOOKS in-process but pulls Conduit) both violate NO-HIDDEN-SIDECAR. Build Swarm **core-only** (`SWARM_CORE_ONLY=1`, Package.swift:86–98 `swarmCoreOnlyExcludes`) → the exclude array drops **Integration/Wax, Integration/Membrane (SessionMembraneAgentAdapter / WaxMembraneStorage), Internal/GraphRuntime, Memory/ContextCoreMemory + Memory/DefaultAgentMemory, Providers/Conduit, Tools/Web, and the Workflow durable bits** (CheckpointCodec/CheckpointStore/DurableEngine). **NOTE: `Hive` is NOT in the core-only exclude array** — it is gated by the separate `integrationTrait`, not by `SWARM_CORE_ONLY=1`; do not claim core-only "drops Hive." The result is a pure-Swift core with `SlidingWindowMemory` fallback (Agent.swift:1156). Do NOT vendor or call the Conduit directory; it is replaced 1:1 by the single Epistemos in-process provider. (Source version is Swarm **0.6.0** / Conduit 0.3.17 — trust source over the donor's stale 0.5.1 CLAUDE.md.)

---

## 6. New Chat route plan (smallest Swarm-backed route that compiles)

The smallest Epistemos-native Chat route, mapping minimum-proof items 1–9. References new-chat-target seams from recon. Lands as the **deliberate Chat/Act/Work chooser** — no "tap anywhere" default; landing remains the only top-level shell.

| # | Min-proof item | New-chat-target seam | What to build |
|---|---|---|---|
| 1 | **Landing route** | `WorkspaceModeKind` enum (`ModeEntryTransition.swift:9–11`, confirmed `.act`/`.work` only) + `HomeRouter.body` (RootView.swift:3867) + `WorkspaceModeToggle` (RootView.swift:4009) + `WorkspaceModePicker` (WorkspaceModeSelection.swift:46) + `WorkspaceModeSelection` persistence | Add a `.chat` case to `WorkspaceModeKind`; add a third `HomeRouter` branch mounting `ChatRouteView`; the toggle/picker become the deliberate Chat/Act/Work chooser. (Touches OntologyRefactorRegressionGuardTests + ModeEntry tests — update together.) |
| 2 | **Prompt** | `Agent.run/stream` String input (Agent.swift:460/533) | New `ChatRouteView` composer (reuse ChatInputBar read-only when Work commits; until then a minimal mono input) feeds a String prompt to the Swarm `Agent`. |
| 3 | **Stream** | `Agent.stream(...)→AsyncThrowingStream<AgentEvent,Error>` (Agent.swift:533); `AgentEvent.output.token/.chunk` (AgentEvent.swift:93–95) | Adapt `.output.token/.chunk` → transcript via `DisplayPacedTextBuffer` + `makeChatTranscriptRows` (adopted into adapter). STREAM-EVERYTHING: forward tokens immediately. |
| 4 | **Transcript** | `MessageBubble` + `AssistantInlineTranscriptView` + `persistChatCompletion` (ChatCoordinator.swift:6142) | Render rows with MessageBubble; persist each turn via the canonical writer so recents/reopen + vault markdown work for free. |
| 5 | **Cancellation** | `Agent.cancel()` (Agent.swift:523) + `AgentEvent.lifecycle.cancelled` (AgentEvent.swift:63) | Wire stop button → `agent.cancel()` + Task cancellation; render `.cancelled`. |
| 6 | **Tool events** | `AgentEvent.tool.started/.partial/.completed/.failed` (AgentEvent.swift:77–89) + `ToolActivityNarrator` + `ProcessDisclosureViews` + `ApprovalModalView` | Map tool events → flat `[TITLE]` cards via the narrator; pause/approve via `ApprovalModalView` → its real `onResolve`/`PendingApproval` resolver (in-process). **Do NOT wire to `RustAgentBridge.resolveApproval` — that symbol is comment-only (ApprovalModalView.swift:25) and does not exist; identify the concrete resolver first.** |
| 7 | **Native MCP** | `SwarmMCPToolMapper` (SwarmMCP/SwarmMCPToolMapper.swift:14) + `SwarmMCPToolAdapter` catalog/executor (+ `.approvalRequired`/`.permissionDenied`) | Expose Epistemos-native tools as `FunctionTool` closures (Tool.swift:1110 — no `@Tool` macro / swift-syntax needed); map to MCP.Tool via the mapper (wire-compatible: both pin swift-sdk 0.12.1). Route tool approvals through the existing Approval UI's real resolver. **Investigate** `SwarmMCPServerService` overlap with Epistemos's own MCP host (Omega/MCPBridge) before adopting outbound hosting. |
| 8 | **Native recents / session identity** | `SDChat`/`SDMessage`/`recentChatsDescriptor` + `AgentSessionLineageStore` + a thin Swarm `Session` (Session.swift:31–90) — **REPLACES the old `ChatSidebarView` (RootView.swift:1535/:3861)** | Build a NEW native recents rail reading `recentChatsDescriptor` (the same query `ChatSidebarView` used) — do NOT reuse `ChatSidebarView` itself (chat-delete). Implement a thin `Session: Actor` over Epistemos chat persistence (do NOT adopt Swarm's SwiftDataMemory/Wax). SDChat stays the recents source of truth; derive lineage from AgentSessionLineageStore (and `ChatThread.parentSessionID` once Work commits). |
| 9 | **Old-destination removal** | bare `ChatView()` sites (RootView.swift:3876/3909/3998) + `ChatSidebarView()` mounts (RootView.swift:1535/:3861) + `submitLandingSearch→MainChatSubmissionRouter` (LandingView.swift:2008/2040) + portal inline-send paths | Detach all old regular-chat edges once the new route streams; delete after ACT migrates off ChatView/ChatState. REAL removal, not hiding. |

Notes the route must honor: cover the **App Store branch** too (`shouldRouteActThroughOsaurus()` returns false on MAS → currently falls to plain `ChatView()` at 3876/3998; the new route must serve MAS or those users lose Chat). The route needs its **own toolbar gating branch** (RootView.swift:1061–1137) or the back-chevron/title/glass leak across surfaces. Run Swarm **core-only**, rely on `SlidingWindowMemory` (or a thin Epistemos Memory over Halo/Shadow) — do NOT pull Wax/ContextCore.

---

## 7. Staged deletion / replacement sequence

Build new first, reroute portals, delete old last — only after the replacement compiles. REAL removal.

1. **BUILD NEW ROUTE FIRST (no deletions).** Author `EpistemosInProcessProvider: InferenceProvider` over `MLXInferenceService`/`LocalAgentLoop.liveLoop`'s MLX path (LocalAgentLoop.swift:232), mirroring `TextOnlyConversationInferenceProviderAdapter`; implement `generate(prompt:options:)` + `stream(prompt:options:)` (honor `options:`); report `.privateInference`; never re-trigger `shouldRouteActThroughOsaurus()`. Vendor Swarm core-only (`SWARM_CORE_ONLY=1`), exclude `Providers/Conduit/*`. Build `ChatRouteView` + a thin `Session` over Epistemos persistence. Adopt (do not re-implement) `DisplayPacedTextBuffer`/`StreamingReasoningTraceBuffer`/`makeChatTranscriptRows`/`ChatPresentationFormatter` into the adapter. Reuse `MessageBubble`/`AssistantInlineTranscriptView`/`ProcessDisclosureViews`/`ToolActivityNarrator`/`ApprovalModalView`/`InlineRuntimePickerPanel(showsOsaurusModelSection:false)` read-only. Persist every turn via `ChatCoordinator.persistChatCompletion` (ChatCoordinator.swift:6142) so recents/vault work. Keep every new file compiling at each save (synchronized-folder build).

2. **EXTRACT shared-preserve primitives that are PRIVATE inside chat-delete files**, to standalone files, BEFORE any deletion: `ChatBrainPanelView`/`BrainPanelSection` (ChatView.swift:1802–2199), `StreamingIndicator` (ChatView.swift:1709–1800), and `ChatBrainSnapshot`/`ChatBrainSection`/`CapturedModelInput` + per-chat snapshot persistence (ChatState.swift:5–130, 396–519). Without this, deleting ChatView.swift/ChatState.swift drops the transparency panel + streaming surface + snapshot history.

3. **ADD the `.chat` ontology:** add `case chat` to `WorkspaceModeKind` (ModeEntryTransition.swift:9), extend `WorkspaceModeSelection`/`isArmed` + `WorkspaceModeToggle` (RootView.swift:4009) + `WorkspaceModePicker` (WorkspaceModeSelection.swift:46) into a deliberate Chat/Act/Work chooser, and add a third `HomeRouter` branch (RootView.swift:3867) mounting `ChatRouteView` with its OWN toolbar gating branch (mirroring RootView.swift:1061–1137) + `.blurFade` transition. Build the new native recents rail (reads `recentChatsDescriptor`) alongside it. Cover the App Store branch. Update `OntologyRefactorRegressionGuardTests` + ModeEntry tests in the same change (they assert the old two-mode reality and will fail until updated).

4. **RETIRE the click-anywhere default ontology together** (or the greeting cycle/theme docs disagree): remove LandingView Background Tap Layer (LandingView.swift:362–390); change `LiquidGreeting` `promptPair` copy (LiquidGreeting.swift:82); replace UIState `LandingGreetingPhrase` 'click anywhere…' (UIState.swift:91); update `EpistemosTheme.swift` comment copy (538/550). Landing stays the only top-level shell; no 'tap anywhere to start a conversation' default.

5. **REROUTE landing into the new route:** in `submitLandingSearch` (LandingView.swift:2008) repoint the chat-delete branch (`chat.startNewChat()`+`MainChatSubmissionRouter.submit` at :2030/:2040) to the new Chat route. DO NOT TOUCH the `onSubmitActPrompt` branch (:2020–2022) — ACT-owned; they share one function, so coordinate with ACT before editing. Repoint `LandingFarmView.onStartChat` (`startFarmAgentChat`) away from `activateLandingSearch`.

6. **REROUTE the mini/graph/note portals into the new Chat session** (portals, not private renderers): repoint `MiniChatWindowController` content host + default-mode wiring; repoint `PinnedInspector.sendMessage` (HologramSearchSidebar inline branch :1265) and NoteDetailWorkspaceView inline asks (:2551) and `DialogueChatState` graph dialogue at the new Chat portal/session identity (preserve `NoteChatState`'s inline NSTextStorage accept/discard UX). Leave the agent/research/Act escalation branches (the **4** `.submitActOsaurusPrompt` posters) intact — ACT owns the receiver; do not remove the channel yet.

7. **DETACH the old regular-chat destination once the new route compiles and streams in-process:** remove the 3 bare `ChatView()` sites (RootView.swift:3876/3909/3998), the 2 `ChatSidebarView()` mounts (RootView.swift:1535/:3861), and the regular `submitQuery`/EventBus path (`ChatState.submitQuery`, `completeProcessing`, `startStreaming`, `MainChatSubmissionRouter`, `ChatCoordinator.handleQuery` at :1785). Do NOT delete `persistChatCompletion` (the shared recents sink Act/agent also use) and do NOT delete the `actUsesOsaurus:true` site at RootView.swift:1565.

8. **HAND OFF act-delete to the ACT agent (do not delete yourself):** `ActEpistemosChatSurface` (RootView.swift:1547), `runActOsaurusTurn` + `actTurnTask`/`persistActTurn` (ChatState.swift:740/604), ChatView `actUsesOsaurus` branches + the ~920 lines of Act native sheets (ChatView.swift:783–1705), `SharedActInference`, `ActTurnStreamCore`, `ActTurnStatsStore`, the `.submitActOsaurusPrompt` RECEIVER (RootView.swift:3959), and the agent-tier path (`handleCommandCenterSubmission` / `runRustAgentPath` / `persistCompletedAgentTurn`). The `.submitActOsaurusPrompt` channel + its NAME (EpistemosApp.swift:1510) retire only AFTER ACT removes the receiver.

9. **DELETE old files once BOTH the new route compiles AND ACT has migrated off ChatView/ChatState:** delete `ChatView.swift`, `ChatState.swift` (regular wiring), `ChatSidebarView.swift`, `MiniChatView`/`MiniChatWindowController`, `DialogueChatState`, `NoteChatState`, `PinnedInspector` chat portions — REAL removal, not hiding. Verify primitives from step 2 are extracted first.

10. **COORDINATION GATES throughout:** do NOT edit any shared-in-flux file (EpistemosApp.swift, ChatTypes.swift, ThreadState.swift, ChatInputBar.swift, ComposerCurrentAccessPlan.swift, VaultSyncService.swift) until Work commits; reuse them read-only. Do NOT edit the three ` M` Work files (WorkOpenCodeRuntime.swift, WorkOpenCodeShell.swift, WorkTerminalView.swift) or WorkCloneSettingsView.swift. Do NOT touch the unrelated non-Work edits (Localizable.xcstrings, HTMLWorkspacePDFExporter.swift, agent_core/src/lib.rs) — if the new route needs Chat strings, add them to Localizable.xcstrings in a separate minimal hunk and re-verify the diff. Add the new Chat `⌘N` command to `EpistemosCommands` only after Work's `⌘4` hunk lands, in a separate hunk, not reusing `⌘4`. Never edit/call `Epistemos/Work/*` or `WorkWebSurfaceWindowController`; do not `git clean`/stash-drop the 23 untracked Work files. If committing, scope `git add` to only new Chat files and verify `git diff --cached` excludes the six shared-in-flux files, the three ` M` Work files, WorkCloneSettingsView.swift, Localizable.xcstrings, and the non-Work edits.

---

## 8. Open questions (need an explicit owner decision before acting)

1. **AgentChatState / Agent Command Center fate:** is ACC a surviving SEPARATE surface (PLAN_V2 line 169) or does it fold into Act? It looks like chat state but must NOT be chat-deleted; deleting it breaks `AgentBlueprintSettingsView` + `AppBootstrap`. Needs an explicit ACT/ACC decision before any change.
2. **DialogueChatState persona box:** keep the graph 'node creature' persona/archetype engine + FFI mouth-animation hook (`onStreamingChanged` into MetalGraphView/HologramOverlay/HologramController) as a portal into the new Chat, or retire the persona box entirely? Determines whether the Metal-view bindings get re-pointed or removed — coordinate with the graph/Work owner.
3. **SwarmMCPServerService overlap:** Epistemos already hosts an MCP server (Omega/MCPBridge). Does the new Chat reuse the inbound `SwarmMCPToolMapper` only, or also adopt Swarm's outbound MCP server actor (risking two competing hosts)? Investigate before wiring native MCP item 7.
4. **Approval resolver seam:** the real in-process approval resolver must be identified — `RustAgentBridge.resolveApproval` is comment-only (ApprovalModalView.swift:25) and does not exist. Confirm the concrete resolver (`ApprovalModalView.onResolve`/`PendingApproval` path) the new route binds to before wiring items 6/7.
5. **Persistence migration:** each portal persists under its own `SDChat` chatType (`'miniChat'` thread-type, `'dialogue'`, `'notes'`). Unifying under one Chat session identity risks orphaning previously-persisted rows from history. Is a migration needed, or do old portal chats remain readable under their existing types?
6. **App Store branch coverage:** `shouldRouteActThroughOsaurus()` is false on MAS, where landing currently falls back to plain `ChatView()` (RootView.swift:3876/3998). Confirm the new in-process Chat route serves MAS (the Osaurus engine is absent there) so App Store users don't lose the Chat surface.
7. **NoteChatState inline UX:** the note portal streams INLINE into NSTextStorage with accept/discard divider semantics, not a bubble list. Confirm the portal can share session identity while preserving inline-edit ergonomics (a naive transcript-view swap breaks note UX).

---

### Appendix — verification done for this recon (read-only)

Spot-checked on HEAD `8af17c841`: `WorkspaceModeKind` is `.act`/`.work` only (ModeEntryTransition.swift:9–11); 3 bare `ChatView()` at RootView.swift:3876/3909/3998 + 2 `ChatSidebarView()` mounts at :1535/:3861 (+ embed ChatView.swift:579) + 1 `actUsesOsaurus:true` at :1565; `struct ChatSidebarView` at ChatSidebarView.swift:31; `HomeRouter` :3814, `WorkspaceModeToggle` :4009; `persistChatCompletion` :6142 + `handleQuery` :1785; `submitLandingSearch` :2008 / `MainChatSubmissionRouter.submit` :2040 / `runActOsaurusTurn` :740 / `MainChatSubmissionRouter` enum :1782; `.submitActOsaurusPrompt` — **1 receiver (RootView:3959) + 4 posters** (AppBootstrap:1243, HologramSearchSidebar:1255, NoteDetailWorkspaceView:2566, MiniChatView:2452) + channel NAME (EpistemosApp.swift:1510); Swarm `LICENSE:3` MIT, `InferenceProvider` at AgentRuntime.swift:221 declaring 3 methods (`generate(prompt:options:)`/`stream(prompt:options:)`/`generateWithToolCalls`); `swarmCoreOnlyExcludes` (Package.swift:86–98) lists Wax/Membrane/GraphRuntime/ContextCoreMemory/DefaultAgentMemory/Conduit/Tools-Web/Workflow-durable and **NOT Hive**. `MiniChatView.swift` is **3219** lines. `git status` confirmed the six shared-in-flux files; **three ` M` tracked Work files** (WorkOpenCodeRuntime/WorkOpenCodeShell/WorkTerminalView) + WorkCloneSettingsView; **23 untracked `Epistemos/Work/*.swift`** (`git ls-files --others --exclude-standard 'Epistemos/Work/' | wc -l` ⇒ 23, `WorkEngineTranscript.swift` included); plus unrelated ` M` non-Work edits (Localizable.xcstrings, HTMLWorkspacePDFExporter.swift + its source-guard test, agent_core/src/lib.rs) and untracked `agent_core/src/act_goose.rs`.
