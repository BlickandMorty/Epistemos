---
state: candidate
created_on: 2026-05-24
purpose: Builder-facing synthesis of the Erdős unit-distance result, OpenAI Parameter Golf, and the Epistemos Unified Cognitive Substrate into one no-compromise invariant, one manageable tier system, and a concrete W/P/T backlog.
promotion_rule: This doc does not promote new laws or theorems to canon by itself. ShadowProjection, L8, E8, E9, T28, W-Lift-N, and F-Erdos-Lift-Optimality remain candidate until local falsifiers and production caller chains exist.
local_anchors:
  - docs/fusion/MASTER_RESEARCH_INDEX_2026_05_02.md
  - docs/fusion/ADDRESSABLE_NEURAL_SUBSTRATE_CANON_2026_05_24.md
  - docs/fusion/UNIFIED_ACTIVE_SUBSTRATE_CANON_2026_05_16.md
  - docs/HELIOS_V5_DOC_6_THEOREM_CANON.md
  - docs/HELIOS_V6_1_NEW_RESEARCH_INTEGRATION_2026_05_16.md
  - docs/fusion/PRIMITIVE_IR_STACK_DOCTRINE_2026_05_17.md
  - docs/fusion/ONLINE_RESEARCH_INTAKE_SHADOW_PROJECTION_2026_05_24.md
  - docs/PHASE_2_TERMINAL_PROMPTS_2026_05_23.md
---

> **2026-06-01 current canon bridge (JUNE1-PATTERNBOOST-LOCK):** This file is preserved as a legacy, planning, research, or witness artifact. For active architecture, route Helios/UAS/ACS/mmap/KV-Direct/70B/NeuralImportance claims through `docs/fusion/RESIDENCY_PATTERNBOOST_DISCOVERY_2026_06_01.md`, `docs/falsifiers/F-RESIDENCY-PATTERNBOOST-BUNDLE_2026_06_01.md`, `docs/fusion/SEMANTIC_WORKING_SET_COMPILER_2026_06_01.md`, and `docs/fusion/COLDSTREAM_RESIDENCY_TRANSPORT_2026_06_01.md`. Legacy claims remain historical until promoted by falsifiers, AnswerPacket evidence, LatticeAbstentionGate, ComputeResumeLease, rollback, and the intentional-copy/zero-copy caveat.

# Shadow Projection + Research Construction Doctrine — 2026-05-24

## 0. No-compromise boundary

This synthesis exists to prevent the architecture from fragmenting into many brilliant but disconnected modules.

It does **not** say:

- every object is literally an EML tree
- the Erdős result proves Epistemos is correct
- Parameter Golf proves ternary or Sherry/Leech VQ is production-safe on the user's M2 Pro
- Pro Research ideas can skip WRV, falsifiers, SCOPE-Rex/SovereignGate admission, or App Store lane rules

It does say:

- the most coherent architectural language is now **one typed substrate object with three possible motions**
- Erdős-style construction search and Parameter-Golf-style compression search are the same product primitive seen from two sides
- performance and intelligence should be optimized by the same loop: **lift, search, project, witness, promote**
- every new grand claim needs a named falsifier before an agent builds it

External source floor:

- [OpenAI unit-distance announcement](https://openai.com/index/model-disproves-discrete-geometry-conjecture/) — the planar unit-distance problem, first posed by Erdős in 1946; OpenAI says an internal model found an infinite family beating the long-held grid-style bound, checked by external mathematicians.
- [Will Sawin, arXiv:2605.20579](https://arxiv.org/abs/2605.20579) — explicit lower bound: arbitrarily large point sets with more than `n^1.014` unit-distance pairs; method is number-theoretic.
- [OpenAI Parameter Golf GitHub](https://github.com/openai/parameter-golf) — 16 MB artifact, 10 minutes on 8xH100, FineWeb bits-per-byte; approaches include parameter tying, depth recurrence, low-rank training, quantization, bitnets, tokenizers, test-time training, long context, and megakernels.
- [OpenAI Parameter Golf writeup](https://openai.com/index/what-parameter-golf-taught-us/) — public synthesis of the challenge's lessons.
- [Online Research Intake](ONLINE_RESEARCH_INTAKE_SHADOW_PROJECTION_2026_05_24.md) — source credibility ladder and public fork/PR mining protocol.

Local source floor:

- `docs/fusion/ADDRESSABLE_NEURAL_SUBSTRATE_CANON_2026_05_24.md` — canonical target for addressable neural clusters, SSM routing, L3 SSD Oracle, and 70B local capability ceiling.
- `docs/fusion/UNIFIED_ACTIVE_SUBSTRATE_CANON_2026_05_16.md` — legacy UAS-ACS canon, read forward as UAS + AcsAnchor in one addressable, recursively governed substrate.
- `docs/HELIOS_V5_DOC_6_THEOREM_CANON.md` — E1-E7, H1-H17, PCF-1..10 theorem namespace and promotion discipline.
- `docs/HELIOS_V6_1_NEW_RESEARCH_INTEGRATION_2026_05_16.md` — L3 SSD Oracle, KV-Direct, L_SE, V6.1/V6.2 falsifier framing.
- `docs/fusion/PRIMITIVE_IR_STACK_DOCTRINE_2026_05_17.md` — EML, Tropical, Scan, Operator, Info, Geometry IR stack; EML is powerful but not a replacement for every domain.
- `docs/PHASE_2_TERMINAL_PROMPTS_2026_05_23.md` — A-G production wiring deck; current WRV and falsifier gates.

## 1. The one invariant: Substrate Motion Invariant

**Substrate Motion Invariant (SMI).** Every meaningful Epistemos object is one substrate object with:

1. a stable `UasAddress`
2. a `RuntimePlane` placement: State, Episodic, Assembly, Controller, Verification
3. a `ProductBuild`: MAS or Pro
4. a `ProStatus` / `ResidencyStatus`: Live, Gated, ResearchCandidate,
   VaultPreserved, Omega, CurrentApp, VerifiedFloor, CapabilityCeiling, or
   explicit exemption
5. an optional `LatticeBudget` / WBO error account if approximate
6. a witness surface: `RunEventLog`, `AnswerPacket`, `ClaimGraph`, `WboLedgerEntry`, falsifier artifact, or Lean proof

Every operation on that object is exactly one of three motions:

| Motion | Direction | Plain meaning | Examples | Required witness |
|---|---|---|---|---|
| **Lift / Ingest** | surface → substrate | Put raw material into the unified substrate. | note bytes → note object; image pixels → tile/vector object; user prompt → `ProblemCard`; model output → `AnswerPacket`; local trace → `RunEventLog` | UAS address + source hash + plane |
| **Project / Compress / Recall** | substrate → surface | Make the object cheaper, smaller, or visible in a specific chart. | vault recall; Eidos citation; graph neighborhood; MLX context pack; Metal buffer; EML term; proof badge; UI row | `ShadowProjection` + WBO budget + citation/proof |
| **Mutate / Promote** | substrate → substrate | Change durable state or promote a candidate into stronger authority. | adapter update; SCOPE-Rex/SovereignGate-approved tool action; kernel promotion; falsifier PASS; Lean certificate; nightly research result | `MutationEnvelope` + SCOPE-Rex/SovereignGate verdict + rollback |

This is the simplest language that preserves the original DNA. It keeps UAS, AcsAnchor, ColdStore, EML, WBO, ActiveAssembly, KV-Direct, Lean, AnswerPacket, and Research Construction, but stops treating them as separate moving parts.

## 1A. Addressable Neural Substrate target

The Substrate Motion Invariant includes the user's original "dormant brain neuron pull" target. See `docs/fusion/ADDRESSABLE_NEURAL_SUBSTRATE_CANON_2026_05_24.md`.

Canonical target:

> Epistemos turns a dense model into an addressable neural substrate. An SSM/state router selects active assemblies of layers, rank-one components, KV pages, adapters, residual islands, and kernels; the residency governor pages only that working set into UMA; verification proves the selected path preserves dense/reference behavior within a budget.

Builder translation:

- **State plane**: SSM / hybrid-SSM semantic router decides whether normal decode, attention, retrieval, tools, adapter, connectome lookup, or dense fallback is needed.
- **Assembly plane**: Gate3 / PacketRouter1bit / connectome anchors select the active neural assembly.
- **Episodic plane**: vault, graph, KV pages, theorem witnesses, tool traces, and claim ledgers provide exact support.
- **Controller plane**: SCOPE-Rex/SovereignGate admits the route, tool calls, mutation, kernel promotion, and fallback.
- **Verification plane**: WBO, AnswerPacket, ClaimKind, RunEventLog, falsifiers, and Lean/EML proofs check drift.

This is canon as the target architecture and vocabulary. It is not a shipped capability until `F-Sparse-Runtime-Split`, `F-KV-Direct-Gate`, `F-UAS-CopyCount`, `F-ActiveAssembly-Minimal`, `F-ULP-Oracle`, and `F-70B-Local-Cocktail` pass.

## 2. The two external results as one shape

| Axis | Erdős unit-distance result | Parameter Golf | Epistemos unifier |
|---|---|---|---|
| Constraint | `n` points in a plane | 16 MB artifact + short training window | bounded memory / latency / trust budget |
| Goal | maximize unit-distance pairs | minimize bits-per-byte loss | maximize useful cognition per byte, per token, per witness |
| Winning move | import algebraic number theory into geometry | search architectures, recurrence, quantization, tokenizers, TTT | choose the best coordinate chart and route |
| Local primitive | `ShadowProjection<H,L>` | active/compressed model component | UAS object with projection + WBO |
| Human role | verify and refine the construction | reproduce, compare, and rank submissions | SCOPE-Rex/SovereignGate + falsifier + Lean / tests promote only checked wins |

The no-compromise claim is not "Erdős proves the app." The claim is:

> Construction search and compression search are the same substrate operation. A useful system should search for better coordinates, not merely bigger models.

## 3. ShadowProjection as candidate primitive

```rust
struct ShadowProjection<H, L> {
    high_coord: H,
    low_surface: L,
    projection_id: ProjectionId,
    inverse_lift_hint: Option<LiftHint>,
    wbo_budget: LatticeBudget,
    witness: WitnessRef,
}
```

`ShadowProjection<H,L>` is the candidate umbrella primitive for:

- vault note → chat citation
- note/image/graph object → vector search hit
- graph neighborhood → UI subgraph
- KV page → active decode window
- EML / Geometry / Scan IR term → Metal kernel
- model component cluster → adapter / active assembly packet
- research problem → construction candidate
- construction candidate → falsifier or Lean proof

Status: **candidate**. Do not treat it as production until T28 and W-Lift-N land.

## 4. Candidate canon additions

| Candidate | Meaning | Promotion gate |
|---|---|---|
| **L8? Shadow Projection** | Every projection from substrate coordinate to user surface preserves source coordinate, pays WBO, and is reversible up to budget. | `F-Erdos-Lift-Optimality` + at least one production caller |
| **E8? Lift-and-Project Advantage** | Some structured tasks are solved better by moving to a richer coordinate chart and projecting back. | Benchmark across recall, Active Assembly, routing, KV gather, kernel selection |
| **E9? Shadow-Witness Closure** | A projected artifact carries enough metadata to reconstruct its substrate coordinate up to WBO. | AnswerPacket + VaultRecall + graph + pixel examples round-trip |
| **T28 Shadow Projection Primitive** | Implement typed lift/project pairs over UAS, vault, graph, pixel, MLX/KV, EML, Geometry, AnswerPacket. | New worktree after Phase 2 WRV production wires |
| **W-Lift-N** | One W-row per production surface that consumes or exposes a shadow projection. | PR body cites caller, visible surface, falsifier |
| **F-Erdos-Lift-Optimality** | Lift/project route beats surface-native baseline across representative tasks. | M2 Pro artifact bundle, reproducible |

## 5. Two-build system and Pro internal ladder

Active build planning has exactly two distributable builds. MAS is the safe
public floor. Pro contains the advanced internal status ladder. Research and
Vault are not separate app builds.

| Build/status | Ships where | Allowed by default | Examples | Promotion requirement |
|---|---|---|---|---|
| **MAS** | MAS build | ON only when behavior is equivalent, safe, visible, and tested | UAS address metadata, WBO ledger, AnswerPacket badge, verified Eidos/VaultRecall surfaces | WRV green + falsifier PASS or parity proof |
| **Pro Live** | Pro build | ON for Pro when verified and visible | Pro-only tools with policy, provenance, and rollback | focused tests + visible route/admission evidence |
| **Pro Gated** | Pro build | OFF by default; user opts in | BitNet b1.58 bundled model, T-MAC ternary path, sparse ternary GEMM, experimental Metal kernels, power-user local model modes | local bench + rollback + honest HealthRow chip |
| **Pro Research** | Pro/private harness | never silently product | T26 L_SE, ShadowProjection<T>, Research Construction Engine, conjecture mode, active-rank-one runtime, auto-adapter mutation | falsifier spec + artifact + user approval |
| **Pro Vault-Preserved** | docs or branch archive | no runtime authority | speculative kernels, failed constructions, unverified theorem candidates, retired worktrees | re-promotion codeword + new falsifier |
| **Pro Omega** | private experimental Pro band | never MAS-bound, never silent | 70B cocktail, model-internals routing, private kernel experiments | crash-safe harness + explicit opt-in + reversible witness |

No agent may promote Pro Gated, Pro Research, Pro Vault-Preserved, or Pro
Omega work by citing Erdős or Parameter Golf alone. External results license a
candidate workstream; local falsifiers promote it.

## 6. Obvious wins backlog

These W-PERF rows are the builder-facing section. Each should become a PR, terminal task, or worktree item only with WRV and falsifiers.

| ID | Win | Build target | Build/status now | Falsifier / gate |
|---|---|---|---|---|
| **W-PERF-1** | Active Assembly as the main performance lever | Production caller before chat / agent / proof work wakes only the smallest support set. | MAS/Pro Live candidate only after caller proof | `F-ActiveAssembly-Minimal` |
| **W-PERF-2** | Sherry/Leech lattice VQ + ternary path | Promote research quantization into a flagged model route only after local quality and copy-count gates. | Pro Gated candidate | `F-UAS-CopyCount`, perplexity/quality rollback |
| **W-PERF-3** | KV-Direct as canonical zero-copy lift | Treat Swift/Rust/Metal/MLX KV movement as one projection with a bit-equivalence gate. | Pro Gated until equivalent; MAS only if bit-equivalent and safe | `F-KV-Direct-Gate` |
| **W-PERF-4** | RRF k=60 as a ShadowProjection | Collapse bespoke retrieval surfaces into one typed projection contract. | MAS / Pro Live when visible and verified | `F-VaultRecall-50`, `F-Eidos-Bridge-RoundTrip` |
| **W-PERF-5** | IR stack as chart atlas | Keep EML, Geometry, Scan, Tropical, Operator, Info; add explicit lift/project operators instead of parallel drift. | Pro Research -> Pro Gated per kernel | `F-ULP-Oracle`, `F-SemiseparableBlockScan-Correctness`, kernel parity |
| **W-PERF-6** | Counterexample-driven hardening | Research Construction Engine searches for failures in W/F/T claims before shipping. | Pro Research | falsifier artifact must improve or quarantine |
| **W-PERF-7** | Proof-sized AnswerPackets | Store claim + construction + citations + proof/falsifier status; avoid bloated trace replay. | MAS / Pro Live | AnswerPacket UI + RunEventLog replay |
| **W-PERF-8** | L_SE self-evolving adapter lane | Auto-specialization is small adapter deltas, not base model mutation. | Pro Research | WBO drift, rollback, user approval |
| **W-PERF-9** | PageGather sketch → residual → exact | Retrieval primitive for vault and long context; reduce disk read amplification. | Pro Gated candidate | `F-PageGather-M2Pro` |
| **W-PERF-10** | Mamba / SSM as Scan-IR shadow | SSM scan state, KV state, and Active Assembly state share the lift/project surface. | Pro Research / Pro Gated | `F-SemiseparableBlockScan-Correctness` |
| **W-PERF-11** | Compounding performance loop | W-PERF-2 shrinks weights; W-PERF-1 wakes less; W-PERF-3 preserves KV headroom; W-PERF-6 repairs failures. | orchestration | `F-Erdos-Lift-Optimality` |

## 7. Research Construction Engine

The new product lane is not "chat with papers." It is a local research compiler:

```text
ProblemCard
  → ActiveAssemblyPacket
  → ConstructionGraph
  → Falsifier / Lean / bench / human-review witness
  → AnswerPacket
  → promote / fork / quarantine
```

Required objects:

| Object | Purpose | Plane |
|---|---|---|
| `ProblemCard` | A bounded research problem with claim, baseline, budget, corpus, and allowed tools. | Controller |
| `ConstructionGraph` | Candidate constructions, failed branches, source links, negative evidence. | Assembly / Verification |
| `ResearchRunEvent` | Stepwise witnessed search trace. | Episodic |
| `PromotionLedgerEntry` | Why a construction became code, theorem, falsifier, or doc. | Verification |
| `ConstructionAnswerPacket` | User-visible result: claim, construction, sources, proof/falsifier, rollback path. | Verification |

Terminal H should own this after A-G Phase 2 wires enough production substrate to make it honest.

## 8. Dispatch-deck changes

Append these to `docs/PHASE_2_TERMINAL_PROMPTS_2026_05_23.md` in the next prompt deck revision:

| Terminal | Add-on from this doctrine |
|---|---|
| A — Eidos | Treat closed citations as witness coordinates for ShadowProjection. |
| B — VaultRecall + AnswerPacket | Surface lift coordinate, not just source text; RRF becomes one projection operator. |
| C — System G | Add `conjecture` MissionPacket only after real MissionPacket path is stable. Include `construction_space_radius`. |
| D — WRV panel | Add Construction Engine activity, Active Assembly hit ratio, WBO consumed, projection status. |
| E — SCOPE-Rex/SovereignGate gate | Verdicts include `lift_allowed`, `lift_denied`, `lift_quarantined`. |
| F — falsifiers | Add candidate `F-Erdos-Lift-Optimality`; run counterexample loop between passes. |
| G — T14 UAS no-orphan | Canonical home for `ShadowProjection<H,L>` and source-coordinate fields. |
| **H — Research Construction Engine** | New worktree: `ProblemCard → ConstructionGraph → verifier → AnswerPacket → promotion`. |

## 9. Agent build rule

Every agent PR touching this doctrine must include:

```text
No-Orphan check:
- Motion: Lift/Ingest | Project/Compress/Recall | Mutate/Promote
- UAS address:
- Plane:
- Residency:
- WBO/error policy:
- Witness:
- Falsifier:
- Tier:
- Rollback:
```

Any missing field is either a blocker or a written waiver.

## 10. Honest summary

The no-compromise architecture is best saved as one thing:

> **A unified cognitive substrate where every object has one address, every view is a projection, every approximation pays WBO, every mutation is admitted by ACS, every output is witnessed, and every research improvement enters through a falsifiable construction loop.**

The current app has many pieces of that substrate, but Phase 2 is still the caller-chain gap. This doctrine gives the next agents a compact invariant and a performance-first backlog so they build the ambition without spreading it into disconnected abstractions.

## 11. Online research intake

Public research now becomes a first-class input, but only through a narrow gate:

```text
source -> motif -> local primitive -> W/P/T/F row -> tier -> falsifier -> rollback -> implementation
```

The current intake doc is `docs/fusion/ONLINE_RESEARCH_INTAKE_SHADOW_PROJECTION_2026_05_24.md`.

Key rules:

- official sources and arXiv can define candidate doctrine, not production behavior
- GitHub forks and PRs are mined for motifs, not raw code
- forum and blog signals are discovery-only unless they point back to a primary paper or repo
- EML/Lean work enters through T5 custody, not ad hoc vendoring
- Parameter Golf motifs enter as W-PERF candidates, each with an M2 Pro falsifier
- Erdos/unit-distance demos are intuition only; the authoritative sources are OpenAI, arXiv companion remarks, and Sawin's explicit bound

This is the compromise-free path for public research: search widely, import carefully, falsify locally.

## 12. Substrate vs LLM — dispatcher, not reasoner (locked clarification)

A canon clarification asked by the user 2026-05-24 and locked here so future agents and Codex/Claude terminals do not drift on the distinction.

### 12.1 The one-sentence definition

> The substrate **dispatches** to the LLM as a typed executor with a witnessed input packet and a verified output packet. The LLM remains the reasoner. The substrate is everything that decides what gets loaded, what enters the context, what output is accepted, and what mutation is allowed to propagate.

### 12.2 Granularity of address — today vs endgame

The substrate addresses the LLM at progressively finer granularity over the release ladder. Today is whole-model; the endgame is component-level. Both stay honest by carrying the same Substrate Motion Invariant (Lift / Project / Mutate) at every granularity.

| Granularity | What the substrate addresses | Status today | Build/status |
|---|---|---|---|
| **Whole-model call** | An LLM identity (Qwen 3 8B / explicit provider / Claude / Apple Intelligence) routed by `RuntimeRouter` (T1) | LIVE (whole-model dispatch is the default) | MAS / Pro Live |
| **Output schema** | The token stream — what shapes are emitted-allowed | LIVE via `LocalToolGrammar` when `MLXStructured + CMLXStructured + JSONSchema` resolve | MAS / Pro Live partial |
| **KV cache pages** | The model's short-term memory — addressable, mmap'd, zero-copy across Swift/Rust/MLX/Metal | SUBSTRATE SHIPPED via `agent_core/src/scope_rex/kv/direct_gate.rs` + `Epistemos/Shaders/kv_direct_gate.metal`; harness pending (F-KV-Direct-Gate) | Pro Gated |
| **Weight quantization** | Bit-layout of weights — Sherry-Leech VQ, ternary b1.58, NF4 | PARTIAL (Pro Research; Parameter-Golf-licensed promotion to Pro Gated candidate per §6 W-PERF-2) | Pro Research -> Pro Gated candidate |
| **Adapter / LoRA delta** | Which task-specialized delta is applied on top of base weights | PARTIAL (L_SE = T26, research-deferred D-06; Erdős licenses bounded exploration per §4) | Pro Research -> Pro Gated license |
| **MoE expert** | Which expert handles a token (when the model is MoE) | DELEGATED — the *model* implements MoE routing; the substrate chooses which MoE model the call goes to | MAS / Pro Live when exposed honestly |
| **Active assembly across all of the above** | The thin slice of model + KV + adapter + context + tools awake for this query | RESEARCH (PCF-5 Active Rank-One Execution candidate; Active Assembly Selector in `agent_core/src/research/active_assembly`) | Pro Research candidate |
| **Attention head / SSM state** | Specific `W_QK^h` decomposition slice **or** a recurrent scan-state region used as a language-router gate (the "SSM that calls neurons per pass" the user described as the original no-compromise mechanism) | RESEARCH (PCF-2 QkEdgeAnchor + Goodfire VPD lineage revalidated 2026-05-07; SSM-router target in `ADDRESSABLE_NEURAL_SUBSTRATE_CANON_2026_05_24.md` §2A row 8 + §3 five-plane execution path) | Pro Research |
| **Parameter anchor** | A frozen-during-call subset of parameters | RESEARCH (PCF-1 ParamAnchor) | Pro Research / Pro Vault-Preserved |
| **Cross-layer attribution circuit** | A traced computational subgraph spanning layers | RESEARCH (PCF-3 ParamAttributionGraph) | Pro Research / Pro Omega |

**The endgame language** for what the substrate becomes:

> The substrate addresses **cognitive circuits, not whole models.** Today the unit of address is the model. Each release pushes the unit of address one level finer (whole-model → output-shape → KV-page → weight-bits → adapter → MoE expert → active assembly → attention head → parameter anchor → circuit). The Substrate Motion Invariant (Lift / Project / Mutate) holds at every granularity. PCF-1..10 + Goodfire VPD/SPD are the formal theory; Active Assembly is the engineering form.

### 12.3 "But isn't this MoE / subquadratic attention?" (also locked)

**MoE** sparsifies the *model*. **Subquadratic attention** sparsifies *one algorithm*. **Epistemos sparsifies the whole runtime.**

| Tradition | Sparsifies | Surface it sees | Epistemos relationship |
|---|---|---|---|
| MoE (Switch · GLaM · Mixtral · Qwen MoE · DeepSeek MoE) | The model's parameters | Token → expert routing | One specific shape of *intra-model* lift-project; the substrate consumes any MoE model as a Tier-1 executor |
| Subquadratic attention (Mamba / RetNet / RWKV / linear attention) | The attention algorithm | Query × Key × Value over sequence | One specific shape of Scan-IR; substrate consumes any subquadratic backbone as an executor |
| **Epistemos substrate** | The *whole runtime*: model + KV + vault + graph + adapter + tool + context + kernel + proof | **All of the above through one typed coordinate fabric** | The substrate is the architecture that can absorb MoE, subquadratic attention, ternary weights, KV-Direct, Active Assembly, and Parameter Connectome interventions as *interchangeable projections* — none of them are "the architecture"; the substrate is |

### 12.4 The honest current state

- **Whole-model dispatch:** LIVE
- **Output schema gating:** PARTIAL (LocalToolGrammar when modules resolve)
- **KV cache addressing:** SUBSTRATE SHIPPED, HARNESS PENDING
- **Weight-bit addressing:** RESEARCH-tier substrate; promotion candidate
- **Adapter selection:** PARTIAL (per-model selection live; per-task LoRA-light deferred to D-06)
- **MoE / sparse attention consumption:** LIVE for the model lanes that ship them
- **Active assembly across model + app + context:** RESEARCH-tier substrate
- **Attention-head / parameter / circuit addressing:** RESEARCH-tier doctrine; PCF-1..10 + Goodfire VPD/SPD; no MAS ship

### 12.5 What this means for builders

Any agent or terminal writing substrate code MUST be able to answer the question *"at what granularity does this work address the LLM?"* with one of the rows in §12.2 above. If a PR claims circuit-level addressing today, that PR is overclaiming and should be reframed to the actual granularity it operates at.

If a feature genuinely needs finer-than-MoE addressing, it lives in `epistemos-research/` (Lane 3) and never ships in MAS until the F-PCF-{1..10} falsifiers (to be defined) PASS on M2 Pro hardware.

### 12.6 The 3-ontology motion grammar holds at every granularity

The Substrate Motion Invariant (§1) is the *complete* motion grammar. "Activate" (picking which model slice wakes) is a **Lift** — the substrate ingests a slice of weights / KV / adapter / context into the executor's working set. There is no fourth motion. The same three motions apply whether the granularity is whole-model or attention-head.

| Motion | At whole-model | At KV-page | At adapter | At attention-head / SSM state (research) |
|---|---|---|---|---|
| **Lift** | Load model into executor | mmap KV page into device | Apply LoRA delta | Activate specific `W_QK^h` slice OR wake SSM scan-state region as language-router gate |
| **Project** | Token stream → AnswerPacket | KV state → cache entry | Adapter activation → output delta | Head/SSM-state output → layer residual stream |
| **Mutate** | Model swap (router decision) | KV invalidation | LoRA delta promotion | Anchor pin/unpin · SSM-router calibration update |

This is the *single typed primitive* the user asked for in the 2026-05-24 simplification ask. One motion grammar; many granularities; same architecture.

## 13. Locked cross-references for builders

- This doc (`SHADOW_PROJECTION_AND_RESEARCH_CONSTRUCTION_2026_05_24.md`) is the authoritative source for the Substrate Motion Invariant + ShadowProjection candidate + Substrate-vs-LLM granularity ladder.
- `ONLINE_RESEARCH_INTAKE_SHADOW_PROJECTION_2026_05_24.md` is the authoritative source for the public-research credibility ladder.
- `PHASE_2_TERMINAL_PROMPTS_2026_05_23.md` is the dispatch deck (T0 / T1 / S / X / A / B / C / D / E / F / G / H / R).
- `LEGENDARY_ARCHITECTURE_NO_COMPROMISE_AUDIT_2026_05_23.md` is the no-compromise audit.
- `DEFERRED_WORK_GUARANTEE_2026_05_23.md` is the anti-loss ledger (D-01..D-26 + re-promotion codewords).
- `HELIOS_V5_DOC_6_THEOREM_CANON.md` is the formal canon for E1-E7 + H1-H17 + PCF-1..10.
- `CANONICAL_CHRONICLE_2026_05_23.md` §1.2 holds the 7 Laws; the candidate 8th (Shadow Projection) is tracked in this doc + the no-compromise audit.

Future agents: read this section (§12) before claiming to "address neurons" or "control the model internally." Be honest about granularity; the substrate already gives you a coordinate fabric at every level.
