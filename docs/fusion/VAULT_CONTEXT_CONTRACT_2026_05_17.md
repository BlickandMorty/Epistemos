# Vault Context Contract - 2026-05-17

Owner: T4 Vault Retrieval Repair  
Falsifier: F-VaultRecall-50  
Status: locked contract, recall pass bars green, trace/MMR/signal/provenance/prompt-threshold/rust-and-swift-confidence-counts/selected-context-confidence-counts/rank-only-rejection/synthesis-under-cited-validation/adversarial-margin-validation/selected-count-validation/selected-low-score-validation/insufficient-evidence/shadow-exact-escalation/shadow-first-trace-envelope/shadow-answerability-summary/shadow-residual-decode-request/shadow-residual-decode-outcome/shadow-residual-visible-summary-gating/shadow-residual-exact-verification-bridge/shadow-residual-exact-query-handoff/shadow-score-margin/shadow-exact-request/shadow-exact-snippet-hints/shadow-exact-target-limit/shadow-exact-target-snippet-bounds/shadow-exact-query-snippet-bounds/shadow-exact-query-normalization/shadow-exact-identity-normalization/shadow-exact-verification-outcome/shadow-exact-answerability-summary/shadow-exact-answerability-bridge/shadow-visible-exact-hit-gating/shadow-visible-exact-hits/shadow-trace-exact-outcome-bridge/swift-rrf-score-margin/swift-rrf-display-title/swift-rrf-visible-surface-requirement/swift-rrf-source-rank-escalation-reason/swift-rrf-exact-escalation-metadata/swift-rrf-exact-escalation-payload-counts/swift-rrf-exact-escalation-metrics/swift-rrf-exact-escalation-metric-counts/swift-rrf-exact-escalation-metric-count-gate/swift-rrf-exact-escalation-metrics-error-clear/swift-rrf-exact-escalation-metrics-schema-caps/swift-rrf-exact-escalation-schema-caps/swift-rrf-exact-target-hints/swift-rrf-exact-target-normalization/swift-rrf-exact-target-match-keys/swift-rrf-exact-query-hints/swift-rrf-exact-query-normalization/swift-rrf-raw-query-exact-hints/prompt-schema-cap-guard/prompt-exact-query-hints/prompt-rank-only-reason-guard/prompt-candidate-not-evidence-guard/prompt-exact-match-verification-guard/low-margin-prompt-guard/fallback-provenance-block/fallback-singleton-margin-guard/fallback-low-margin-multihit-label/fallback-provenance-card-dedupe/fallback-provenance-signal-badges/fallback-provenance-escalation-badge/fallback-provenance-badge-priority/fallback-multiline-reason-merge/fallback-reason-priority violation mapping landed

## Contract Rules

When a user request touches the vault, the runtime must satisfy every rule below before it can claim it used the user's notes.

1. Never enumerate the first N notes as if they represent the vault. Index order, filesystem order, and `LIMIT 7` are not retrieval.
2. Check vault inventory completeness before search: note count, manifest hash, newest mtime, and index freshness.
3. Search the full manifest with lexical BM25 and dense note sketches. If either signal is unavailable, emit a degraded trace and compensate with broader lexical retrieval.
4. Pull a 50-200 candidate pool before context selection. The answer context may be small, but the candidate search may not be.
5. Rerank candidates with BM25, embedding similarity, graph proximity, recency exponential decay, user-attached priority, and Cognitive DAG resonance when available.
6. Run an MMR diversity pass before building the context pack, so near duplicates cannot crowd out distinct relevant notes.
7. Build a compact context pack that says it sampled top-K by relevance from the full inventory.
8. If confidence is low or ambiguous, ask the user or broaden the search. Adversarial lanes must treat a near-tied top fused score as `ADVERSARIAL_CONFUSION`, and chat/local-agent prompt contracts must treat a low top score margin as ambiguous evidence rather than silently picking the first hit. Do not answer from weak retrieval, and do not collapse rejected weak hits into a false "no results" claim.
9. Source rank alone is never grounding. A candidate or fused hit is contract-sufficient only when it carries non-rank evidence, such as title/path/snippet/body, lexical, semantic, graph, recency, or priority reasons. Rust `VaultCandidateTrace` validation and Swift `FusedResult` contract checks both reject rank-only selected evidence.
10. Emit a trace for every vault lookup: searched-note count, candidate count, selected count, selected distinct-note count for synthesis checks, top reasons, confidence band, top score margin where ranked fused results exist, contract-sufficient/high/medium/low confidence counts for the full candidate pool and selected context where applicable, and any degraded signals.
11. Surface loaded note provenance to the user: title/path plus lexical, semantic, graph, recency, and priority reasons where present. When vault notes ground a chat answer, end with a compact `Vault provenance:` block whose cited notes each include a `Why:` line.
12. Selected candidates must be individually answerable: a trace-level high confidence band cannot mask a selected candidate whose fused score falls in the low-confidence band. Low selected evidence is a `LOW_CONFIDENCE` violation until broader or exact retrieval replaces it.
13. Shadow-first retrieval may propose candidates from Model2Vec/HNSW/RRF, but dense-only, low-score, ambiguous-margin, evidence-hidden, or empty-query Shadow traces are not answerable until exact lexical/body escalation verifies them. Shadow traces must expose the top score margin from the same deterministic ordering used for answerability decisions, and they must build an exact-escalation request containing the trimmed query, bounded ranked candidate hints with visible snippets where present, bounded snippet/title/path exact-query candidates deduped case-insensitively, and escalation reasons. Exact verification only clears the escalation when it returns a matching visible title/snippet/body hit; targetless no-hit Shadow requests may be satisfied by a visible exact hit from the original query. Swift fused-search completion payloads must expose the same bounded exact-query hint shape when RRF evidence requires exact escalation. If exact escalation is unavailable, report insufficient evidence.

## Dataset

F-VaultRecall-50 contains 50 deterministic notes sampled from the user's local vault and 50 queries:

| Lane | Count | Requirement |
| --- | ---: | --- |
| Exact-title | 10 | Query is the literal title or a near title. Top-1 must hit. |
| Paraphrase | 10 | Query asks for note content without relying on title-only lookup. Top-5 must hit. |
| Recent | 10 | Query includes recency language. Rerank must not ignore freshness. |
| Synthesis | 10 | Query requires 2-3 distinct notes. Context must include at least two. |
| Adversarial | 10 | A similarly titled recent distractor exists. Retrieval must reject it. |

The baseline harness is `agent_core/tests/vault_recall_baseline.rs`. It samples by SHA-256 over `F-VaultRecall-50:2026-05-17:v1:<relative path>`, records content hashes, indexes a temporary mirror, and injects adversarial distractors only into that temp mirror.

## Pass Conditions

| Condition | Pass bar |
| --- | ---: |
| Top-1 exact-title recall | >= 95% |
| Top-5 paraphrase recall | >= 90% |
| Agent context recall | >= 90% |
| First-N enumeration failures | 0 |
| UI provenance visible | 100% of loaded notes |
| Synthesis distinct-note coverage | >= 2 notes per synthesis query |
| Adversarial distractor rejection | >= 85% |

## Failure Taxonomy

| Code | Failure | Required behavior |
| --- | --- | --- |
| `FIRST_N_SUBSTITUTION` | Runtime lists arbitrary early notes instead of searching. | Block answer, run full retrieval, emit trace. |
| `INVENTORY_UNKNOWN` | Note count or manifest hash is missing. | Mark trace degraded and avoid confidence claims. |
| `CANDIDATE_POOL_TOO_SMALL` | Search returns fewer than 50 candidates before rerank when inventory supports more. | Broaden query or add fallback source before context pack. |
| `SIGNAL_MISSING` | Lexical, dense, graph, recency, or priority signal is unavailable. | Emit degraded signal reason and continue only if confidence remains sufficient. |
| `LOW_CONFIDENCE` | Top result margin, absolute confidence, or a selected candidate's fused score is below threshold. | Ask user or broaden search. |
| `DUPLICATE_CROWDING` | Near duplicate notes fill the selected context. | Apply MMR and report diversity decisions. |
| `SYNTHESIS_UNDER_CITED` | Synthesis context contains fewer than two target notes. | Broaden candidates and rerank; do not answer as synthesis. |
| `ADVERSARIAL_CONFUSION` | Distractor outranks the target without an explicit ambiguity trace. | Reject or ask user to disambiguate. |
| `PROVENANCE_HIDDEN` | User cannot see why a note was loaded. | Show provenance card/badges before claiming vault grounding. |
| `SELECTED_COUNT_MISMATCH` | Trace selected count does not match selected candidate records. | Treat trace as dishonest; rebuild trace before answering. |
| `TRACE_ABSENT` | No machine-readable retrieval trace exists. | Treat as contract failure. |
| `SHADOW_EXACT_ESCALATION_REQUIRED` | Shadow-first evidence is dense-only, low-score, ambiguous, or missing visible title/snippet/body evidence. | Run exact residual lexical/body verification before answering, or say evidence is insufficient if exact verification is unavailable. |

## Implementation Order

1. Rust contract types in `agent_core/src/retrieval/`: inventory, signal scores, candidate trace, confidence band/counts for all candidates and selected context, MMR decision, validation, first-N failure typing, rank-only provenance rejection, synthesis distinct-note validation, selected-count consistency, selected low-score rejection, and adversarial top-margin validation. Complete.
2. Extend `VaultStore::hybrid_search` without changing the public fallback shape: retrieve a 50-200 pool, preserve raw lexical score, emit `hybrid_search_with_trace`, MMR-rerank selected context, and attach recency, priority, and vault-link graph proximity signals. Complete for the lexical/title VaultStore path; dense sketch and Cognitive DAG resonance still emit degraded trace entries.
3. Swift RRF hardening: reason labels and renderable provenance summaries are live for page/block/readable-block hits, fused search completion traces and in-memory metrics snapshots expose contract-sufficient/high/medium/low confidence counts plus top score margin, exact-escalation required/reason fields, target/query counts, exact-escalation schema/cap fields, metrics contract-shape helpers, and shared cap constants for fused payload/metadata emission, gate target/query metric counts behind a computed exact-escalation requirement, and clear stale result-derived contract counts on error snapshots instead of preserving a previous search's escalation state. Rank-only fused hits are not contract-sufficient. Fused results now carry a display title/title path where the index can provide one, and contract sufficiency requires a visible title/title-path or snippet in addition to a non-rank reason. Fused completion payloads and metadata also expose exact-escalation requirement plus reasons for empty, missing, weak, hidden-evidence, low-margin, or source-rank-only evidence, payloads include bounded ranked target hints with normalized display titles/snippets, normalized match keys, parent document IDs, snippets capped at 240 chars, scores, confidence, reasons, contract schema, target/query counts, and target/query/snippet cap fields, and payloads now include bounded case-insensitive exact-query hints from raw user query plus title/path/snippet evidence with FTS highlight markup stripped so callers do not have to infer Shadow Search 1.0 escalation from raw scores or sanitized MATCH syntax. Graph proximity in Swift RRF remains deferred to Shadow Search 1.0 unless backed by the Rust trace path.
4. Chat enforcement: indexed fallback searches a 50-200 candidate pool, rejects source-rank-only matches, emits per-hit reasons, and appends an explicit `Vault provenance:` block with parseable bold title/path bullets plus `Why:` lines for every grounded fallback note. Prompt contracts require title/path/snippet/body evidence plus that visible block. Low top-score margin, `exact_escalation_required=true`, and `top_hit_source_rank_only` fused-search metadata are ambiguity guards in both cloud/chat and local-agent prompt surfaces, and prompt contracts now require using bounded `exact_escalation_queries` as the verification candidates when fused search provides them while preserving the emitted contract schema and exact-escalation cap fields, and forbidding answers from exact-escalation targets or queries before visible exact verification returns a hit matching the escalated target title/path or bounded exact query. Single-result indexed fallback now refuses to promote one note when the top two contract-sufficient candidates are separated only by source-order noise; multi-hit fallback keeps the useful candidate context but labels the top match as ambiguous instead of presenting the list as decisive. If indexed hits exist but all fail the evidence contract, the user sees an explicit insufficient-evidence response instead of a no-results claim. Synthesis prompts require at least two independently retrieved vault notes or an honest insufficient-evidence response.
5. Notes UI provenance cards: surface why each indexed fallback note was loaded; RRF results carry renderable reasons for callers that surface fused results, and the note chat parser recognizes explicit `Vault provenance:` blocks. Repeated visible-match and explicit-block citations for the same title/path merge into one provenance card with combined reasons, multiple `Why:` lines under one citation merge without duplicating reasons, exact-verification/title/snippet reasons are prioritized ahead of weaker source-rank reasons in the capped card display, and compact Exact/Lexical/Semantic/Graph/Recency badges are derived from those reasons for visible signal provenance with escalation and source-rank badges ordered behind stronger evidence.
6. Shadow Search 1.0 contract: `agent_core/src/retrieval/` now defines Shadow-first candidates, source parsing, RRF score/margin thresholds, exact-escalation decisions, a serializable answerability summary with contract schema, target/query/snippet/residual cap fields, contract-shape helpers for rejecting stale schema/caps, and candidate/visible-evidence/exact-target/exact-query counts, bounded residual-decode request/outcome payloads, bounded exact-escalation request payloads with normalized 240-character snippet hints, bounded case-insensitive exact-query candidates from query/title/path/snippet evidence for both residual and exact stages, exact-query and exact-identity normalization that strips FTS highlight markup and ellipsis noise, exact verification outcomes that require matching visible hits before answering and emit post-verification answerability summaries with the same schema/cap fields, `visible_matching_hits` for diagnostics, `citable_visible_hits` for the subset safe to cite after the full exact contract clears, `validate_after_exact_verification`/`answer_allowed_after_exact_verification`/`answerability_summary_after_exact_verification` to bridge residual exact hits back into the answerability contract while preserving the original Shadow top-score margin, residual-decode outcome helpers that validate exact hits against the enriched exact request, a trace-to-verification bridge for pairing exact hits with the originating escalation request, top-score margin tracing, a serializable `ShadowFirstTrace` envelope, and shared `ShadowExactEscalationRequired` violation mapping. Dense-only, empty-query, ambiguous, low-score, or evidence-hidden Shadow traces must decode residual summaries, accept snippet enrichment only from matching hits with visible summaries, and then escalate to exact lexical/body verification before they can ground an answer.

## T4 Gate And Deferral Punch List

This section is the T4-local punch list for merge planning. It intentionally lives in the T4 Vault contract doc rather than a shared audit doc, so other terminals can read it without inheriting a write conflict.

| ID | Status | Where | Gate / why it matters | Acceptance |
| --- | --- | --- | --- | --- |
| T4-G1 | DONE | `agent_core/src/storage/vault.rs`, `agent_core/src/retrieval/`, `agent_core/tests/vault_recall_baseline.rs` | F-VaultRecall-50 must prove honest recall before any substrate claim can cite vault evidence. | Ignored harness writes the baseline report, current report is green, `cargo test --manifest-path agent_core/Cargo.toml --lib` stays at or above 1741 passing, and no first-N enumeration path is introduced. |
| T4-G2 | DONE | `Epistemos/Sync/SearchIndexService.swift`, `Epistemos/Sync/RRFFusionQuery.swift`, `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift` | Swift fused search must not hand weak RRF evidence to chat as if it were grounded. | Completion payload and metadata expose schema, confidence counts, top-score margin, exact-escalation reasons, bounded targets, bounded exact queries, query/target counts, match keys, and cap fields; source-rank-only top hits emit `top_hit_source_rank_only`. |
| T4-G3 | DONE | `Epistemos/App/ChatCoordinator.swift`, `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`, `EpistemosTests/F_VaultRecall_50_FallbackTests.swift` | Prompt surfaces must refuse low-margin, rank-only, or exact-escalation-required evidence unless exact verification returns visible evidence. | Prompt contract names `exact_escalation_required=true`, `top_hit_source_rank_only`, `exact_escalation_queries`, schema/cap preservation, candidate-not-evidence behavior, and exact target/query match requirements. |
| T4-G4 | DONE | `Epistemos/Views/Notes/*`, `EpistemosTests/F_VaultRecall_50_FallbackTests.swift` | User-visible provenance is the user-facing proof that retrieval happened. | Note-chat provenance cards parse explicit `Vault provenance:` blocks, dedupe repeated title/path entries, merge multi-line `Why:` reasons, prioritize exact/title/snippet reasons, and show compact signal badges ahead of source-rank badges. |
| T4-O1 | OPEN | `Epistemos/Sync/SearchIndexService.swift`, `Epistemos/App/ChatCoordinator.swift` | Shadow-first contract is defined, but live Swift callers still need to consume the Shadow-first trace as the authority for answerability. | A live Shadow retrieval path emits `ShadowFirstTrace`, preserves residual decode request/output, runs exact lexical/body verification when `ShadowExactEscalationRequired` is present, and refuses answer construction until `answer_allowed_after_exact_verification` is true. |
| T4-O2 | OPEN | `Epistemos/Views/Notes/*` | Provenance cards cover indexed fallback chat answers; any future native fused-result UI must render the same contract fields instead of only title/snippet. | A direct fused-result surface shows contract schema, confidence band, top-score margin or ambiguity, exact escalation state, reason badges, and visible title/path/snippet evidence before allowing citation. |
| T4-O3 | OPEN / BLOCKED BY OTHER TERMINALS | `agent_core/src/retrieval/` consumer boundary only | Cognitive DAG resonance is listed in the Vault Context Contract, but the stable resonance inputs are owned outside T4. | Once the owning substrate exposes stable claim/evidence inputs, T4 can add a non-degraded resonance signal to `VaultSignalScores` without touching T1/T2/T3 internals. |
| T4-O4 | OPEN / CI FOLLOW-UP | `agent_core/tests/vault_recall_baseline.rs`, `docs/falsifiers/F-VaultRecall-50_2026_05_17.md` | The recall harness is intentionally ignored because it depends on a local user vault; merge planning still needs a repeatable human command. | Pre-merge operator runs `EPISTEMOS_FVAULT_RECALL_WRITE_REPORT=1 cargo test --manifest-path agent_core/Cargo.toml --test vault_recall_baseline -- --ignored --nocapture`, confirms the report is green, and commits only the report if the sampled vault changes. |
| T4-O5 | OPEN / SCOPE-GATED | `epistemos-shadow/src/` producer, `agent_core/src/retrieval/` consumer | Model2Vec residual summaries and HNSW/BM25/RRF backend production wiring live in the Shadow producer path, which this branch did not edit under the T4 scope lock. | The producer emits bounded residual summaries and exact-hit identities matching T4 normalization; T4 consumer accepts only visible matching residual summaries and exact hits already covered by the contract tests. |

Pre-merge sanity for T4:

1. Keep `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d` unstaged if Cargo touches it.
2. Run `cargo test --manifest-path agent_core/Cargo.toml --lib`.
3. Run Swift parse checks for touched Swift surfaces, using `-enable-bare-slash-regex` for `Epistemos/App/ChatCoordinator.swift`.
4. Re-run the §5.0 scan for accidental first-N note enumeration.
5. Push `origin/codex/t4-vault-2026-05-16` every 5-10 commits.
