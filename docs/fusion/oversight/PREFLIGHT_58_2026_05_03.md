# Preflight 58 - agent-event-local-runtime-recorder-mount-pr26

Round: 58
Date: 2026-05-03
Slice: `agent-event-local-runtime-recorder-mount-pr26`
Tier: Core

## Canon Read

- `MASTER_RESEARCH_INDEX_2026_05_02.md` §§2, 8, 11, 22
- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md` AgentEvent PR24/PR25 and next-build queue
- `AGENT_BUILD_WORKCARDS_2026_05_01.md` Card 7
- `docs/fusion/fleet/REGISTRY.md`

## Decision

PR24 and PR25 proved local GGUF generation and local backend streaming
provenance behind injectable recorders, but `AppBootstrap` still constructs the
live local runtime clients without passing a recorder. This slice mounts one
shared `AgentToolProvenanceRecorder` into the existing local GGUF client and
local backend router construction only.

## Protected-Path Check

- Allowed implementation files: `Epistemos/App/AppBootstrap.swift`,
  `EpistemosTests/LocalBackendLLMClientTests.swift`.
- Allowed docs: Round 58 fleet/deliberation/preflight docs, registry, current
  state, workcards, post-merge guards.
- Forbidden: graph renderer, `Epistemos/Graph/**`, `graph-engine/**`,
  `agent_core/**`, EventStore schema, generated bindings, Hermes/MCP,
  LocalAuthentication, ANE/private API, Xcode project files.

## Preflight Greps

- `LocalBackendLLMClient` and `LocalGGUFClient` both already accept
  `agentProvenanceRecorder`.
- `AppBootstrap` constructs `LocalGGUFClient` and `LocalBackendLLMClient`
  without recorder arguments before this slice.
- No web validation required; this is local wiring over existing committed API.

## Usefulness

usefulness: +1
usefulness_reason: Turns already-proven local runtime AgentEvent instrumentation into the live app mount without changing routing semantics.
