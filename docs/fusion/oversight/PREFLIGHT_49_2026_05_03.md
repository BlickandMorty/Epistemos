# Round 49 Preflight - R15 MLX Live Token Throughput PR8 Closure

Date: 2026-05-03
Slice: `r15-mlx-live-token-throughput-pr8-closure`
Result: blocked before live benchmark execution

## Canon Anchors

- `MASTER_RESEARCH_INDEX_2026_05_02.md` §9 - Local model / MLX inference
- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md:952` - R15 live MLX token throughput remains open
- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md:969` - R15 remaining specialized baselines are the next code-safe lane
- `AGENT_BUILD_WORKCARDS_2026_05_01.md:247` - Card 3 R15 Benchmark Harness Foundation
- `AGENT_BUILD_WORKCARDS_2026_05_01.md:333` - PR8 is blocked-run evidence only until a real JSON report exists

## Commands

```bash
sysctl hw.memsize
vm_stat
find "$HOME/Library/Application Support/Epistemos/Models" -maxdepth 4 -type f \( -name '*.safetensors' -o -name '*.gguf' -o -name 'config.json' \)
pagesize=$(getconf PAGESIZE); free=$(vm_stat | awk '/Pages free/ {gsub("\\.","",$3); print $3}'); inactive=$(vm_stat | awk '/Pages inactive/ {gsub("\\.","",$3); print $3}'); purgeable=$(vm_stat | awk '/Pages purgeable/ {gsub("\\.","",$3); print $3}'); bytes=$(( (free + inactive + purgeable) * pagesize )); gib=$(( bytes / 1073741824 )); echo "pagesize=$pagesize free_pages=$free inactive_pages=$inactive purgeable_pages=$purgeable available_bytes=$bytes available_gib_floor=$gib required_gib=12 headroom_gib=6 decision=$(if [ $((gib + 6)) -lt 12 ]; then echo block; else echo allow; fi)"
```

## Raw Result

```text
hw.memsize: 17179869184
DeepSeek active model exists:
/Users/jojo/Library/Application Support/Epistemos/Models/text/active/mlx-community--DeepSeek-R1-Distill-Qwen-7B-4bit/model.safetensors
/Users/jojo/Library/Application Support/Epistemos/Models/text/active/mlx-community--DeepSeek-R1-Distill-Qwen-7B-4bit/config.json

pagesize=16384 free_pages=4950 inactive_pages=278291 purgeable_pages=410 available_bytes=4647337984 available_gib_floor=4 required_gib=12 headroom_gib=6 decision=block
```

## Decision

Do not launch the opt-in live MLX benchmark under current memory conditions. The canonical loader preflight in `MLXInferenceService` would block the run with the same `availableGB=4` class result recorded in the original PR8 deliberation, and forcing it would risk swap-thrash rather than creating authoritative evidence.

## Next

Leave PR8 open as blocked-run evidence and continue with the next non-manual code-safe queue item.
