# Round 60 Preflight - AgentEvent LocalMLX Direct Stream PR28

Slice: `agent-event-local-mlx-stream-pr28`
Date: 2026-05-03
Tier: Core
Usefulness: +1 - selects the next narrow runtime provenance slice after PR27.

## Canon Reads

- `MASTER_RESEARCH_INDEX_2026_05_02.md` §8 anchors streaming / FFI / BoltFFI provenance.
- `MASTER_RESEARCH_INDEX_2026_05_02.md` §9 anchors local model / MLX inference surfaces.
- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md` records PR27 as closed and names broader runtime AgentEvent coverage as the next safe lane.
- `AGENT_BUILD_WORKCARDS_2026_05_01.md` Card 7 remains the executable workcard for bounded AgentEvent tool provenance.
- `docs/fusion/fleet/REGISTRY.md` had no live PR28 rows before this round.

## Current Code Truth

- `LocalBackendLLMClient.stream(...)` already records sanitized stream AgentEvents under PR25.
- `LocalMLXClient.generate(...)` already records sanitized direct generate AgentEvents under PR27.
- `LocalMLXClient.stream(...)` still streams through the runtime control plane without direct AgentEvent requested/started/completed/failed records.

## Scope

Allowed:
- `Epistemos/Engine/MLXInferenceService.swift`
- `EpistemosTests/LocalBackendLLMClientTests.swift`
- PR28 docs under `docs/fusion/`

Forbidden:
- UI, graph, Rust, generated bindings, EventStore schema, Hermes/MCP, browser/computer-use, LocalAuthentication, ANE/private API, routing policy, model loading semantics, and AppBootstrap remounting.

## Verification Plan

- Red: focused `LocalBackendLLMClientTests` must fail before implementation for direct MLX stream AgentEvent expectations.
- Green: focused `LocalBackendLLMClientTests` must pass after implementation.
- Guard: staged diff must avoid forbidden surfaces and persist no prompt/system/steering/output/model/path/localized error text in AgentEvents.
