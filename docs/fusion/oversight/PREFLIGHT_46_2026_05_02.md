# PREFLIGHT round 46 - sovereign-gate-settings-vault-disconnect-pr16

## Scope

- Slice: `sovereign-gate-settings-vault-disconnect-pr16`
- Tier: Core
- Surface: existing Settings > Vault `Disconnect` destructive button in `Epistemos/Views/Settings/SettingsView.swift`
- Canon anchors: `MASTER_RESEARCH_INDEX_2026_05_02.md §3.2`, `EPISTEMOS_FINAL_DOCTRINE_2026_05_01.md §4.2`, `AGENT_BUILD_WORKCARDS_2026_05_01.md` Card 9

## Dirty-tree note

The worktree already contains many unrelated modified and untracked files. This slice must exact-stage only:

- `Epistemos/Views/Settings/SettingsView.swift` hunks for Settings destructive target + Vault disconnect authorization
- `EpistemosTests/SovereignGateTests.swift` hunks for Settings vault disconnect source guards
- `docs/fusion/**` artifacts for this round

Known unrelated Settings diagnostics hunks in `SettingsView.swift` must remain unstaged:

- `BackgroundIndexingHealthRow()`
- `SearchFusionHealthRow()`
- Diagnostics text mentioning Halo backend, Background Indexing, and Search Fusion

## Preflight checks

- `rg -n "LocalAuthentication|LAContext|canEvaluatePolicy|evaluatePolicy" Epistemos/Views/Settings/SettingsView.swift` returned no matches.
- Existing adjacent PRs closed: Settings Reset Everything PR14 and Settings Workspace Delete PR15.
- External web validation skipped: this is a pure local Core UI authorization migration using the already-shared `SovereignGate`.

## Verification logs

- Red: `/tmp/epistemos-sovereign-gate-settings-vault-disconnect-pr16-red-20260502.log`
- Green: `/tmp/epistemos-sovereign-gate-settings-vault-disconnect-pr16-green-20260502-rerun.log`
- Green result: focused `SovereignGateTests` executed 33 tests in 1 suite, all passed, and xcodebuild printed `** TEST SUCCEEDED **`.
- Xcode footer still printed known SwiftLint package-plugin "failed build commands" for CodeEdit packages after success; this matches prior focused runs.

## Stop triggers

None. This slice names one exact existing destructive Settings surface, has focused tests, and does not require new Sovereign Gate internals.
