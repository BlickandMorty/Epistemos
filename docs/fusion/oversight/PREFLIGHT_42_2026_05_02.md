PREFLIGHT round=42 slice=command-center-tool-surface-policy-pr1 captured_at=2026-05-03T03:52Z

## git rev-parse --short HEAD
e39bbec1

## git branch --show-current
feature/landing-liquid-wave

## focused git status
Only one planned target file was dirty before this slice:

```text
 M EpistemosTests/AgentCommandCenterStateTests.swift
```

`Epistemos/State/AgentCommandCenterState.swift`, `Epistemos/Bridge/ToolTierBridge.swift`,
the fusion current-state docs, workcards, post-merge guards, and registry were
clean for the focused paths at slice start.

## stale round-42 registry sweep
No `round=42` rows with `spawned` or `running` status existed before this slice.

## selected slice
`command-center-tool-surface-policy-pr1`: keep the dormant Agent Command Center
context-provider suggestions aligned with the closed Core/MAS tool-surface
policy. The ACC tool catalog already flows through `ToolTierBridge` and
`ToolSurfacePolicy`; this slice audits the remaining `@` agent context
providers.
