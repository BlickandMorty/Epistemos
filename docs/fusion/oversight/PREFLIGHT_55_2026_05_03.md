# Round 55 Preflight - AgentEvent Local GGUF Generate PR24

ANCHOR:

`ANCHOR: slice=next-provenance-slice-selection | round=55 | terminal=unknown | claude-side=off | claude-red-team=off | reading=[MASTER_RESEARCH_INDEX_2026_05_02.md,UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md,AGENT_BUILD_WORKCARDS_2026_05_01.md,REGISTRY.md]`

## Scope

- Slice selected: `agent-event-local-gguf-generate-pr24`
- Tier: Core
- Candidate source: `Epistemos/Engine/LocalGGUFClient.swift`
- Candidate tests: `EpistemosTests/LocalGGUFClientTests.swift`
- Workcard: `docs/fusion/agent-build-scaffolding/AGENT_BUILD_WORKCARDS_2026_05_01.md` Card 7
- Master index anchor: `docs/fusion/MASTER_RESEARCH_INDEX_2026_05_02.md` §9

## Selection Rationale

- The current state keeps "remaining broader runtime AgentEvent coverage" open after PR23.
- The master research index names GGUF as the primary local text-generation path.
- `LocalGGUFClient.swift` and `LocalGGUFClientTests.swift` were clean in `git status --short` at selection time.
- This slice instruments non-streaming `LocalGGUFClient.generate(...)` only. `stream(...)` remains a separate future slice.

## Preflight Results

- `git status --short -- Epistemos/Engine/LocalGGUFClient.swift EpistemosTests/LocalGGUFClientTests.swift` returned no dirty rows.
- `rg` over the candidate source and tests found no Core-leakage hits for Hermes, MCP, subprocesses, computer-use, private ANE, library validation, LocalAuthentication, URLSession, or `Process.`.
- Wider doc grep did return expected historical/canonical references to Hermes and LocalAuthentication in fusion docs. These are not candidate source/test hits.
- Claude CLI smoke succeeded in this round with `claude --print`, returning `ok`.

## Stop Triggers

- Do not alter streaming GGUF behavior in this slice.
- Do not persist prompt text, system prompt text, steering hints JSON, generated output, model URL, artifact ID, filesystem paths, or localized/arbitrary error text in AgentEvents.
- Do not change runtime routing, model loading, prepared runtime configuration, BackendRuntimeControlPlane behavior, UI, graph, Rust, generated bindings, or EventStore schema.
- If the focused red test cannot fail before the implementation, stop and reframe the test.
