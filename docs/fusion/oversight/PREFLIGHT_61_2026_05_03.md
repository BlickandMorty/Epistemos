# Round 61 Preflight - AgentEvent LocalBackend Direct Generate PR29

Slice: `agent-event-local-backend-generate-pr29`
Date: 2026-05-03
Tier: Core
Usefulness: +1 - selects the next narrow runtime provenance gap after PR28.

## Canon Reads

- `MASTER_RESEARCH_INDEX_2026_05_02.md` §8 anchors streaming / FFI / BoltFFI provenance.
- `MASTER_RESEARCH_INDEX_2026_05_02.md` §9 anchors local model / MLX inference surfaces.
- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md` names `LocalBackendLLMClient.generate(...)` as intentionally not covered by PR26.
- `AGENT_BUILD_WORKCARDS_2026_05_01.md` Card 7 remains the executable workcard for bounded AgentEvent tool provenance.
- `docs/fusion/fleet/REGISTRY.md` had no live PR29 rows before this round.

## Current Code Truth

- `LocalBackendLLMClient.stream(...)` records sanitized stream AgentEvents under PR25.
- `LocalGGUFClient.generate(...)` records sanitized direct GGUF generate AgentEvents under PR24.
- `LocalMLXClient.generate(...)` records sanitized direct MLX generate AgentEvents under PR27.
- `LocalMLXClient.stream(...)` records sanitized direct MLX stream AgentEvents under PR28.
- `LocalBackendLLMClient.generate(...)` still routes to lower clients without router-level requested/started/completed/failed AgentEvents.

## Scope

Allowed:
- `Epistemos/Engine/LocalBackendLLMClient.swift`
- `EpistemosTests/LocalBackendLLMClientTests.swift`
- PR29 docs under `docs/fusion/`

Forbidden:
- UI, graph, Rust, generated bindings, EventStore schema, Hermes/MCP, browser/computer-use, LocalAuthentication, ANE/private API, AppBootstrap remounting, lower GGUF/MLX runtime behavior, and routing policy changes.

## Verification Plan

- Red: focused `LocalBackendLLMClientTests` must fail before implementation for direct LocalBackend generate AgentEvent expectations.
- Green: focused `LocalBackendLLMClientTests` must pass after implementation.
- Guard: staged diff must avoid forbidden surfaces and persist no prompt/system/steering/output/model/path/localized error text in AgentEvents.
