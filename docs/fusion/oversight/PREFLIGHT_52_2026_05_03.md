# Round 52 Preflight - AgentEvent SearchIndex Direct Page PR21

Slice: `agent-event-search-index-direct-page-pr21`
Result: approved for brief and Claude red-team before code edits

## Local Canon Read

- `MASTER_RESEARCH_INDEX_2026_05_02.md` §2 names the substrate spine as typed events feeding audit/projection consumers.
- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md:285` through `:333` closes SearchIndex fused async/sync AgentEvent PR19/PR20 only.
- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md:1042` leaves remaining broader runtime AgentEvent coverage open behind exact gates.
- `AGENT_BUILD_WORKCARDS_2026_05_01.md:1001` through `:1008` allows future broader runtime instrumentation only after exact runtime files and focused tests are named.
- Round 51 scouts selected direct `SearchIndexService.search` / `searchAsync` page-search provenance over dirty `VaultSyncService` wrapper work.

## Current Code Check

- `Epistemos/Sync/SearchIndexService.swift` is clean before this slice.
- `EpistemosTests/SearchIndexServiceFusionTests.swift` is clean before this slice.
- `SearchIndexService` already owns `AgentToolProvenanceSyncRecorder` and async recorder plumbing for fused search.
- `VaultSyncService.swift`, `QueryRuntime.swift`, Graph, and R16-related Engine files have unrelated dirty work and are out of scope.

## Decision

Proceed with a narrow Core AgentEvent provenance slice for direct page search only:

- sync `SearchIndexService.search(query:limit:)`
- async `SearchIndexService.searchAsync(query:limit:)`

Do not touch direct block search, fused RRF, VaultSync wrappers, QueryRuntime, Graph, UI, Rust, EventStore schema, or generated bindings.
