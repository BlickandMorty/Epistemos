# Preflight 62 - Runtime Contract Error-Class Bridge PR30 - 2026-05-03

## Scope

- Slice: `runtime-contract-error-class-bridge-pr30`
- Tier: Core
- Workcard: Card 7 - AgentEvent Tool Provenance / local runtime provenance hardening
- Canon anchors: `MASTER_RESEARCH_INDEX_2026_05_02.md` §8 and §9

## Local Canon Read

- `UNIFIED_SUBSTRATE_CURRENT_STATE_2026_05_01.md` now marks AgentEvent PR28 and PR29 closed, while both green logs still surface `Can't lift flat errors` from the existing MLX stream cancellation cleanup path.
- `AGENT_BUILD_WORKCARDS_2026_05_01.md` marks the same cleanup issue outside PR28/PR29 and suitable for a follow-up runtime-contract slice.
- `POST_MERGE_GUARDS_2026_05_02.md` records the repeated cleanup log in PR28 and PR29.

## Code Read

- `epistemos-core/uniffi/epistemos_core.udl` embeds `RuntimeContractError? error_class` inside `RuntimeGenerationSummary` and `RuntimeGenerationEvent`.
- `epistemos-core/src/runtime_contract.rs` stores `Option<RuntimeContractError>` in the same two record structs.
- `RuntimeContractError` is a UniFFI flat error. Flat errors are valid as thrown errors, but they are unsafe as record payload fields crossing FFI.
- `BackendRuntimeContract.swift` maps generated `RuntimeGenerationSummary.errorClass` and `RuntimeGenerationEvent.errorClass` back into `BackendRuntimeContractError`.

## Preflight Verdict

Proceed with a narrow contract bridge slice. Do not suppress logs. Fix the payload shape so failed/cancelled runtime records cross FFI as string error-class labels while thrown API errors remain typed `RuntimeContractError`.

## Guard Commands

- `xcodebuild -project Epistemos.xcodeproj -scheme Epistemos -destination 'platform=macOS' test -only-testing:EpistemosTests/BackendRuntimeContractTests`
- `rg -n "RuntimeContractError\\? error_class|Option<RuntimeContractError>|error_class: Some\\(" epistemos-core/uniffi/epistemos_core.udl epistemos-core/src/runtime_contract.rs`
- `rg -n "Can't lift flat errors|failed/cancelled runtime events carry string error classes|Test run" /tmp/epistemos-runtime-contract-error-class-bridge-pr30-green-20260503.log`
