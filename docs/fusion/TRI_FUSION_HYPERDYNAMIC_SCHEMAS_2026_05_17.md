# Tri-Fusion Hyperdynamic Schemas Doctrine

Date: 2026-05-17  
Owner: T1 Tri-Fusion content fabric  
Status: Phase B doctrine, iteration 12

This doctrine starts from `docs/audits/HYPERDYNAMIC_SCHEMAS_AUDIT_2026_05_17.md`. It does not claim Tri-Fusion exists today. The audit established the current substrate:

- ProseMirror JSON is the only editor-emitted canonical state.
- Markdown exists as one-way paste/input parsing.
- HTML exists through Tiptap node-local `parseHTML`/`renderHTML` hooks.
- Hyperdynamic Schemas are scalar and flat.
- EML is arithmetic substrate, not a document grammar.
- LocalAgent and Epdoc do not yet expose typed Tri-Fusion mutations.

The doctrine target is a deterministic content fabric across JSON, Markdown, and HTML, with model-authored mutations carried as typed operations and witnessed into provenance.

## 1. Three Formats

Tri-Fusion has exactly three document-facing formats:

1. JSON canonical body.
2. Markdown canonical projection within a declared subset.
3. HTML semantic tree projection.

### 1.1 JSON Canonical Body

JSON is the authoritative state format because the live editor already emits `JSON.stringify(editor.getJSON())` over `contentDidChange`, Swift already persists ProseMirror JSON snapshots, and `.epdoc` body handling already treats ProseMirror JSON as the editor substrate.

The canonical JSON body must be:

- A ProseMirror-compatible document tree.
- Deterministically serialized with sorted object keys where Rust owns serialization.
- Stable across parse/serialize cycles for the same semantic document.
- Block-addressable across every block-like node, not only the existing `UniqueId` subset of heading, paragraph, codeBlock, and blockquote.
- Validated by Tri-Fusion before a mutation witness is accepted.

Current gap: `js-editor/src/index.ts` gives stable outbound JSON, but block IDs are incomplete and Rust has no `TriFusionDocument` parser/canonicalizer yet.

### 1.2 Markdown Projection

Markdown is not the canonical state today. It is an authoring and interchange projection. The existing parser `parseMarkdownPaste(source)` can turn some Markdown-like text into Epdoc JSON nodes, but there is no serializer and no byte-equal round-trip contract.

Tri-Fusion Markdown must be explicitly subsetted before implementation:

- Paragraphs and headings.
- Inline code, bold, italic, strike, highlight, links, and wikilinks.
- Ordered, unordered, and task lists.
- Blockquote and callout representation.
- Tables matching the existing pipe-table parser.
- Fenced code blocks with language.
- Mermaid and chart fences as typed blocks.
- Images only through safe source rules.
- Footnotes and math only once their serializer shape is pinned.

Markdown byte equality can only be required for Markdown generated by Tri-Fusion's own serializer or fixtures declared as canonical. Arbitrary pasted Markdown may normalize into canonical Markdown and should report normalization in its witness.

Current gap: no file under `js-editor/src/markdown/` serializes JSON back to Markdown.

### 1.3 HTML Semantic Tree Projection

HTML is a semantic projection, not a byte-equal format. Tiptap and custom nodes already define node-local parse/render hooks:

- Callout: `div[data-callout]`.
- Mermaid: `div[data-mermaid]` with preserved whitespace.
- Chart: `div[data-epdoc-chart]` with source text.
- Image: `img[src]` gated by safe source checks.
- Code blocks: code-block extension HTML attributes.

Tri-Fusion HTML equality must compare normalized semantic trees, not raw HTML bytes. The normalizer must:

- Sort attributes where order is meaningless.
- Preserve text exactly inside code-like nodes, Mermaid, and chart blocks.
- Normalize insignificant whitespace outside whitespace-preserving nodes.
- Preserve safe image attributes and represent blocked unsafe image sources deterministically.
- Treat Tiptap-generated wrapper artifacts as non-semantic only when explicitly declared.

Current gap: no Rust or JS test harness exists for `HTML -> JSON -> HTML` semantic tree equality.

### 1.4 Format Authority Rules

The format authority order is:

1. JSON owns document state.
2. Markdown owns human-readable interchange for the supported subset.
3. HTML owns rendered semantic projection for editor/browser interchange.

No model may mutate an Epdoc by replacing arbitrary Markdown and claiming it updated structured content. A model-authored edit must be one of the typed Tri-Fusion mutations described in Section 3 and witnessed through Section 6.

### 1.5 Deterministic Failure Rules

Tri-Fusion must fail closed when a projection is ambiguous or unsupported:

- Unsupported Markdown constructs must produce a typed unsupported-node error or a lossy-normalization witness.
- Unsafe HTML must be rejected or normalized into a deterministic blocked representation.
- Missing block IDs must be repaired before mutation or rejected with a witnessable error.
- Schema repair must never silently weaken user-authored constraints for a model-authored mutation.

## 2. Round-Trip Lemmas

The round-trip contract is the first proof surface for Tri-Fusion. It is intentionally stricter than the current editor behavior: today's editor can emit JSON snapshots, ingest some Markdown, and parse/render HTML through Tiptap, but no path is currently witnessed as deterministic. Implementation must therefore make each lemma executable before Swift or model-facing grammar claims a format guarantee.

### 2.1 Canonicalization Inputs

Every parser entrypoint must return either a `TriFusionDocument` or a typed error. A `TriFusionDocument` is canonical only after all of these inputs are pinned:

- `source_format`: `json`, `markdown`, or `html`.
- `source_bytes_hash`: BLAKE3 over the exact caller input bytes.
- `canonical_json`: deterministic ProseMirror-compatible JSON with stable object ordering.
- `block_identity_report`: inserted, preserved, repaired, and rejected block identifiers.
- `schema_report`: Hyperdynamic Schema validation and repair result.
- `normalization_report`: whitespace, unsafe image handling, unsupported Markdown, and HTML wrapper normalization decisions.

The parser must not silently repair missing block IDs, schema violations, or unsafe content without recording the decision in the witness. A repair that changes user-authored semantics is a hard error until the user grants a stronger mutation capability.

### 2.2 Lemma TF-RT-JSON-IDENTITY

Statement: for canonical JSON input `J`, `parse_json(J).to_json_bytes() == J`.

Test shape:

- Use fixtures generated by Tri-Fusion's canonical serializer, not hand-formatted JSON with arbitrary object key order.
- Assert byte equality, not `serde_json::Value` equality.
- Include every supported block kind: paragraph, heading, list, task item, blockquote, callout, table, code block, Mermaid, chart, image, math, footnote, link, wikilink, and transclusion placeholder once those blocks are admitted.
- Include a negative fixture for missing IDs and a positive fixture where deterministic ID repair is explicitly requested and witnessed.

Reason: JSON is the authority format, so this lemma must be the first implementation test in `tests/tri_fusion_json_round_trip.rs`. If it fails, Markdown and HTML lemmas are not meaningful.

### 2.3 Lemma TF-RT-MD-BYTE-EQUAL

Statement: for canonical Markdown input `M`, `parse_markdown(M).to_markdown_bytes() == M`.

Canonical Markdown means Markdown emitted by Tri-Fusion or checked into the canonical fixture corpus. It does not mean arbitrary clipboard Markdown. The supported subset must reject or normalize anything outside Section 1.2 before byte equality is claimed.

Markdown serializer rules:

- Normalize line endings to LF.
- Preserve code, Mermaid, chart, and math fence bodies byte-for-byte between fence delimiters.
- Use exactly one trailing newline at document end.
- Preserve ordered-list numbering only when the numbering is semantic; otherwise emit canonical monotonic numbering.
- Escape table pipes and inline delimiters deterministically.
- Render callouts, wikilinks, links, footnotes, and task checkboxes with one canonical spelling.

Test shape:

- `tests/tri_fusion_markdown_round_trip.rs` owns the byte-equal fixtures.
- Fixtures should be small and composable before the 200-document corpus expands them.
- Every unsupported construct gets a fixture that asserts a stable error code or a normalization witness.
- The existing JS `parseMarkdownPaste(source)` is an audited input precedent, not proof of this lemma, because it has no serializer.

### 2.4 Lemma TF-RT-HTML-TREE-EQUAL

Statement: for supported HTML input `H`, `normalize_html(parse_html(H).to_html_bytes()) == normalize_html(H)`.

HTML equality is semantic tree equality. Attribute ordering, harmless wrapper noise, and insignificant whitespace may normalize. Text inside whitespace-preserving nodes must not normalize.

HTML normalizer rules:

- Parse into a tree; never compare raw HTML strings.
- Lowercase tag and attribute names where HTML semantics permit.
- Sort attributes except where value order is semantic.
- Preserve `pre`, `code`, Mermaid, chart, math, and textarea-like content exactly.
- Normalize editor wrappers only when the wrapper has no semantic node payload.
- Represent unsafe image sources with one blocked-image node shape.
- Preserve data attributes that define custom Epdoc semantics, including `data-callout`, `data-mermaid`, and `data-epdoc-chart`.

Test shape:

- `tests/tri_fusion_html_round_trip.rs` owns semantic tree fixtures.
- Each custom JS node with a `parseHTML` or `renderHTML` hook gets at least one positive and one negative fixture.
- Unsafe HTML and unsafe image sources must prove deterministic blocked or rejected behavior.

### 2.5 Lemma TF-RT-CROSS-FORMAT-COHERENCE

Statement: for any document `D` inside the common supported subset, all declared projections converge to the same canonical JSON:

1. `parse_json(D.json).to_markdown().parse_markdown().to_json() == D.json`.
2. `parse_json(D.json).to_html().parse_html().to_json() == D.json`.
3. `parse_markdown(D.md).to_html().parse_html().to_json() == parse_markdown(D.md).to_json()`.

This lemma is intentionally scoped to the common subset. A chart block can enter through JSON or Markdown fence if the Markdown syntax is declared; unsafe HTML cannot become a successful cross-format fixture unless its blocked representation is part of the common subset.

Test shape:

- `tests/tri_fusion_cross_format.rs` owns coherence fixtures.
- Fixture metadata states which projections are expected to exist.
- The assertion target is canonical JSON bytes, not editor-rendered appearance.

### 2.6 Lemma TF-RT-FAILURE-DETERMINISM

Statement: unsupported, ambiguous, or unsafe inputs produce stable errors and stable witness data.

Required error families:

- `unsupported_markdown_construct`.
- `unsupported_html_construct`.
- `ambiguous_projection`.
- `missing_block_id`.
- `duplicate_block_id`.
- `schema_validation_failed`.
- `schema_repair_denied`.
- `unsafe_uri`.
- `transclusion_cycle`.
- `lossy_normalization_requires_ack`.

The same input bytes, parser version, schema version, and repair policy must produce byte-identical error JSON. Error payloads may include source spans where practical, but source spans must be deterministic and must not include host-specific paths.

### 2.7 Corpus Plan

The acceptance corpus must reach at least 200 documents, but it should not start as 200 large fixtures. Build it as deterministic fixture families:

- 30 JSON identity fixtures across individual block kinds and nested structures.
- 50 Markdown byte-equal fixtures across inline marks, block structures, fences, tables, links, callouts, and negative syntax.
- 40 HTML tree-equal fixtures across standard nodes, custom nodes, unsafe inputs, and wrapper normalization.
- 40 cross-format fixtures over the common subset.
- 20 mutation-before/after fixtures that pair round-trip checks with `TriFusionWitness`.
- 20 regression fixtures copied from real Epdoc/template shapes after personal or user content is removed.

The corpus generator must report the fixture count by family so the acceptance claim cannot be inflated by repeating equivalent documents.

### 2.8 Test Naming and Gate Order

Implementation tests should land in this order:

1. `tri_fusion_json_round_trip.rs` for TF-RT-JSON-IDENTITY.
2. `tri_fusion_markdown_round_trip.rs` for TF-RT-MD-BYTE-EQUAL.
3. `tri_fusion_html_round_trip.rs` for TF-RT-HTML-TREE-EQUAL.
4. `tri_fusion_cross_format.rs` for TF-RT-CROSS-FORMAT-COHERENCE.
5. `tri_fusion_failure_determinism.rs` for TF-RT-FAILURE-DETERMINISM.

Cargo count must grow by at least 50 tests during implementation. The first test added in Phase C should assert the zero-feature JSON identity for a minimal paragraph document, then grow outward; Swift FFI and Epdoc highlighting must wait until the Rust proof surface has a stable document hash and witness hash.

## 3. Agent-Facing API

Doctrine target for iteration 14.

The Rust API must define:

- `TriFusionDocument`.
- `TriFusionMutation`.
- `TriFusionWitness`.
- Block references compatible with existing `BlockRef` where possible.
- Mutation variants: `insert-block`, `mutate-block`, `link-block`, `transclude-block`.
- Serialization calls for JSON, Markdown, and HTML projections.
- Validation calls that return typed errors instead of lossy strings.

## 4. Model Wiring

Doctrine target for iteration 15.

LocalAgent must learn the Tri-Fusion mutation ABI without losing the existing tool-call boundary:

- Prompt text must forbid opaque full-document Markdown rewrites for Epdoc edits.
- `LocalToolGrammar` must constrain mutation arguments through structured tool schemas when available.
- The soft-guidance fallback must still name the exact ABI and required fields.
- Model output must include actor, rationale, target document, target block, and requested mutation.

## 5. Editor Wiring

Doctrine target for iteration 16.

Epdoc editor wiring must add a typed path:

- Swift command case for a Tri-Fusion mutation envelope.
- JS `window.epistemos.applyTriFusionMutation(...)` receiver.
- Deterministic ProseMirror transactions for all mutation variants.
- JS-to-Swift acknowledgement message carrying witness data.
- Visible marking for model-authored blocks.
- Snapshot emission remains post-transaction evidence, not the mutation contract.

## 6. Provenance Hook

Doctrine target for iteration 17.

Every accepted mutation must create or link:

- A `MutationEnvelope`.
- A ClaimGraph node.
- A Cognitive DAG edge.
- A `TriFusionWitness`.
- A replay-serializable record compatible with `ReplayBundle`.

The witness must include pre-state hash, post-state hash, mutation hash, actor, source format, projection results, and any schema repair decision.

## 7. Open Theorems

Doctrine target for iteration 18.

Open theorem list:

1. Markdown serializer completeness for the supported subset.
2. HTML semantic normalizer correctness for every custom node.
3. Nested Hyperdynamic Schema repair safety under model-authored mutations.
4. EML ambiguity scoring bounds for parse-choice ranking.
5. Mixed-format diff minimization without hiding semantic changes.
6. Block identity preservation across paste, split, merge, transclusion, and model mutation.
7. Corpus sufficiency for the 200-document property test suite.
