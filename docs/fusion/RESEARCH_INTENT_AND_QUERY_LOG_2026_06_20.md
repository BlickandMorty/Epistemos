# Research Intent & Verbatim Query Log — 2026-06-20

> **Purpose.** Capture the owner's *original intent* verbatim so it is never lost.
> In the past, research was dropped because it was not saved. This document is the
> durable record of what the owner asked for, in their own words, alongside the
> research consolidation work it triggered.
>
> **Companion docs (do not lose):**
> - `docs/fusion/RESEARCH_LOOP_LEDGER_2026_06_20.md` — the append-only research/design ledger (every loop pass appends here).
> - The hardened deliberation, full inventory, theorem catalog, dropped-idea register, and date lineage produced this session (in agent transcripts; key conclusions mirrored into the ledger).
>
> **Standing preservation rule (must hold every loop pass):**
> 1. Every owner query is saved here verbatim, in order, capturing initial intent.
> 2. All research found is saved to the ledger — nothing is dropped.
> 3. Persistence is double/triple-checked: write → read back → confirm.
> 4. Authority docs (Living Index, lattice explainer, MASTER_* canon) are never edited without explicit owner green-light; proposed edits live as write-plans in the ledger.

---

## Session theme (one line)

Abstract the owner's entire body of local research (substrate / full architecture / lattice explainer / living index / 70B cocktail / ternary-BitNet / hyper-deterministic loop / theorems) into a **real, post-Transformer model architecture fused with the Epistemos app** over the unified address space — model emits signals → app deliberates → app signals back — bespoke-engineered for the owner's M2 Pro, grounded in the no-compromise canon, and proven via the existing falsifier/tier-promotion discipline.

---

## Verbatim query log (in order)

### Q1 — Opening intent
> So "proven path or go deep with my own logic" isn't either/or — it's an order. Build the proven skeleton first, small MLX model with a baseline, your interrupt as the one new variable, and let it earn the right to carry the deeper layers and the ternary quant on top. Which level do you want to pin down first: where the interrupt lives, control loop versus soft gate, or whether the mechanism even moves the loss at toy scale?so I have architecture that I want to integrate on and I actually wanna turn into a real model architecture and it's the 70 B large model thing it's the it's the entire cocktail of all the research I've been doing like the deep research of deep architecture things from the lettuce, explainer the living index and even deeper like I have a lot of local research that the tangents on the Terary bit net bit style Quan and then all the other ones I just have a lot of research a lot of sparse research a lot of dense research trickled around my my laptop, so I really want you to see if you can research it and also just let me know how I can do this how I can do this cause I really want to take it to the next level I want it to be where my app is like the second part of the LLM in the element architecture has a lot of optimizations a lot of things, but it seems specific signals out to my app for my app to deliberate and then send signal back send signals back to the model, etc. so it could be like that but yeah, I just cause there's a lot of research I have and I really lost track of all of it. I know that most of it is some keywords is substrate, full architecture, latest explainer living index, but also have other keywords like 70 B and other keywords that date all the way back to like the obscura era when I was working on Obscura. and then I did research after that and yeah.

### Q2
> Start multitasking

### Q3 — Scope: post-Obscura, multi-LLM, fusion folders
> but I really have research on my so it's after obscure after the obscure simulation hyper deterministic loop research around those terms I did and it's not just a 70 B cocktail that's where they just one part of it but the full architecture is like it's kind of like scaffold around the entire local research cause I had had Kimmy do research. I had ChatGPT do research at Claude. Do research Gemini do research. I had multiple folders in my downloads directory and I had multiple research in my fusion folder, which is in my epic demos directory and also a fusion folder somewhere else, but they have a lot of up-to-date research as well, etc. so I just wanna make sure that

### Q4 — Lattice explainer, PARS, theorems, Codex proof work, hybrid/post-Transformer
> and another important part is that the lettuce explainer should have everything if not everything it should have the most we're not even the most up-to-date, but it should have a lot of up-to-date things and then things that you know mention things like pars architecture you know my theorems I have a lot of theorems some even improving some proved, and then I was I was literally making tests to prove to iterate improved theorems as I go, so there was like a lot of unfinished work that one of the old agents did Codex was working on it a lot and then it stopped doing it cause it was just a long process. A lot of that is already built and a lot of debt is like steel in the queue and I want to begin just now see what things I can make a model architecture when things I can keep the app site so that the app in the model kit you communicate together in this process I really want to make a larger model in a large model comes from the direct optimizations that my architecture does you know that it's gonnaConnect my app to my model it should have that you know the unified address space it should if you don't utilize all of the specific things that I built in the things that I queued to you know great test for for my app so that I can create the app in the model hybrid, so it's gonna be like a hybrid thing and yeah should be highly optimized because of my architecture and all of the research I've done already tried to abstract that to a model a real model architecture maybe even something post trans Transformers or you know just something different right

### Q5 — Deep exhaustive sweep: standalone GPT folder, date arc, already-built-in-app
> One of the folders I have is named just GPT. It's just named GPT and then I have other ones even made around that date so like even around the date like the June one all the way to June 19 research all the main research April research so research around all of these different dates and then I don't know like just just make sure you do the deep deliberate reverse deep deliberate research on all the parts that you know researching you know my local folders, etc., and just even things that are already built in my app so that as well and just trying to like abstract, all what I have into like a model Architecture

### Q6 — Harden + no-compromise; BitNet turn; new model architecture = end of the road; both model+app
> One of the researchers did not finish, but I don't know if it was supposed to start anyway so make sure that all the agents are good and have their deep research and that they're all up-to-date and just deliberate it in harden please and do as much research as you can before you start knowing crafting and the arising what can become model and what can be what can stay brain what can be hard in both sides in the the optimistic view of this and how to make this work and if I could do the bid net turn that could be something as well, but of course let's just yeah I actually do wanna try that as well cause that was part of the no compromise plan so one of the one of other plants was really making a new model architecture, but that was the absolute end of the road and I wanna start that well. Also combining it with the app architecture so both please

### Q7 — GPT/GPT-research nested folders; full repo + home dir walk
> its GPT research and GPT i have nested folders in downloads dir and also the jojo dir but in downlaods i have more folders in tue epistmeos i have canon docs plan docs research lists erc. please look through all of it.

### Q8 — Folder-name exhaustion: kimi/gpt/claude/epistemos/epistenos (misspelled), phrase-named folders
> not just those but look at the names of my research folders in jojo fownloads and neated folders kimi gpt claude epistemos and epistenos with n because some research is misspeled. but i hae non mentioend terms as research folders so bascially just do deep research on as much ad you can prioritize downloads even longname fodlers like 'the last of it' rhats not the name but thre are folders that have phrases as names the last research last of research etc. and in downloads in epistemos in fusion folders etc.

### Q9 — Add cold assembly, fast/auto-updating weights, Koopman ("K"), Helios 6.2/6.3
> things like cold assembly and even the theoretical things like how has the fast weights or the weight that auto update with every pass with every turn I don't you can add those into into the research as well because those were interesting things as well. The Komodo or Kemono whatever is called starts with a K all Helios Helios 6.2 or 306.2 6.3.Estamos 6.2

### Q10 — Full explainer+living-index capture; revive dropped theoretical ideas; date lineage from Hermes/Obscura era
> make sure the entire letter is in the entire living index in all the newest updated things so the things that I had created from that long Codex loop where it was working on things improving things and falsifying things with the sorry the link, etc. all those things it needs to be robust deterministic enough, etc. so you know. like all of the F the K the W all of those the ease, all of the the numbers and the letters on lax explainer so clearly everything from explain is what I need and everything from the living index, if that needs to be updated as well so literally pull from all of the most updated research and also from the old research as well because I loved old research for things that I dropped that were too theoretical, and I really want to pick them up turn one bit was one of the theoretical ones that I dropped of which there are hosts of many theoretical ones and other architecture addendum that that I could you know add that I forgot about and just dropped since that research but yeah I wanna make sure I add all that as well and around the time that I started researching about the Hermes agent and the hackathon and Obscura and simulation keywords like that the dates that those were researched that's around the time that I started researching for the architecture as well, so based off of the dates as well, so look at the dates and make sure the dates try to find the earliest iteration didn't go up from there, but the latest iteration is the most updated one but still there are things that I dropped so I wanna make sure that you go and fix not fix a bit like goduty research on all the old ones as well

### Q10b — Decision (selected): fix the explainer drift (honest proof tags + Lean burn-down); hold M0/M1 crafting; GPT folder = GPT Research
> [Form selections] doc_action = explainer_fix · next_build = docs_first · gpt_folder = is_gpt_research

### Q11 — /loop: perpetual research + split + master connection + control panel (5 min)
> /loop so literally all the research all the places I told you to look for research and just based off your own intuition, every single past create new keywords and new ways to find old research and even new research that you forgot about that you forgot to add in things that I dropped a long time ago emphasis on updated things, but also make sure that you include all things from like the art is arcs Ivy The get hub hugging face X local research there's even pasted things that I had but that's going a little bit too deeper hard to find so just try to find as much as you can to make sure that it's all there. You know that nothing is missed out in loop this forever so make sure that this loop goes on and definitely to try and find as much research as you can and then also research ways to split them so split for app and for the model architecture both and then create the master connection where the models always say link to the app in the app cycling Signal link to the model, etc., and they have like a very robust and harden connection and communication between each other control panel will be the app as well, so there should be a new part of the app this like a full control panel that controlled Lucan is able to control the model in the model can speak to the app, etc. but please make sure you do this in loop go through all of my research all the things I mentioned all of my past queries on this and also have different even different like files on Codex and Claude. Codex has the updated research. Claude is the one I started research on I have threads in Codex over the past month that I have research some of them I've been using the past two weeks. They have been updated so you can look for that as well, but if you are able to look for that you can, but if not, then keep trying to focus in like Lily get through as much research as you can emphasis on the recent research and then most emphasis on the updated Doc it's already have like the lettuce in the living index things like that the master prompts there's really prompt docs that I have that are called prompts in the prompts docs have a lot of interesting information and research as well so please dig into that as well the loop can be 5 minutes

### Q12 — Honesty (not removal); robustness incl. edge cases, perf/speed, reasoning quality; keep loop on + queryable
> what a planner drift I don't wanna remove anything that's beneficial. I just wanna make it more honest and make sure that you know everything is good.and I quite literally want this research to be super robust and it should also touch on all the important parts and even edge cases like you know how the model will talk to the app and optimization performance speed, etc. reasoning qualityand just make sure that the loop stays on and that I'm always able to just ask you updates about the loop and you tell me what's going on with the loop

### Q13 — Bespoke hardware co-design for the owner's M2 Pro; instant recall; app-side too; deeper GitHub/HF/arXiv (lattice, Erdős, orthogonal math)
> and a really important thing is that I want the model to be able to be like bespoke engineer for my Mac because this is the one I'm gonna trade it on so I want it to be able to instantly read information from the memory of my Mac, the Cash, etc. my app has something called instant recall and it's being hardened right now but like there's parts of my app that are super fast and super accurate and I want my models be able to like be able to integrate with my actual laptop in a way that other models just can't like it's gonna be so bespoke engineered for the hardware of the laptop that it's a can to so that's important as well. I've been the same for the app site as well. Make sure engineering macOS, M2 pro useful, etc. network should be ambitious pursuit, but it also definitely should be grounded in no compromise. Important things there's no compromise everything that I want search in all the good things that you want to be added should be added 100% and then the research loop that you're doing I also want that to go into figure out you know the best things to add in the architecture and the best things to code how to implement certain things, etc. so even go deeper look look on good get hub on all the model architectures look on hugging face look on ARXIV all the best architectures that use lettuce that uses the denude no Erdos Ortho math as well

### Q14 — All primitives (not just EML) should have a role: geometry primitives and others
> also, my app doesn't just have eml primitive. It also have geometry, primitives and other types of primitive so I want the primitive also to have their own time to shine in the architecture if it's something beneficial please.

### Q15 — Save everything: verbatim queries + all research, double/triple-checked, never dropped
> also, all of the research that's being done. I really wanted to be added to some Doc that could be saved on other things that were researching is lost. Please make sure that all of my queries are saved verbatim in the Doc as whale so that it captures my initial intent and make sure that just like all of the research is saved in the past a lot of things were dropped because research was not saved so please make sure it's safe double triple check, etc.

### Q16 — Standing-brief: model↔app comms edge cases + optimization/perf + reasoning quality (typed loop directive)
> STANDING-BRIEF UPDATE — applies to THIS pass and EVERY future loop pass (record these as permanent dimensions at the top of the ledger so they persist across passes). The owner wants the research to be super robust and to explicitly cover these dimensions and their edge cases, in addition to research discovery and the split/master-connection/control-panel design:
> 1. MODEL↔APP COMMUNICATION (deep + edge cases): not just the happy-path signal contract, but the hard cases — backpressure when the app deliberates slower than the model decodes; cancellation/teardown mid-generation; partial/streaming signals; race conditions and ordering on the bidirectional bus; what happens when the app says "abstain/wake heavy lane" mid-token; lease revocation; security/authority (no silent reroute, no hidden authority); failure/rollback modes; and the latency budget of the signal bus itself (it must not stall the decode loop). Enumerate edge cases each pass and propose how the hardened contract handles them.
> 2. OPTIMIZATION / PERFORMANCE / SPEED: memory-bandwidth-bound reality on Apple Silicon (M2 Pro 16GB ~200GB/s), latency vs throughput, serial GPU→SSD→GPU transport, KV residency cost, ternary kernel speed, interrupt-gate overhead, control-panel observability cost. Tie every design choice to a performance consequence and a measurable target.
> 3. REASONING QUALITY: how the model⇄app deliberation actually improves answer quality — abstention correctness ("defer beats wrong"), verification via the Cognitive DAG, retrieval grounding, the interrupt firing at the right tokens, and how to measure quality (the seven task families, held-out scorers) without overclaiming.
> Keep all prior loop instructions [...]. Continue PASS 1 now with these dimensions folded in [...]. Honesty per tier-promotion canon throughout — nothing beneficial is ever removed; the goal is robust + honest.

### Q17 — Standing-brief expansion: S-HW bespoke M2 Pro co-design + S-APP-FAST + D-IMPL + lattice/Erdős/orthogonal external research (typed loop directive)
> STANDING-BRIEF EXPANSION — add these as PERMANENT surfaces/dimensions to the ledger header [...]. NEW PERMANENT SURFACES:
> S-HW — BESPOKE HARDWARE CO-DESIGN (this exact machine): the model + app must be engineered for the owner's specific M2 Pro 14" (the training/target machine) [...] Direct/instant reads from Mac memory + cache: UMA/zero-copy, IOSurface/shared buffers, mmap vs the ColdStream serial-transport stance, page cache, KV in unified memory [...] Tie into the app's "INSTANT RECALL" feature [...] Apple Silicon specifics: M2 Pro (12-core CPU, 19-core GPU, 16GB UMA, ~200GB/s), Metal compute, ANE/Neural Engine access (Research-tier) [...] Treat single-box training as an ambitious-but-no-compromise target [...]
> S-APP-FAST — the app's already-fast/accurate subsystems (instant recall, Eidos shadow search BM25+HNSW+RRF, KV-Direct, the Rust FFI hot paths) are the bespoke advantage; design the model to lean on them rather than reimplement. Equally bespoke-engineer the APP side for macOS/M2 Pro.
> D-IMPL — IMPLEMENTATION GUIDANCE (not just research): each pass must also produce concrete "what to ADD to the architecture" + "HOW to code/implement it" proposals — file targets, crate/module placement, FFI seams, the smallest falsifier that proves it [...]
> WIDEN EXTERNAL RESEARCH each pass: go deeper on GitHub, Hugging Face, and arXiv for the BEST model architectures relevant to this design — especially LATTICE-based methods [...], ERDŐS / extremal & construction-search methods (PatternBoost/"Erdos-Lift" lineage), and ORTHOGONAL math (orthogonal/unitary parameterizations, Koopman/spectral, RoPE-as-rotation, orthogonal init/regularization) [...] Ground everything in NO-COMPROMISE: ambitious target, but each claim tier-honest.

### Q18 — S-PRIM primitive family (not EML-only) + preservation duty + PASS 3 (typed loop directive)
> Two PERMANENT additions to the ledger header (persist across all future passes), then run PASS 3. ADD TO HEADER: S-PRIM — PRIMITIVE FAMILY: the architecture must not be EML-only. Inventory ALL of the owner's primitives from the actual code/research and give each an evaluated role. Known/likely: EML (eml(x,y)=exp(x)−ln(y); research/eml*, bin/epistemos_eml.rs), GEOMETRY primitives (search for geometry/geometric primitives, lattice/E8/Leech sherry_lattice/e8.rs, Koopman/spectral, orthogonal/rotation primitives, simplex/curvature/Apollonian per H14, ultrametric/p-adic per E2), and any OTHERS you find [...] For EACH primitive: what it is, where it lives, which side it earns a role on (model-internal operator / app-side deliberation / signal bus), honest tier, and the smallest falsifier proving it's beneficial. Only promote a primitive if it genuinely helps — be honest, no forced inclusion.
> PRESERVATION DUTY (every pass): at the start of each pass, confirm [the intent log] and [the ledger] still exist and are intact; append any new owner queries to the intent log verbatim; double-check every write (write → read-back → confirm). The failure mode to prevent: "research dropped because it wasn't saved."
> THEN RUN PASS 3 [...]: Codex/Claude thread reachability on disk [...]; one Erdős / orthogonal-math source-card [...] mapped to the architecture; and concrete control-bus latency numbers for S-CONN [...]. Plus advance S-PRIM [...] this pass.
> NOTE: a separate worker is ingesting a pasted Gemini 70B blueprint into docs/fusion/pasted/ and will hand back a "ledger integration block." On a LATER pass (once that file exists), fold its accepted items (Engram O(1) lookup/MoLKV, ReLU² activation sparsity, SpQt zigzag, sliding-window FFN cache, pre-attention prefetch router, the third 'Lookup Plane') into the ledger [...]. Don't do it this pass unless the file is already present.

### Q19 — PASS 4 directive: integrate Gemini blueprint + Codex slice + orthogonal source + S-PRIM + cockpit D-IMPL (typed loop directive)
> Run PASS 4. Start with the standing preservation duty [...]. This pass's focus (you queued these):
> 1. INTEGRATE THE GEMINI BLUEPRINT — docs/fusion/pasted/ now has the verbatim capture + evaluation + a paste-ready ledger block from the ingestion worker. You are the single writer of the ledger: read that evaluation file and fold its ACCEPTED items into RESEARCH_LOOP_LEDGER_2026_06_20.md (Engram O(1) lookup/MoLKV, ReLU² activation-sparsity spine, SpQt zigzag layout, sliding-window FFN cache, pre-attention prefetch router, the third 'Lookup Plane'), each with side/tier/smallest-falsifier, and adopt its merged build order that keeps the M0/M1 gate first. Respect the ingestion worker's honesty corrections (SpQt is NEW not pre-existing; Engram is only a Lane-3 type surface today; MoLKV absent).
> 2. Mine the 2026-06-01 Codex session slice (recent-first recipe you recorded) — surface any architecture decisions/ideas not yet in the ledger; don't get stuck (one slice only).
> 3. One orthogonal-math source-card (orthogonal/unitary parameterizations, RoPE-as-rotation) mapped to Geometry-IR / the model spine.
> 4. Advance S-PRIM: evaluate the queued Tropical, ultrametric/p-adic (E2), and simplex/Apollonian (H14) primitives (role/side/tier/falsifier, honest — skip any that don't genuinely help).
> 5. D-IMPL: a concrete proposal for wiring the control-panel ("Model Cockpit") InterruptScore feed (file targets, FFI seam, flag, smallest falsifier).
> All T0/T1 write-plan; no authority-doc edits without green-light; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Append to the ledger, double-check writes, and point the next pass at the next Codex date-slice + remaining primitives + S-PANEL uplink controls.

### Q20 — PASS 5 directive: line-level Codex mine + locate E2/H14 + verify Engram/MoLKV/SpQt + design S-PANEL uplink controls (typed loop directive)
> Run PASS 5. Begin with the standing preservation duty [...]. This pass's focus (queued from pass 4):
> 1. LINE-LEVEL mine the dense 2026-06-01 Codex session file + the 2026-06-02 cluster — extract concrete architecture DECISIONS/ideas not yet in the ledger (not just corroboration). One or two files; don't get stuck on all 519.
> 2. Locate the E2 (ultrametric/p-adic) and H14 (simplex/Apollonian) primitive docs in the corpus; if found, upgrade their S-PRIM entries from T0 doc-tier with a real role/falsifier; if genuinely absent, record that honestly and stop forcing them.
> 3. Verify the DeepSeek Engram + MoLKV + SpQt sources against primary literature (arXiv/GitHub/HF) — confirm or correct the items integrated last pass; fix any claim that doesn't hold (esp. the unverified 1/100000 activation ratio).
> 4. DESIGN THE S-PANEL UPLINK (write) CONTROLS — the control-panel side where the user actually steers the model: interrupt threshold τ, route/lane selection, residency/cold-assembly budget, ternary/quant lane toggle, fast-weight quarantine TTL, abstention policy. For each control: what it writes, to which Rust/UAS seam, the safety bound (no-hidden-authority, lease-gated, rollback), and the smallest falsifier. Build on pass-4's signal_bus.rs downlink so uplink+downlink form the full bidirectional Model Cockpit.
> All T0/T1 write-plan; append to the ledger; double-check writes; no authority-doc edits without green-light; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Point the next pass at the next Codex date-slice + remaining primitives + a first end-to-end M0 falsifier spec (falsify_interrupt_moves_loss) draft.

### Q21 — PASS 6 directive: June-2 Codex mine + verify activation-sparsity stack + draft M0 falsifier spec (docs-only) (typed loop directive)
> Run PASS 6. Preservation duty first [...]. Focus this pass:
> 1. Mine the 2026-06-02 Codex cluster (one slice) — extract concrete decisions/ideas not yet in the ledger; don't get stuck.
> 2. Verify the activation-sparsity source stack against primary literature: ReLU² (Primer/"Squared ReLU"), PowerInfer, DejaVu (contextual sparsity), and Apple "LLM in a flash" — confirm the >90% sparsity and I/O-reduction claims, correct anything that doesn't hold, and map each to the S-HW prefetch/sliding-window items.
> 3. DRAFT THE M0 FALSIFIER SPEC — `falsify_interrupt_moves_loss`: a written SPECIFICATION/write-plan ONLY (do NOT write code or create the .rs file — owner deferred M0/M1 crafting with "docs_first"). The spec should define: the toy SSM + interrupt-gate setup, the two baselines (always-SSM, always-attention), the synthetic "interrupt-needed" task, the held-out loss-delta + ablation measurement, the AUROC≥0.85 bar reused from interrupt_calibration.rs, the result.json schema (mirroring falsify_70b_local_cocktail_lite.rs's artifact shape), and the exact pass/fail thresholds. Record it in the ledger as the M0 gate spec so it's build-ready the moment the owner green-lights crafting.
> All T0/T1 write-plan; append to ledger; double-check writes; NO code/no .rs files; no authority-doc edits; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Point the next pass at the M1 Lean spec (InterruptInvariant + Bauer-Fike) and the next Codex date-slice.

### Q22 — PASS 7 directive: M1 Lean spec (docs-only) + June-9 Codex mine + Kuramoto/IR-stack primitives (typed loop directive)
> Run PASS 7. Preservation duty first [...]. Focus this pass:
> 1. DRAFT THE M1 LEAN SPEC (docs-only write-plan, NO Lean code, NO discharging sorry yet — owner docs_first hold): specify exactly how to close (a) `InterruptInvariant` (replace the `: True` placeholder with the real statement over AnswerPacket/AttentionMode/ClaimKind: every emission tags attention_mode ∈ {dynamic, static_fallback, unavailable}; static_fallback must pair with a StaticFallbackAcknowledged claim) and (b) the Bauer-Fike WBO-6 bound (quantizing the SSM A-matrix shifts Koopman eigenvalues by ≤ κ(V)·‖ΔA‖ — the provable error bound for the ternary lane). For each: the precise theorem statement to write, the proof strategy, the Lean file target (ResearchCanon.lean and/or the in-repo Epistemos/lean/ project), and how epistemos_doctrine_lint would bind it. Record as the M1 spec so it's build-ready alongside M0.
> 2. Mine the 2026-06-09 Codex slice (one slice) — extract concrete decisions/ideas not yet in the ledger; don't get stuck.
> 3. Evaluate the Kuramoto-sync and IR-stack primitives for S-PRIM (role/side/tier/falsifier, honest — skip if they don't genuinely help).
> All T0/T1 write-plan; append to ledger; double-check writes; no code/no Lean proofs; no authority-doc edits; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Point the next pass at the next Codex date-slice + S-APP-FAST (the W-51 shadow-recall unification design) + any remaining primitives.

### Q23 — PASS 8 directive: mine 05-24/05-06 Codex + design S-APP-FAST W-51 shadow-recall unification + advance a primitive (typed loop directive)
> Run PASS 8. Preservation duty first [...]. Focus this pass:
> 1. Mine the older 2026-05-24 and 2026-05-06 Codex rollouts (one or two slices) — these likely hold the ORIGINAL split / interrupt / two-brain deliberations. Extract concrete decisions/ideas not yet in the ledger; don't get stuck.
> 2. DESIGN THE S-APP-FAST W-51 SHADOW-RECALL UNIFICATION (the highest-value bespoke-hardware win from pass 2): the model currently queries a separate colder index (VaultStore + InMemorySemanticIndex) while the sidebar uses the warm epistemos-shadow BM25+HNSW+RRF k=60 fusion. Design the unification — the `VaultBackend` adapter in agent_core/src/eidos/, reuse of the shadow_handle_search FFI seam (no JSON round-trip), the EPISTEMOS_SHADOW_RECALL_V1 flag, the migration/coexistence plan, and the falsify_shadow_recall_parity smallest-falsifier. Make it a concrete D-IMPL build-plan (write-plan only, no code).
> 3. If a new owner query arrived, honor it; otherwise advance one remaining S-PRIM primitive honestly.
> All T0/T1 write-plan; append to ledger; double-check writes; no code; no authority-doc edits; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Point the next pass at the next Codex date-slice + an S-CONN failure-mode/edge-case deep-dive (D1-COMMS).

### Q24 — PASS 9 directive + THREE permanent standing-brief updates: 3-cycle depth rule, dual-brain/Rust spine framing, reinforced preservation (typed loop directive)
> THREE PERMANENT standing-brief updates to the ledger header (persist across all passes), then run PASS 9.
> ADD TO HEADER:
> 1. DEPTH RULE — "3 cycles deep per pass type": each research-pass TYPE (Codex/Claude mining, primitive evaluation, source verification, design surface like S-CONN/S-PANEL/S-HW, M0/M1 specs) must run ~3 deepening cycles before being treated as done — don't skim one pass and move on. Go deep enough to exhaust the understanding; record cycle 1/2/3 progress per topic.
> 2. SPINE FRAMING — DUAL-BRAIN / SPLIT-BRAIN with RUST as the low-latency interrupt substrate: treat this as the architecture's spine, not a wrapper. The kernel AND the app both do heavy lifting; Rust is the fast layer that makes interrupting and co-working with the model cheap (signal_bus.rs, interrupt_calibration, the Helios organs). Every design pass must respect: model = one brain (generation/spine), app = the other brain (authority/deliberation), Rust = the fast bus between them.
> 3. PRESERVATION (reinforced) — ALL research goes to the ledger; ALL owner queries go to the intent log VERBATIM. At the start of every pass, append any owner queries not yet logged (verbatim), and verify with read-back. Confirm the recent "do multiple cycles / 3 cycles deep" directive and the dual-brain/Rust directive are both captured verbatim in the intent log; append if missing.
> THEN RUN PASS 9 (apply the 3-cycle depth rule to it):
> - Try Claude's `~/.claude*`/history.jsonl (and any Claude export on disk) for the ORIGINAL split/interrupt/two-brain deliberations — the owner says Claude is where research began. If unreachable, say so and move on.
> - Do the S-CONN FAILURE-MODE / EDGE-CASE DEEP-DIVE (D1-COMMS) — the long-outstanding bidirectional-bus hardening spec — and run it ~3 cycles deep: cycle 1 enumerate all failure modes (backpressure, mid-token abstain, lease revocation, ordering/races, cancellation/teardown, partial signals, no-hidden-authority, bus-latency stalls); cycle 2 specify how the hardened contract handles each (with the Rust signal_bus.rs seam); cycle 3 define the falsifiers that prove each is handled. Make it the definitive D1-COMMS hardening spec.
> All T0/T1 write-plan; append to ledger; double-check writes; no code; no authority-doc edits; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Point the next pass at the next deep cycle (continue Claude mining + the S-PANEL uplink safety cycle 2/3 + remaining primitives).

### Q25 — PASS 10 directive: Claude mining cycle 3 + S-PANEL uplink cycles 2&3 + active_assembly/acs primitives + Mamba-3/attention-sinks sources (typed loop directive)
> Run PASS 10. Preservation duty first [...]. Apply the 3-cycle DEPTH RULE and the dual-brain/Rust SPINE framing. Focus this pass:
> 1. Claude mining CYCLE 3 — line-level read of Claude history/exports for the ORIGINAL model↔app deliberation (the genesis of S-SPLIT/S-CONN). Extract concrete decisions not yet in the ledger; if the origin truly isn't there, conclude that honestly and stop the Claude thread.
> 2. S-PANEL uplink-safety CYCLES 2 & 3 — building on pass-5's 6 controls and pass-9's SPSC/lease/boundary-apply primitives: cycle 2 = specify the exact safety mechanism for each of the 6 uplink controls (lease-gated, apply-at-token-boundary, atomic rollback, no-hidden-authority); cycle 3 = define the falsifier per control. Deliver the definitive S-PANEL uplink hardening spec so the Model Cockpit (uplink+downlink) is fully falsifier-covered.
> 3. Evaluate the active_assembly / acs primitives for S-PRIM (role/side/tier/falsifier, honest).
> 4. Pull the still-outstanding Mamba-3 and attention-sinks external source-cards (primary literature) and map them to the model spine + interrupt.
> All T0/T1 write-plan; append to ledger; double-check writes; no code; no authority-doc edits; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Point the next pass at the remaining primitives + a first consolidated "architecture readout" draft (the single coherent picture) once the deep cycles are exhausted.

### Q26 — PASS 11 directive: begin the consolidated ARCHITECTURE READOUT (single coherent picture) + finish last primitives (typed loop directive)
> Run PASS 11. Preservation duty first [...]. Apply the dual-brain/Rust spine framing. Primary deliverable: BEGIN THE CONSOLIDATED ARCHITECTURE READOUT — the single coherent picture that ties every loop pass together, written into the ledger as a top-level section (and consider whether it deserves its own companion doc docs/fusion/ARCHITECTURE_READOUT_2026_06_20.md — a NON-authority consolidation doc). It must cover: BRAIN 1 (model: Mamba-3 SSM spine + attention-sinks default lane + interrupt gate + ternary/quant lane + Engram/Lookup-plane + KV-Direct, honest tiers); RUST BUS (signal_bus.rs SPSC downlink+uplink, M0 + M1 gate specs, hardened D1-COMMS contract 10 downlink + 6 uplink falsifiers); BRAIN 2 (app: RuntimeRouter/authority, active_assembly nervous system, Model Cockpit, W-51 shadow-recall, Cognitive DAG/verification, continual-learning/Never Retrain); THE SIGNAL CONTRACT (model emits → app deliberates → app signals back, typed primitives, where the interrupt lives); THE BUILD ORDER (M0 → M1 → heavy kernels/ternary/cold lane, each milestone's tier + promoting proof; built vs spec'd vs ambition); THE PRIMITIVE FAMILY (S-PRIM) roll-up (EML, Geometry-IR, Koopman, Belnap, E2, scan_ir, active_assembly, continual_learning, Tropical — role/side/tier). Run it ~3 cycles deep (cycle 1 structure, cycle 2 fill with real artifacts + tiers, cycle 3 honest gaps + single recommended next action). Finish any last unevaluated primitive honestly. All T0/T1 write-plan; no authority-doc edits without green-light. After this, point the next pass at refreshing newest external research + re-deepening the weakest segment.

### Q27 — PASS 12 directive: first maintenance/refresh pass — external research refresh + re-deepen weakest readout segment (typed loop directive)
> Run PASS 12 — first maintenance/refresh pass. Preservation duty first [...]. Dual-brain/Rust spine framing + 3-cycle depth apply. Focus:
> 1. EXTERNAL RESEARCH REFRESH (fresh keyword set, primary sources — arXiv/HF/GitHub, current as of June 2026): Mamba-3 follow-ons / SSM-Transformer hybrids, BitNet/ternary 2026 advances (QAT, kernels on Apple Silicon), test-time-training / fast-weight successors, MoE SSD-streaming / activation-sparsity serving, Mirror Speculative Decoding / ANE drafting. For each genuinely new finding: source-card + which readout segment it strengthens or challenges + honest tier. Skip anything already covered.
> 2. RE-DEEPEN THE WEAKEST READOUT SEGMENT (3 cycles): pick the weakest of {the Mamba-3 ↔ M0 spine link, the W-51 embedding-parity risk} (or another you judge weakest) and harden it — concrete design, the integration risk, and the falsifier that retires the risk.
> 3. Keep the ARCHITECTURE_READOUT_2026_06_20.md doc current if this pass changes any segment's tier or adds a load-bearing finding (double-check the write).
> All T0/T1 write-plan; append to ledger; double-check writes; no code; no authority-doc edits without green-light; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. Point the next pass at the next weakest segment or a fresh external sweep; the loop is now in sustainable refresh mode — each pass must add something genuinely new or honestly report a null result and redirect.

### Q28 — PASS 13 directive: external sweep (Mamba-3 followons/TTT/MoE-SSD) + re-deepen the dead RuntimeRouter keystone (typed loop directive)
> Run PASS 13 (sustainable refresh mode). Preservation duty first [...]. Dual-brain/Rust spine + 3-cycle depth. Focus:
> 1. EXTERNAL SWEEP (the queued set, primary sources, June 2026): Mamba-3 follow-ons / SSM-Transformer hybrids, test-time-training successors, MoE SSD-streaming serving. Source-card only genuinely-new findings; map each to a readout segment + honest tier; skip already-covered.
> 2. RE-DEEPEN THE NEXT-WEAKEST SEGMENT (3 cycles) — the dead RuntimeRouter keystone (the biggest already-built-but-unwired gap per MASTER_SYNTHESIS): cycle 1 = what RuntimeRouter is, where it's defined, and exactly why it's "dead" (not wired/flag-off/no caller); cycle 2 = the concrete wiring plan to make it the live authority for the model↔app split (file targets, the seam, the flag, no-hidden-authority preserved); cycle 3 = the falsifier(s) that prove it's actually live and routing. This is brain-2's authority core, so it's load-bearing for the whole split.
> 3. Keep ARCHITECTURE_READOUT_2026_06_20.md current if anything shifts (double-check writes).
> All T0/T1 write-plan; append to ledger; double-check writes; no code; no authority-doc edits without green-light; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc. If a pass ever finds nothing genuinely new across both research and re-deepening, say so honestly and recommend pausing the loop rather than churning. Point the next pass at the next-weakest segment or a fresh external angle.

### Q29 — PASS 14 directive + TWO permanent standing-brief items: nuanced-keyword mining + S-UAS-COMPUTE optimization lens (typed loop directive)
> Run PASS 14 (refresh mode). Preservation duty first [...]. ADD TWO PERMANENT standing-brief items to the ledger header:
> 1. NUANCED-KEYWORD MINING — "dual brain" is a recent label; older research used different names for the same model↔app split idea. When mining old research, ALSO search synonyms: "Brain + Hands", "two-brain", "model wrapper", "coprocessor", "split brain", "J-limb/M-limb", "Brain 1/Brain 2", "controller plane", "device agent", "Mirror Speculative Decoding", "hands/actuator", "deliberator". AND explicitly surface what the owner's ORIGINAL term for the split was (don't assume it was called dual-brain). Apply this nuance to every old-research/Codex/Claude/doc mining pass.
> 2. S-UAS-COMPUTE (cross-cutting lens) — exploit the Unified Address Space in EVERY architecture segment to get capability with MINIMAL compute. For each segment, ask "can this be done with less compute via UAS instead of dense matrix multiply?" and prefer compute-light paths: ternary add/sub instead of FMA, Engram/lookup-table recall instead of FFN compute, lattice VQ, activation-sparsity skip, zero-copy pointer passing across Swift/Rust/Metal, KV-Direct residency instead of recompute. Optimize as deeply as honestly possible; tier-flag each.
> THEN RUN PASS 14: Apply S-UAS-COMPUTE as a 3-cycle deep-dive THIS pass: enumerate every place the architecture currently implies dense matmul/heavy compute, propose the UAS-based compute-light alternative for each, and name the falsifier that proves the alternative preserves correctness. This is the owner's "optimize it as deeply as it can be" directive — make it concrete. Re-deepen the next-weakest segment if time: the Mamba-3↔M0 spine link OR the AnswerPacket "implemented-but-no-caller" downlink gap. Keep ARCHITECTURE_READOUT_2026_06_20.md current. All T0/T1 write-plan; no authority-doc edits without green-light. If nothing genuinely new emerges on both research + re-deepening, say so honestly and recommend pausing the loop.

### Q30 — PASS 15 directive: re-deepen Mamba-3↔M0 + AnswerPacket no-caller gap + SSM-hybrid query + honesty/pause gate (typed loop directive)
> Run PASS 15 (refresh mode). Preservation duty first [...]. Focus:
> 1. RE-DEEPEN the Mamba-3↔M0 spine link (3 cycles): cycle 1 = what state-tracking weakness the interrupt must compensate for; cycle 2 = decide the simplest SSM for the M0 toy that exhibits that weakness (vs deferring full Mamba-3 to B3) and justify; cycle 3 = lock the M0 toy-SSM choice into the M0 spec so it's unambiguous when crafting starts.
> 2. RE-DEEPEN the AnswerPacket "implemented-but-no-caller" downlink gap (3 cycles): where it's defined, why nothing emits it, the concrete first-caller wiring, and the falsifier proving it's emitted+consumed.
> 3. One dedicated SSM-Transformer-HYBRID external query (primary sources) — the queued item not yet surfaced; source-card only if genuinely new.
> 4. Keep ARCHITECTURE_READOUT_2026_06_20.md current (double-check writes).
> All T0/T1 write-plan; append to ledger; double-check writes; no code; no authority-doc edits without green-light; exclusions ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc.
> IMPORTANT HONESTY GATE: the loop is converging. If this pass finds nothing genuinely new on BOTH the research axis AND the re-deepening axis (i.e., it only re-confirms what's already in the readout), say so plainly and RECOMMEND PAUSING THE LOOP — list the few remaining open questions that only building (M0) or a new owner directive can resolve. Don't churn.

### Q31 — Loop RESUMED at 2-min cadence in HARDENING + IDEA-EXPANSION mode; HOLD OFF building (git-autocommit context) (typed loop directive)
> The loop is RESUMED at a 2-minute cadence in HARDENING + IDEA-EXPANSION mode. HARD CONSTRAINT (owner directive): HOLD OFF on building/coding — no .rs/.lean/.swift code, no M0 build, no implementation. This is research/deliberation/hardening only. (Files live at absolute paths under /Users/jojo/Downloads/Epistemos/; an external build loop auto-commits via git add -A, so never leave half-written code around — another reason to stay docs-only.)
> Standing duties each pass (unchanged): preservation duty FIRST (both ledgers + readout intact; append any new owner queries verbatim — including the recent "/loop 2 minutes, hold off on building, continue hardening + add to my ideas" directive and the git-verification context; write→read-back→confirm). Keep the no-redundant-research rule, honesty/tier-promotion, and the "last auditor" check (every "done" claim must be REAL + test-backed + cites-slice + user-facing end-to-end). Avoid ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc.
> GOAL this mode: truly HARDEN and EXPAND the owner's ideas — go deeper than the converged readout. Each pass picks a surface/primitive/mechanism and (a) hardens it (more edge cases, failure modes, falsifier coverage, alternatives, cross-links), and (b) EXPANDS it with new beneficial ideas (don't just re-confirm — add). Rotate focus. Candidate targets: controller-plane/model↔app split (authority + lease semantics, edge cases); S-UAS-COMPUTE map U1–U9 (push each further; find U10+); primitive family S-PRIM (enrich roles; find new primitives/compositions); ternary/BitNet + Engram/Lookup (QAT/distill, kernel co-design, KV); dropped-idea register (revive + flesh out more); cold assembly/residency/fast-weights (transport + quarantine); each falsifier (strengthen pass/fail + adversarial cases).
> PASS 16 (this pass): append the new owner directives verbatim, then pick the highest-value hardening+expansion target and go ~3 cycles deep on it. Append results to the ledger (and readout if it adds durable structure). End each pass by naming the next pass's target. If a pass finds nothing genuinely new to harden or add, say so honestly and recommend lowering cadence rather than churning.

### Q32 — PASS 17 directive: push S-UAS-COMPUTE beyond U1–U9, find U10+ (typed loop directive)
> Run PASS 17 (hardening + idea-expansion mode, docs-only, NO code — owner hold stands). Preservation duty first [...]. Target (named last pass): push the S-UAS-COMPUTE map beyond U1–U9 — find and design U10+ compute-light mechanisms, 3 cycles deep, each with: the dense/expensive thing it replaces, the UAS-based compute-light alternative, the honest tier, and a correctness-preserving falsifier. Candidate U10+ to evaluate (add others you find): speculative-decode as a cheap *verified* lane (MLX-native draft→verify, lease-gated); attention-sink KV pinning (pin the 4 sinks, evict the rest — compute-light long context); Engram-hash as a Bloom pre-check (skip the FFN entirely on a confident lookup hit); the lease ledger itself as compute-light governance (scalar accounting, not compute); any UAS zero-copy / dedup / content-address reuse that avoids recompute. Be honest: if a candidate doesn't actually save compute on the bandwidth-bound M2 Pro, mark it skip with the reason (as you did for lattice-VQ U8). End by naming PASS 18's target. If this pass finds nothing genuinely new to add, say so and recommend lowering the 2-min cadence rather than churning. All T0 design behind the M0/M1 gate; append to ledger (+ readout if durable); avoid ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc.

### Q33 — PASS 18 directive: harden Engram/Lookup plane + ternary lane co-design (typed loop directive)
> Run PASS 18 (hardening + idea-expansion, docs-only, NO code — owner hold stands). Preservation duty first [...]. Target (named last pass): harden the Engram/Lookup plane + ternary lane CO-DESIGN, 3 cycles deep: The QAT/distill path (BitDistill 16→1.58-bit, ~10× memory) as the feasible single-box route to a ternary model — concrete steps, what's acquired off-box vs done on the 16 GB M2 Pro, honest tier. The kernel co-design: I2_S/TL1/TL2 (bitnet.cpp, 100% lossless) + Litespark NEON-SDOT — how the lossless ternary matmul interacts with U12 (Engram Bloom-gate, skip FFN) and U14 (prefix-KV reuse). Where do they compose, where do they conflict (SpQt-style quant×sparsity layout warning)? The ternary-vs-fp16 KV RESIDENCY question: KIVI asymmetric-KV (already a repo source-card) — per-channel key / per-token value quant; how it interacts with U11 (attention-sink KV pinning) and KV-Direct. Quantify the residency budget on 16 GB. Falsifiers for each co-design seam (lossless-parity, Bloom-gate-no-false-negative-under-ternary, KV-quant-quality). Be honest about composition conflicts and tiers (all T0 behind M0/M1). End by naming PASS 19's target (candidates: flesh out the dropped-idea register's "hosts of many", or harden cold-assembly transport, or the B6 deliberation tree). If this pass + one more come up with nothing genuinely new, recommend lowering the 2-min cadence. Append to ledger (+ readout if durable; double-check writes); avoid ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc.

### Q34 — PASS 19 directive: flesh out the dropped-idea register's "hosts of many" (typed loop directive)
> Run PASS 19 (hardening + idea-expansion, docs-only, NO code — owner hold stands). Preservation duty first [...]. Target (named last pass): flesh out the dropped-idea register's "hosts of many" — take the ideas catalogued only as one-liners and develop the genuinely beneficial ones into real design, 3 cycles deep. Prioritize: Modern-Hopfield associative recall (H17) — how it composes with Engram/Eidos recall; is it a beneficial brain-2 retrieval primitive? hyper-deterministic loop → selective verify-rollback determinism — flesh out where determinism is enforced vs relaxed (the owner cared about this thread). Helios 6.2/6.3 organs not yet pulled forward — anything beneficial still parked. exotic routing (Berry-phase holonomy H12, CRT storage routing H16) — beneficial or skip-with-reason? For each: develop role/side/tier + falsifier, OR honestly mark skip with the reason (don't force-include). This directly serves the owner's Q10 "I dropped theoretical ones I want to revive." End by naming PASS 20's target. HONESTY WATCH: passes 16–18 each added new structure; if THIS pass (19) and the next (20) both fail to surface anything genuinely new/beneficial, explicitly recommend LOWERING the 2-minute cadence (or pausing) rather than churning. All T0 behind M0/M1; append to ledger (+ readout if durable); avoid ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc.

### Q35 — PASS 20 directive: harden cold-assembly transport + ACTIVE honesty gate (typed loop directive)
> Run PASS 20 (hardening + idea-expansion, docs-only, NO code — owner hold stands). Preservation duty first [...]. Target (named last pass): harden the COLD-ASSEMBLY TRANSPORT, 3 cycles deep — ColdStream PageRun / PageRunScheduler / SlabArena / MetalBufferLease / CodecStage / TransportTrace / ColdPanicFallback. Cover: the serial GPU→SSD→GPU transport discipline (Flash-MoE bandwidth-bound rationale) and where overlap is forbidden; p95/p99 stall budgets, cancellation/teardown mid-transport, copy-count (zero-copy slab leases), SSD wear, cache-pollution, panic-fallback; how it composes with U14 prefix-KV reuse, U11 sink-pinning, KIVI KV residency, and the ComputeResumeLease (residency lease = pre-granted transport budget); falsifiers per seam (stall, cancellation, copy-count, wear, panic). HONESTY GATE (now active per the armed watch): if this pass mostly re-confirms existing transport design without adding genuinely-new hardening or ideas, SAY SO PLAINLY and recommend lowering the 2-minute cadence (e.g., to 10–15 min) or pausing the loop — the hardening surface is finite and converging. Don't manufacture findings. If it IS productive, name PASS 21's target. All T0/T1 behind M0/M1; append to ledger (+ readout if durable); avoid ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc.

### Q36 — PASS 21 (FINAL, one-shot): falsifier-coverage audit before the loop pauses for build (typed loop directive)
> Run PASS 21 — the FINAL deliberation pass before the loop pauses for build (the auto-loop is now stopped; this is a one-shot, not part of a recurring loop). Docs-only, NO code. Preservation duty first [...]. Target: a FALSIFIER-COVERAGE AUDIT across the entire architecture. For every segment/primitive/mechanism in the readout (brain-1 organs, the Rust bus, brain-2 organs, the 15 S-PRIM primitives, the 14 U-mechanisms, the lease lifecycle, the transport, M0/M1): Confirm each has ≥1 named falsifier with honest pass/fail criteria. Flag the ones whose falsifier is still VAGUE or missing (B6 HeavySkill deliberation, Hopfield spurious-attractor bound) — list them with what's needed. Produce a single consolidated FALSIFIER INDEX in the ledger (id → what it proves → status: concrete / vague / missing). This is the last pass. End with: (a) the falsifier index, (b) the short list of vague/missing falsifiers to tighten during build, and (c) a final one-paragraph "state of the architecture: ready for M0" statement. Do NOT recommend more loop passes — after this, the loop is paused pending the owner's build decision. All T0/T1 behind M0/M1; append to ledger + readout; avoid ~/Epistemos-RETRO, src-tauri, ~/meta-analytical-pfc.

---

*This log is append-only. New owner queries on this thread are added verbatim as they arrive. Last updated: 2026-06-20 (PASS 21 — FINAL; loop paused pending build decision).*
