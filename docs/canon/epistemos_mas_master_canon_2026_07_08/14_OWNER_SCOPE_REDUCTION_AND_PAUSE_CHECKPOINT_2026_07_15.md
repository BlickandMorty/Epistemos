# 14 - Owner Scope Reduction and Pause Checkpoint

ID: `EPISTEMOS-MAS-OWNER-SCOPE-REDUCTION-2026-07-15`
Lock: `MAS-ONLY-SHIP-LOCK-2026-07-07`
Status: active latest owner addendum; supersedes conflicting execution language
in `00` through `13` without deleting their historical provenance

Coverage companion:
`15_OWNER_DIRECTIVE_COVERAGE_AND_HARDENING_CHECKPOINT_2026_07_15.md` preserves
the complete add/remove/harden/test register, current per-family status, subtle
P0 boundaries, and exact resume evidence. Read it immediately after this file.

Later execution authority:
`16_TWO_LANE_REMOVAL_AND_REBUILD_DIRECTIVE_2026_07_15.md` supersedes this
file's review pause only for its two disjoint implementation lanes. The
canceled/parked/retained product boundary in this file remains unchanged.

## Owner intent checkpoint

Verbatim owner excerpts:

> “I am canceling LumenLens and all of the AI parts of it.”

> “I'm also ... canceling the Reckoner because I don't want to have the sheets,
> the database stuff on there, at least not now.”

> “what i still want ... is all non ai work minus recloner ... sync and things
> like that i def still want”

> “it is best to pause and separate what is now cancelled competely deferred vs
> what i still want ... tell me what u did and did not finish and what u did not
> audit hardened ... lots of them im not sure got tests”

Interpreted intent:

- Pause implementation at the current source checkpoint so the owner can review
  one honest scope and evidence inventory.
- Cancel LumenLens as a named roadmap/execution lane and cancel all AI, agent,
  model, provider, generative, chat-assist, and AI-suggestion work.
- Park Reckoner, spreadsheet, sheet, dataset-grid, formula-engine, and database-
  surface work reversibly. “At least not now” does not authorize deletion of
  research receipts, compatibility parsers, or history.
- Retain all requested non-AI work except Reckoner. Non-AI editor correctness
  that older files grouped under LumenLens is re-owned by the Epistemos Editor
  Core and KEELSTONE; it is not canceled merely because the codename is.
- Preserve Kokoro read-aloud as the owner's repeatedly named exception. It is a
  local speech-output feature, not authorization for chat, inference settings,
  generative editing, background transcription, or another model picker.

## Latest canonical product boundary

### Canceled now

- LumenLens as an active codename, prompt, dependency, or completion claim.
- June, MiniChat/Epdoc Assist, Copilot/AI partner, agent automation, agent tools,
  general/local/chat models, model and provider selection, generative actions,
  AI summaries, inference routing/state, AI-only background work, and AI-owned
  suggestion/provenance promotion.
- The old shared Markdown-derived LumenLens rich-object/suggestion architecture
  as the future Epdoc authority. The current Epdoc authority is its standalone
  JSON `.epdoc` document/package and a small non-AI typed block schema.
- Any AI extension inside future Browser or ResearchHub work.

Cancellation means absent from the Free V1 compile and visible/runtime surface,
not merely hidden by a disabled button. Existing user chat/session records may
remain readable for migration/export or historical compatibility, but no Free
V1 query, recall, graph, notebook, settings, route, shortcut, restoration,
provider startup, or background job may present or activate them.

### Parked reversibly

- Reckoner itself.
- IronCalc and Univer integration, spreadsheet/grid UI, formulas/calculation,
  workbook/CSV/XLSX/`.icalc` product surfaces, dataset tabs/embeds, dataset
  charts, tabular tools, Data Room, database UI, and the Sheet/Datasheet launcher.
- The quarantined IronCalc and Univer checkouts and receipts remain provenance
  only. They are not app dependencies and must not be built, installed, or wired.
- Teable and Baserow remain historical provenance only.

Compatibility decoding may preserve old sheet/chat reference tokens so existing
documents are not damaged, but the references are not selectable Free V1
features. GRDB/SQLite used for ordinary derived search/index/cache/migration is
not Reckoner and must not be removed merely because database *product UI* is
parked.

### Active non-AI work

1. KEELSTONE vault truth, security-scoped access, coordinated/atomic writes,
   external-change reconciliation, conflict/recovery, pruning, and release gate.
2. Free V1 product boundary: canceled AI and parked Reckoner are not compiled,
   initialized, restored, queried, or presented.
3. Epistemos Editor Core:
   - standalone native TextKit 2 JSON `.epdoc` documents;
   - Markdown `.md` Prose, Preview, and MarkEdit-based Source lenses over one
     Markdown file, plus the distinct code editor;
   - save/autosave truth, crash recovery, large-document performance, stable
     selection/scroll/undo, surface switching, title identity/popover, ASCII/
     blur reveal, owner toolbar, fonts, palette, and accessibility;
   - Epdoc JSON rich blocks and attachments without synchronous Markdown
     mirroring; Markdown/PDF are explicit derived/export paths.
4. Notes Home/Multitask routing, Landing commands, both Home Graph and Multitask
   Graph, Epdoc graph navigation, graph/search, and removal of the unsupported
   third faceless graph presentation.
5. Non-AI Epdoc product growth: native lists/checklists/projects, images and
   attachments, audio/voice notes, explicit system dictation, drawings,
   Meeting references, calendar/reminders/tasks/planner views, dynamic document/
   outline/timeline/object presentations, and honest cross-format inspection.
6. The full-size fifth PDF lens, Epdoc-to-PDF rendition/export, imported-PDF
   fidelity, and later public PDFKit annotation/form/Save-a-Copy work.
7. Sync, Quick Capture/EMBERCATCH, Meeting, EventKit calendar/reminders, Kokoro
   read-aloud, Vision/OCR/import where admitted, graph/search, and portable export.
8. Native MAS integration: App Intents/Shortcuts, Spotlight, share/open-in,
   Services, drag/drop, document associations, Finder/Quick Look, notifications,
   optional widgets, accessibility, restoration/NSUserActivity/Handoff where
   each is independently proven and does not destabilize the editor hot path.
9. Settings simplification, whole-app performance/memory/hang audit, and final
   MAS archive/privacy/entitlement/App Review evidence.

### Deferred, not canceled

- Browser and ResearchHub remain possible deterministic future paid features
  because the owner previously kept them in the paid edition and the latest
  steer retains all non-AI work. They remain completely absent from Free V1.
  No AI assistant, autonomous research, model/provider, scraping, Chromium,
  browser-use, local server, or sidecar may return through either feature.
- StoreKit, payment, Apple enrollment, distribution signing, upload, review, and
  subscription work remain deferred. Missing credentials do not block local
  non-AI source, build, test, or runtime evidence.
- Pro, Developer-ID, Experimental, 1Code, OpenChamber, Goose, Kindred, terminal/
  code-exec, stdio MCP, subprocess/local-server/sidecar, second chat/tool/
  transcript/data authority remain parked or forbidden under the prior canon.

## Tolaria and editor-source correction

There is no current literal Tolaria clone in the shipping app. Historical
Tolaria material was AGPL behavior-study/clean-room research and was already
partly superseded by the instruction not to chase parity. Its code may not be
copied into the MAS product. The current Epdoc direction is native TextKit 2
plus JSON `.epdoc`, while the Markdown Source lens uses the already-vendored
MIT MarkEdit core through Epistemos-owned chrome, palette, toolbar, and safety
boundaries. Tolaria is therefore neither a Reckoner component nor an active
dependency to remove.

## Evidence checkpoint: what is and is not proven

### Proven for an older exact source/artifact

- R109 produced one Free V1 Release archive and passed its artifact scanners.
  It excluded JuneWeb, agent/model/provider resources, `agent_core`, and other
  named paid runtime artifacts. It was unsigned local evidence, not App Store
  distribution proof, and it is stale relative to later source changes.
- R110 launched that exact R109 app once. It confirmed no obvious June/Browser/
  ResearchHub/chat entry on onboarding and no matching Settings accessibility
  label, plus small Markdown Prose edit/backspace/save/Source switching and the
  Source title popover. It also found stale Settings copy, Source-width residue,
  missing Epdoc discoverability, toolbar overflow, and unexecuted graph routes.
  Its Epdoc typing observation was contaminated and proves neither success nor
  failure.
- R113 compiled the Free V1 App Store target and passed four selected guards for
  Source wrapping/no Source-width slider, Landing document commands, Epdoc graph
  source, and Notes Home/Multitask plus two graph routes. R113 is also stale.
- R108's earlier 225/225 automated editor batch and 72k-character timings are
  useful historical evidence, but they predate the newest dirty editor work and
  are not current manual, memory, visual, or release evidence.

### Implemented in current source but not built/typechecked/runtime-proven

- Historical `MotionTitle` recovery and editor-title reveal wiring.
- Epdoc autosave/explicit-save state, coalesced derived projection, adaptive
  checkpointing, reduced duplicate JSON validation, no-op width handling, and
  native link/image/find toolbar actions.
- Native-toolbar capability filtering, retired complexity meter/query/graph
  hot-path removal, and removal of the obsolete code-editor left identity chip.
- Source wrap toggle, Landing/format labels, Notes Home/Multitask routing, two
  graph destinations, Epdoc graph nodes/routes, and the new Home document host.
- New scheme-member tests exist for much of this batch. Only Swift parse and
  `git diff --check` have passed after the latest edits; no current replacement
  Xcode build/test or app launch has run.

### Known unclosed contradictions and defects

- `ContextualShadowsPanel` and `ContextualShadowsState` can still expose stored
  chats in Free V1. This is a current product-boundary defect.
- The Free editor bundle still imports HandleWithCare/AI-diff suggestion code;
  AI diff/bridge/provenance types and dormant AI views/services remain compiled.
  The Free composition root still initializes inert chat/LLM-adjacent services.
- `.reckoner` is still classified as a Free V1 capability in
  `ProductCapabilityPolicy` and its tests, and `VaultIndexActor` retains a
  pending Reckoner dataset hook.
- HTML Workspace diagnostics and Setup Assistant retain stale AI/chat wording.
- The KEELSTONE release-gate source checks still require some June/Goose/
  `agent_core` sources even though the Free artifact gate rejects those products.
- Standalone Epdoc title identity does not yet prove parity with the shared
  Name/Tags/Where and rename/move popover.
- Current-source manual proof is absent for 67k-72k documents, text deletion,
  selection/scroll/undo, surface switching, save/reopen, graph navigation,
  title animation, toolbar selection scope, memory growth, accessibility,
  MarkEdit visual fidelity, Settings cleanup, or Kokoro audibility.

## Remaining work in safe order

1. Reconcile this addendum into the master canon, minimal prompt, handoff, and
   capability truth. Preserve history; do not execute canceled Prompt 3 or the
   old combined Prompt 4.
2. Fail closed the Free V1 boundary: seal Contextual Shadows, remove Reckoner
   capability truth, split/remove AI editor extensions and inert AI services
   from the Free composition root, and correct stale AI copy and release gates.
3. Finish the current non-AI editor batch without expanding features. Resolve
   Epdoc title-popover parity and inspect the complete diff.
4. Retire stale R113, run the mandatory below-16-GiB resource preflight, and
   produce exactly one current Debug App Store build/test product. Run affected
   scheme-member tests plus the justified broad regression set serially.
5. Run a finite manual/runtime matrix on that exact app: Markdown and Epdoc
   creation/opening, 67k-72k edit/backspace/scroll/selection/undo/save/reopen,
   surface and tab navigation, Home/Multitask and both graphs, title/popover/
   toolbar, Settings, MarkEdit fidelity, accessibility, memory, and Kokoro.
6. Close the existing KEELSTONE key: correct remaining storage/runtime/settings/
   warning debt, produce one fresh Release archive, run all artifact gates, and
   record the final verdict. Do not start another execution key.
7. Only after the editor and KEELSTONE are current-green, admit retained non-AI
   capability families one at a time with fail-first tests and exact runtime
   evidence. Start with the owner-selected priority among Sync, Quick Capture,
   planner/calendar/Meeting, Epdoc rich objects, PDF, and native integrations.

## Pause boundary

Implementation and builds are paused at the owner's request. Preserve the dirty
worktree and stale evidence artifacts; do not reset, overwrite, launch a stale
app, or claim current behavior. The safe resume action after owner review is
Step 2 above, expanded by canon `15`, under the same KEELSTONE execution key.
Private Columbia/VA/funding work is outside this repository and must not be
inferred.
