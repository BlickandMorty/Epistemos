# PageGather Packetized Witness - 2026-05-27

Status: packetized scheduled mitigation witness landed; Rust caller-side packet
contract now exists; dense PageGather restore remains pending.

## Why This Exists

The dense scheduled kernel preserves caller-visible order by writing:

```text
out[logicalPositions[i]] = source[indices[i]]
```

That contract is correct but slow on M2 Pro because the destination writes are
random. The packetized scheduled kernel keeps the same witness coordinate while
avoiding immediate dense restore:

```text
packetValues[i] = source[indices[i]]
packetLogicalPositions[i] = logicalPositions[i]
```

This matches the Substrate Motion Invariant: recall may move as compact packets
with explicit coordinates, and dense projection is paid only when a consumer
actually needs dense logical order.

## Artifact

The on-disk witness is:

```text
artifacts/falsifiers/page_gather/locality_probe_result.json
```

Generated by:

```text
swift Tools/metal-witness-gates/page-gather-metal-artifact.swift --probe-locality --working-sets-mb 256,512 --window-seconds 2 --trials 2 --warmup-iterations 1 --write-artifact
```

Result digest:

```text
sha256:15fd8e7eddb23536f025305126f67960564278e15ea50c5c90879fc882cd4c7f
```

## Measured Result

| Axis | 256 MB | 512 MB |
|---|---:|---:|
| STREAM triad | `204.73 GB/s` | `214.01 GB/s` |
| Dense scheduled restore ratio | `0.092x` | `0.058x` |
| Packetized scheduled ratio | `0.729x` | `0.752x` |
| Packetized correctness violations | `0` | `0` |
| Packetized stability range/mean | `0.061` | `0.013` |

## Interpretation

This is not a green promotion for the original dense-output
`F-PageGather-M2Pro` gate. It is a new mitigation witness showing that the lean
packet stream clears the `0.70x` M2 Pro floor at the 256/512 MB envelope named
by `Epistemos/Shaders/PageGather.metal`.

The next product decision is structural:

- Callers can now consume `(logical_position, value)` packets directly through
  `agent_core::helios::gather_packetized` /
  `gather_block_sorted_packetized`, and project dense order lazily with
  `restore_packets` only when needed.
- If callers require dense order at the kernel boundary, the dense scheduled
  restore still needs optimization before promotion.

## No-Orphan Check

- Motion: Project / Recall.
- UAS: no new address shape; packet logical positions are witness coordinates
  for the existing PageGather schedule.
- Plane: episodic / page plane.
- Residency: Apple Silicon UMA through shared Metal buffers.
- WBO/error: dense restore failure remains recorded; packetized mitigation is a
  separate measured axis.
- Witness: `pageGatherPacketizeScheduled`, `MetalWitnessGatesTests`,
  `agent_core::helios::gather_packetized`, `restore_packets`, the locality
  probe artifact, and this audit note.
- Falsifier: `F-PageGather-M2Pro` remains orange/pending for dense output.
- Tier: VerifiedFloor diagnostic / product-candidate mitigation, not MAS-green.
- Rollback: remove `pageGatherPacketizeScheduled` and the packetized harness
  axes; keep the dense failure artifact intact.
