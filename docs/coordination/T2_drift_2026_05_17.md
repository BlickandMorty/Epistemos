# T2 Drift Notice - 2026-05-17

**From:** T9 coordination  
**To:** T2 agent/runtime lane  
**Observed:** 2026-05-17 06:54:27 CDT  
**T2 branch:** `codex/t2-agent-2026-05-16`  
**T2 HEAD:** `86f0ec84f`

## Finding

T2 has in-scope untracked doctrine docs:
- `docs/agent-system/MODEL_GRAMMAR_MATRIX_2026_05_17.md`
- `docs/audits/AGENT_RUNTIME_AUDIT_2026_05_17.md`
- `docs/audits/MODEL_GATING_MATRIX_2026_05_17.md`
- `docs/fusion/LOCAL_AGENT_EXCELLENCE_DOCTRINE_2026_05_17.md`

Those docs match the T2 scope lock.

T2 also has many tracked modifications under:
- `syntax-core/target/aarch64-apple-darwin/debug/...`
- `syntax-core/target/debug/...`

These are generated build artifacts and are outside T2's scope lock. They are also outside the T9 docs-only lane, so T9 is not modifying or reverting them.

## Required Action For T2

Before T2 commits or opens a PR:
1. Remove the tracked `syntax-core/target/**` artifact changes from the T2 worktree.
2. Verify `git status --short` contains only intentional files from the T2 scope lock.
3. If any `target/**` file is intentionally tracked for this repo, document why in the commit body and cite the owning build-system rule.

## Why This Matters

Generated target artifacts in a feature commit would create review noise, false scope drift, and merge conflict risk. They are not part of T2's agent/runtime, local-agent, provider, Settings, bridge, or audit-doc ownership.

## Current Verdict

Pre-commit scope drift. No code review verdict on T2's actual docs yet because the work is uncommitted.

## Recheck - 2026-05-17 07:14 CDT

T9 iter 3 rechecked `/Users/jojo/Downloads/Epistemos-t2-agent`.

Status remains unresolved:
- `syntax-core/target/aarch64-apple-darwin/debug/**` tracked artifacts still modified.
- `syntax-core/target/debug/**` tracked artifacts still modified.
- T2's four untracked doctrine/audit docs remain in-scope, but should not be committed together with generated target artifacts.

Required action remains unchanged: clean the generated `syntax-core/target/**` modifications before any T2 commit or PR.

## Recheck - 2026-05-17 07:18 CDT

T2 committed and pushed `4f7b9df60` without including the generated `syntax-core/target/**` artifacts. That commit is scope-clean; see `docs/coordination/T9_to_T2_2026_05_17.md`.

The T2 worktree still has the same tracked generated artifact modifications after the commit, so this drift notice remains open for the next T2 slice.

## Recheck - 2026-05-17 07:28 CDT

T2 committed and pushed `ed7ff2531`; that commit only touches `Epistemos/Views/Settings/SettingsView.swift`, which is inside T2's Settings diagnostics lane. See `docs/coordination/T9_to_T2_2026_05_17.md`.

The T2 worktree remains blocked before the next commit / PR:
- tracked `syntax-core/target/aarch64-apple-darwin/debug/**` artifact modifications remain present.
- tracked `syntax-core/target/debug/**` artifact modifications remain present.
- uncommitted `Epistemos/Models/ChatTypes.swift` and `Epistemos/Models/SDMessage.swift` modifications are outside the T2 scope lock as written.

Required action: T2 should either drop the generated target artifacts and move the model-file edits to the owning lane, or file a coordination doc that explains why T2 needs those `Epistemos/Models/**` edits before committing them.

## Recheck - 2026-05-17 07:33 CDT

T2 remains blocked before the next commit / PR. `git diff --name-only` now reports these non-artifact Swift paths:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/Bridge/StreamingDelegate.swift`
- `Epistemos/Models/ChatTypes.swift`
- `Epistemos/Models/SDMessage.swift`
- `Epistemos/State/AgentChatState.swift`
- `Epistemos/State/ChatState.swift`
- `Epistemos/Views/Chat/MessageBubble.swift`

Only `Epistemos/Bridge/StreamingDelegate.swift` is explicitly inside T2's scope lock. The rest are outside T2's lane as written, including T4-owned `ChatCoordinator.swift` and chat UI/state/model surfaces that require coordination before commit.

The tracked `syntax-core/target/**` generated artifacts also remain dirty. Required action is unchanged: clean artifacts and either move or coordinate the out-of-lane Swift edits before committing.

## Recheck - 2026-05-17 07:45 CDT

T2 committed and pushed `9b090203d feat(chat): persist answer packets with assistant turns`.

`git show --name-only 9b090203d` reports:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/Bridge/StreamingDelegate.swift`
- `Epistemos/Models/ChatTypes.swift`
- `Epistemos/Models/SDMessage.swift`
- `Epistemos/State/AgentChatState.swift`
- `Epistemos/State/ChatState.swift`
- `Epistemos/Views/Chat/MessageBubble.swift`

Only `Epistemos/Bridge/StreamingDelegate.swift` is explicitly inside T2's scope lock. The remaining chat coordinator, model, state, and chat UI paths are outside the T2 lane as written.

The T2 worktree still has tracked `syntax-core/target/**` generated artifact modifications after the commit, and now has an in-scope dirty Settings row:
- `Epistemos/Views/Settings/AnswerPacketHealthRow.swift`

Current verdict: committed scope violation for `9b090203d`, plus unresolved generated-artifact blocker before the next T2 commit / PR. T9 is not reverting the commit; T2 needs an explicit coordination rationale or follow-up lane owner sign-off before this slice is treated as merge-ready.

## Recheck - 2026-05-17 07:52 CDT

T2 committed and pushed `edb69ec47 feat(settings): audit persisted answer packets`.

`git show --name-status edb69ec47` reports:
- `M Epistemos/Views/Settings/AnswerPacketHealthRow.swift`

That path is inside T2's Settings diagnostic-row lane. This commit is scope-clean, but it does not resolve the committed scope violation in `9b090203d`.

The T2 worktree still has tracked `syntax-core/target/**` generated artifact modifications after the commit. Current verdict remains: T2 is blocked before PR / merge-readiness until the generated artifacts are cleaned and `9b090203d` receives coordination rationale / sign-off.

## Recheck - 2026-05-17 08:00 CDT

T2 HEAD remains `edb69ec47`. No new T2 commit was visible in `git log --oneline -3`, but the uncommitted worktree drift widened substantially.

`git diff --stat` now reports 234 modified files, roughly 5014 insertions and 1738 deletions. The dirty set includes in-lane T2 surfaces:
- `Epistemos/LocalAgent/**`
- `Epistemos/Views/Settings/SettingsView.swift`
- `agent_core/src/agent_runtime/**`
- `agent_core/src/providers/openai_compatible.rs`

It also includes multiple paths outside T2's written lane, including explicit other-terminal ownership:
- `agent_core/src/storage/vault.rs` (T4 owns)
- `agent_core/src/research/eml/**` (T7 owns)
- many `agent_core/src/research/**`, `agent_core/src/helios/**`, `agent_core/src/live_files/**`, `agent_core/src/schemas/mod.rs`, `agent_core/src/routing.rs`, and other Rust modules not listed in the T2 touch list
- tracked generated `syntax-core/target/**` artifacts

Current verdict: pre-commit blocker is now broader than the earlier artifact-only drift. T2 should not commit this worktree as a single slice. It needs cleanup / split-by-lane coordination before any further commit or PR, especially for Vault (`T4`) and EML (`T7`) owned files.

## Recheck - 2026-05-17 08:04 CDT

T2 made a new local commit, `46ac80bba feat(local-agent): route native tool grammar profiles`, and is now `ahead 1` of `origin/codex/t2-agent-2026-05-16`.

`git show --name-status 46ac80bba` reports:
- `M Epistemos/LocalAgent/LocalAgentLoop.swift`
- `M Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `M Epistemos/LocalAgent/LocalToolGrammar.swift`
- `M Epistemos/Views/Settings/SettingsView.swift`
- `M agent_core/src/agent_runtime/function_call.rs`
- `M agent_core/src/agent_runtime/prompt_format.rs`

Those paths are inside T2's written LocalAgent / Settings / `agent_runtime` lane. The prior broad uncommitted Rust drift has been narrowed for this commit.

Remaining blockers:
- `9b090203d` is still a committed scope violation until coordination rationale / owner sign-off lands.
- tracked generated `syntax-core/target/**` artifact drift remains dirty in the T2 worktree.
- `46ac80bba` is local-only / unpushed at this check.

Current verdict: latest local commit is scope-clean, but branch merge-readiness remains blocked by prior `9b090203d`, generated artifacts, and missing push.

## Recheck - 2026-05-17 08:14 CDT

T2 pushed `46ac80bba feat(local-agent): route native tool grammar profiles`; the commit remains scope-clean for LocalAgent / Settings / `agent_runtime` paths. See `docs/coordination/T9_to_T2_2026_05_17.md`.

The current T2 worktree now reports:
- `Epistemos/LocalAgent/ConfidenceRouter.swift`
- `EpistemosTests/ConfidenceRouterTests.swift`
- many tracked generated `syntax-core/target/**` artifacts

`Epistemos/LocalAgent/ConfidenceRouter.swift` is inside T2's LocalAgent lane. `EpistemosTests/ConfidenceRouterTests.swift` is not listed in the exact written T2 test scope, which names Rust `tests/agent_runtime_*.rs` but not Swift `EpistemosTests/**`; treat it as a pre-commit coordination item unless T2 records a scope rationale before committing it. The generated target artifacts remain out of scope.

Current verdict: pushed `46ac80bba` is clean, but T2 remains blocked before PR / merge-readiness by the earlier committed `9b090203d` scope violation, dirty generated artifacts, and the uncommitted Swift test path needing explicit coordination.

## Recheck - 2026-05-17 08:20 CDT

T2 committed and pushed `6c5526fa8 feat(local-agent): select models by task class`.

`git show --name-status 6c5526fa8` reports:
- `M Epistemos/LocalAgent/ConfidenceRouter.swift`
- `M EpistemosTests/ConfidenceRouterTests.swift`

`Epistemos/LocalAgent/ConfidenceRouter.swift` is inside T2's LocalAgent lane. `EpistemosTests/ConfidenceRouterTests.swift` is the same Swift test path T9 flagged in the 08:14 recheck as not listed in the exact written T2 test scope. The commit body mentions ConfidenceRouter tests but does not record a lane-scope rationale or owner sign-off for the Swift test path.

Current T2 worktree drift is back to tracked generated `syntax-core/target/**` artifacts only.

Current verdict: `6c5526fa8` is a useful model-selection slice, but it is not fully scope-clean as committed because the Swift test path crossed outside T2's written test lane after a pre-commit warning. The branch remains blocked by this new exact-scope issue, prior `9b090203d`, and generated artifacts.

## Recheck - 2026-05-17 08:30 CDT

T2 HEAD remains pushed `6c5526fa8`. The current uncommitted worktree now reports:
- `Epistemos/Engine/MLXInferenceService.swift`
- `Epistemos/Views/Settings/PerformanceSettingsSection.swift`
- `EpistemosTests/RuntimeValidationTests.swift`
- `EpistemosTests/TriageServiceTests.swift`
- tracked generated `syntax-core/target/**` artifacts

Scope review:
- `Epistemos/Views/Settings/PerformanceSettingsSection.swift` is inside T2's Settings lane.
- `Epistemos/Engine/MLXInferenceService.swift` is not named in T2's written touch list; T2's engine scope lists `LocalModelInfrastructure.swift`, not MLXInferenceService.
- `EpistemosTests/RuntimeValidationTests.swift` and `EpistemosTests/TriageServiceTests.swift` are not named in T2's exact written test scope.
- `syntax-core/target/**` remains generated artifact drift.

The MLX/Settings diff appears to wire `IdleMemoryMode.lowMemory` into a 30-second deep unload path, which is relevant to `ISSUE-2026-05-12-007`, but it needs a scope rationale before commit because the engine/test paths are outside the written T2 lock.

Current verdict: pre-commit coordination required. T2 should not commit this current dirty set without either narrowing to written-scope paths or documenting the MLXInferenceService / Swift-test scope rationale.

## Recheck - 2026-05-17 08:37 CDT

T2 committed and pushed `f0c0fbace feat(local-agent): honor low memory idle unload mode`.

`git show --name-status f0c0fbace` reports:
- `M Epistemos/Engine/MLXInferenceService.swift`
- `M Epistemos/Views/Settings/PerformanceSettingsSection.swift`
- `M EpistemosTests/RuntimeValidationTests.swift`
- `M EpistemosTests/TriageServiceTests.swift`

`PerformanceSettingsSection.swift` is inside T2's Settings lane. `MLXInferenceService.swift` is not named in T2's written touch list, and the two Swift test files are not named in T2's exact test lane. This is the same scope issue T9 flagged before the commit.

Current T2 worktree drift is generated `syntax-core/target/**` artifacts only.

Current verdict: `f0c0fbace` is functionally relevant to `ISSUE-2026-05-12-007`, but it is not scope-clean as committed. T2 branch still carries prior `9b090203d`, `6c5526fa8` Swift-test scope debt, this new MLX/test scope debt, and generated artifacts.

## Recheck - 2026-05-17 08:44 CDT

T2 HEAD remains pushed `f0c0fbace`, but the current uncommitted worktree has widened again. Non-artifact dirty paths now include:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/Models/ChatTypes.swift`
- `Epistemos/Models/SDMessage.swift`
- `Epistemos/State/AgentChatState.swift`
- `Epistemos/State/ChatState.swift`
- `Epistemos/Views/Chat/MessageBubble.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`
- `EpistemosTests/AnswerPacketCodableTests.swift`
- `EpistemosTests/SDMessageTests.swift`
- `Epistemos/Views/Chat/AgentRunTimelineView.swift` (untracked)

Scope review:
- AnswerPacket runtime emission is part of T2's mission, and `Epistemos/Bridge/StreamingDelegate.swift` is explicitly listed for runtime emission.
- The current dirty set reaches outside T2's exact written touch list into broad chat models, state, chat UI, and Swift tests.
- `Epistemos/Views/Chat/AgentRunTimelineView.swift` is a new untracked UI surface and should not be committed without a T2 scope rationale and T6 UI-audit coordination.
- tracked generated `syntax-core/target/**` artifacts remain dirty.

Current verdict: pre-commit coordination required. T2 should either narrow the slice to written-scope paths or record an explicit scope exception / owner sign-off before committing this broad AnswerPacket/chat timeline work.

## Recheck - 2026-05-17 08:51 CDT

T2 committed and pushed `a3e177e92 feat(chat): replay agent run timelines from event log`.

`git show --name-status a3e177e92` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M Epistemos/Models/ChatTypes.swift`
- `M Epistemos/Models/SDMessage.swift`
- `M Epistemos/State/AgentChatState.swift`
- `M Epistemos/State/ChatState.swift`
- `A Epistemos/Views/Chat/AgentRunTimelineView.swift`
- `M Epistemos/Views/Chat/MessageBubble.swift`
- `M EpistemosTests/AgentEventVisibilityTests.swift`
- `M EpistemosTests/AnswerPacketCodableTests.swift`
- `M EpistemosTests/SDMessageTests.swift`

This is the same broad AnswerPacket/chat timeline path set T9 flagged before commit. It is product-relevant, but it is outside T2's exact written touch list and creates a new chat UI surface without prior T6 coordination.

Current T2 worktree drift is generated `syntax-core/target/**` artifacts only.

Current verdict: `a3e177e92` is a committed scope issue unless T2 records explicit scope rationale / owner sign-off. T2 branch still carries prior `9b090203d`, `6c5526fa8`, `f0c0fbace`, and generated artifacts.

## Recheck - 2026-05-17 09:04 CDT

T2 HEAD remains pushed `a3e177e92 feat(chat): replay agent run timelines from event log`; no new commit landed during the iter 17 main baseline.

Current worktree drift reports:
- `Epistemos/Views/Settings/AgentSectionDetailView.swift`
- `Epistemos/LocalAgent/AgentBlueprint.swift` (untracked)
- `Epistemos/Views/Settings/AgentBlueprintSettingsView.swift` (untracked)
- `EpistemosTests/AgentBlueprintTests.swift` (untracked)
- generated `syntax-core/target/**` artifacts

Scope review:
- AgentBlueprint UI / Settings / LocalAgent work is part of T2's stated mission, and the Settings / LocalAgent paths are in the T2 lane.
- `EpistemosTests/AgentBlueprintTests.swift` again raises the Swift-test exact-scope issue unless T2 records rationale / sign-off before committing it.
- Generated `syntax-core/target/**` artifacts remain out of scope.

Current verdict: pre-commit coordination required for Swift-test scope rationale and artifact cleanup. Prior committed T2 scope debts remain unresolved.

## Recheck - 2026-05-17 09:12 CDT

T2 remains at pushed `a3e177e92` with no new commit. Non-artifact live drift is still:
- `Epistemos/Views/Settings/AgentSectionDetailView.swift`
- `Epistemos/LocalAgent/AgentBlueprint.swift` (untracked)
- `Epistemos/Views/Settings/AgentBlueprintSettingsView.swift` (untracked)
- `EpistemosTests/AgentBlueprintTests.swift` (untracked)

Settings / LocalAgent AgentBlueprint work remains aligned with T2's mission, but the Swift test path still needs exact-scope rationale / sign-off before commit. Generated `syntax-core/target/**` artifacts remain dirty.

Current verdict: unchanged from iter 17; T2 is not commit/PR-ready until test-scope rationale and artifact cleanup are handled.

## Recheck - 2026-05-17 09:17 CDT

T2 committed `79cb183ee feat(settings): add agent blueprint mission runner`.

`git show --name-status 79cb183ee` reports:
- `A Epistemos/LocalAgent/AgentBlueprint.swift`
- `A Epistemos/Views/Settings/AgentBlueprintSettingsView.swift`
- `M Epistemos/Views/Settings/AgentSectionDetailView.swift`
- `A EpistemosTests/AgentBlueprintTests.swift`

Scope review:
- The LocalAgent and Settings paths align with T2's AgentBlueprint UI / LocalAgent mission.
- `EpistemosTests/AgentBlueprintTests.swift` is the same Swift-test exact-scope issue T9 flagged before commit; it needs explicit rationale / owner sign-off before merge-readiness.
- Current T2 worktree drift is generated `syntax-core/target/**` artifacts only.

Current verdict: product-relevant but committed with unresolved Swift-test scope debt. Artifact cleanup and prior T2 scope debts remain open.

## Recheck - 2026-05-17 09:22 CDT

T2 HEAD remains pushed `79cb183ee`. Current non-artifact live drift now reports:
- `Epistemos/LocalAgent/AgentBlueprint.swift`
- `Epistemos/Views/Settings/AgentBlueprintSettingsView.swift`
- `EpistemosTests/AgentBlueprintTests.swift`

The LocalAgent / Settings refinements remain aligned with T2's AgentBlueprint mission. The Swift test path remains exact-scope debt unless T2 documents rationale / sign-off. Generated `syntax-core/target/**` artifacts remain dirty.

Current verdict: T2 has in-lane AgentBlueprint refinements, but Swift-test rationale and artifact cleanup are still blockers before the next commit / PR.

## Recheck - 2026-05-17 09:31 CDT

T2 committed and pushed `ea2792cd1 feat(settings): badge agent blueprint model choices`.

`git show --name-status ea2792cd1` reports:
- `M Epistemos/LocalAgent/AgentBlueprint.swift`
- `M Epistemos/Views/Settings/AgentBlueprintSettingsView.swift`
- `M EpistemosTests/AgentBlueprintTests.swift`

Scope review:
- LocalAgent / Settings AgentBlueprint badge work is aligned with T2's AgentBlueprint UI and LocalAgent mission.
- The Swift test path continues the same exact-scope issue T9 flagged before earlier AgentBlueprint commits.
- Current T2 worktree drift is generated `syntax-core/target/**` artifacts only.

Current verdict: product-relevant branch progress, but Swift-test scope rationale and artifact cleanup remain blockers before PR.

## Recheck - 2026-05-17 09:38 CDT

T2 HEAD remains pushed `ea2792cd1 feat(settings): badge agent blueprint model choices`; no new commit landed during iter 22.

Current non-artifact live drift remains:
- `Epistemos/LocalAgent/AgentBlueprint.swift`
- `Epistemos/Views/Settings/AgentBlueprintSettingsView.swift`
- `EpistemosTests/AgentBlueprintTests.swift`

The LocalAgent / Settings AgentBlueprint work is aligned with T2's per-model agent-creation mission. The Swift test path continues the exact-scope debt already flagged before and after `79cb183ee` / `ea2792cd1`. Generated `syntax-core/target/**` artifacts remain dirty.

Current verdict: no new committed drift, but T2 is still not PR-ready until Swift-test scope rationale and artifact cleanup are handled.

## Late Recheck - 2026-05-17 09:49 CDT

T2 committed and pushed `59abb152a feat(settings): persist agent blueprint mission runs`.

`git show --name-status 59abb152a` reports:
- `M Epistemos/LocalAgent/AgentBlueprint.swift`
- `M Epistemos/Views/Settings/AgentBlueprintSettingsView.swift`
- `M EpistemosTests/AgentBlueprintTests.swift`

LocalAgent / Settings AgentBlueprint persistence remains aligned with T2's mission, but `EpistemosTests/AgentBlueprintTests.swift` continues the same Swift-test exact-scope debt.

Current live non-artifact drift is now:
- `Epistemos/LocalAgent/LocalAgentLoop.swift`
- `Epistemos/LocalAgent/LocalToolGrammar.swift`

Those current dirty paths are inside T2's `Epistemos/LocalAgent/` scope. Generated artifacts remain dirty.

Current verdict: `59abb152a` is product-relevant but still carries Swift-test scope debt; current live WIP is in-lane, with artifact cleanup still required.

## Recheck - 2026-05-17 10:00 CDT

No new T2 commit after `59abb152a`.

Current worktree drift is generated `syntax-core/target/**` artifacts only. The committed `EpistemosTests/AgentBlueprintTests.swift` exact-scope debt remains unresolved.

Current verdict: no new committed drift; artifact cleanup and Swift-test rationale remain blockers.

## Recheck - 2026-05-17 10:08 CDT

T2 pushed `a7a39595f`.

Committed path review:
- `Epistemos/LocalAgent/LocalAgentDiagnostics.swift`
- `Epistemos/LocalAgent/LocalAgentLoop.swift`
- `Epistemos/LocalAgent/LocalToolGrammar.swift`
- `Epistemos/Views/Settings/LocalAgentDiagnosticsHealthRow.swift`
- `Epistemos/Views/Settings/SettingsView.swift`
- `EpistemosTests/LocalAgentDiagnosticsTests.swift`

The LocalAgent and Settings paths are in T2's lane. `EpistemosTests/LocalAgentDiagnosticsTests.swift` is another committed Swift-test path outside T2's exact written `tests/agent_runtime_*.rs` scope and needs rationale / sign-off before PR, matching the earlier AgentBlueprint / ConfidenceRouter test debt pattern.

Current live non-artifact drift is still mission-aligned (`LocalAgentDiagnostics.swift`, `SettingsView.swift`, `ActiveConstellationRow.swift`), but the Swift test and generated `syntax-core/target/**` artifacts remain blockers. `ISSUE-2026-05-16-015` remains `Investigating`.

## Recheck - 2026-05-17 10:18 CDT

T2 pushed `996f0831b`.

Committed path review:
- `Epistemos/LocalAgent/LocalAgentDiagnostics.swift`
- `Epistemos/Views/Settings/ActiveConstellationRow.swift`
- `Epistemos/Views/Settings/SettingsView.swift`
- `EpistemosTests/LocalAgentDiagnosticsTests.swift`

The LocalAgent and Settings paths are in scope. `EpistemosTests/LocalAgentDiagnosticsTests.swift` remains a committed Swift-test exact-scope item needing rationale / sign-off.

Current live non-artifact drift is now `Epistemos/Engine/AgentToolProvenanceRecorder.swift`, which is outside T2's written scope lock. This is a pre-commit blocker unless explicitly coordinated. Generated `syntax-core/target/**` artifacts remain dirty.

## Recheck - 2026-05-17 10:28 CDT

No new T2 commit after `996f0831b`, but live non-artifact drift widened:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/Engine/AgentToolProvenanceRecorder.swift`
- `Epistemos/Views/Chat/AgentRunTimelineView.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`

Scope review:
- `ChatCoordinator.swift` overlaps T4's prompt seam and is not listed in T2's scope lock.
- `AgentToolProvenanceRecorder.swift` remains outside T2's only listed Engine path (`LocalModelInfrastructure.swift`).
- `AgentRunTimelineView.swift` is outside T2's Settings-only UI lane, despite being product-relevant to the run timeline.
- `AgentEventVisibilityTests.swift` is another Swift test outside T2's exact `tests/agent_runtime_*.rs` lane.

Current verdict: pre-commit blocker. T2 should narrow or coordinate this set before committing, and generated `syntax-core/target/**` artifacts must remain out of any commit.

## Recheck - 2026-05-17 10:38 CDT

T2 committed and pushed `05a0133c4 feat(agent): stamp answer packets into run replay`.

Committed path review:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/Engine/AgentToolProvenanceRecorder.swift`
- `Epistemos/Views/Chat/AgentRunTimelineView.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`

This is the same broad set flagged as pre-commit drift in the prior check. It is now committed scope debt:
- `ChatCoordinator.swift` is outside T2's lock and overlaps T4's prompt seam.
- `AgentToolProvenanceRecorder.swift` is outside T2's only listed Engine path.
- `AgentRunTimelineView.swift` is outside the written Settings UI lane.
- `AgentEventVisibilityTests.swift` is outside the exact `tests/agent_runtime_*.rs` lane.

Current live non-artifact drift is narrower and mostly model-gating aligned:
- `Epistemos/Engine/LocalModelInfrastructure.swift`
- `Epistemos/State/InferenceState.swift`
- `Epistemos/Views/Settings/SettingsView.swift`
- `EpistemosTests/LocalModelInfrastructureTests.swift`

The Swift test remains exact-scope debt. Generated artifacts remain dirty.

Current verdict: committed drift escalated. T2 needs explicit coordination / rationale for `05a0133c4` before PR, then should keep the current model-gating WIP narrowed to its written lanes.

## Recheck - 2026-05-17 10:49 CDT

T2 committed and pushed `dc2d17fb3 feat(settings): expose power-user local model risk gate`.

Committed path review:
- `Epistemos/Engine/LocalModelInfrastructure.swift`
- `Epistemos/State/InferenceState.swift`
- `Epistemos/Views/Settings/SettingsView.swift`
- `EpistemosTests/LocalModelInfrastructureTests.swift`

The product paths are aligned with T2's model-gating / Settings lane. `EpistemosTests/LocalModelInfrastructureTests.swift` remains another committed Swift-test exact-scope item that needs rationale / sign-off.

Current live non-artifact drift now reports:
- `Epistemos/Engine/ProvenanceConsoleProjectionService.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`

Those paths are tied to the event/provenance visibility work from `05a0133c4`, not the model-gating lane. If T2 keeps them, they need explicit coordination with T6's Provenance Console UI surface and the same Swift-test rationale discipline.

Current verdict: latest model-gating commit is mostly in-lane but still carries Swift-test debt; prior committed AnswerPacket replay drift remains unresolved, and new live provenance/test drift needs coordination before commit.

## Recheck - 2026-05-17 10:58 CDT

T2 committed and pushed `faf76214a feat(settings): surface answer packets in provenance console`.

Committed path review:
- `Epistemos/Engine/ProvenanceConsoleProjectionService.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`

This is the same live drift T9 flagged at 10:49. The projection service is outside T2's written Engine lane and overlaps T6's Provenance Console surface. The Swift test repeats exact-scope debt.

Current T2 worktree drift is generated `syntax-core/target/**` artifacts only.

Current verdict: committed T2 drift widened again; T2 needs explicit coordination / rationale before PR, and generated artifacts must stay out of commits.

## Recheck - 2026-05-17 11:10 CDT

No new T2 commit after `faf76214a`.

Current non-artifact live drift:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/LocalAgent/AgentBlueprint.swift`
- `Epistemos/LocalAgent/LocalAgentLoop.swift`
- `Epistemos/Views/Chat/AgentRunTimelineView.swift`
- `EpistemosTests/AgentBlueprintTests.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`
- `EpistemosTests/LocalAgentLoopTests.swift`

The LocalAgent files are in T2's lane, but `ChatCoordinator.swift` overlaps T4's prompt seam, `AgentRunTimelineView.swift` is still outside the written Settings / ViewModel / Bridge UI list, and all three Swift tests are outside the exact `tests/agent_runtime_*.rs` lane.

Current verdict: pre-commit blocker. T2 should narrow this WIP or record explicit T4/T6 coordination and Swift-test rationale before committing.

## Recheck - 2026-05-17 11:22 CDT

T2 committed and pushed `6b3dc77a9 feat(agent): replay local blueprint mission runs`.

Committed paths:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/LocalAgent/AgentBlueprint.swift`
- `Epistemos/LocalAgent/LocalAgentLoop.swift`
- `Epistemos/Views/Chat/AgentRunTimelineView.swift`
- `EpistemosTests/AgentBlueprintTests.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`
- `EpistemosTests/LocalAgentLoopTests.swift`

This is the same broad LocalAgent / prompt seam / chat UI / Swift-test set T9 flagged live in the 11:10 check. The LocalAgent files are inside T2's mission lane. `ChatCoordinator.swift` overlaps T4, `AgentRunTimelineView.swift` overlaps T6, and all three Swift test paths need exact-scope rationale because the written T2 test lane names Rust `tests/agent_runtime_*.rs`.

Current live non-artifact drift:
- `Epistemos/Engine/ProvenanceConsoleProjectionService.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`

Generated `syntax-core/target/**` artifacts remain dirty.

Current verdict: committed scope debt widened; T2 needs explicit coordination / rationale before PR. This commit does not provide verified 36B-on-16GB runtime proof.

## Recheck - 2026-05-17 11:46 CDT

T2 pushed `119ef24de feat(settings): project mission packets in provenance console`.

Committed paths:
- `Epistemos/Engine/ProvenanceConsoleProjectionService.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`

This is the same Provenance Console projection / Swift-test scope pattern already recorded for T2. The projection service is outside T2's written Engine lane and overlaps T6. Current live non-artifact drift now spans `ChatCoordinator.swift`, `AgentChatState.swift`, `AgentRunTimelineView.swift`, `AgentChatStateTests.swift`, and `AgentEventVisibilityTests.swift`. Those paths require T4/T6 coordination and Swift-test rationale before commit / PR.

Current verdict: committed scope debt widened again; no verified 36B-on-16GB runtime proof.

## Recheck - 2026-05-17 11:59 CDT

T2 pushed `896bae766 feat(agent): terminate failed local runs in replay`.

Committed paths:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/State/AgentChatState.swift`
- `Epistemos/Views/Chat/AgentRunTimelineView.swift`
- `EpistemosTests/AgentChatStateTests.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`

This is the same chat/state/test WIP T9 flagged live at 11:46. It is now committed scope debt: prompt seam overlap with T4, chat UI/state overlap with T6, and Swift-test exact-scope debt. Current live drift is generated artifacts only.
