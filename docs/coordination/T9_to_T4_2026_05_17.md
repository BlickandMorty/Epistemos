# T9 to T4 - Scope Review 2026-05-17

**From:** T9 coordination  
**To:** T4 vault retrieval lane  
**Reviewed:** 2026-05-17 06:54:27 CDT  
**T4 branch:** `codex/t4-vault-2026-05-16`  
**Reviewed commit:** `0c0ddfe15 docs(vault-recall): establish F-VaultRecall-50 baseline`

## Scope Review

`git show --stat --name-status 0c0ddfe15` reports:
- `M agent_core/src/storage/vault.rs`
- `A agent_core/tests/vault_recall_baseline.rs`
- `A docs/falsifiers/F-VaultRecall-50_baseline_2026_05_17.md`
- `A docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

All touched files are inside T4's scope lock:
- `agent_core/src/storage/vault.rs`
- `tests/vault_recall_*.rs`
- `docs/falsifiers/F-VaultRecall-50_<date>.md`
- `docs/fusion/VAULT_CONTEXT_CONTRACT_<date>.md`

## Diff-Content Check

Per PASS-2 Lesson #17, T9 checked diff content, not just the commit message.

Findings:
- The `vault.rs` code change is limited to Unicode-safe excerpt truncation and a unit test.
- The F-VaultRecall-50 references to "first N" and `LIMIT 7` are doctrine/failure-taxonomy references, not new retrieval enumeration logic.
- The new ignored falsifier harness deterministically samples 50 notes for a local-vault baseline. It does not replace runtime retrieval with "first N notes."

## Open Coordination Notes

- The T4 prompt asked for `docs/audits/VAULT_RETRIEVAL_AUDIT_<date>.md` in the iter-1 start sequence. That audit doc did not land in `0c0ddfe15`. Treat as a missing follow-up, not a scope violation.
- T4 now has an uncommitted in-scope change in `Epistemos/App/ChatCoordinator.swift`. T9 has not reviewed it because it is not committed yet.

## Verdict

`0c0ddfe15` is scope-clean. No T4 drift file required for this commit.

## Iter 5 Recheck - 2026-05-17 07:28 CDT

**Reviewed commit:** `4e0f74372 fix(vault-recall): widen fallback candidate pool`

`git show --stat --name-status 4e0f74372` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `A EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

Both paths are inside T4's scope lock:
- `Epistemos/App/ChatCoordinator.swift`
- `EpistemosTests/F_VaultRecall_50_*.swift`

Diff-content findings:
- The patch moves the indexed fallback query off `resultLimit * 2` and toward a contract-sized candidate pool: at least 50 candidates when inventory supports it, capped at inventory size.
- The regression test proves a three-result fallback requests 50 candidates for a 120-note vault.
- T9 found no first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion in the commit.

Open coordination notes:
- `origin/codex/t4-vault-2026-05-16` still points at `0c0ddfe15`; `4e0f74372` is local-only at this check.
- T4's current dirty files are `Epistemos/Sync/RRFFusionQuery.swift` and `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`, both inside T4's lane.
- The previously flagged `syntax-core/target/**` artifact drift is no longer present in T4's worktree; see `docs/coordination/T4_drift_2026_05_17.md`.

**Verdict:** `4e0f74372` is scope-clean, pending T4 push / PR review.

## Iter 7 Recheck - 2026-05-17 07:45 CDT

**Reviewed commit:** `2c4f0d1bf fix(vault-recall): apply true recency half-life`

`git show --stat --name-status 2c4f0d1bf` reports:
- `M Epistemos/Sync/RRFFusionQuery.swift`
- `A EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Both paths are inside T4's scope lock:
- `Epistemos/Sync/RRFFusionQuery.swift`
- `EpistemosTests/F_VaultRecall_50_*.swift`

Diff-content findings:
- The patch applies a real recency half-life in the RRF fusion score instead of leaving a nominal tunable unused.
- The new Swift integration test covers the recency-weighted ordering behavior.
- T9 found no first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion in the commit.

Open coordination notes:
- T4 branch still has no upstream configured in `git branch -vv`; `2c4f0d1bf` is local-only at this check.
- The prior generated-artifact drift is resolved again; current T4 worktree is clean.

**Verdict:** `2c4f0d1bf` is scope-clean, pending T4 push / PR review.

## Iter 8 Recheck - 2026-05-17 07:52 CDT

**Reviewed commit:** `6998cc41f fix(vault-recall): strip vault query boilerplate`

`git show --stat --name-status 6998cc41f` reports:
- `M Epistemos/Sync/SearchIndexService.swift`
- `A EpistemosTests/F_VaultRecall_50_QueryNormalizationTests.swift`

Both paths are inside T4's scope lock:
- `Epistemos/Sync/SearchIndexService.swift`
- `EpistemosTests/F_VaultRecall_50_*.swift`

Diff-content findings:
- The patch strips command boilerplate from FTS terms when signal terms remain, preserving nonempty behavior when every term is boilerplate.
- The new Swift test covers exact-title/original-note query normalization.
- T9 found no first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion in the commit.

Open coordination notes:
- T4 branch still has no upstream configured in `git branch -vv`; `6998cc41f` is local-only at this check.
- Current T4 worktree remains clean.

**Verdict:** `6998cc41f` is scope-clean, pending T4 push / PR review.

## Iter 9 Recheck - 2026-05-17 08:00 CDT

**Reviewed commit:** `5bbe32951 fix(vault-recall): recover exact titles by path`

`git show --stat --name-status 5bbe32951` reports:
- `M agent_core/src/storage/vault.rs`
- `A docs/falsifiers/F-VaultRecall-50_2026_05_17.md`

Both paths are inside T4's scope lock:
- `agent_core/src/storage/vault.rs`
- `docs/falsifiers/F-VaultRecall-50_<date>.md`

Diff-content findings:
- The patch adds a deterministic path-title recall lane before BM25, exact-title / original-note normalization helpers, duplicate suppression, and original-note distractor filtering by path/tag.
- The BM25 candidate pool now uses a contract floor / cap of 50-200 instead of `limit * 2`.
- T4 reports local validation at 1674 lib tests plus the ignored `vault_recall_baseline` harness.
- The falsifier report now shows exact-title 10/10, paraphrase Top-5 9/10, agent-context proxy 48/50, zero first-7 enumeration failures, and adversarial reject 10/10 passing.
- The same falsifier report still fails the UI provenance bar (`UI shows why notes were selected` 0/50) and synthesis diversity bar (`Synthesis cites >=2 distinct notes` 8/10 vs 10/10 required).

Open coordination notes:
- T4 worktree is clean after the commit.
- `git branch -vv` still shows no upstream for `codex/t4-vault-2026-05-16`; the branch remains local-only at this check.

**Verdict:** `5bbe32951` is scope-clean and materially improves F-VaultRecall-50, but it is not a full Verified Fixed state because provenance UI and synthesis diversity still fail.

## Iter 11 Recheck - 2026-05-17 08:14 CDT

**Reviewed commit:** `2657a6469 fix(vault-recall): seed synthesis side titles`

`git show --stat --name-status 2657a6469` reports:
- `M agent_core/src/storage/vault.rs`
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`

Both paths are inside T4's vault retrieval / falsifier lane.

Diff-content findings:
- The patch seeds synthesis-side titles for "connect A with B" style recall.
- The F-VaultRecall-50 report now passes exact-title, Top-5 paraphrase, agent-context proxy, zero first-7 enumeration, synthesis diversity, and adversarial reject bars.
- The remaining falsifier failure is provenance visibility: `UI shows why notes were selected` remains 0/50.
- T4 reports local validation at 1676 lib tests plus the ignored vault recall baseline harness.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed diff.

Open coordination notes:
- T4 branch still has no upstream configured; `2657a6469` is local-only at this check.
- Current dirty T4 files include in-lane `Epistemos/Sync/RRFFusionQuery.swift` and `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`, plus out-of-scope generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`.

**Verdict:** `2657a6469` is scope-clean and closes the synthesis-diversity falsifier bar, but F-VaultRecall remains PARTIAL until provenance UI is wired and the branch is pushed / reviewed.

## Iter 12 Recheck - 2026-05-17 08:20 CDT

**Reviewed commit:** `dd11893d2 fix(vault-recall): emit RRF provenance reasons`

`git show --stat --name-status dd11893d2` reports:
- `M Epistemos/Sync/RRFFusionQuery.swift`
- `M EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Both paths are inside T4's vault retrieval / F-VaultRecall Swift test lane.

Diff-content findings:
- The patch extends fused vault search results with `matchReasons` and `provenanceSummary`.
- It projects source-hit flags out of the RRF SQL path and derives concise reasons for page, block, readable-block, source-rank, and recency signals.
- The Swift tests cover SQL provenance projections and renderable summaries, with FTS-backed source/rank checks runtime-gated on this SQLite runtime.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed diff.

Open coordination notes:
- T4 branch still has no upstream configured; `dd11893d2` is local-only at this check.
- Current T4 worktree is clean.
- This narrows the provenance gap at the query/result layer, but no UI file was touched; visible why-selected UI remains the remaining F-VaultRecall acceptance risk until wired and validated.

**Verdict:** `dd11893d2` is scope-clean and resolves the current generated-artifact drift, but F-VaultRecall remains PARTIAL until provenance is visibly surfaced in UI and the branch is pushed / reviewed.

## Iter 13 Recheck - 2026-05-17 08:30 CDT

**Reviewed branch state:** T4 HEAD remains `dd11893d2 fix(vault-recall): emit RRF provenance reasons`.

Live worktree findings:
- `Epistemos/App/ChatCoordinator.swift` is dirty and inside T4's prompt-build seam lane.
- `Epistemos/Views/Notes/NoteChatSidebar.swift` is dirty and inside T4's provenance-card UI lane.
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift` is dirty and inside T4's Swift integration-test lane.
- `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d` is dirty generated artifact drift.

Diff-content findings:
- The current dirty diff surfaces fallback provenance reasons in assistant output and parses them into visible note-chat provenance cards.
- This is the missing visible why-selected UI path that `dd11893d2` prepared at the query/result layer.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the scanned dirty diff.

Open coordination notes:
- T4 still has no upstream configured; the branch remains local-only at this check.
- The current live work is not commit-ready until generated artifacts are excluded.

**Verdict:** current T4 UI/provenance work is in-lane and strategically important, but the generated artifact drift is re-opened as a pre-commit blocker.

## Iter 14 Recheck - 2026-05-17 08:37 CDT

**Reviewed commits:** `7c258ad95 fix(vault-recall): show fallback provenance cards`; `6e07a2ed3 docs(vault-recall): mark provenance slice passing`

`git show --stat --name-status 7c258ad95` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M Epistemos/Views/Notes/NoteChatSidebar.swift`
- `M EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

`git show --stat --name-status 6e07a2ed3` reports:
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- All committed paths are inside T4's vault prompt seam, Notes provenance card UI, F-VaultRecall Swift test, falsifier doc, or Vault Context Contract lane.
- The generated `syntax-core/target/**` drift from iter 13 was not committed.

Diff-content findings:
- `7c258ad95` emits per-hit fallback provenance reasons and renders a separate Vault provenance panel below assistant messages in note chat.
- It avoids nested cards inside message bubbles and extends fallback tests for contract-sized pools, reason emission, and UI parser extraction.
- `6e07a2ed3` marks the provenance slice passing in the falsifier/contract docs while leaving trace types, MMR, graph proximity, confidence bands, and low-confidence enforcement as remaining work.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed diffs.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current T4 worktree is clean.

**Verdict:** scope-clean and strategically important. The provenance UI bar is branch-patched, but T4 remains local-only / unreviewed before merge-readiness.

## Iter 15 Recheck - 2026-05-17 08:44 CDT

**Reviewed commits:** `ffc4c8722 feat(vault-recall): add vault context trace types`; `7fbb27ad5 fix(vault-recall): emit VaultStore search traces`

`git show --stat --name-status ffc4c8722` reports:
- `M agent_core/src/lib.rs`
- `A agent_core/src/retrieval/mod.rs`

`git show --stat --name-status 7fbb27ad5` reports:
- `M agent_core/src/storage/vault.rs`

Scope review:
- `agent_core/src/retrieval/` is explicitly listed as T4's new Vault Search 2.0 home.
- `agent_core/src/storage/vault.rs` is explicitly T4-owned.
- `agent_core/src/lib.rs` is not in T4's exact written touch list. The change appears to be the required one-line module registration for the T4-owned retrieval module, so this is a narrow scope exception, not a broad lane collision. It still needs an explicit rationale / owner sign-off before merge-readiness.

Diff-content findings:
- `ffc4c8722` adds serializable Vault Context trace types for inventory, retrieval mode, signal scores, degraded signals, candidate traces, MMR decisions, confidence bands, and contract violations.
- `7fbb27ad5` adds `VaultStore::hybrid_search_with_trace` as an additive API that widens to the contract candidate pool and emits inventory/candidate/confidence/provenance trace data while keeping existing `hybrid_search` behavior unchanged.
- The scan found contract tests rejecting first-N index-order enumeration and requiring candidate-pool breadth / non-low confidence; current MMR decisions are still placeholders.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed diff.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current T4 worktree is clean.

**Verdict:** strategically aligned and mostly scope-clean, with a narrow `agent_core/src/lib.rs` module-registration exception to document before PR / merge.

## Iter 15 Follow-up - 2026-05-17 08:51 CDT

**Reviewed commits:** `a22dae6fa fix(vault-recall): apply MMR trace rerank`; `4e0aadd3b fix(vault-recall): emit recency decay trace signal`

Both `git show --stat --name-status` reports include:
- `M agent_core/src/retrieval/mod.rs`
- `M agent_core/src/storage/vault.rs`

Scope review:
- `agent_core/src/retrieval/` is explicitly listed as T4's new Vault Search 2.0 home.
- `agent_core/src/storage/vault.rs` is explicitly T4-owned.

Diff-content findings:
- `a22dae6fa` implements a reusable Vault Context Contract MMR selector and records real relevance/diversity/final-score trace decisions instead of rank-order placeholders.
- `4e0aadd3b` loads each traced candidate's `updated_at`, converts it to an exponential recency decay signal, and feeds that signal into MMR relevance/provenance.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed diffs.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current dirty T4 work is in-lane `agent_core/src/storage/vault.rs`.
- The earlier `ffc4c8722` `agent_core/src/lib.rs` module-registration exception still needs rationale before merge-readiness.

**Verdict:** latest T4 commits are scope-clean and remove the prior MMR-placeholder gap. T4 remains local-only and still needs the `lib.rs` registration exception documented.

## Iter 16 Recheck - 2026-05-17 08:57 CDT

**Reviewed commits:** `262b90214 fix(vault-recall): honor user priority metadata`; `20c60ae67 fix(vault-recall): derive graph proximity from vault links`

Both `git show --stat --name-status` reports include:
- `M agent_core/src/storage/vault.rs`

Scope review:
- `agent_core/src/storage/vault.rs` is explicitly T4-owned.

Diff-content findings:
- `262b90214` derives user-priority signals from explicit priority/pinned/favorite/starred/important/urgent metadata and uses a small MMR relevance boost.
- `20c60ae67` parses candidate wikilinks and detects direct links inside the candidate pool to derive a graph-proximity signal.
- Both commits keep provenance honest by marking signals degraded only when the candidate pool lacks the required evidence.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed diffs.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current dirty T4 work is in-lane `Epistemos/App/ChatCoordinator.swift` and `EpistemosTests/F_VaultRecall_50_FallbackTests.swift`.
- The earlier `ffc4c8722` `agent_core/src/lib.rs` module-registration exception still needs rationale before merge-readiness.

**Verdict:** latest T4 commits are scope-clean and close the graph-proximity trace-signal gap. T4 remains local-only and still needs the `lib.rs` registration exception documented.

## Iter 17 Recheck - 2026-05-17 09:04 CDT

**Reviewed commit:** `627eef6ea fix(vault-recall): reject weak fallback matches`

`git show --stat --name-status 627eef6ea` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

Scope review:
- `ChatCoordinator.swift` is inside T4's prompt-seam integration lane.
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift` is inside T4's F-VaultRecall Swift-test lane.
- No T1/T2/T3/T5/T6/T7/T8-owned paths appear in the commit.

Diff-content findings:
- The commit rejects low-confidence indexed fallback results when provenance only has source rank and lacks title, path, or snippet evidence.
- The focused fallback tests now cover weak-indexed rejection and strong indexed fallback acceptance.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed path scan.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current dirty T4 worktree drift is generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`.
- The earlier `ffc4c8722` `agent_core/src/lib.rs` module-registration exception still needs rationale before merge-readiness.

**Verdict:** latest T4 commit is scope-clean and closes the low-confidence fallback enforcement branch gap. T4 still needs artifact cleanup, branch push/PR, and the `lib.rs` exception note.

## Iter 18 Recheck - 2026-05-17 09:12 CDT

T4 remains local-only at `627eef6ea`; no new commit appeared after the weak-fallback rejection slice.

Current live drift:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift`
- `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`

Scope review:
- `ChatCoordinator.swift` and `LocalAgentPromptBuilder.swift` are explicitly listed as T4 prompt-build seam / Vault Context Contract enforcement paths.
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift` is in T4's F-VaultRecall Swift-test lane.
- The generated `syntax-core/target/**` file remains out of scope.

**Verdict:** live work is in-lane, but T4 remains blocked from commit / PR by generated artifact drift and the earlier `agent_core/src/lib.rs` exception note.

## Iter 19 Recheck - 2026-05-17 09:17 CDT

**Reviewed commit:** `f35f5e624 fix(vault-recall): enforce prompt evidence threshold`

`git show --stat --name-status f35f5e624` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `M EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

Scope review:
- `ChatCoordinator.swift` and `LocalAgentPromptBuilder.swift` are explicitly listed as T4 Vault Context Contract prompt-build seam paths.
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift` is inside T4's F-VaultRecall Swift-test lane.
- No other terminal-owned path appears in the commit.

Diff-content findings:
- The commit enforces that source rank alone is not evidence in user-facing chat and LocalAgent fallback prompts.
- It requires loaded note title/path provenance and at least two independently retrieved notes for synthesis, or refusal/broadening instead of guessing.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed path scan.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current dirty T4 worktree drift is generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`.
- The earlier `ffc4c8722` `agent_core/src/lib.rs` module-registration exception still needs rationale before merge-readiness.

**Verdict:** `f35f5e624` is scope-clean and closes the prompt evidence threshold branch gap. T4 still needs artifact cleanup, branch push/PR, and the `lib.rs` exception note.

## Iter 20 Recheck - 2026-05-17 09:22 CDT

**Reviewed commit:** `93ad1953a docs(vault-recall): refresh contract status`

`git show --stat --name-status 93ad1953a` reports:
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- Both docs are inside T4's Vault Context Contract / F-VaultRecall falsifier lane.
- No other terminal-owned path appears in the commit.

Diff-content findings:
- The commit updates the contract and falsifier notes to mark trace types, candidate-pool validation, MMR, recency decay, user priority, graph proximity, UI provenance, indexed fallback confidence threshold, and prompt evidence threshold as branch-implemented.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed path scan.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current non-artifact live drift is in-lane `Epistemos/Sync/RRFFusionQuery.swift` plus `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`.
- Generated artifacts remain dirty, and the earlier `ffc4c8722` `agent_core/src/lib.rs` module-registration exception still needs rationale.

**Verdict:** `93ad1953a` is scope-clean docs-only. T4 still needs artifact cleanup, branch push/PR, and the `lib.rs` exception note.

## Iter 21 Recheck - 2026-05-17 09:31 CDT

**Reviewed commit:** `719b032e0 fix(vault-recall): expose RRF confidence metadata`

`git show --stat --name-status 719b032e0` reports:
- `M Epistemos/Sync/RRFFusionQuery.swift`
- `M EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Scope review:
- `RRFFusionQuery.swift` is inside T4's RRF / vault retrieval Swift surface.
- `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift` is inside T4's F-VaultRecall Swift-test lane.
- No other terminal-owned path appears in the commit.

Diff-content findings:
- The commit exposes source-hit breadth, confidence band, and `isContractSufficient` metadata to callers.
- It preserves fused result ordering while marking weak tail hits insufficient for asserted vault grounding.
- No first-N runtime enumeration, cloud hot-path fallback, `agent_core::hermes` resurrection, 36B hardware claim, or feature deletion appears in the committed path scan.

Open coordination notes:
- T4 branch still has no upstream configured; it remains local-only.
- Current dirty T4 worktree drift is generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`.
- The earlier `ffc4c8722` `agent_core/src/lib.rs` module-registration exception still needs rationale before merge-readiness.

**Verdict:** `719b032e0` is scope-clean and closes the RRF confidence metadata branch gap. T4 still needs artifact cleanup, branch push/PR, and the `lib.rs` exception note.

## Iter 22 Recheck - 2026-05-17 09:38 CDT

T4 HEAD remains `719b032e0`; no new commit landed during this pass.

Current dirty paths:
- `Epistemos/Sync/SearchIndexService.swift`
- `EpistemosTests/SearchIndexServiceFusionTests.swift`
- `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`

Scope review:
- `SearchIndexService.swift` is explicitly listed in T4's fused-search / Vault Retrieval Repair lane.
- The focused SearchIndexService fusion test is an expected validation surface for the same behavior.
- The generated `syntax-core/target/**` artifact remains out of scope.

**Verdict:** current live work is in-lane; artifact cleanup, branch push/PR, and the earlier `lib.rs` registration exception remain blockers.

## Iter 22 Late Recheck - 2026-05-17 09:49 CDT

**Reviewed commit:** `eacc32d78 fix(vault-recall): trace fused confidence counts`

`git show --stat --name-status eacc32d78` reports:
- `M Epistemos/Sync/SearchIndexService.swift`
- `M EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Scope review:
- Both committed paths are inside T4's SearchIndexService / F-VaultRecall RRF test lane.
- Current live `ChatCoordinator.swift`, `LocalAgentPromptBuilder.swift`, `NoteChatSidebar.swift`, and fallback-test WIP is also inside T4's prompt / provenance-card / fallback-test lane.
- Generated `syntax-core/target/**` artifact drift remains out of scope.

**Verdict:** `eacc32d78` is scope-clean; T4 remains blocked by local-only branch state, generated artifact cleanup, and the earlier root-module exception note.

## Iter 22 Final Recheck - 2026-05-17 09:56 CDT

**Reviewed commit:** `66881f0c8 fix(vault-recall): standardize provenance cards`

`git show --stat --name-status 66881f0c8` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `M Epistemos/Views/Notes/NoteChatSidebar.swift`
- `M EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

Scope review:
- All committed paths are inside T4's prompt-build seam, LocalAgentPromptBuilder, note provenance-card, and F-VaultRecall fallback-test lanes.
- Current live `RRFFusionQuery.swift` / F-VaultRecall RRF test WIP is in-lane.
- Generated `syntax-core/target/**` artifact drift remains out of scope.

**Verdict:** `66881f0c8` is scope-clean; T4 remains local-only with artifact drift and root-module exception debt.

## Iter 23 Recheck - 2026-05-17 10:00 CDT

No new T4 commit after local-only `66881f0c8`.

Current non-artifact WIP:
- `Epistemos/Sync/RRFFusionQuery.swift`
- `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Both paths are in T4's RRF / F-VaultRecall test lane. Generated artifacts remain dirty.

**Verdict:** no new committed drift; current WIP is in-lane, but T4 remains local-only with artifact and root-module exception blockers.

## Iter 24 Recheck - 2026-05-17 10:08 CDT

**Reviewed commits:** `36d338a8a`, `ee9325094`

`git show --name-status` reports:
- `M Epistemos/Sync/RRFFusionQuery.swift`
- `M EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- The RRF query file and focused F-VaultRecall Swift tests are inside T4's RRF / F-VaultRecall lanes.
- The Vault Context Contract and F-VaultRecall falsifier docs are inside T4's doctrine/reporting lane.
- No T1/T2/T3/T7-owned path appears in the reviewed commits.

Diff-content findings:
- `36d338a8a` rejects rank-only fused evidence in the RRF evidence gate.
- `ee9325094` records the rank-only evidence guard in the Vault contract / falsifier docs.
- Current live `RRFFusionQuery.swift` / `F_VaultRecall_50_RRFFusionTests.swift` WIP remains in-lane.

Open coordination notes:
- T4 remains local-only with no upstream configured.
- Generated `syntax-core/target/**` artifact drift and the earlier `agent_core/src/lib.rs` module-registration exception remain blockers.

**Verdict:** committed T4 movement is scope-clean and doctrine-aligned; PR-readiness still requires push/PR, artifact cleanup, and root-module exception rationale.

## Iter 25 Recheck - 2026-05-17 10:18 CDT

**Reviewed commit:** `604356040 fix(vault-recall): retain fused confidence metrics`

`git show --name-status 604356040` reports:
- `M Epistemos/Sync/RRFFusionQuery.swift`
- `M EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Scope review:
- Both paths are inside T4's RRF / F-VaultRecall Swift-test lanes.
- No other terminal-owned path appears in the commit.

Current live non-artifact drift:
- `Epistemos/App/ChatCoordinator.swift`
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

Both live paths are in T4's prompt seam / F-VaultRecall fallback-test lanes. Generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d` is dirty again and remains a pre-commit blocker. T4 remains local-only with no upstream configured.

**Verdict:** committed slice and live product WIP are in-lane; artifact cleanup, push/PR, and the earlier `lib.rs` exception note remain blockers.

## Iter 26 Recheck - 2026-05-17 10:28 CDT

**Reviewed commits:** `d4e118744`, `2313f9ee1`

`git show --name-status` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M EpistemosTests/F_VaultRecall_50_FallbackTests.swift`
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- `ChatCoordinator.swift`, the focused F-VaultRecall fallback test, the Vault falsifier doc, and the Vault Context Contract doc are inside T4's prompt-seam / F-VaultRecall lanes.
- No T1/T2/T3/T7-owned path appears in the reviewed commits.

Diff-content findings:
- `d4e118744` distinguishes no indexed hits from indexed hits that fail the Vault Context Contract, reporting insufficient evidence instead of false no-results.
- `2313f9ee1` records the insufficient-evidence path in the contract and falsifier docs.

Current live non-artifact drift:
- `Epistemos/Sync/SearchIndexService.swift`
- `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Both live paths are in T4's SearchIndexService / RRF test lanes. `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d` is dirty again. T4 remains local-only with no upstream configured.

**Verdict:** committed and live T4 movement is in-lane; merge readiness remains blocked by local-only branch state, generated artifact cleanup, and the earlier `lib.rs` exception note.

## Iter 27 Recheck - 2026-05-17 10:38 CDT

**Reviewed commit:** `81bcdd5c1 fix(vault-recall): mirror confidence metadata counts`

`git show --name-status 81bcdd5c1` reports:
- `M Epistemos/Sync/SearchIndexService.swift`
- `M EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Scope review:
- Both committed paths are inside T4's SearchIndexService / F-VaultRecall RRF test lanes.
- No other terminal-owned path appears in the commit.

Diff-content findings:
- The commit mirrors high/medium confidence result counts into fused-search completion metadata and adds an RRF source guard.

Current live drift:
- `agent_core/src/retrieval/mod.rs`
- `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`

`agent_core/src/retrieval/mod.rs` is inside T4's Vault Search 2.0 module lane. The generated `syntax-core/target/**` artifact remains out-of-scope. T4 remains local-only with no upstream configured.

**Verdict:** committed and live product work is in-lane; local-only branch state, generated artifact cleanup, and the historical `lib.rs` exception remain blockers.

## Iter 28 Recheck - 2026-05-17 10:49 CDT

**Reviewed commits:** `e162c7db1`, `53a1c17fa`, `d246b088d`

`git show --name-status` reports:
- `M agent_core/src/retrieval/mod.rs`
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- `agent_core/src/retrieval/mod.rs` is inside T4's Vault Search 2.0 / retrieval lane.
- The F-VaultRecall falsifier and Vault Context Contract docs are inside T4's doctrine/reporting lane.
- No T1/T2/T3/T6/T7-owned path appears in the reviewed commits.

Diff-content findings:
- `e162c7db1` records the Rust rank-only guard in the contract and falsifier docs.
- `53a1c17fa` adds synthesis-specific distinct selected-note validation and reports `SynthesisUnderCited` for under-cited synthesis answers.
- `d246b088d` records synthesis trace validation in the Vault Context Contract and falsifier docs.
- T4's commit messages claim local `cargo test --manifest-path agent_core/Cargo.toml --lib` counts of 1701 / 1703, but the main baseline for T9 remains 1671 tests at this iteration; treat the count discrepancy as branch-local growth, not main regression.

Current live drift:
- `agent_core/src/retrieval/mod.rs`
- `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`

The live retrieval file remains in-lane; the generated artifact is out-of-scope. T4 is still local-only with no upstream configured.

**Verdict:** committed T4 VaultRecall movement is scope-clean and doctrine-aligned; PR-readiness is still blocked by local-only branch state, generated artifact cleanup, and earlier `lib.rs` exception rationale.

## Iter 29 Recheck - 2026-05-17 10:58 CDT

**Reviewed commits:** `910c578ea`, `b44f80373`, `704ab4858`

`git show --name-status` reports:
- `M agent_core/src/retrieval/mod.rs`
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- `agent_core/src/retrieval/mod.rs` is inside T4's Vault Search 2.0 / retrieval lane.
- The F-VaultRecall falsifier and Vault Context Contract docs are inside T4's doctrine/reporting lane.
- No other terminal-owned path appears in the reviewed commits.

Diff-content findings:
- `910c578ea` adds a trace-level adversarial margin validator that reports `AdversarialConfusion`.
- `b44f80373` records that guard in the Vault Context Contract and falsifier docs.
- `704ab4858` maps Shadow-first exact-escalation violations into the shared Vault Context Contract vocabulary.
- The commit messages again report local branch cargo counts (1705 passed), while main remains at 1671 tests in T9's baseline.

Current live drift is generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d` only. T4 is still local-only with no upstream configured.

**Verdict:** committed T4 movement is scope-clean; local-only branch state, generated artifact cleanup, and the historical `lib.rs` exception remain blockers.

## Iter 30 Recheck - 2026-05-17 11:10 CDT

**Reviewed commits:** `5b16257ad`, `5dff1f532`, `32ba74ce9`

`git show --name-status` reports:
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`
- `M agent_core/src/retrieval/mod.rs`

Scope review:
- `agent_core/src/retrieval/mod.rs` is inside T4's Vault Search 2.0 / retrieval lane.
- The F-VaultRecall falsifier and Vault Context Contract docs are inside T4's reporting lane.
- No other terminal-owned path appears in the reviewed commits.

Diff-content findings:
- `5b16257ad` documents `SelectedCountMismatch` as a trace-honesty failure.
- `5dff1f532` rejects traces whose selected count exceeds the candidate pool.
- `32ba74ce9` rejects selected candidates with NaN, infinite, or negative fused scores.
- Commit messages report local branch cargo counts increasing to 1708, while T9's main baseline remains 1671 tests.

Current live drift:
- `M agent_core/src/retrieval/mod.rs`
- `M syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d`

The live retrieval file is in-lane; the generated `syntax-core/target/**` artifact remains out-of-scope. T4 is still local-only with no upstream configured.

**Verdict:** committed T4 work remains scope-clean and deepens Vault Context Contract trace honesty. PR readiness is still blocked by local-only branch state, generated artifact cleanup, and the historical `agent_core/src/lib.rs` exception.

## Iter 31 Recheck - 2026-05-17 11:22 CDT

**Reviewed commits:** `b666b7c13`, `6342246a8`, `e69e6e03d`

`git show --name-status` reports:
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`
- `M agent_core/src/retrieval/mod.rs`

Scope review:
- `agent_core/src/retrieval/mod.rs` is inside T4's Vault Search 2.0 / retrieval lane.
- The F-VaultRecall falsifier and Vault Context Contract docs are inside T4's reporting lane.
- No other terminal-owned path appears in the reviewed commits.

Diff-content findings:
- `b666b7c13` documents selected candidates needing to clear the low-confidence band even when trace confidence is high.
- `6342246a8` adds selected-only confidence counts in the Rust Vault Context Contract while distinguishing the full candidate pool from selected context.
- `e69e6e03d` records the selected-context confidence count contract in docs.
- T4's commit messages report local branch cargo counts at 1710/1711; T9's main baseline remains 1671 tests.

Current live drift is generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d` only. T4 remains local-only with no upstream configured.

**Verdict:** latest T4 VaultRecall trace-honesty work is scope-clean. PR readiness remains blocked by local-only branch state, generated artifact cleanup, and the historical `agent_core/src/lib.rs` exception.

## Iter 32 Recheck - 2026-05-17 11:46 CDT

**Reviewed commits:** `7a1241d93`, `b67a8d69e`, `d1fbe8432`

`git show --name-status` reports:
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`
- `M agent_core/src/retrieval/mod.rs`

Scope review:
- The falsifier / contract docs and retrieval module are inside T4's VaultRecall and Vault Search 2.0 lanes.
- No T1/T2/T3/T6/T7-owned path appears in these commits.

Diff-content findings:
- `7a1241d93` records the Shadow-first score margin.
- `b67a8d69e` adds shadow exact escalation request handling.
- `d1fbe8432` records the Shadow exact request in the Vault Context Contract and falsifier.

Current live non-artifact drift:
- `Epistemos/Sync/RRFFusionQuery.swift`
- `Epistemos/Sync/SearchIndexService.swift`
- `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`

Those live paths are T4-owned. T4 remains local-only and again has generated `syntax-core/target/**` artifact drift.

**Verdict:** committed and live T4 product work is in-lane. PR readiness is still blocked by local-only branch state, generated artifacts, and historical `agent_core/src/lib.rs` exception rationale.

## Iter 33 Recheck - 2026-05-17 11:59 CDT

**Reviewed commits:** `f4b3c2738`, `9359dba3f`

`git show --name-status` reports:
- `M Epistemos/Sync/RRFFusionQuery.swift`
- `M Epistemos/Sync/SearchIndexService.swift`
- `M EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift`
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- All committed paths are in T4's RRF/SearchIndex/F-VaultRecall/contract lanes.
- No cross-terminal source path appears.

Diff-content findings:
- `f4b3c2738` traces the RRF top score margin in Swift RRF/SearchIndex paths and focused RRF tests.
- `9359dba3f` records the Swift RRF score margin in the Vault Context Contract and F-VaultRecall falsifier docs.

Current live non-artifact drift:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

Those live paths are T4-owned prompt/fallback lanes. Generated `syntax-core/target/**` artifact drift remains.

**Verdict:** committed and live T4 work is in-lane. Local-only branch state, generated artifacts, and historical `agent_core/src/lib.rs` exception remain blockers.

## Iter 34 Recheck - 2026-05-17 12:10 CDT

**Reviewed commits:** `939a1c882`, `1130cea3f`, `2989bd550`, `d4be53b1b`

`git show --name-status` reports:
- `M agent_core/src/retrieval/mod.rs`
- `M docs/falsifiers/F-VaultRecall-50_2026_05_17.md`
- `M docs/fusion/VAULT_CONTEXT_CONTRACT_2026_05_17.md`

Scope review:
- `agent_core/src/retrieval/mod.rs` is inside T4's Vault Search 2.0 / retrieval lane.
- The F-VaultRecall falsifier and Vault Context Contract docs are inside T4's reporting lane.
- No cross-terminal product path appears in the reviewed commits.

Diff-content findings:
- `939a1c882` includes Shadow escalation snippets in retrieval traces.
- `1130cea3f` records those snippets in the falsifier and Vault Context Contract docs.
- `2989bd550` bounds Shadow escalation targets.
- `d4be53b1b` records bounded target doctrine.

Current live drift:
- `Epistemos/App/ChatCoordinator.swift`
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift`
- generated `syntax-core/target/**` artifact drift

The live Swift/test files are T4-owned prompt/fallback lanes. T4 remains local-only with generated artifact cleanup and the historical `agent_core/src/lib.rs` exception pending.

**Verdict:** latest committed T4 VaultRecall movement is scope-clean. Merge readiness still requires branch push/PR, artifact cleanup, and exception rationale.

## Iter 35 Recheck - 2026-05-17 12:24 CDT

**Reviewed commit:** `73d007188 fix(vault-recall): add fallback provenance block`

`git show --name-status 73d007188` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M EpistemosTests/F_VaultRecall_50_FallbackTests.swift`

Scope review:
- `ChatCoordinator.swift` is in T4's prompt/fallback seam for F-VaultRecall.
- `F_VaultRecall_50_FallbackTests.swift` is the focused VaultRecall fallback test lane.
- No cross-terminal product path appears in this commit.

Diff-content findings:
- The branch now adds the fallback provenance block to grounded fallback answers and covers the path in focused fallback tests.
- Hygiene scan over `73d007188` found no new `agent_core::hermes` module, 36B-on-16GB claim, first-N notes path, cloud hot-path fallback, or feature deletion signal.

Current worktree drift:
- generated `syntax-core/target/aarch64-apple-darwin/debug/libsyntax_core.d` only.

**Verdict:** T4 iter35 fallback provenance work is scope-clean. T4 remains local-only with generated artifact cleanup and the historical `agent_core/src/lib.rs` exception pending before PR.
