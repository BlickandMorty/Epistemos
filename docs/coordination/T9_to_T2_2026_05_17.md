# T9 to T2 - Scope Review 2026-05-17

**From:** T9 coordination
**To:** T2 agent/runtime lane
**Reviewed:** 2026-05-17 07:18:52 CDT
**T2 branch:** `codex/t2-agent-2026-05-16`
**Reviewed commit:** `4f7b9df60 audit(local-agent): capture runtime gating baseline`

## Scope Review

`git show --stat --name-status 4f7b9df60` reports:
- `A docs/agent-system/MODEL_GRAMMAR_MATRIX_2026_05_17.md`
- `A docs/audits/AGENT_RUNTIME_AUDIT_2026_05_17.md`
- `A docs/audits/MODEL_GATING_MATRIX_2026_05_17.md`
- `A docs/fusion/LOCAL_AGENT_EXCELLENCE_DOCTRINE_2026_05_17.md`

Those paths are inside T2's docs lane for local-agent excellence, model gating, grammar, and runtime audit work.

## Diff-Content Check

Per PASS-2 Lesson #17, T9 checked actual committed paths and scanned the new docs for live drift hazards.

Findings:
- The commit did not include the dirty `syntax-core/target/**` generated artifacts previously flagged by T9.
- 36B references are framed through the 32 GB default gate and the captured Qwen 3 8B fallback on an 18 GB host, not as a 16 GB product claim.
- The `agent_core::hermes` mention is recorded as stale-comment / compatibility vocabulary drift; no new `agent_core::hermes` module is introduced.
- The doctrine explicitly says no CLI subprocess on the hot path and no hidden cloud fallback.

## Open Coordination Notes

- T2's worktree still has tracked generated `syntax-core/target/**` modifications after the commit. That remains a pre-commit blocker for the next T2 slice; see `docs/coordination/T2_drift_2026_05_17.md`.

## Verdict

`4f7b9df60` is scope-clean. The committed T2 docs are reviewable, but the dirty worktree still needs artifact cleanup before the next commit or PR.

## Iter 5 Recheck - 2026-05-17 07:28 CDT

**Reviewed commit:** `ed7ff2531 feat(settings): expose local agent constellation diagnostics`

`git show --stat --name-status ed7ff2531` reports:
- `M Epistemos/Views/Settings/SettingsView.swift`

That path is inside T2's scope lock for Settings model-picker badges, diagnostic rows, and AgentBlueprint / local-agent UI.

Diff-content findings:
- The new "Active Constellation", "Strict Grammar", "Schema Drift", and "Constellation Health" rows read existing `InferenceState` / `LocalToolGrammar` truth rather than hard-coded runtime claims.
- The new "Power-user 36B gate" exposes an existing UserDefaults gate. The copy keeps the canonical 32 GB default and says the 16 GB path may swap, throttle, or fail. T9 treats this as an experimental opt-in affordance, not a Verified Fixed hardware claim.
- `ISSUE-2026-05-16-015` remains `Investigating`: the Settings toggle and diagnostics are partial evidence, but per-model capability badges, runtime proof, and actual ternary / lattice / KV-Direct substrate work remain open.

Open coordination notes:
- T2's worktree is still dirty with tracked `syntax-core/target/**` artifacts.
- T2 now also has uncommitted modifications to `Epistemos/Models/ChatTypes.swift` and `Epistemos/Models/SDMessage.swift`, which are not listed in the T2 scope lock. See `docs/coordination/T2_drift_2026_05_17.md`.

**Verdict:** `ed7ff2531` is committed-slice scope-clean. The current dirty worktree is a blocker before the next T2 commit / PR.

## Iter 7 Recheck - 2026-05-17 07:45 CDT

**Reviewed commit:** `9b090203d feat(chat): persist answer packets with assistant turns`

`git show --stat --name-status 9b090203d` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M Epistemos/Bridge/StreamingDelegate.swift`
- `M Epistemos/Models/ChatTypes.swift`
- `M Epistemos/Models/SDMessage.swift`
- `M Epistemos/State/AgentChatState.swift`
- `M Epistemos/State/ChatState.swift`
- `M Epistemos/Views/Chat/MessageBubble.swift`

Scope review:
- `Epistemos/Bridge/StreamingDelegate.swift` is inside T2's AnswerPacket runtime-emission lane.
- The remaining paths are not listed in the T2 scope lock as written. `Epistemos/App/ChatCoordinator.swift` is explicitly owned by T4 for Vault Context Contract enforcement, and chat model/state/UI surfaces are not in T2's touch list.

Diff-content findings:
- The implementation carries emitted `AnswerPacket` snapshots through stream completion, emits stubs for direct local completions, persists packet payloads on `SDMessage`, and lets `MessageBubble` render persisted chips after live-ring eviction.
- This is coherent AnswerPacket work, but it crossed outside the written lane before coordination landed.
- No `agent_core::hermes` module, 36B-on-16 GB product claim, cloud hot-path fallback, first-N vault runtime path, or feature deletion appears in the diff.

Open coordination notes:
- `9b090203d` is a committed scope violation until T2 records a cross-lane rationale / owner sign-off.
- T2 still has tracked generated `syntax-core/target/**` artifact drift and a dirty in-scope `Epistemos/Views/Settings/AnswerPacketHealthRow.swift`.

**Verdict:** committed-slice scope violation; not merge-ready as-is.

## Iter 8 Recheck - 2026-05-17 07:52 CDT

**Reviewed commit:** `edb69ec47 feat(settings): audit persisted answer packets`

`git show --stat --name-status edb69ec47` reports:
- `M Epistemos/Views/Settings/AnswerPacketHealthRow.swift`

That path is inside T2's Settings diagnostic-row scope. The row now inspects recent persisted `SDMessage` assistant turns for AnswerPacket ids, VRM labels, and encoded packet coverage.

Diff-content findings:
- The commit is a Settings diagnostics addition and does not touch chat coordinator, models, state, backend, vault, or UI surfaces outside Settings.
- No `agent_core::hermes` module, 36B-on-16 GB product claim, cloud hot-path fallback, first-N vault runtime path, or feature deletion appears in the diff.

Open coordination notes:
- `edb69ec47` is scope-clean, but it builds on the earlier `9b090203d` branch state that T9 flagged as a committed scope violation.
- T2's worktree still carries tracked generated `syntax-core/target/**` artifact drift.

**Verdict:** `edb69ec47` is scope-clean; the branch remains not merge-ready because of `9b090203d` and artifact drift.

## Iter 9 Recheck - 2026-05-17 08:00 CDT

**Reviewed branch state:** T2 HEAD still `edb69ec47 feat(settings): audit persisted answer packets`.

No new T2 commit was present. The live risk is the uncommitted worktree:
- `git diff --stat` reports 234 modified files, approximately 5014 insertions and 1738 deletions.
- The dirty set includes some T2-owned paths (`Epistemos/LocalAgent/**`, Settings, `agent_core/src/agent_runtime/**`, provider grammar work).
- It also includes out-of-lane paths such as `agent_core/src/storage/vault.rs` (T4), `agent_core/src/research/eml/**` (T7), broad `agent_core/src/research/**` and `agent_core/src/helios/**` modules, and tracked `syntax-core/target/**` artifacts.

Diff-content check is intentionally limited at this stage because the slice is uncommitted and too broad for a single T2 lane review.

**Verdict:** T2 remains blocked. The branch's latest commit is still `edb69ec47`, but the current worktree must be cleaned or split before any commit.

## Iter 10 Recheck - 2026-05-17 08:04 CDT

**Reviewed commit:** `46ac80bba feat(local-agent): route native tool grammar profiles`

`git show --stat --name-status 46ac80bba` reports:
- `M Epistemos/LocalAgent/LocalAgentLoop.swift`
- `M Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `M Epistemos/LocalAgent/LocalToolGrammar.swift`
- `M Epistemos/Views/Settings/SettingsView.swift`
- `M agent_core/src/agent_runtime/function_call.rs`
- `M agent_core/src/agent_runtime/prompt_format.rs`

Scope review:
- All committed paths are inside T2's LocalAgent, Settings, and `agent_runtime` ownership.
- The previously observed broad uncommitted Rust drift was not committed in this slice.

Diff-content findings:
- The commit adds native tool grammar profiles for Qwen, Hermes, DeepSeek-Coder, Llama, Mistral, and Phi families.
- It routes the active grammar profile through Swift and Rust prompt construction.
- It normalizes model-native tool wrappers in the `agent_runtime` parser.
- No new `agent_core::hermes` module, 36B-on-16 GB product claim, cloud hot-path fallback, first-N vault runtime path, or feature deletion appears in the committed path list.

Open coordination notes:
- `46ac80bba` is local-only / unpushed at this check.
- T2 worktree still has tracked generated `syntax-core/target/**` artifact drift.
- `9b090203d` remains a committed scope violation and still blocks branch merge-readiness.

**Verdict:** `46ac80bba` is scope-clean, but T2 branch remains not merge-ready.

## Iter 11 Recheck - 2026-05-17 08:14 CDT

**Reviewed branch state:** T2 HEAD `46ac80bba feat(local-agent): route native tool grammar profiles`, now pushed to `origin/codex/t2-agent-2026-05-16`.

No new committed paths appeared since the iter 10 review. The pushed commit remains scope-clean for:
- `Epistemos/LocalAgent/LocalAgentLoop.swift`
- `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `Epistemos/LocalAgent/LocalToolGrammar.swift`
- `Epistemos/Views/Settings/SettingsView.swift`
- `agent_core/src/agent_runtime/function_call.rs`
- `agent_core/src/agent_runtime/prompt_format.rs`

Live worktree findings:
- `Epistemos/LocalAgent/ConfidenceRouter.swift` is dirty and in T2's LocalAgent lane.
- `EpistemosTests/ConfidenceRouterTests.swift` is dirty but not named in T2's exact written test scope; T2 should document the Swift-test scope rationale before committing it.
- tracked generated `syntax-core/target/**` artifacts remain dirty.

Open coordination notes:
- `9b090203d` remains a committed scope violation and still blocks branch merge-readiness.
- `ISSUE-2026-05-16-015` remains `Investigating`; `46ac80bba` improves grammar-profile honesty but does not prove 36B-on-16GB runtime viability.

**Verdict:** branch HEAD is pushed and scope-clean; current worktree is not commit-ready.

## Iter 12 Recheck - 2026-05-17 08:20 CDT

**Reviewed commit:** `6c5526fa8 feat(local-agent): select models by task class`

`git show --stat --name-status 6c5526fa8` reports:
- `M Epistemos/LocalAgent/ConfidenceRouter.swift`
- `M EpistemosTests/ConfidenceRouterTests.swift`

Scope review:
- `Epistemos/LocalAgent/ConfidenceRouter.swift` is inside T2's LocalAgent lane.
- `EpistemosTests/ConfidenceRouterTests.swift` is not listed in T2's exact written test scope, which names Rust `tests/agent_runtime_*.rs`. T9 flagged this path before the commit; the commit body does not include a scope rationale.

Diff-content findings:
- The commit adds `ConfidenceRouter.TaskClass` routing for coding, debugging, reasoning, synthesis, structured output, tool use, fast chat, and general work.
- It selects a local model from the installed constellation using per-task preference tables rather than inheriting a single selected model.
- The preference table includes 36B model IDs, but the diff does not claim verified 36B-on-16GB runtime viability.
- No new `agent_core::hermes` module, first-N vault runtime path, silent cloud hot-path fallback, or feature deletion appears in the committed path list.

Open coordination notes:
- `6c5526fa8` is pushed to `origin/codex/t2-agent-2026-05-16`.
- T2 worktree still has tracked generated `syntax-core/target/**` artifact drift.
- Prior `9b090203d` remains a committed scope violation.
- `ISSUE-2026-05-16-015` remains `Investigating`; this is progress on task-class model routing, not a verified 36B-on-16GB proof or full per-model capability surface.

**Verdict:** functionally relevant, but not fully scope-clean as committed because of the Swift test path.

## Iter 13 Recheck - 2026-05-17 08:30 CDT

**Reviewed branch state:** T2 HEAD remains `6c5526fa8 feat(local-agent): select models by task class`.

Live worktree findings:
- `Epistemos/Engine/MLXInferenceService.swift` is dirty and appears to add an idle-unload mode / low-memory deep-unload path.
- `Epistemos/Views/Settings/PerformanceSettingsSection.swift` is dirty and is inside T2's Settings lane.
- `EpistemosTests/RuntimeValidationTests.swift` and `EpistemosTests/TriageServiceTests.swift` are dirty; neither is listed in T2's exact written test scope.
- tracked generated `syntax-core/target/**` artifacts remain dirty.

Diff-content findings:
- The current dirty diff is relevant to the Startup / Idle Memory settings backlog: `IdleMemoryMode.lowMemory` appears to select a 30-second deep unload path outside critical thermal state.
- No new 36B-on-16 GB product claim, `agent_core::hermes` module, first-N vault runtime path, silent cloud hot-path fallback, or feature deletion appears in the scanned dirty diff.

Open coordination notes:
- `Epistemos/Engine/MLXInferenceService.swift` is not in T2's written touch list; the listed engine file is `LocalModelInfrastructure.swift`.
- The Swift test files repeat the same exact-scope issue as `ConfidenceRouterTests.swift`.
- `ISSUE-2026-05-12-007` should not be bumped for this dirty work until the patch is committed and verified.

**Verdict:** T2 is not commit-ready in its current worktree. The likely fix is useful, but T2 needs a scope-rationale note or a split before committing the MLX engine and Swift test changes.

## Iter 14 Recheck - 2026-05-17 08:37 CDT

**Reviewed commit:** `f0c0fbace feat(local-agent): honor low memory idle unload mode`

`git show --stat --name-status f0c0fbace` reports:
- `M Epistemos/Engine/MLXInferenceService.swift`
- `M Epistemos/Views/Settings/PerformanceSettingsSection.swift`
- `M EpistemosTests/RuntimeValidationTests.swift`
- `M EpistemosTests/TriageServiceTests.swift`

Scope review:
- `Epistemos/Views/Settings/PerformanceSettingsSection.swift` is inside T2's Settings lane.
- `Epistemos/Engine/MLXInferenceService.swift` is outside T2's exact written engine scope; the prompt names `LocalModelInfrastructure.swift`, not MLXInferenceService.
- `EpistemosTests/RuntimeValidationTests.swift` and `EpistemosTests/TriageServiceTests.swift` are outside T2's exact written test scope.

Diff-content findings:
- The patch routes the persisted Low Memory setting into `LocalMLXRuntimePolicy`.
- It adds a typed idle unload mode and maps Low Memory to the canonical 30-second deep MLX/Metal release path while preserving the default Keep Warm working-set-only behavior.
- It adds Swift tests/source guards for both paths.
- No new 36B-on-16 GB product claim, `agent_core::hermes` module, first-N vault runtime path, silent cloud hot-path fallback, or feature deletion appears in the committed diff.

Open coordination notes:
- `ISSUE-2026-05-12-007` may be marked branch-patched but not Verified Fixed or main-landed.
- T2 worktree still has generated `syntax-core/target/**` artifact drift.
- Prior T2 committed scope debts (`9b090203d`, `6c5526fa8`) remain open.

**Verdict:** useful behavior, committed outside exact scope. T2 needs retroactive scope rationale / owner sign-off before this can be treated as merge-ready.

## Iter 15 Recheck - 2026-05-17 08:44 CDT

**Reviewed branch state:** T2 HEAD remains `f0c0fbace feat(local-agent): honor low memory idle unload mode`.

Live worktree findings:
- `Epistemos/App/ChatCoordinator.swift`
- `Epistemos/Models/ChatTypes.swift`
- `Epistemos/Models/SDMessage.swift`
- `Epistemos/State/AgentChatState.swift`
- `Epistemos/State/ChatState.swift`
- `Epistemos/Views/Chat/MessageBubble.swift`
- `EpistemosTests/AgentEventVisibilityTests.swift`
- `EpistemosTests/AnswerPacketCodableTests.swift`
- `EpistemosTests/SDMessageTests.swift`
- `Epistemos/Views/Chat/AgentRunTimelineView.swift` (untracked)
- tracked generated `syntax-core/target/**` artifacts

Scope review:
- T2's mission includes AnswerPacket runtime emission, but the exact written path list names `Epistemos/Bridge/StreamingDelegate.swift` for that seam, plus LocalAgent, Settings, and `agent_core/src/agent_runtime/**`.
- The current dirty set reaches into broad chat app/model/state/UI files and Swift tests that are not in the exact written T2 touch list.
- The new `AgentRunTimelineView.swift` UI surface should be coordinated with T6 before commit if it becomes a visible user surface.

Diff-content findings:
- The path set appears aligned with AnswerPacket / agent event visibility, but it is too broad to call scope-clean without a lane-owner rationale.
- No new 36B-on-16 GB product claim, `agent_core::hermes` module, first-N vault runtime path, silent cloud hot-path fallback, or feature deletion appears in the path scan.

Open coordination notes:
- Prior T2 committed scope debts remain open: `9b090203d`, `6c5526fa8`, and `f0c0fbace`.
- The generated artifact drift remains open.

**Verdict:** T2 is not commit-ready in its current worktree. This should be narrowed or explicitly documented before commit / PR.

## Iter 15 Follow-up - 2026-05-17 08:51 CDT

**Reviewed commit:** `a3e177e92 feat(chat): replay agent run timelines from event log`

`git show --stat --name-status a3e177e92` reports:
- `M Epistemos/App/ChatCoordinator.swift`
- `M Epistemos/Models/ChatTypes.swift`
- `M Epistemos/Models/SDMessage.swift`
- `M Epistemos/State/AgentChatState.swift`
- `M Epistemos/State/ChatState.swift`
- `A Epistemos/Views/Chat/AgentRunTimelineView.swift`
- `M Epistemos/Views/Chat/MessageBubble.swift`
- `M EpistemosTests/AgentEventVisibilityTests.swift`
- `M EpistemosTests/AnswerPacketCodableTests.swift`
- `M EpistemosTests/SDMessageTests.swift`

Scope review:
- The behavior is related to T2's AnswerPacket / agent runtime mission.
- The exact written T2 touch list names `Epistemos/Bridge/StreamingDelegate.swift` for AnswerPacket runtime emission, plus LocalAgent, Settings, and `agent_core/src/agent_runtime/**`.
- This commit crosses into broad chat app/model/state/UI paths and Swift tests outside the exact list.
- `AgentRunTimelineView.swift` is a new chat UI surface and should have T6 audit coordination before merge.

Diff-content findings:
- The commit binds Rust agent run IDs to completed assistant messages, persists run IDs through SwiftData chat messages, and renders a compact replayable timeline from durable AgentEvents.
- No new 36B-on-16 GB product claim, `agent_core::hermes` module, first-N vault runtime path, silent cloud hot-path fallback, or feature deletion appears in the committed path scan.

Open coordination notes:
- T2 worktree is back to generated `syntax-core/target/**` artifacts only.
- Prior T2 committed scope debts remain open: `9b090203d`, `6c5526fa8`, and `f0c0fbace`.

**Verdict:** product-relevant but committed outside exact scope. Treat as scope debt until T2 records rationale / owner sign-off and T6 reviews the new chat timeline UI.

## Iter 17 Recheck - 2026-05-17 09:04 CDT

T2 HEAD remains `a3e177e92`; no new commit appeared after iter 15.

Current live drift:
- `Epistemos/Views/Settings/AgentSectionDetailView.swift`
- `Epistemos/LocalAgent/AgentBlueprint.swift` (untracked)
- `Epistemos/Views/Settings/AgentBlueprintSettingsView.swift` (untracked)
- `EpistemosTests/AgentBlueprintTests.swift` (untracked)
- generated `syntax-core/target/**` artifacts

Scope review:
- Settings and LocalAgent AgentBlueprint work appears aligned with T2's AgentBlueprint UI / LocalAgent mission.
- The Swift test path should not be committed without the same explicit exact-scope rationale requested for earlier Swift-test additions.
- Generated artifacts remain a hard pre-commit cleanup item.

**Verdict:** useful in-lane product work is visible, but T2 remains not commit/PR-ready until test-scope rationale and artifact cleanup are handled.

## Iter 18 Recheck - 2026-05-17 09:12 CDT

T2 remains at `a3e177e92`; non-artifact drift is unchanged at AgentBlueprint Settings / LocalAgent files plus `EpistemosTests/AgentBlueprintTests.swift`.

Scope note:
- Settings / LocalAgent files remain in-lane for AgentBlueprint work.
- The Swift test path still needs explicit rationale before commit.
- Generated artifacts remain dirty.

**Verdict:** unchanged from iter 17; no new committed drift, but T2 still carries live pre-commit blockers.

## Iter 19 Recheck - 2026-05-17 09:17 CDT

**Reviewed commit:** `79cb183ee feat(settings): add agent blueprint mission runner`

`git show --stat --name-status 79cb183ee` reports:
- `A Epistemos/LocalAgent/AgentBlueprint.swift`
- `A Epistemos/Views/Settings/AgentBlueprintSettingsView.swift`
- `M Epistemos/Views/Settings/AgentSectionDetailView.swift`
- `A EpistemosTests/AgentBlueprintTests.swift`

Scope review:
- AgentBlueprint LocalAgent / Settings work is aligned with T2's written LocalAgent and AgentBlueprint UI mission.
- The Swift test path was flagged before commit as outside T2's exact written test lane unless rationale/sign-off is documented.
- The new Settings UI should be available for T6 audit under T6's Agent UI / AgentBlueprint follow-up list.

Diff-content findings:
- The commit adds typed AgentBlueprint and MissionPacket contracts, a Settings tab for model/tool/scope/approval selection, and dispatch through the Command Center runtime path.
- No 36B-on-16 GB hardware claim, `agent_core::hermes` module, first-N vault path, cloud hot-path fallback, or feature deletion appears in the committed path scan.

Open coordination notes:
- T2 worktree is generated artifacts only after the commit.
- Prior committed T2 scope debts remain unresolved.

**Verdict:** product-relevant but still carries Swift-test exact-scope debt until T2 records rationale / sign-off.

## Iter 20 Recheck - 2026-05-17 09:22 CDT

T2 has no new commit after `79cb183ee`. Current non-artifact live drift:
- `Epistemos/LocalAgent/AgentBlueprint.swift`
- `Epistemos/Views/Settings/AgentBlueprintSettingsView.swift`
- `EpistemosTests/AgentBlueprintTests.swift`

Scope review:
- LocalAgent / Settings changes remain aligned with AgentBlueprint mission work.
- The Swift test path remains the unresolved exact-scope issue.
- Generated artifacts remain dirty.

**Verdict:** no new committed drift after `79cb183ee`, but T2 is not commit/PR-ready until test rationale and artifact cleanup are handled.
