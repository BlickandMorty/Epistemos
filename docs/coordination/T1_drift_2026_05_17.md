# T1 Drift Notice - 2026-05-17

**From:** T9 coordination
**To:** T1 Tri-Fusion lane
**Observed:** 2026-05-17 07:45 CDT
**T1 branch:** `codex/t1-trifusion-2026-05-16`
**T1 HEAD:** `87d336711`

## Finding

T1 has in-scope uncommitted Tri-Fusion implementation work:
- `agent_core/src/tri_fusion/`
- `agent_core/tests/tri_fusion_json_round_trip.rs`

Those paths match the T1 Tri-Fusion module and Rust integration-test intent.

T1 also has an uncommitted modification to:
- `agent_core/src/lib.rs`

That file is not listed in the written T1 scope lock. The diff currently only adds:

```rust
pub mod tri_fusion;
```

This is likely the normal module-registration seam, but it is still outside the exact "touch only" list as written.

## Required Action For T1

Before committing this implementation slice:
1. Keep `agent_core/src/lib.rs` to the minimal module-registration line only, or document why a wider root-module change is required.
2. Mention the `agent_core/src/lib.rs` scope exception explicitly in the commit body.
3. Keep the Tri-Fusion code and test paths free of T2/T3/T4/T5/T6/T7/T8 surfaces.

## Current Verdict

Pre-commit coordination required. No code review verdict yet because the implementation is uncommitted.

## Recheck - 2026-05-17 07:52 CDT

T1 committed `e117ef855 feat(tri-fusion): add canonical json document floor`.

`git show --name-status e117ef855` reports:
- `M agent_core/src/lib.rs`
- `A agent_core/src/tri_fusion/mod.rs`
- `A agent_core/tests/tri_fusion_json_round_trip.rs`

The `agent_core/src/tri_fusion/` module and integration test are within the T1 Tri-Fusion intent. `agent_core/src/lib.rs` remains outside the exact written touch list. The commit body did not explicitly acknowledge the T9-requested `lib.rs` scope exception. It also uses `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt.

Current verdict: committed scope exception remains unresolved until T1 documents the root-module registration exception or receives explicit lane-owner sign-off. Commit-message hygiene also needs correction before PR. T1's current dirty paths after the commit are inside the written T1 lane:
- `agent_core/src/research/hyperdynamic_schemas/mod.rs`
- `agent_core/src/research/hyperdynamic_schemas/document.rs`
- `agent_core/tests/tri_fusion_schema_validation.rs`

## Recheck - 2026-05-17 08:00 CDT

T1 committed and pushed `116257807 feat(tri-fusion): add path-aware document schema validation`.

`git show --name-status 116257807` reports:
- `A agent_core/src/research/hyperdynamic_schemas/document.rs`
- `M agent_core/src/research/hyperdynamic_schemas/mod.rs`
- `A agent_core/tests/tri_fusion_schema_validation.rs`

Those paths are inside T1's written lane. The commit extends Hyper-Dynamic Schemas with path-aware document validation and a Tri-Fusion schema validation test, and it reports T1-local validation at 1682 lib tests plus focused schema targets.

The earlier `e117ef855` scope exception remains unresolved, and `116257807` repeats the commit-footer hygiene issue: it uses `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt.

Current T1 dirty paths after the commit are in-lane:
- `agent_core/src/tri_fusion/mod.rs`
- `agent_core/tests/tri_fusion_mutations.rs`

Current verdict: latest committed slice is scope-clean, but T1 branch still needs the `agent_core/src/lib.rs` exception acknowledged and commit-footer hygiene corrected before PR / merge-readiness.

## Recheck - 2026-05-17 08:04 CDT

T1 committed and pushed `44936eb9e feat(tri-fusion): add mutation witness floor`.

`git show --name-status 44936eb9e` reports:
- `M agent_core/src/tri_fusion/mod.rs`
- `A agent_core/tests/tri_fusion_mutations.rs`

Those paths are inside T1's written lane. The commit adds typed Tri-Fusion mutations and deterministic witness metadata over the canonical JSON document floor.

The commit again uses `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt. Current T1 worktree is clean.

Current verdict: latest committed slice is scope-clean, but T1 branch still carries the earlier `agent_core/src/lib.rs` scope-exception issue and repeated coauthor-footer hygiene gap.

## Recheck - 2026-05-17 08:14 CDT

T1 committed and pushed `499130ad9 feat(tri-fusion): expose document handle over uniffi`.

`git show --name-status 499130ad9` reports:
- `M agent_core/src/bridge.rs`

That path is inside T1's written Tri-Fusion bridge lane. The commit adds an opaque Swift-facing `TriFusionDocumentHandle`, a parser entrypoint, and bridge tests for canonical JSON/hash behavior and malformed JSON handling.

Open issues remain:
- The earlier `e117ef855` `agent_core/src/lib.rs` scope exception still needs explicit rationale / sign-off.
- `499130ad9` again uses `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt.
- The T1 worktree now has dirty generated `syntax-core/target/**` artifacts in addition to in-lane `Epistemos/LocalAgent/LocalToolGrammar.swift`.

Current verdict: latest committed slice is scope-clean, but T1 is blocked before the next commit / PR until the generated artifacts are removed from the worktree and the earlier root-module exception / footer hygiene are resolved.

## Recheck - 2026-05-17 08:30 CDT

T1 committed and pushed two more Tri-Fusion slices:
- `c953fa00e feat(tri-fusion): add local mutation grammar`
- `d37833ca4 feat(tri-fusion): apply mutations through document handle`

`git show --name-status c953fa00e` reports:
- `M Epistemos/LocalAgent/LocalToolGrammar.swift`

`git show --name-status d37833ca4` reports:
- `M agent_core/src/bridge.rs`

Both committed paths are inside T1's written Tri-Fusion lane. The LocalToolGrammar commit adds the model-facing `epdoc.apply_tri_fusion_mutation` schema surface, and the bridge commit applies serialized mutations through the canonical Rust document handle.

Open issues remain:
- The earlier `e117ef855` `agent_core/src/lib.rs` scope exception still needs explicit rationale / sign-off.
- Both new commits repeat the prompt-footer mismatch: `Co-Authored-By: Codex (T1)` is missing the required email form.
- Current T1 worktree drift includes in-lane `agent_core/src/bridge.rs` and `agent_core/src/tri_fusion/mod.rs`, plus tracked generated `syntax-core/target/**` artifacts.

Current verdict: the latest committed slices are scope-clean, but T1 is not PR-ready until generated artifacts are cleaned and the root-module / footer hygiene issues are resolved.

## Recheck - 2026-05-17 08:37 CDT

T1 committed and pushed two more Tri-Fusion slices:
- `20e74e8eb feat(tri-fusion): validate mutation envelopes`
- `15321659d feat(tri-fusion): carry envelope metadata in witnesses`

Both commits touch:
- `M agent_core/src/bridge.rs`
- `M agent_core/src/tri_fusion/mod.rs`

Those paths are inside T1's written Tri-Fusion bridge / module lane. The commits add grammar-shaped mutation envelopes, stale base-hash rejection, and envelope metadata carried into witnesses.

Open issues remain:
- The earlier `e117ef855` `agent_core/src/lib.rs` scope exception still needs explicit rationale / sign-off.
- Both commits again use `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt.
- Current T1 worktree drift is generated `syntax-core/target/**` artifacts only at this check.

Current verdict: latest committed slices are scope-clean, but branch merge-readiness remains blocked by generated artifacts and unresolved footer/root-module debt.

## Recheck - 2026-05-17 08:44 CDT

T1 committed and pushed `ed1b8c058 feat(tri-fusion): mark deferred provenance in witnesses`.

`git show --name-status ed1b8c058` reports:
- `M agent_core/src/bridge.rs`
- `M agent_core/src/tri_fusion/mod.rs`

Those paths are inside T1's written Tri-Fusion bridge / module lane. The commit adds explicit deferred provenance status and reserves optional MutationEnvelope / ClaimGraph / Cognitive DAG linkage fields in `TriFusionWitness`.

Open issues remain:
- The earlier `e117ef855` `agent_core/src/lib.rs` scope exception still needs explicit rationale / sign-off.
- `ed1b8c058` again uses `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt.
- Current T1 worktree drift includes in-lane `Epistemos/LocalAgent/LocalToolGrammar.swift` plus generated `syntax-core/target/**` artifacts.

Current verdict: latest committed slice is scope-clean, but T1 is not commit/PR-ready until generated artifacts are excluded and the footer/root-module debt is resolved.

## Recheck - 2026-05-17 11:46 CDT

T1 pushed:
- `cd12d1ac7 test(tri-fusion): add cross format coherence proof`
- `58ddcd4d0 test(tri-fusion): expand cross format corpus`
- `a460f399f test(tri-fusion): expand html tree corpus`

Committed paths:
- `agent_core/tests/tri_fusion_cross_format.rs`
- `agent_core/tests/tri_fusion_html_round_trip.rs`

Both committed paths are inside T1's Tri-Fusion test lane. Current non-artifact drift is untracked `agent_core/tests/tri_fusion_corpus_report.rs`, which is mission-aligned but should be explicitly rationalized before PR if it lands. Generated `syntax-core/target/**` artifacts and the footer/root-module debt still carry.

## Recheck - 2026-05-17 10:08 CDT

T1 pushed `cefcbb89d` and `ff23b1c67`.

Committed path review:
- `agent_core/src/bridge.rs`
- `agent_core/src/tri_fusion/mod.rs`
- `agent_core/tests/tri_fusion_provenance.rs`

These paths are inside T1's Tri-Fusion / bridge / `tests/tri_fusion_*.rs` scope. No committed cross-lane path is present. The remaining live drift is generated `syntax-core/target/**` artifact churn; footer hygiene and the earlier `agent_core/src/lib.rs` exception remain open.

## Recheck - 2026-05-17 10:18 CDT

No new T1 commit after `ff23b1c67`.

Current non-artifact live drift:
- `Epistemos/Engine/EpdocEditorBridge.swift`
- `EpistemosTests/EpdocEditorBridgeTests.swift`

`EpdocEditorBridge.swift` is in T1's Epdoc receiver lane. `EpistemosTests/EpdocEditorBridgeTests.swift` is outside the exact written `tests/tri_fusion_*.rs` test lane and should not be committed without rationale / sign-off. Generated artifacts remain dirty.

## Recheck - 2026-05-17 09:38 CDT

T1 committed and pushed two in-lane corpus slices:
- `c035afd25 test(tri-fusion): double deterministic json corpus`
- `327a2a42c test(tri-fusion): reach json corpus floor`

`git show --name-status` reports only:
- `M agent_core/tests/tri_fusion_json_corpus.rs`

Those commits are inside T1's Tri-Fusion Rust integration-test / corpus lane. They move the deterministic JSON round-trip corpus from 64 -> 128 -> 200 generated document cases and meet the current JSON-side corpus floor.

New live drift is severe and must not be committed as-is:
- `git diff --shortstat` reports `229 files changed, 4644 insertions(+), 1727 deletions(-)`.
- Non-artifact dirty paths span broad `agent_core/src/**` modules outside T1, including `agent_core/src/storage/vault.rs`, `agent_core/src/research/eml/**`, `agent_core/src/research/acs/**`, `agent_core/src/tools/**`, and `agent_core/src/lib.rs`.
- There is also untracked in-lane `agent_core/tests/tri_fusion_provenance.rs`.
- Generated `syntax-core/target/**` artifacts remain dirty.

Open issues remain:
- Repeated T1 footer mismatch: `Co-Authored-By: Codex (T1)` lacks the prompt-required email form.
- The earlier `agent_core/src/lib.rs` exception remains unresolved, and the current broad live `lib.rs` reorder makes that risk worse.
- The broad non-artifact WIP is outside T1's scope lock and is a pre-commit blocker until narrowed or explicitly rehomed.

Current verdict: `c035afd25` and `327a2a42c` are scope-clean, but T1's current worktree is not commit/PR-ready.

## Late Recheck - 2026-05-17 09:49 CDT

T1 then committed two more Tri-Fusion provenance slices:
- `ec1544b1b feat(tri-fusion): derive pending mutation envelopes`
- `2b91f561f feat(tri-fusion): commit witnesses to claim ledger`

`git show --name-status` reports:
- `M agent_core/src/tri_fusion/mod.rs`
- `A/M agent_core/tests/tri_fusion_provenance.rs`

Those paths are in T1's Tri-Fusion module / test lane. The earlier broad 229-file live drift is no longer present in the T1 worktree; current non-artifact drift is back to the in-lane Tri-Fusion module and provenance test files.

Open issues remain:
- T1 commits still use `Co-Authored-By: Codex (T1)` without the prompt-required email.
- Generated `syntax-core/target/**` artifacts remain dirty.
- The historical `agent_core/src/lib.rs` exception still needs explicit sign-off.

Current verdict: latest committed T1 slices are scope-clean; T1 still needs footer, artifact, and root-module exception cleanup before PR.

## Final Recheck - 2026-05-17 09:56 CDT

T1 committed two additional Tri-Fusion DAG provenance slices:
- `f292e3709 feat(tri-fusion): derive cognitive dag provenance ids`
- `28eca4075 feat(tri-fusion): verify cognitive dag provenance`

`git show --name-status` reports:
- `M agent_core/src/tri_fusion/mod.rs`
- `M agent_core/tests/tri_fusion_provenance.rs`

Both paths remain inside T1's Tri-Fusion module / test lane.

Current live non-artifact drift is only:
- `agent_core/src/tri_fusion/mod.rs`

Current verdict: latest T1 commits are scope-clean. T1 still carries repeated footer mismatch, generated artifacts, in-lane WIP, and the historical `agent_core/src/lib.rs` exception.

## Recheck - 2026-05-17 11:22 CDT

T1 committed and pushed:
- `e304b6034 feat(tri-fusion): add canonical markdown projection`
- `9d3dff13c test(tri-fusion): expand markdown corpus fixtures`
- `2d1dbe9fb feat(tri-fusion): expose markdown document ffi`

Committed path review:
- `agent_core/src/tri_fusion/markdown.rs`
- `agent_core/src/tri_fusion/mod.rs`
- `agent_core/src/bridge.rs`
- `agent_core/tests/tri_fusion_ffi.rs`
- `agent_core/tests/tri_fusion_markdown_round_trip.rs`

The Tri-Fusion module, bridge, and FFI test paths are in T1's written lane. `agent_core/tests/tri_fusion_markdown_round_trip.rs` is conceptually aligned with the Tri-Fusion round-trip suite but still needs the same exact-path rationale note before PR because the scope prompt shorthand did not spell out the `agent_core/tests/` prefix.

Current live non-artifact drift is only `agent_core/src/tri_fusion/mod.rs`, which is in-lane. Generated `syntax-core/target/**` artifacts and footer mismatch still carry, as does the historical `agent_core/src/lib.rs` exception.

Current verdict: latest Markdown projection / FFI commits are mission-aligned, with exact test-path rationale and existing hygiene debt still open.

## Recheck - 2026-05-17 10:00 CDT

No new T1 commit after `28eca4075`.

Current live non-artifact drift:
- `agent_core/src/bridge.rs`
- `agent_core/src/tri_fusion/mod.rs`

Both paths are inside T1's bridge / Tri-Fusion lanes. Generated artifacts remain dirty.

Current verdict: no new committed drift; live WIP is in-lane, with footer/artifact/root-module debt still open.

## Recheck - 2026-05-17 09:04 CDT

T1 HEAD remains pushed `bf08a43dd feat(tri-fusion): pin mutation envelope actor contract`; no new commit landed during the iter 17 main baseline.

Current worktree drift now reports:
- `Epistemos/Engine/EpdocEditorBridge.swift`
- generated `syntax-core/target/**` artifacts

`Epistemos/Engine/Epdoc*.swift` is explicitly inside T1's written structured-mutation receiver lane, so the live Swift bridge work is in-lane. The generated `syntax-core/target/**` files remain outside scope.

Open issues remain:
- The earlier `e117ef855` `agent_core/src/lib.rs` scope exception still needs explicit rationale / sign-off.
- T1 commits continue using `Co-Authored-By: Codex (T1)` without the required email form.
- Generated artifacts must be excluded before the next T1 commit / PR.

Current verdict: live implementation work appears in-lane, but T1 is not commit/PR-ready until generated artifacts are excluded and footer/root-module debt is resolved.

## Recheck - 2026-05-17 09:12 CDT

T1 remains at pushed `bf08a43dd` with no new commit. Non-artifact live drift is still:
- `Epistemos/Engine/EpdocEditorBridge.swift`

This remains in T1's explicit `Epistemos/Engine/Epdoc*.swift` structured-mutation receiver lane. Generated `syntax-core/target/**` artifacts remain dirty and outside scope.

Current verdict: no new committed drift; artifact cleanup is still required before T1's next commit / PR.

## Recheck - 2026-05-17 09:17 CDT

T1 committed `6b7e5f46b feat(tri-fusion): add epdoc mutation receiver gate`.

`git show --name-status 6b7e5f46b` reports:
- `M Epistemos/Engine/EpdocEditorBridge.swift`

That path is inside T1's explicit `Epistemos/Engine/Epdoc*.swift` structured-mutation receiver lane. The commit adds Swift-side envelope validation and a canonical `applyTriFusionMutation` command rather than full-document replacement.

Open issues remain:
- The commit again uses `Co-Authored-By: Codex (T1)` without the required email form.
- Current non-artifact live drift includes untracked `agent_core/tests/tri_fusion_json_corpus.rs`, which appears aligned with T1's Tri-Fusion corpus-test lane but should be reviewed before commit.
- Generated `syntax-core/target/**` artifacts remain dirty and outside scope.

Current verdict: `6b7e5f46b` is scope-clean, but footer hygiene and artifact cleanup remain open.

## Recheck - 2026-05-17 09:22 CDT

T1 committed `fc9efe18d test(tri-fusion): grow deterministic json corpus`.

`git show --name-status fc9efe18d` reports:
- `A agent_core/tests/tri_fusion_json_corpus.rs`

The path is in the established T1 Rust integration-test lane for Tri-Fusion corpus coverage. The commit adds 64 deterministic ProseMirror-shaped JSON documents and asserts canonical JSON reparse/hash/version preservation.

Current non-artifact live drift:
- `Epistemos/Engine/EpdocEditorBridge.swift`

Open issues remain:
- T1 commits continue using `Co-Authored-By: Codex (T1)` without the prompt-required email.
- Current live Epdoc bridge work is in-lane but uncommitted after `fc9efe18d`.
- Generated `syntax-core/target/**` artifacts remain dirty and outside scope.

Current verdict: `fc9efe18d` is scope-clean, but T1 still needs footer hygiene and artifact cleanup before PR.

## Recheck - 2026-05-17 09:31 CDT

T1 committed and pushed `b2b6c1ec4 feat(tri-fusion): add epdoc mutation js fallback`.

`git show --name-status b2b6c1ec4` reports:
- `M Epistemos/Engine/EpdocEditorBridge.swift`

That path is inside T1's `Epistemos/Engine/Epdoc*.swift` structured-mutation receiver lane. The commit extends the Epdoc receiver with a Swift-generated JS fallback for `applyTriFusionMutation` and marks model-authored touched blocks.

Current non-artifact live drift is clean after the commit. Generated `syntax-core/target/**` artifacts remain dirty and outside scope.

Current verdict: `b2b6c1ec4` is scope-clean. T1 still needs footer hygiene and artifact cleanup before PR.

## Recheck - 2026-05-17 08:57 CDT

T1 committed and pushed `bf08a43dd feat(tri-fusion): pin mutation envelope actor contract`.

`git show --name-status bf08a43dd` reports:
- `M agent_core/src/tri_fusion/mod.rs`
- `A agent_core/tests/tri_fusion_envelopes.rs`

Those paths are inside T1's written Tri-Fusion module / test lane. The commit adds integration coverage for mutation envelopes, stale base-hash rejection, and actor decoding rules.

Open issues remain:
- The earlier `e117ef855` `agent_core/src/lib.rs` scope exception still needs explicit rationale / sign-off.
- `bf08a43dd` again uses `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt.
- Current T1 worktree drift is generated `syntax-core/target/**` artifacts only.

Current verdict: latest committed slice is scope-clean, but T1 is not PR-ready until generated artifacts are excluded and the footer/root-module debt is resolved.

## Recheck - 2026-05-17 10:28 CDT

T1 committed and pushed `7a00d7400 fix(epdoc): require tri-fusion rationale`.

Committed path review:
- `Epistemos/Engine/EpdocEditorBridge.swift`
- `EpistemosTests/EpdocEditorBridgeTests.swift`

The Epdoc bridge path is inside T1's structured-mutation receiver lane. The Swift integration test is outside T1's exact written `tests/tri_fusion_*.rs` lane; T9 had flagged this as a pre-commit scope item in Iter 25, and it has now landed. Treat it as committed exact-scope debt requiring explicit rationale / sign-off before PR.

Open issues remain:
- Latest footer still omits the prompt-required email: `Co-Authored-By: Codex (T1)`.
- Current T1 worktree drift is generated `syntax-core/target/**` artifacts only.
- Historical `agent_core/src/lib.rs` exception still needs PR-level sign-off.

Current verdict: product-relevant Epdoc hardening, but T1 now carries a committed Swift-test exact-scope issue in addition to artifact, footer, and root-module debt.

## Recheck - 2026-05-17 10:38 CDT

T1 committed and pushed `06b7822d5 feat(tri-fusion): commit mutation provenance over ffi`.

Committed paths:
- `agent_core/src/bridge.rs`
- `agent_core/tests/tri_fusion_ffi.rs`

Both committed paths are inside T1's FFI / `tests/tri_fusion_*.rs` lanes. Current live non-artifact drift now includes:
- `Epistemos/Engine/RustTriFusionDocumentClient.swift`
- `EpistemosTests/RustTriFusionDocumentClientTests.swift`

The live Swift client is not an `Epdoc*.swift` receiver file, and the Swift test is outside the exact Rust `tests/tri_fusion_*.rs` lane. Treat both as pre-commit blockers unless T1 records coordination / sign-off.

Open issues remain:
- Footer still omits the prompt-required email form.
- Generated `syntax-core/target/**` artifacts remain dirty.
- Historical `agent_core/src/lib.rs` exception still needs PR-level sign-off.

Current verdict: committed Rust FFI work is clean; live Swift FFI client/test work needs explicit scope resolution before commit.

## Recheck - 2026-05-17 10:49 CDT

T1 committed and pushed `c27f92572 test(tri-fusion): add swift ffi document round trip`.

Committed paths:
- `Epistemos/Engine/RustTriFusionDocumentClient.swift`
- `EpistemosTests/RustTriFusionDocumentClientTests.swift`

These are the same paths T9 flagged as live out-of-lane at 10:38. They are product-relevant to the Tri-Fusion FFI seam, but the Swift client is not an `Epdoc*.swift` receiver file and the Swift test is outside the exact Rust `tests/tri_fusion_*.rs` lane.

Open issues remain:
- This is now committed scope debt requiring explicit coordination / rationale before PR.
- Footer still omits the prompt-required email form.
- Generated `syntax-core/target/**` artifacts remain dirty.
- Historical `agent_core/src/lib.rs` exception still needs PR-level sign-off.

Current verdict: T1 has useful Swift FFI coverage, but it landed outside the exact written lane after pre-commit warning.

## Recheck - 2026-05-17 10:58 CDT

T1 committed and pushed `83b4ea215 feat(tri-fusion): expose swift provenance mutation path`.

Committed paths:
- `Epistemos/Engine/RustTriFusionDocumentClient.swift`
- `EpistemosTests/RustTriFusionDocumentClientTests.swift`

This extends the same Swift FFI client/test scope debt recorded for `c27f92572`.

Current live non-artifact drift:
- `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `EpistemosTests/HermesPromptBuilderTests.swift`

The live prompt guidance is useful for Tri-Fusion tool usage, but `LocalAgentPromptBuilder.swift` is outside T1's exact listed Swift lanes and the Swift prompt-builder test is outside the Rust `tests/tri_fusion_*.rs` lane.

Current verdict: committed Swift FFI exact-scope debt widened, and the live prompt-guidance WIP needs coordination before commit.

## Recheck - 2026-05-17 11:10 CDT

T1 committed and pushed `db5e03e7e feat(tri-fusion): prompt epdoc mutation calls`.

Committed paths:
- `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `EpistemosTests/HermesPromptBuilderTests.swift`

This is the same prompt-guidance work T9 flagged live at 10:58. It is useful for Tri-Fusion tool use, but `LocalAgentPromptBuilder.swift` is outside T1's exact listed Swift lanes and the Swift prompt-builder test repeats exact-scope debt.

Current live non-artifact drift:
- `agent_core/src/tri_fusion/mod.rs`
- untracked `agent_core/src/tri_fusion/markdown.rs`
- untracked `agent_core/tests/tri_fusion_markdown_round_trip.rs`

The live Rust module and markdown file are in the `agent_core/src/tri_fusion/` lane. The untracked test needs an exact-path rationale because T1's prompt names `tests/tri_fusion_*.rs`, not `agent_core/tests/...`.

Current verdict: committed prompt-builder scope debt is now real; live Rust markdown WIP is mostly in-lane but should carry a test-path rationale before commit / PR.

## Recheck - 2026-05-17 08:51 CDT

T1 committed and pushed `ccef1c9ab feat(tri-fusion): require run id for agent grammar actors`.

`git show --name-status ccef1c9ab` reports:
- `M Epistemos/LocalAgent/LocalToolGrammar.swift`

That path is inside T1's written Tri-Fusion mutation grammar lane. The commit aligns the Swift grammar with the Rust mutation-envelope actor contract by requiring `run_id` for agent-authored mutations while keeping user/system actors closed.

Open issues remain:
- The earlier `e117ef855` `agent_core/src/lib.rs` scope exception still needs explicit rationale / sign-off.
- `ccef1c9ab` again uses `Co-Authored-By: Codex (T1)` without the email form required by the T1 prompt.
- Current T1 worktree drift includes untracked in-lane `agent_core/tests/tri_fusion_envelopes.rs` plus generated `syntax-core/target/**` artifacts.

Current verdict: latest committed slice is scope-clean, but T1 is not commit/PR-ready until generated artifacts are excluded and the footer/root-module debt is resolved.

## Recheck - 2026-05-17 11:59 CDT

T1 pushed `f51cd6bc3` and `f476b9d1c`, adding `agent_core/tests/tri_fusion_corpus_report.rs` and `agent_core/tests/tri_fusion_mutation_corpus.rs`. The files are Tri-Fusion test/corpus work and mission-aligned, with only exact-path rationale debt.

Current non-artifact drift is back in `Epistemos/Engine/RustTriFusionDocumentClient.swift` and `EpistemosTests/RustTriFusionDocumentClientTests.swift`, the same Swift FFI scope-debt paths recorded earlier. Generated artifacts remain dirty.

## Recheck - 2026-05-17 12:10 CDT

T1 pushed `4fdd9f63c test(tri-fusion): cover swift markdown html ffi round trips`.

Committed paths:
- `Epistemos/Engine/RustTriFusionDocumentClient.swift`
- `EpistemosTests/RustTriFusionDocumentClientTests.swift`

This widens the same Swift FFI coordination debt recorded for `c27f92572` and `83b4ea215`. The work is Tri-Fusion relevant, but the Swift client and Swift test remain outside T1's exact written `Epdoc*.swift` / Rust `tests/tri_fusion_*.rs` lanes unless coordination/rationale is recorded.

Current live drift:
- `Epistemos/Engine/EpdocEditorBridge.swift`
- `EpistemosTests/EpdocEditorBridgeTests.swift`
- generated `syntax-core/target/**` artifacts

The Epdoc bridge path is in-lane; the Swift test path remains exact-scope debt. T1 should not open PR without addressing Swift FFI/test rationale, footer hygiene, generated artifacts, and the historical `agent_core/src/lib.rs` exception.

## Recheck - 2026-05-17 12:24 CDT

T1 pushed `19daab802 fix(tri-fusion): gate epdoc model-authored highlights`.

Committed paths:
- `Epistemos/Engine/EpdocEditorBridge.swift`
- `EpistemosTests/EpdocEditorBridgeTests.swift`

The Epdoc bridge file is in T1's written structured-mutation receiver lane. The Swift test repeats the exact-scope debt because T1's prompt names Rust `tests/tri_fusion_*.rs` rather than Swift test files.

Current live drift:
- `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `EpistemosTests/HermesPromptBuilderTests.swift`
- `EpistemosTests/LocalToolGrammarTests.swift`
- generated `syntax-core/target/**` artifacts

`LocalAgentPromptBuilder.swift` / `HermesPromptBuilderTests.swift` carry the prior prompt-builder coordination debt, and `LocalToolGrammarTests.swift` is another Swift-test rationale item. Footer mismatch and the historical `agent_core/src/lib.rs` exception still need PR-level handling.

## Recheck - 2026-05-17 12:37 CDT

T1 pushed:
- `a9ac16c04 test(tri-fusion): harden local agent mutation prompt`
- `739ed2643 test(tri-fusion): prove provenance mirror commit`

Committed paths:
- `agent_core/src/tri_fusion/mod.rs`
- `Epistemos/LocalAgent/LocalAgentPromptBuilder.swift`
- `EpistemosTests/HermesPromptBuilderTests.swift`
- `EpistemosTests/LocalToolGrammarTests.swift`

The Tri-Fusion Rust path is in-lane. The prompt-builder file and Hermes prompt-builder test are the same coordination debt recorded earlier; `LocalToolGrammarTests.swift` adds another Swift-test exact-scope rationale item. Current non-artifact drift is clear, but generated `syntax-core/target/**` artifacts remain dirty.

## Recheck - 2026-05-17 12:50 CDT

T1 pushed:
- `d71e19d20 test(tri-fusion): prove ffi provenance commit`
- `1be496d8f test(tri-fusion): harden swift provenance witness`
- `59ded876b test(tri-fusion): add round-trip corpus shards`

Committed paths:
- `agent_core/src/bridge.rs`
- `EpistemosTests/RustTriFusionDocumentClientTests.swift`
- `agent_core/src/tri_fusion/mod.rs`

`bridge.rs` and `tri_fusion/mod.rs` are in T1's FFI/Tri-Fusion lanes. The Swift test remains exact-scope rationale debt. Current non-artifact drift is clear; generated artifacts remain dirty.
