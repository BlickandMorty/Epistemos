# F-KVRuntimeSourceCard - 2026-06-06

Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as a metadata-only T1/L1 primary witness on 2026-06-06.

- Command: `Tools/falsifiers/f_kv_runtime_source_card.sh`
- Artifact: `artifacts/falsifiers/kv_runtime_source_card/result.json`
- Primitive: `agent_core/src/uas/kv_runtime_source_card.rs`
- Falsifier binary: `agent_core/src/bin/falsify_kv_runtime_source_card.rs`
- Scope: Pro ResearchCandidate / L1 only. Product capability, user-facing runtime, live dense 70B, live KV cache, prompt-cache file opening, server/daemon execution, and MAS promotion remain unproved.

## What It Proves

`F-KVRuntimeSourceCard` turns Pass 61 large-local-model runtime research into typed source cards before any KV/page/offload/cache motif can affect RuntimeRouter/System G.

Accepted source cards cover:

- vLLM paged attention / virtual block table
- LMCache reusable KV / daemon cache
- SGLang HiCache/radix cache hierarchy
- KTransformers heterogeneous placement
- FlexGen offload policy optimization
- PowerInfer activation locality
- KIVI asymmetric KV quantization
- Hugging Face Transformers quantized cache
- llama.cpp prompt-cache file behavior

The witness binds each card to a `SourceSignalGraph` source digest, an upstream `F-CompressedModelSourceCard-Intake` ref, cache identity fields, compatibility fields, byte-ledger fields, cache policy fields, quality caveat, MAS/Pro boundary, rollback, RunEventLog, and AnswerPacket refs.

## Hard Rejections

The artifact rejects 38 red fixtures, including duplicate/unknown/blocked sources, digest drift, missing cache identity, missing compatibility, missing byte ledger, missing cache policy, bad proof refs, server-as-product, daemon-as-product, remote-storage-as-local, prompt-cache compatibility gaps, missing KV quantization residual caveats, missing offload latency/throughput boundaries, missing activation-locality fallback, unsupported Apple promotion, MAS server/daemon leakage, hidden route/cache authority, model/KV/index/runtime/provider/source-tree/product bytes, live dense-70B claims, SSD-as-RAM claims, product promotion from research, missing L1/L2/L3 separation, and metadata budget overflow.

## Non-Claims

This is not a runtime proof. It does not run vLLM, LMCache, SGLang, KTransformers, FlexGen, PowerInfer, KIVI, Transformers, llama.cpp, MLX, GGUF, LiteRT, or Gemma 4. It does not open model, KV, prompt-cache, source-tree, index, or runtime bytes. It does not prove quality, latency, memory fit, Apple Silicon support, MAS support, product wiring, WRV, or user-facing large-model capability.

Correct phrasing: architecture cursor/source-card coverage advanced; product capability and user-facing runtime did not.

## Next Unit

`kv_source_card_fork_and_daemon_boundary` should split server/daemon/distributed motifs from product-eligible in-process/command motifs before any fork quarantine, daemon bridge, or local command harness can cite these cards.
