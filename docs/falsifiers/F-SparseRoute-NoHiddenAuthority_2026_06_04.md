# F-SparseRoute-NoHiddenAuthority - 2026-06-04

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Result

- Status: PASS, metadata-only primary witness.
- Command: `Tools/falsifiers/f_sparse_route_no_hidden_authority.sh`
- Artifact: `artifacts/falsifiers/sparse_route_no_hidden_authority/result.json`
- UAS address: `uas:sparse-route-no-hidden-authority:sha256:9f3e1c123edb7122f089bb77373ba73267e99c6ac5825f1ee55ff68ba7890710`
- L1 downstream cursor at landing: `F-ColdStream-NoHiddenAuthority`; current cursor after downstream `F-TransportCancellation` is `cache_policy_pollution`
- L2 status: `vault_research_route_with_packetized_mitigation`
- L2 next bottleneck after downstream `F-TransportCancellation`: `cache_policy_pollution`
- L3 user-facing/product runtime: unchanged; no live sparse route, no live 70B, and no user-visible route promotion is claimed.

## What Passed

The witness proves a sparse-route compiler fixture can bind source priors, proof traces, oracle labels, PatternBoost motifs, fast-weight deltas, scout proposals, sparse wake certificates, selected UAS units, budgets, SCOPE-Rex/SovereignGate admission, rollback, RunEventLog, AnswerPacket, privacy class, held-out split, and compatibility fence while keeping all authority visible and proposal-only.

It covers 2 fixtures, 6 route records, 5 route families, 5 evidence classes, 4 held-out cases, 1 abstention case, and `9180` bps route success. It beats hidden-router, policy-mutation, oracle-laundering, and no-AnswerPacket baselines, loads zero runtime/model bytes, and remains Pro Research / metadata-only evidence.

## Hardening

Invalid fixtures reject empty fixtures, duplicate fixture IDs, duplicate route IDs, missing mission/task/route/evidence/governance fields, invalid UAS selected units, incompatible fences, invalid privacy, live route authority, source-prior/proof/oracle/PatternBoost authority, unquarantined fast weights, byte wake without lease, policy/base-weight/fast-weight/cache mutation, SCOPE-Rex/SovereignGate override, AnswerPacket suppression, hidden chain, hidden cloud, runtime/model bytes, high-uncertainty non-abstention, low verifier margin, missing diversity, low proof/admission/route metrics, unbeaten baselines, and metadata overflow.

## Truth Layers

L1 advanced at landing. The architecture cursor moved from `F-SparseRoute-NoHiddenAuthority` to `F-ColdStream-NoHiddenAuthority`; downstream ColdStream, large-model deferral, ProviderRoute copy-source guard, TransportTrace, SSD wear-budget, ColdStream-vs-mmap, SlabArena copy-count, Metal I/O feature-gate, CodecStage latency, TransportCancellation, and CachePolicy witnesses now pass metadata-only and the current cursor is `small_model_runtime_harness_first_token_runtime_probe` after downstream `F-SmallModelRuntimeHarnessLoggedRuntimeSmoke`.

L2 did not advance to product-green. The capability kernel still reports `overall_pass=false`, route status `vault_research_route_with_packetized_mitigation`, and next bottleneck `cache_policy_pollution`.

L3 did not advance. This witness does not make sparse routing live, does not promote PatternBoost to live route authority, does not enable a dense/local 70B product claim, and does not change MAS/Pro user-facing capability copy.
