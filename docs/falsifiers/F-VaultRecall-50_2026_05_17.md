# F-VaultRecall-50 Current Run - 2026-05-17

Iteration: T4 iter 102, after Rust path-title recall lane, original-note distractor suppression, synthesis-side title seeding, MMR rerank trace, recency decay, user-priority boost, graph-proximity signal, Rust support-only reason rejection, Rust unknown-metadata reason rejection, RRF reason labels, RRF source-rank-only escalation reason, RRF no-visible-evidence reason, RRF visible-reason whitelist tightening, prompt source-rank-only/no-visible-evidence reason guard, note-chat provenance cards, note-chat provenance signal badges, note-chat phrase-evidence guard, note-chat provenance ambiguity badges, note-chat provenance escalation badge, note-chat stale-schema escalation badge, note-chat contract-shape escalation badge, note-chat provenance badge priority, note-chat multi-line provenance reasons, note-chat exact-reason priority, indexed fallback confidence threshold, indexed fallback explicit provenance blocks, indexed fallback single-top margin rejection, indexed fallback low-margin multi-hit labeling, no arbitrary recent-note body fallback, empty mention no recent-note enumeration, source guard against order-only note substitution, parseable and deduped fallback provenance cards, prompt evidence threshold, fused confidence trace counts, fused display titles, fused visible-surface requirement, fused recency-only evidence rejection, fused exact-escalation metadata, fused exact-escalation payload counts, fused exact-escalation query-count limit, fused exact-target count overflow guard, fused payload contract-shape flag, fused exact-escalation metric counts, fused exact-escalation metric gating, fused metrics no-visible-evidence reason guard, fused exact-escalation metrics, fused exact-escalation error clearing, fused exact-escalation trace schema/caps, fused metrics schema/caps, fused metrics contract-shape checks, fused contract count bounds, fused cap constants, fused exact-escalation target hints, fused exact-escalation target normalization, fused exact-escalation target match keys, fused exact-escalation query hints, fused exact-query hint normalization, fused raw-query exact hints, fused exact-escalation prompt guard, fused exact-query prompt guard, prompt stale-schema guard, prompt contract-shape flag guard, prompt schema/cap guard, prompt candidate-not-evidence guard, prompt exact-match verification guard, Rust confidence count snapshots, Rust selected-context confidence count snapshots, metrics snapshot confidence counters, Swift RRF top-score margin tracing, explicit `Vault provenance:` block handling, Swift and Rust rank-only/support-only evidence rejection, Rust synthesis distinct-note validation, Rust selected-count consistency, Rust selected low-score rejection, Rust adversarial margin validation, insufficient-evidence fallback messaging, Shadow-first exact-escalation violation mapping, Shadow-first trace envelopes, Shadow-first answerability summaries, Shadow-first answerability schema/caps, Shadow-first answerability contract-shape checks, Shadow-first contract count bounds, Shadow-first exact-query count limit, Shadow-first unavailable-escalation request gating, Shadow-first post-verification margin preservation, Shadow-first residual decode request payloads, Shadow-first residual decode outcomes, Shadow-first residual visible-summary gating, Shadow-first residual exact-verification bridge, Shadow-first residual exact-query handoff, Shadow-first top-score margin tracing, Shadow-first exact-escalation request payloads, Shadow-first exact verification outcomes, Shadow-first exact-verification answerability summaries, Shadow-first exact-verification answerability bridge, Shadow-first normalized exact-hit identity matching, Shadow-first citable exact-hit gating, Shadow-first visible exact-hit selection, Shadow-first trace-to-verification bridge, Shadow-first snippet hints, Shadow-first bounded target snippets, Shadow-first bounded target lists, Shadow-first bounded snippet exact-query candidates, Shadow-first exact-query normalization, case-insensitive exact-query dedupe, and low-margin prompt guards.

Command:

```bash
cargo test --manifest-path agent_core/Cargo.toml --test vault_recall_baseline -- --ignored --nocapture
```

Dataset manifest hash: `6272a00acfcb321f6bb5aa195603bb7498bce00a89870063f5b27f0b0ca077c8`

| Condition | Current | Pass bar | Status |
| --- | ---: | ---: | --- |
| Top-1 exact-title recall | 10/10 (100.0%) | 95% | PASS |
| Top-5 paraphrase recall | 9/10 (90.0%) | 90% | PASS |
| Agent context recall proxy | 48/50 (96.0%) | 90% | PASS |
| Zero first-7 enumeration failures | 0 observed | 0 | PASS |
| UI shows why notes were selected | Swift fallback per-hit reasons + explicit answer blocks + RRF provenance + explicit block parser tests passing | visible for loaded notes | PASS |
| Synthesis cites >=2 distinct notes | 10/10 (100.0%) | 100% | PASS |
| Adversarial rejects distractor in top-5 | 10/10 (100.0%) | 85% | PASS |

Retrieval pass bars are now green on the F-VaultRecall-50 baseline, and the user-facing provenance surface is covered by focused Swift tests:

- `EpistemosTests/F_VaultRecall_50_RRFFusionTests.swift` verifies fused RRF results carry renderable match reasons and display titles where available, fused completion payloads and metrics snapshots expose confidence counts and exact-escalation metadata, payload counts, metric counts, metrics ignore nonzero exact-escalation counts when escalation is not actually required, metrics reject target-count and query-count overflow as stale contract shape, metrics reject recency-only/no-visible-reason top hits with `top_hit_no_visible_evidence_reason`, error snapshots clear stale exact-escalation state, low-confidence fused results fail the contract, fused rows without a visible title/title-path or snippet fail the contract, recency-only fused rows are not contract-sufficient and emit `top_hit_no_visible_evidence_reason`, broad reason substrings such as paraphrase metadata do not count as visible evidence, low-margin fused evidence reports exact-escalation reasons, fused completion payloads expose bounded exact-escalation target hints, and source-rank-only fused results are not contract-sufficient and emit a distinct `top_hit_source_rank_only` escalation reason.
- `EpistemosTests/F_VaultRecall_50_FallbackTests.swift` verifies indexed fallback answers emit per-hit `Why:` reasons plus a compact, parseable `Vault provenance:` block, source-rank-only matches are rejected with explicit insufficient-evidence messaging, single-result fallback rejects low top-score margin instead of silently promoting one note, multi-hit fallback labels low top-score margin as ambiguous candidate context, All Notes/manifest context does not load arbitrary recent note bodies when search misses, empty note mention suggestions do not enumerate recent note entries, chat fallback helpers do not reintroduce manifest-order or recent-body substitution in source, note chat sidebar provenance parsing accepts explicit provenance blocks, repeated title/path provenance entries dedupe into one card with combined reasons, multiple `Why:` lines under one citation merge without duplicate reasons, exact-verification/title/snippet reasons are displayed before weaker source-rank reasons, note chat provenance cards derive compact Exact/Lexical/Semantic/Graph/Recency/Confidence/Ambiguous signal badges from reasons, avoid broad `phrase` substring matching when classifying Lexical evidence, surface escalation reasons as an Escalate badge, and prioritize stronger evidence badges ahead of source-rank badges, and prompt contracts reject low-confidence/source-rank-only/no-visible-evidence/low-margin/exact-escalation-required synthesis claims, including the machine-readable `top_hit_source_rank_only` and `top_hit_no_visible_evidence_reason` reasons, unless exact verification returns a hit matching the escalated target or bounded exact query.

Swift metrics snapshots now expose `usesCurrentContractShape` so fused consumers can reject stale schema, cap metadata, or query-count overflow before trusting a snapshot. `SearchIndexService` now emits fused payload and metadata schema/cap fields from the same `SearchFusionMetrics` constants used by the metrics snapshot, including the exact-query count limit and an explicit `uses_current_contract_shape` flag.

Machine-readable tracing and rerank quality are now present in the Rust VaultStore path: candidate-pool trace fields, selected-count consistency, MMR decisions, recency decay, user priority, graph proximity, confidence bands/counts, and validation violations. Rust `VaultCandidateTrace` validation rejects selected candidates whose only visible reason is rank position, support-only boost metadata, or unknown paraphrase/resonance metadata, `VaultContextTrace::validate` rejects traces whose selected candidates have low fused scores, `VaultContextTrace::selected_confidence_counts` separates selected-context confidence from full-pool confidence, `VaultContextTrace::validate_synthesis_min_distinct_notes` reports `SynthesisUnderCited` when synthesis context has fewer than the required distinct selected notes, and `VaultContextTrace::validate_adversarial_margin` reports `AdversarialConfusion` when the top fused score is too close to its runner-up. Swift fused search completion metadata, Rust `VaultConfidenceCounts`, and SearchFusionMetrics snapshots now report aggregate contract-sufficient, high-confidence, medium-confidence, low-confidence, top-score margin, exact-escalation required/reason fields, and exact-escalation schema/cap fields so callers can refuse weak or rank-only evidence before prompt construction; Swift fused-search completion payloads, metadata, and metrics snapshots expose `top_score_margin` so adversarial ambiguity is observable without re-sorting results. Swift fused-search completion payloads now emit bounded, case-insensitive-deduped `exact_escalation_queries` from raw user query plus title/path/snippet evidence whenever exact escalation is required, stripping FTS highlight markup and ellipsis noise before bounding, with metadata carrying the query count and contract schema/cap fields, and exact-escalation targets normalize display title/snippet text before capping snippets at 240 chars and include normalized `match_keys` from display title, parent document ID, and entity ID so residual verification callers do not have to reconstruct the query set from target hints or sanitized MATCH syntax. Chat and local-agent prompt contracts now name low top-score margin as ambiguous vault retrieval evidence, explicitly treat `top_hit_source_rank_only` and `top_hit_no_visible_evidence_reason` as ungrounded until exact verification returns visible evidence, require bounded `exact_escalation_queries` as exact verification candidates when provided, preserve emitted `vault_context_contract_schema` and exact-escalation cap fields without expanding target/query/snippet bounds, require verified hits to match the escalated target title/path or bounded query before citation, explicitly forbid answering from exact-escalation targets or query candidates themselves, and otherwise require broadening, asking, or returning insufficient evidence rather than silently choosing the top hit. `agent_core/src/retrieval/` now also carries the Shadow-first answerability contract: RRF/lexical Shadow hits with visible evidence and sufficient margin can proceed, `ShadowFirstTrace::top_score_margin` reports the same ordered top-two gap used by the answerability decision, `ShadowFirstTrace::answerability_summary` emits a compact serializable summary with answerability, escalation state, reasons, violations, contract schema, bounded target/query/snippet/residual caps, candidate count, visible evidence count, exact target/query counts, confidence, and margin, `ShadowAnswerabilitySummary::uses_current_contract_shape` lets consumers reject stale schema or cap fields, `ShadowFirstTrace::answerability_summary_after_exact_verification` and `ShadowExactVerificationOutcome::answerability_summary` preserve the same schema/cap fields after residual exact verification, `ShadowFirstTrace::residual_decode_request` emits a broader bounded top-K decode request for compressed residual summaries before the smaller exact-escalation target set and exposes the same bounded normalized exact-query candidates for the residual-to-exact handoff when exact escalation is available, `ShadowResidualDecodeOutcome` can enrich the final exact-escalation target snippets only from matching hits with visible normalized residual summaries and can build/validate exact-verification outcomes from that enriched request while still requiring exact verification, `ShadowFirstTrace::exact_escalation_request` emits the trimmed query plus bounded ranked candidate hints with normalized 240-character snippet caps only when exact escalation is available, for residual lexical/body verification, `ShadowFirstTrace::exact_verification_outcome` pairs exact hits with the originating escalation request, `ShadowFirstTrace::validate_after_exact_verification`, `answer_allowed_after_exact_verification`, and `answerability_summary_after_exact_verification` bridge blocked Shadow-first traces to answerability only when exact verification clears the contract, `ShadowExactEscalationRequest::exact_queries` now includes bounded snippet evidence, strips FTS highlight markup and ellipsis noise, collapses whitespace, and dedupes title/path/query candidates case-insensitively, `ShadowExactVerificationOutcome` normalizes exact hit and target identities before matching, only clears the escalation when exact verification returns a matching visible hit, emits its own post-verification answerability summary, exposes `visible_matching_hits` for diagnostics, and gates citation through `citable_visible_hits` so visible hits from an invalid outcome cannot be treated as evidence, targetless no-hit Shadow requests can recover through visible exact hits from the original query, and dense-only, empty-query, ambiguous, low-score, or evidence-hidden traces require exact escalation and map to `ShadowExactEscalationRequired` in the shared violation vocabulary. Remaining deeper work is wiring that Shadow-first contract into live callers and adding Cognitive DAG resonance once the owning substrates expose stable inputs.
