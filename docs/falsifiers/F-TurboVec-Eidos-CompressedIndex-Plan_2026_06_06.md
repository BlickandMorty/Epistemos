---
falsifier: F-TurboVec-Eidos-CompressedIndex-Plan
created_on: 2026-06-06
hardware_floor: M2 Pro 16 GB UMA
status: PASS - PRIMARY WITNESS
artifact: artifacts/falsifiers/turbovec_eidos_compressed_index_plan/result.json
---

# F-TurboVec-Eidos-CompressedIndex-Plan

## Purpose

This is the executable metadata-only witness that turns TurboVec research into
an Epistemos Eidos/AppColdStore compressed-index plan after
`F-CompressedModelSourceCard-Intake`.

Epistemos is a local cognitive substrate where every meaningful object has an
address, plane, budget, status, and witness; MAS ships the safe floor, Pro
contains the gated/research/vault/omega ladder, and no claim promotes without
visible proof.

## Command

```bash
Tools/falsifiers/f_turbovec_eidos_compressed_index_plan.sh
```

## Artifact

- `artifact_kind`: `primary_witness`
- `fallback_tier`: `Primary`
- path:
  `artifacts/falsifiers/turbovec_eidos_compressed_index_plan/result.json`
- scope: metadata-only; `index_bytes_loaded=0`, `runtime_bytes_loaded=0`,
  `model_bytes_loaded=0`, and `provider_calls_made=0`

The artifact passes with 1 accepted plan and 53 red-fixture rejections. The
accepted plan binds the upstream TurboVec source card, the upstream
compressed-model source-card intake artifact, Eidos/AppColdStore placement,
SemanticWorkingSetPlan/AnswerPacket visibility, UAS-truth external IDs,
allowlist-before-rank privacy, rebuildable-cache semantics, rollback,
RunEventLog, AnswerPacket, and compatibility-fence refs.

The byte ledger records a 1536-dimensional vector with corrected planning math:
`float32_vector_bytes=6144`, `q4_coordinate_payload_bytes=768`, and
`q2_coordinate_payload_bytes=384`, before side-table overhead. No compressed
index is built, imported, queried, scored, or loaded.

## Hard Rejections

The witness rejects duplicate plan IDs, bad upstream refs, `rowid` identity,
non-upstream source locators, missing license/revision/API refs, missing
Eidos/AppColdStore organs, q4/q2 byte-math regressions, misaligned dimension
counts, metadata overflow, index/runtime/model byte loads, provider calls,
copied product files, SQLite row ID identity, missing stable external-ID
registry, missing tombstone/generation or collision ledger, external-ID rewrite
without rebuild, post-filtering after rank, missing allowlist-before-rank,
empty-allowlist packets missing, unknown allowlist IDs not rejected, scoring
forbidden/private IDs, treating AppColdStore as non-truth, treating the
compressed index as durable truth, missing exact-source/corrupt-cache/atomic
manifest rules, missing rollback/RunEventLog/AnswerPacket, bad proof refs,
index-build/runtime activation, Eidos scores selecting routes, route mutation,
hidden route authority, hidden cloud fallback, MAS/Live/T2+ promotion, live
recall-quality claims, product capability claims, live dense 70B, and
SSD-as-RAM.

## Non-Promotion Rule

This falsifier does not import TurboVec code, run TurboVec, build or persist a
compressed index, prove recall quality, choose an inference route, mutate
RuntimeRouter/System G, prove local model quality, advance live dense 70B, or
make any L2/L3 product capability green. It advances T1/L1 metadata
architecture only. L2 capability and L3 user-facing/product runtime remain
unchanged.

## Queue Effect

`F-TurboVec-Eidos-CompressedIndex-Plan` gives Eidos/AppColdStore a strict
compressed retrieval contract for later stable external-ID registry, privacy,
byte, index-build, recall-quality, and WRV witnesses. The stable-ID registry
step is now covered by `F-TurboVec-UASAddressStableExternalIds`; the
filter-before-rank privacy step is now covered by
`F-TurboVec-FilterBeforeRankPrivacyGate`; the crash-safe persistence step is
now covered by `F-TurboVec-CrashSafePersistentIndex`; exact-baseline recall
quality is now covered by `F-TurboVec-RecallQualityExactBaseline`;
latency/memory abstention is now covered by
`F-TurboVec-LatencyMemoryAbstention`; runtime shadow benchmark planning is now
covered by `F-TurboVec-RuntimeShadowBenchmarkPlan`; the next
research-to-build unit is `turbovec_quarantine_real_adapter_source_pin_probe`.
The guard-owned product cursor remains
`small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe`.
