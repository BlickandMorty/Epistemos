# F-SyntheticPayloadMaterializationGateV0 - 2026-06-08

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as a metadata-only L1/T1 side-ladder witness.

Artifact:
`artifacts/falsifiers/synthetic_payload_materialization_gate/result.json`

Command:
`Tools/falsifiers/f_synthetic_payload_materialization_gate.sh`

Witness address:
`sha256:0b528533a6e531312926fd634328f212ddd849554d1805d3c60528525eb0b32d`

Upstream blueprint address:
`sha256:d94ba97b3f8259f8d4faa74828560713e3db85f46f2829e89118e2796e1af083`

## Claim Boundary

This witness binds the synthetic payload materialization gate to
`F-SyntheticMaterializerPrimitiveBlueprintV0`, refuses materialization when
owner approval is absent, and keeps every filesystem/runtime counter at zero.

It proves only an L1/T1 metadata contract:

- owner approval is absent and the exact future phrase remains
  `APPROVE_SYNTHETIC_FIXTURE_MATERIALIZATION_V0`
- approved write roots are zero
- repo-relative fixture and staging roots are bound
- final-root/direct-final writes are denied
- absolute paths, parent escape, undeclared hidden segments, symlinks,
  hardlinks, case collisions, cross-device rename shortcuts, and pre-existing
  final collisions are rejected
- six planned payload families remain bound to the exact inventory digest
- Draft 2020-12, closed fields, duplicate-key rejection, invalid-Unicode
  rejection, NaN/Infinity rejection, and JCS-compatible digest requirements are
  required before fixture identity can promote
- privacy, provenance, and benchmark-contamination scans are required
- rollback, RunEventLog, and AnswerPacket proof surfaces are required
- zero staging dirs, final files, payload files, fixture bytes, model/runtime/
  provider/cache/index bytes, and armed commands are recorded
- L1/L2/L3/T4/T5/product/release/live-dense-70B/SSD-as-RAM/hidden-route
  promotion is denied

## Red Fixtures

The falsifier rejects 39 red fixtures including upstream-address drift,
approval smuggling, wrong phrase, approved-write-root smuggling, direct final
write, absolute paths, parent-segment escape, undeclared hidden path segments,
symlinks, hardlinks, missing case-collision denial, cross-device rename,
pre-existing final collision, payload/inventory drift, wrong JSON Schema
draft, open fields, duplicate-key acceptance, invalid-Unicode acceptance,
NaN/Infinity acceptance, non-JCS digest acceptance, missing privacy/
provenance/benchmark scans, missing rollback/RunEventLog/AnswerPacket,
staging/final/payload writes, fixture/runtime/model/provider/cache/index byte
leaks, command arming, L1/L2/product-green claims, and hidden route authority.

## Three-Layer Truth

- L1 architecture cursor: side-ladder advanced for the synthetic materializer;
  the guard-owned product cursor remains
  `small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe`.
- L2 capability route: unchanged; the capability kernel remains
  `vault_research_route_with_packetized_mitigation`.
- L3 user-facing / north star: unchanged and red for large-local-model product
  capability. This witness creates no user-facing fixture, eval win, runtime
  route, model load, release readiness, live dense 70B, or SSD-as-RAM claim.

## Next

`jcs_canonical_json_writer_parity_gate` is the next side-ladder unit because
fixture identity cannot safely materialize until Epistemos has its own
duplicate-key, invalid-Unicode, NaN/Infinity, sorted-property, UTF-8 canonical
JSON parity proof.
