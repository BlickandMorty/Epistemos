# F-GemmaOfficialConvenienceCommandDenylistGate - 2026-06-09

North-star sentence: Epistemos is a local cognitive substrate where every
meaningful object has an address, plane, budget, status, and witness; MAS ships
the safe floor, Pro contains the gated/research/vault/omega ladder, and no
claim promotes without visible proof.

## Status

PASS as a metadata-only L1/T1 primary witness.

Artifact:
`artifacts/falsifiers/gemma_official_convenience_command_denylist_gate/result.json`

Command:
`Tools/falsifiers/f_gemma_official_convenience_command_denylist_gate.sh`

Commit recorded by artifact:
`1da9d88a3e1702dfd8df9feabfa37c4f775f598d`

## What It Proves

This witness consumes `F-GemmaLocalArtifactAcquisitionReceiptGate` and proves
that official Gemma convenience commands remain source references until the
local receipt rail exists.

It binds:

- 4 official source refs.
- 8 denied convenience commands.
- 14 replacement proofs.
- 30 rejection policies.
- rollback, RunEventLog, AnswerPacket, abstention, and non-promotion refs.
- deterministic UAS address and next-cursor evidence.

It rejects:

- official model cards as runtime proof.
- `llama-cli -hf` as acquisition receipt or runtime proof.
- `llama-server` as route admission.
- LiteRT-LM `serve` as product default or System G proof.
- local OpenAI-compatible endpoints as hidden authority.
- HF cache paths as local artifact identity.
- raw path/token leakage.
- network, command, server, model, runtime, provider, route, and default-state
  actions.
- MAS/L2/L3/T4, live-Gemma, live dense 70B, and SSD-as-RAM claims.

## What It Does Not Prove

This witness does not acquire a model, open or hash a model file, verify disk
space, run llama.cpp, run LiteRT-LM, start a local server, produce a token,
prove quality, prove memory fit, admit a route, or make Gemma the live default
model.

Correct phrasing:

> Gemma official convenience commands are source evidence only; Epistemos still
> requires a local artifact receipt and direct local-file runtime receipt before
> any runtime or product route can promote.

## Layer Truth

- L1 architecture/canon: advanced as metadata-only T1/L1.
- L2 capability route: unchanged and still red.
- L3 user-facing / release readiness: unchanged and still red.

## Metrics

- official source refs: 4
- denied convenience commands: 8
- replacement proofs: 14
- rejection policies: 30
- rejected red fixtures: 32
- command executions: 0
- servers started: 0
- network runtime probes allowed: 0
- model/runtime/provider bytes loaded: 0
- RuntimeRouter/System G/settings/default mutations: 0

## Next

The next Gemma side-ladder remains
`gemma_direct_harness_owner_approved_first_runtime_execution_probe`, but only
after a real owner-approved acquisition receipt exists.
