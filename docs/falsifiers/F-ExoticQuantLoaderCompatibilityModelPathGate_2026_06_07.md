# F-ExoticQuantLoaderCompatibilityModelPathGate - 2026-06-07

Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as a metadata-only primary witness.

- Command: `Tools/falsifiers/f_exotic_quant_loader_compatibility_model_path_gate.sh`
- Artifact: `artifacts/falsifiers/exotic_quant_loader_compatibility_model_path_gate/result.json`
- Upstream: `F-ExoticQuantRuntimeLaneOwnerApprovalGate`
- Scope: T1/L1 research-to-build architecture only
- ProductBuild: Pro
- ProStatus: ResearchCandidate
- L2 capability route: unchanged / still red
- L3 user-facing route: unchanged / not green

## What This Proves

`F-ExoticQuantLoaderCompatibilityModelPathGate` consumes the runtime-lane owner gate and proves that exact exotic quant rows are bound to metadata-only loader compatibility classes and fail-closed model-path states before any local artifact, loader import, command envelope, or runtime probe can open.

Accepted rows:

- `YTan2000/Qwopus3.5-27B-v3-TQ3_4S` -> GGUF/llama.cpp metadata-only class, owner path manifest required, no local path verified.
- `caiovicentino1/Qwopus3.5-27B-v3-HLWQ-Q5` -> Transformers local-directory metadata-only class, owner path manifest required, no local path verified.
- `mudler/Qwopus-MoE-35B-A3B-APEX-GGUF` -> GGUF/llama.cpp metadata-only class, owner path manifest required, no local path verified.
- `nvidia/Gemma-4-31B-IT-NVFP4` -> CUDA Blackwell/server-only class, denied for Mac path/runtime.
- `Intel/gemma-4-31B-it-int4-AutoRound` -> Transformers/AutoRound server-research class, denied for Mac path/runtime.

## Measurements

- Gate cards: `5`
- Loader classes bound: `5`
- Loader runtime proofs: `0`
- Owner path manifests required: `3`
- Directory-entry scans recorded for Mac candidates: `3`
- Local path verified count: `0`
- Server-only path denied count: `2`
- Red fixtures rejected: `63`
- Command executions: `0`
- Path open attempts: `0`
- File stat calls: `0`
- File hash attempts: `0`
- Model bytes loaded: `0`
- Runtime bytes loaded: `0`
- Provider calls: `0`
- Source tree bytes read: `0`
- Product bytes copied: `0`
- Benchmark runs: `0`
- Next research-to-build cursor: `exotic_quant_local_artifact_availability_owner_gate` (now landed downstream; downstream owner path-manifest intake, path-canonicalization preflight, and byte-envelope preflight are also landed; downstream next is `exotic_quant_crash_safe_command_envelope_preflight_gate`)

## Non-Promotion Boundary

This witness does not prove a local model exists on disk, a loader can execute, Apple Silicon fit, first token, quality, coding/research usefulness, MAS readiness, L2 capability, or L3 WRV. It only prevents future work from treating loader labels or filenames as runtime proof.

The correct phrasing is: architecture cursor advanced; product capability / user surface did not.

## Failure Classes Rejected

The red fixtures reject duplicate IDs, missing expected models, bad source-pin binding, bad selected artifact paths, bad hardware/runtime/loader/path/action profiles, server-only Mac path allowance, owner-approval leaks, loader import/runtime proof shortcuts, armed commands, path opens, file hashes, runtime probes, missing rollback/RunEventLog/AnswerPacket/abstention surfaces, MAS/product route enablement, hidden route/cloud/PatternBoost/lattice/Eidos authority, L2/L3 promotion, live dense 70B claims, SSD-as-RAM claims, source import, benchmark-as-fit laundering, nonzero model/runtime/provider/source/product bytes, bad proof refs, and bad next cursor.

## Why It Exists

The large-local-model track needs ambition without accidental promotion. Exotic quant rows are promising because QAT, TurboQuant-like formats, GGUF, HLWQ, APEX, NVFP4, and AutoRound may eventually shrink active or selected memory footprints. But Epistemos cannot let a plausible file name, a loader family, a Hugging Face card, or a benchmark become hidden runtime authority.

The downstream `F-ExoticQuantLocalArtifactAvailabilityOwnerGate` has landed as a metadata-only T1/L1 witness at `artifacts/falsifiers/exotic_quant_local_artifact_availability_owner_gate/result.json`.

The downstream `F-ExoticQuantOwnerPathManifestIntakeGate` has landed as a metadata-only T1/L1 witness at `artifacts/falsifiers/exotic_quant_owner_path_manifest_intake_gate/result.json`.

The downstream `F-ExoticQuantOwnerPathCanonicalizationPreflightGate` and `F-ExoticQuantOwnerPathByteEnvelopePreflightGate` have now landed. The next required proof object is `exotic_quant_crash_safe_command_envelope_preflight_gate`, which should prove unarmed command serialization, cancellation, rollback, RunEventLog, AnswerPacket, no subprocess execution, and no product promotion before any runtime probe can be armed.
