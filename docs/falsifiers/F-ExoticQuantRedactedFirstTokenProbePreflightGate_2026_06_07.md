# F-ExoticQuantRedactedFirstTokenProbePreflightGate

Status: PASS as a metadata-only T1/L1 primary witness on 2026-06-07.

Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Scope

`F-ExoticQuantRedactedFirstTokenProbePreflightGate` compiles the redacted first-token probe contract after `F-ExoticQuantOwnerApprovedDryRunTranscriptPreflightGate` and before any owner-approved one-token runtime probe.

It is not a model run. It does not capture a token. It does not load model, runtime, provider, source-tree, benchmark, prompt, stdout, stderr, or product bytes. It does not promote MAS, L2, L3, live dense 70B, SSD-as-RAM, or user-facing capability.

## Witness

- Command: `Tools/falsifiers/f_exotic_quant_redacted_first_token_probe_preflight_gate.sh`
- Artifact: `artifacts/falsifiers/exotic_quant_redacted_first_token_probe_preflight_gate/result.json`
- Falsifier id: `F-ExoticQuantRedactedFirstTokenProbePreflightGate`
- Fixture id: `exotic_quant_redacted_first_token_probe_preflight_gate_v1`
- Scope: metadata-only / Pro Gated / T1-L1
- Artifact commit_sha at generation time: `ffba615defa4f0a00f195b41f2a4f53b3ce69ec3`

## What Passed

- 5 redacted first-token preflight cards accepted for the exact exotic rows.
- 3 Mac-candidate rows held at owner-approval-pending.
- 2 server/GPU rows denied for Mac first-token probes.
- 45 red fixtures rejected.
- Synthetic prompt descriptor is visible without raw prompt text.
- Prompt digest policy is bound without prompt bytes.
- First-token digest policy is bound as future-only without raw token text.
- `max_new_tokens = 1`, `context_cap_tokens <= 4096`, and `batch_cap = 1`.
- Four memory sample slots per row are required.
- Cancellation, teardown, rollback, RunEventLog, and AnswerPacket refs are required.
- Command execution, model path open, local artifact verification, runtime probe, stdout/stderr capture, raw prompt capture, raw token capture, model bytes, runtime bytes, provider bytes, and product bytes are all zero.
- Hidden route authority, PatternBoost authority, lattice authority, Eidos authority, cloud fallback, source import, benchmark-as-fit, MAS promotion, L2 green, L3 green, live dense 70B, and SSD-as-RAM claims are rejected.
- Deterministic UAS address produced: `F-ExoticQuantRedactedFirstTokenProbePreflightGate:1d0fa264465835c0bf6510a70d0c3fee129923ede54c075a3468ad1bc2893ba9@1`.
- Next side-ladder cursor: `exotic_quant_owner_approved_redacted_first_token_runtime_probe_gate`.

## L1/L2/L3 Truth

- L1 architecture cursor: advanced for this side ladder only. The witness is schema/metadata proof.
- L2 product route: unchanged. Large-model runtime remains red; capability kernel still reports the product bottleneck separately.
- L3 user-facing: unchanged. No user-facing large-local-model route is green.

Correct phrasing: architecture cursor advanced; product capability and user surface did not.

## Why It Matters

This gate prevents the next large-model step from turning into a privacy or stability hazard. A future owner-approved first-token runtime probe must inherit this contract: no raw prompt, no raw token, bounded one-token generation, explicit lane caveat, memory samples, cancellation, teardown, rollback, RunEventLog, AnswerPacket, and non-promotion.

The large-model ambition remains intact: Gemma 4 QAT, TurboVec, GGUF/llama.cpp, LiteRT-LM, MLX, Transformers, MoE/exotic quant rows, and 70B-class cold-assembly work stay in Pro Gated/Research lanes until measured runtime, packetized proof, WRV, and build-green evidence exist.

## Next Required Proof

`exotic_quant_owner_approved_redacted_first_token_runtime_probe_gate` may only run after explicit owner approval and must use a small, redacted, bounded, rollbackable one-token probe. It must not open hidden providers, hidden servers, hidden downloads, or hidden route authority, and it must not promote product capability from a token digest alone.
