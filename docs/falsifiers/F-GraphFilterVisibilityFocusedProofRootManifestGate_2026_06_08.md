# F-GraphFilterVisibilityFocusedProofRootManifestGate - 2026-06-08

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as metadata-only L1/T1 focused proof-root manifest gate.

Artifact:

`artifacts/falsifiers/graph_filter_visibility_focused_proof_root_manifest_gate/result.json`

Command:

`Tools/falsifiers/f_graph_filter_visibility_focused_proof_root_manifest_gate.sh`

## What This Proves

`F-GraphFilterVisibilityFocusedProofRootManifestGate` consumes the landed `F-AutomatedChecksFreshTestProductsEvidenceEnvelope` witness and binds the manifest/parser contract that a future focused graph-filter proof root must satisfy before any Xcode result can count.

It proves:

- Upstream evidence-envelope address: `sha256:5c24af9bc099863d5dba8398403175f2642caf45201e2ec8169042c2b26ac49f`
- Required manifest fields: `13`
- Selected test-product kinds: `2` (`xctestrun`, `xctestproducts`)
- Required rejection policies: `12`
- Minimum executed-test count policy: `1`
- Rejected red fixtures: `23`
- Selected test-product bytes opened: `0`
- Model/runtime/app bytes loaded: `0`
- Deterministic address: `sha256:bef74a16a07327e94b3b4fa36c619bbbc80957072f43886390bf1a920fdbc05c`
- Next cursor: `small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe`

## Bound Manifest Contract

The future proof root must bind source commit, pre-build status digest, post-test status digest, selected `.xctestrun` or `.xctestproducts` path/kind/digest, enumeration JSON digest, seed-selector digest, enumerated-selector digest, focused `.xcresult` digest, executed-test count, focused result-bundle status, full automated-check row status, scheme pre-action accounting, rollback, RunEventLog, and AnswerPacket.

## Rejected Claims

The witness rejects failed upstream evidence, missing upstream address, wrong upstream cursor, global DerivedData, missing manifest fields, missing selected product kinds, missing rejection policies, zero executed tests, missing source/product/enumeration/result-bundle digests, missing scheme pre-action accounting, focused proof replacing the full automated-check row, Xcode execution claims, product-code change claims, selected test-product byte opens, runtime byte leaks, raw note/prompt/model byte logging, L2/L3/product green claims, release-ready claims, live dense-70B claims, and SSD-as-RAM claims.

## Three-Layer Truth

L1 advanced only as metadata-only manifest-gate proof.

Next landed metadata gate: `F-GraphFilterVisibilityFocusedProofRootCommandCard`.

L2 product route did not advance. Capability kernel remains `vault_research_route_with_packetized_mitigation`.

L3 user-facing/release readiness did not advance. The actual fresh Xcode proof root, full automated-check rerun, log correlation, manual runtime verification, distribution/compliance review, and release audit remain required.

## Not Proven

This does not run Xcode, does not open test products, does not prove Swift tests execute, does not repair product code, does not make the automated-checks row green, does not promote MAS/Pro product capability, and does not prove live dense 70B or local large-model runtime capability.
