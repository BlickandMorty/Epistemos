---
falsifier: F-RuntimePlural-QATLaneTournamentPlan
artifact: artifacts/falsifiers/runtime_plural_qat_lane_tournament_plan/result.json
script: Tools/falsifiers/f_runtime_plural_qat_lane_tournament_plan.sh
status: PASS
scope: metadata-only L1/T1 architecture witness
---

# F-RuntimePlural-QATLaneTournamentPlan

North-star sentence: Epistemos is a local cognitive substrate where every
meaningful object has an address, plane, budget, status, and witness; MAS
ships the safe floor, Pro contains the gated/research/vault/omega ladder, and
no claim promotes without visible proof.

## Result

`F-RuntimePlural-QATLaneTournamentPlan` passes as a metadata-only witness. It
turns the runtime-plural Gemma/QAT/MTP research into a same-fixture tournament
contract before any GGUF, LiteRT-LM, MLX Swift, MLX-LM, local endpoint, or
server sidecar can claim route authority.

The witness accepts five lane cards:

- `gguf_e2b_qat_llama_cpp_future_probe`: E2B Gemma 4 QAT GGUF through
  `ggml-org/llama.cpp`, marked only as a future tiny owner-approved probe
  candidate.
- `gguf_12b_qat_llama_cpp_flagship_abstain`: 12B Gemma 4 QAT GGUF through
  `ggml-org/llama.cpp`, carried as the Pro Gated flagship target but
  abstaining on the 16 GB UMA floor.
- `litert_e2b_mtp_swift_blocked_until_package_proof`: Gemma 4 E2B/MTP through
  LiteRT-LM Swift, blocked until local package/binary/runtime proof exists.
- `mlx_swift_gemma4_loader_blocked`: Gemma 4 E2B through MLX Swift, blocked
  behind the Swift MLX Gemma loader caveat.
- `mlxlm_python_12b_research_reference`: Gemma 4 12B through MLX-LM as a
  Python research reference only, not a product lane.

All five cards bind current model/runtime source metadata, one redacted future
fixture, rollback, RunEventLog, AnswerPacket, cancellation, memory ledger,
quality ledger, latency ledger, tool-JSON ledger, compatibility fence, and
abstention refs.

## Hardening

The artifact rejects 61 red fixtures covering duplicate lane identity, missing
runtime coverage, bad URLs, bad revisions, bad licenses, fixture drift, MAS or
Pro Live overclaims, T2+ promotion, missing proof surfaces, bad proof refs,
nonzero metadata/runtime/model/provider/product/command/benchmark bytes,
package resolution, product dependency import, runtime/model loading, command
or benchmark execution, first-token claims, product winners, speed/quality
claims, MAS/L2/L3/live-70B/SSD-as-RAM claims, hidden cloud fallback, hidden
route authority, server sidecar defaults, MLX loader caveat removal, LiteRT or
MLX premature future-probe status, MLX-LM promotion out of research-only, and
explicit local endpoints not being denied by default.

## Non-Promotion

This witness does not resolve LiteRT-LM, import MLX, run `llama-cli`, run
`llama-server`, start a local endpoint, download or open model files, load
runtime bytes, run a benchmark, produce a first token, choose a winner, prove
Gemma 4 12B local usability, prove MTP speed, prove MAS safety, advance L2, or
advance L3.

Correct phrasing:

> Architecture cursor side-ladder advanced; product capability / user surface
> did not.

## Promotion Truth

- L1/T1: advanced. A same-fixture runtime-plural tournament plan now exists.
- L2: unchanged. Capability kernel remains red on the product runtime harness.
- L3: unchanged. No user-facing runtime lane is wired, reachable, visible, and
  verified from this witness.
- T4/T5: not green.

## Next

The next runtime-plural source-card unit is
`runtime_plural_qat_lane_tournament_owner_approval_gate`. The guard-owned
product cursor remains
`small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe`.
