# F-GraphFilterVisibilityFocusedProofRootCommandCard - 2026-06-08

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as metadata-only L1/T1 focused proof-root command-card gate.

Artifact:

`artifacts/falsifiers/graph_filter_visibility_focused_proof_root_command_card/result.json`

Command:

`Tools/falsifiers/f_graph_filter_visibility_focused_proof_root_command_card.sh`

## What This Proves

`F-GraphFilterVisibilityFocusedProofRootCommandCard` consumes the landed `F-GraphFilterVisibilityFocusedProofRootManifestGate` witness and binds the unarmed command envelope that a future focused graph-filter proof-root run must satisfy before any selected test product, enumeration JSON, or focused `.xcresult` can count.

It proves:

- Upstream manifest-gate address: `sha256:bef74a16a07327e94b3b4fa36c619bbbc80957072f43886390bf1a920fdbc05c`
- Required command templates: `3`
- Required proof surfaces: `9`
- Required safety policies: `12`
- Seed selector count: `8`
- Minimum executed-test count policy: `1`
- Rejected red fixtures: `32`
- Command armed count: `0`
- Selected test-product bytes opened: `0`
- Xcode result bytes opened: `0`
- App/model/runtime/provider bytes loaded or called: `0`
- Deterministic address: `sha256:e7095c8391930693cd93aa9d4e69ce36f45e2b9d178cf7c95a16b81a06aad743`
- Next cursor: `small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe`

## Bound Command Card

The future proof root must use proof-root-scoped DerivedData under `artifacts/xcode/graph-filter-visibility-test-products/`, carry `$SELECTED_TEST_PRODUCT` and `$FOCUSED_SELECTOR` placeholders, bind `build-for-testing`, `enumerate-tests`, and focused `test-without-building` command templates, write `focused-proof-root-manifest.json`, capture pre/post source status, account for the scheme pre-action, record rollback, RunEventLog, and AnswerPacket surfaces, and preserve the full automated-check row requirement.

## Rejected Claims

The witness rejects failed upstream evidence, missing upstream address, wrong upstream cursor, global DerivedData, missing command templates, missing proof surfaces, missing safety policies, bad selected-product or selector placeholders, zero selector/test policy, missing timeout/cancellation/teardown, missing source-status capture, missing scheme pre-action accounting, missing manifest write, focused proof replacing the full automated-check row, owner approval already granted, armed or executed commands, selected test-product or `.xcresult` byte opens, app/model/runtime/provider byte leaks, product-code change claims, raw note/prompt/model byte logging, L2/L3/product green claims, release-ready claims, live dense-70B claims, and SSD-as-RAM claims.

## Three-Layer Truth

L1 advanced only as metadata-only command-card proof.

L2 product route did not advance. Capability kernel remains `vault_research_route_with_packetized_mitigation`.

L3 user-facing/release readiness did not advance. The actual owner-approved focused proof-root run, full automated-check rerun, log correlation, manual runtime verification, distribution/compliance review, and release audit remain required.

## Not Proven

This does not run Xcode, does not open test products or `.xcresult` bundles, does not prove Swift tests execute, does not repair product code, does not make the automated-checks row green, does not promote MAS/Pro product capability, and does not prove live dense 70B or local large-model runtime capability.
