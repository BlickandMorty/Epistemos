# F-KVSourceCard-ForkAndDaemonBoundary - 2026-06-07

Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as a metadata-only T1/L1 primary witness on 2026-06-07.

- Command: `Tools/falsifiers/f_kv_source_card_fork_and_daemon_boundary.sh`
- Artifact: `artifacts/falsifiers/kv_source_card_fork_and_daemon_boundary/result.json`
- Primitive: `agent_core/src/uas/kv_source_card_fork_and_daemon_boundary.rs`
- Falsifier binary: `agent_core/src/bin/falsify_kv_source_card_fork_and_daemon_boundary.rs`
- Scope: Pro ResearchCandidate / L1 only. Product capability, user-facing runtime, live dense 70B, live KV cache, fork import, server/daemon execution, command execution, benchmarks, and MAS promotion remain unproved.

## What It Proves

`F-KVSourceCard-ForkAndDaemonBoundary` consumes the already-passed `F-KVRuntimeSourceCard` artifact and classifies all nine KV/runtime motifs before they can influence RuntimeRouter/System G:

- vLLM paged attention: quarantine server/daemon
- LMCache reusable KV: quarantine server/daemon
- SGLang HiCache/RadixAttention: remote/distributed denied
- KTransformers heterogeneous placement: research-only
- FlexGen offload policy optimizer: research-only
- PowerInfer activation locality: research-only
- KIVI asymmetric KV: research-only
- Transformers quantized cache: research-only
- llama.cpp prompt-cache file: owner-approval-pending command, unarmed

The witness records 9 boundary decisions, 4 classification classes, 6 runtime-shape classes, 33 rejected red fixtures, deterministic UAS addressing, rollback, RunEventLog, AnswerPacket, compatibility, privacy, MAS/Pro, and boundary refs. It keeps model, KV, index, runtime, provider, source-tree, product-copy, command-execution, and benchmark bytes at zero.

## Hard Rejections

The artifact rejects empty plans, duplicate decision IDs, duplicate source-card IDs, unknown source-card IDs, bad upstream refs, server-as-product, daemon-as-command, remote-as-local, missing owner approval, armed commands, executed commands, research-only product routes, MAS live server claims, hidden route/cache authority, model/KV/index/runtime/provider/source-tree/product bytes, command execution, benchmark execution, L2/L3 promotion, live dense 70B, SSD-as-RAM, bad proof refs, metadata-budget overflow, missing L1/L2/L3 separation, and Pro Live promotion.

## Non-Claims

This is not a runtime proof. It does not run vLLM, LMCache, SGLang, KTransformers, FlexGen, PowerInfer, KIVI, Transformers, llama.cpp, MLX, GGUF, LiteRT, Gemma 4, or any local runtime. It does not clone forks, inspect source-tree bytes, start servers, arm commands, run benchmarks, load model/KV bytes, prove quality, prove latency, prove memory fit, prove Apple Silicon support, prove MAS support, or make any user-facing large-model capability green.

Correct phrasing: architecture source-card boundary advanced; product capability and user-facing runtime did not.

## Next Unit

`hardware_tiered_model_catalog_source_card` should convert local Downloads model research and current model-card metadata into UAS-addressed hardware-tiered model cards before any 12B/27B/31B/MoE/70B-class row can influence RuntimeRouter/System G.
