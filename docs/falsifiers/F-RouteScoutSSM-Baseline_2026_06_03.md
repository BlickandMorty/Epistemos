# F-RouteScoutSSM-Baseline -- 2026-06-03

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Result

- Status: PASS as a metadata-only primary witness.
- Command: `Tools/falsifiers/f_route_scout_ssm_baseline.sh`
- Artifact: `artifacts/falsifiers/route_scout_ssm_baseline/result.json`
- Scope: Meta Control evidence only; no live route authority, no policy mutation, no model/runtime bytes, no hidden chain, no hidden cloud, and no product promotion.
- L1 next cursor: `F-TwoStageRouteScout-Abstain`
- L2 product route: still `vault_research_route_with_packetized_mitigation`
- L3 user-facing/runtime: unchanged

## What It Proves

`F-RouteScoutSSM-Baseline` proves the first tiny-scout rung is not just a plausible name. The witness requires a shadow-only RouteScoutSSM-style fixture to predict route family and verifier need on held-out tasks better than static, random, recency, and embedding-only baselines while binding task signatures, mission IDs, source/cache/trace/verifier features, hidden state refs, route logits refs, rollback, RunEventLog, AnswerPacket refs, and deterministic UAS scout address.

The fixture covers two training rows and seven held-out rows across Apple-private summary, Eidos retrieval, local Qwen coding, proof tools, KV recall, sovereign note mutation, and abstain/escalate route families. The scout reports `10000` bps route-family accuracy and `10000` bps verifier-need accuracy on the held-out slice, with calibration errors below the best baseline.

## Rejection Fixtures

The falsifier rejects duplicate task signatures, missing labels, missing features, missing logits, unknown route families, missing predictions, no held-out split, unbeaten static/random/recency/embedding route baselines, unbeaten verifier baselines, missing rollback, missing RunEventLog, missing AnswerPacket, hidden live route authority, live policy mutation, hidden-chain exposure, cloud routes, over-budget scouts, scouts that are not cheaper than the heavy route, and uncalibrated scout confidence.

## Promotion Boundary

This advances L1 only. It does not make RouteScoutSSM a live router and does not promote sparse wake execution. The next architecture unit must prove `F-TwoStageRouteScout-Abstain`: route-family choice and family-specific selection are separate, cheap, abstention-capable, rollback-bound, and AnswerPacket-visible before any sparse wake proposal can move closer to live authority.
