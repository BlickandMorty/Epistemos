# F-SmallModelRuntimeHarnessFreshProductRuntimeLiveProbe - 2026-06-05

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Verdict

- Status: PASS, L1-only fresh product-runtime live sidecar.
- Command: `Tools/falsifiers/f_small_model_runtime_harness_fresh_product_runtime_live_probe.sh`
- Artifact: `artifacts/falsifiers/small_model_runtime_harness_fresh_product_runtime_live_probe/result.json`
- Sidecar: `artifacts/falsifiers/small_model_runtime_harness_fresh_product_runtime_live_probe/live_probe.json`
- Current L1 cursor: `small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe` after downstream `F-SmallModelRuntimeHarnessFreshProductRuntimeL3ReleaseAuditZeroFailProbe`
- Scope: consumes `F-SmallModelRuntimeHarnessFreshProductRuntimeSafetyLease` and validates one bounded, redacted Qwen3-4B local MLX first token on the fresh product-runtime path. It does not promote L2, L3, MAS live-agent, live 70B, KV-Direct 128K, or autogenous-kernel claims.

## What It Proves

This witness validates a retained sidecar generated by a bounded `mlx_lm.stream_generate` helper against the local Qwen3-4B MLX 4-bit snapshot. The sidecar uses a synthetic non-user prompt, observes exactly one output token, retains only prompt/token SHA-256 digests plus byte/timing counts, and records rollback, RunEventLog, AnswerPacket, SCOPE-Rex/SovereignGate, privacy, and budget refs.

The artifact records one chunk, one output token, token UTF-8 length `2`, `load_ms=1305`, `first_token_ms=700`, `total_ms=2006`, fresh product model bytes `2137326367`, fresh product runtime bytes `16777216`, capability route status `vault_research_route_with_packetized_mitigation`, deterministic address evidence, and the next cursor `small_model_runtime_harness_fresh_product_runtime_answer_packet_probe`.

## Required Rejections

The primitive rejects missing safety-lease refs, missing sidecar fields, prompt user-data retention, missing first token, multi-chunk or multi-token output, raw token text retention, invalid token digests, zero or over-budget runtime/model bytes, over-budget latency, missing AnswerPacket/RunEventLog/rollback refs, product-route or MAS claim promotion, false L2/L3 green claims, hidden cloud or hidden chain, route mutation, app-path subprocess spawn, autogenous-kernel attempts, 70B probe attempts, long-context shard probe attempts, next-cursor mismatch, and metadata overflow.

## Three-Layer Truth

- L1: Advanced at landing to `small_model_runtime_harness_fresh_product_runtime_answer_packet_probe`; downstream `F-SmallModelRuntimeHarnessFreshProductRuntimeAnswerPacketProbe` and `F-SmallModelRuntimeHarnessFreshProductRuntimeWrvProbe` now pass, and the regenerated guard reports `next_existing_work=small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe` after downstream `F-SmallModelRuntimeHarnessFreshProductRuntimeL3ReleaseAuditPreflightProbe` with duplicate risk `0`.
- L2: Not green. The capability kernel remains `overall_pass=false`, route status `vault_research_route_with_packetized_mitigation`, and `next_bottleneck=small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe` after downstream `F-SmallModelRuntimeHarnessFreshProductRuntimeL3ReleaseAuditPreflightProbe`.
- L3: Not promoted. Fresh AnswerPacket packaging and WRV now pass, but broader product capability still waits on the fresh capability recheck.

## Caveat

This is a bounded L1 runtime sidecar, not a general product-runtime green light. It proves one redacted local Qwen3-4B token under the safety lease; it does not prove live 70B, KV-Direct 128K, dense 70B residency, MAS live-agent mode, or autogenous-kernel work.
