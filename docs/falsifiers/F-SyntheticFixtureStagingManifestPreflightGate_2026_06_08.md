# F-SyntheticFixtureStagingManifestPreflightGate - 2026-06-08

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as a metadata-only L1/T1 side-ladder witness.

Artifact:
`artifacts/falsifiers/synthetic_fixture_staging_manifest_preflight_gate/result.json`

Command:
`Tools/falsifiers/f_synthetic_fixture_staging_manifest_preflight_gate.sh`

Witness address:
`sha256:066e9509f28f034208a983446697f8f88b8a578931cd7d05ab210d6d51f2c6b2`

Upstream writer dry-run address:
`sha256:f2c70a74d099c73facce3422d0689b35234658c455b871b0ccfdbca008bcebcb`

## Claim Boundary

This witness consumes `F-JcsFixtureWriterFailClosedDryRun` and binds the
manifest fields, staging/final path policy, digest policy, rollback,
RunEventLog, AnswerPacket, privacy/provenance scans, and benchmark-
contamination scan requirements that must exist before any synthetic fixture
bytes can be written.

It does not create a manifest file, staging directory, fixture file, eval win,
runtime route, model load, or product capability.

## Required Proofs

- upstream writer dry-run address binding
- dry-run fragment consumption
- 16 required manifest fields
- repo-relative staging and final roots
- absolute paths, parent segments, hidden segments, symlinks, hardlinks,
  direct final writes, cross-device renames, and pre-existing final collisions
  denied
- JCS canonical SHA-256, fragment, manifest, inventory, duplicate-path, and
  duplicate-digest policies required
- exact owner approval phrase retained
- rollback receipt, RunEventLog, AnswerPacket, privacy scan, provenance scan,
  and benchmark-contamination scan required
- product route authority denied
- zero manifest/staging/final files
- zero fixture/model/runtime/provider/cache/index bytes
- zero filesystem stat calls
- zero armed commands
- explicit non-promotion across L1/L2/L3/T4/T5/product/release/70B claims

## Red Fixtures

The falsifier rejects 33 red fixtures including upstream-address drift,
unconsumed dry-run fragments, manifest-file or owner-approval claims, path
policy bypasses, digest policy bypasses, wrong approval phrase, missing
rollback or AnswerPacket requirements, product route authority, manifest field
name/required/digest drift, manifest/staging/final file writes, fixture bytes,
runtime/model/provider/cache/index byte leaks, filesystem stat calls, command
arming, materialization enablement, metadata-boundary disablement, L2/product
green claims, and hidden route authority.

## Three-Layer Truth

- L1 architecture cursor: side-ladder advanced for synthetic fixture staging
  manifest preflight; the guard-owned product cursor remains
  `small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe`.
- L2 capability route: unchanged; the capability kernel remains
  `vault_research_route_with_packetized_mitigation`.
- L3 user-facing / north star: unchanged and red for large-local-model product
  capability. This witness creates no fixture files, eval win, runtime route,
  model load, release readiness, live dense 70B, or SSD-as-RAM claim.

## Next

`synthetic_fixture_owner_approval_write_gate` is the next side-ladder unit. It
must bind explicit owner consent before any staging manifest or synthetic
fixture file can be written.
