# F-GemmaQATRedactedFirstTokenProbe - 2026-06-08

North-star sentence: Epistemos is a local cognitive substrate where every meaningful object has an address, plane, budget, status, and witness; MAS ships the safe floor, Pro contains the gated/research/vault/omega ladder, and no claim promotes without visible proof.

## Status

PASS as a metadata-only T1/L1 architecture witness.

Artifact: `artifacts/falsifiers/gemma_qat_redacted_first_token_probe/result.json`

Command: `Tools/falsifiers/f_gemma_qat_redacted_first_token_probe.sh`

Scope: metadata-only. No owner path was approved, canonicalized, opened, hashed, or read. No local model, runtime, provider, KV cache, stdout, stderr, raw prompt, or raw token bytes were loaded or captured. No command was armed or executed. No MAS, L2, L3, T4, product, quality, benchmark-fit, live dense 70B, SSD-as-RAM, or user-facing Gemma capability claim is promoted.

## What This Proves

`F-GemmaQATRedactedFirstTokenProbe` consumes `F-GemmaQATByteKVAppEnvelopePreflight` and defines the first safe Gemma runtime-entry contract before any future owner-approved local first-token probe can count.

The witness accepts four cards:

- Gemma 4 E2B QAT GGUF / llama.cpp first-token preflight.
- Gemma 4 E2B QAT LiteRT-LM first-token preflight.
- Gemma 4 E4B QAT GGUF / llama.cpp first-token preflight.
- Gemma 4 E4B QAT LiteRT-LM first-token preflight.

It binds:

- owner approval still pending;
- fresh memory sample requirement;
- synthetic prompt descriptor visibility;
- prompt digest policy with no raw prompt text;
- future first-token digest policy with no raw token text;
- one-token, context, and batch bounds;
- 16 memory sample slots;
- cancellation, teardown, rollback, RunEventLog, and AnswerPacket refs;
- lane caveat and non-promotion refs;
- deterministic UAS address;
- next cursor `gemma_qat_same_fixture_runtime_replay`.

## Red Fixtures

The artifact rejects 48 red fixtures, including:

- 12B insertion into the E2B/E4B warmup pack;
- duplicate model/lane pairs;
- bad runtime lane or mismatched surface;
- owner approval laundering;
- missing fresh memory sample requirement;
- hidden prompt descriptor;
- missing prompt/token digest policy;
- raw prompt, raw user prompt, raw token, stdout, or stderr capture;
- first-token observation or digest presence before runtime proof;
- max-token, context, or batch overrun;
- missing or duplicate memory sample slots;
- missing cancellation, teardown, rollback, RunEventLog, or AnswerPacket;
- command arming/execution, runtime probe allowance, path open, or artifact verification;
- model/runtime/provider byte leaks;
- route mutation, hidden route authority, hidden cloud fallback;
- MAS/L2/L3/product/live-70B/SSD-as-RAM/quality/benchmark-fit claims;
- bad proof refs, metadata overflow, bad upstream ref, and wrong next cursor.

## Layer Truth

L1 architecture/canon: advanced for the Gemma side-ladder only. The redacted first-token contract is now addressable, witnessed, and schema-bound.

L1 guard-owned product cursor: unchanged. The global architecture guard still owns `small_model_runtime_harness_fresh_product_runtime_l3_release_audit_automated_checks_probe`.

L2 capability route: unchanged and red. This does not admit Gemma through RuntimeRouter/System G as a usable app route.

L3 user-facing / north star: unchanged and red. This does not wire Gemma into settings, onboarding, the note chat surface, or user-visible runtime behavior.

Correct phrasing: "Gemma E2B/E4B redacted first-token preflight is L1 metadata-proofed; no Gemma prompt, token, model file, runtime, or product route has been opened, captured, loaded, or promoted."

## Why It Matters

This gate keeps the "make Gemma the main model family" ambition from skipping privacy and stability. A first-token probe is where accidental raw prompt logging, stdout/stderr leaks, route mutation, memory overrun, and false product claims can sneak in. This witness requires the future runtime step to be one-token, cancellable, rollbackable, logged, AnswerPacket-visible, and redacted before it can matter.

## Next

Next Gemma side-ladder unit: `F-GemmaQATSameFixtureRuntimeReplay`.

The next unit must not compare quality or promote Gemma until it binds the same fixture, source/search freshness packet, prompt digest policy, tokenizer/chat-template policy, tool schema, memory sample, cancellation, rollback, RunEventLog, AnswerPacket, abstention, and no-hidden-authority proof.
