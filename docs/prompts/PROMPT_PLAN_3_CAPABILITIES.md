# PLAN 3 — Capabilities build prompt (paste to a build agent)

> OWNER OVERRIDE — 2026-07-07, `MAS-ONLY-SHIP-LOCK-2026-07-07`: read
> `docs/prompts/MAS_ONLY_STRATEGIC_PIVOT_2026_07_07.md` first. This plan now
> targets MAS only. Keep MAS-safe capabilities: PDF, provenance, vault tools,
> arXiv/ResearchHub, native/WKWebView Browser, Apple-native views, STT/voice,
> landing buttons, and June tool integrations. Park browser-use Chromium, Python,
> Developer-ID/Pro packaging, terminal/code-exec, stdio MCP, local servers, and
> subprocess lanes. Any "current surface = Experimental/1Code" text below is
> historical unless rebuilt through MAS/June.

> 🔴 **OWNER OVERRIDE — 2026-07-06 (READ FIRST; SUPERSEDES CONTRADICTORY TEXT BELOW).**
> OpenChamber/ProAgent are deletion targets, not current surfaces. The 2026-07-06 two-surface
> wording is superseded by the 2026-07-07 MAS-only lock: the active surface is **MAS/June**.
> Everywhere this doc says "Goose-only," "GOOSE stays
> the ONE user-facing agent," "OpenChamber," or "the user only talks to Goose," read it as:
> **the user talks to the current agent surface: MAS/June.** Goose/1Code,
> browser-use, Developer-ID, and Experimental references are historical or
> parked unless rebuilt as App Store-safe June + `agent_core` capability seams.
> 2. **arXiv is KEPT as a DEDICATED feature (room) AND an agent capability.** Polish a visible "agents read / agents engaged with" section; when an agent reads arXiv its **companion mascot appears on the arXiv button/surface** and the landing shows "currently reading arXiv." Not demoted, not cut.
> 3. **⚠️ OBSCURA CONTRADICTION — FLAGGED FOR OWNER, do NOT silently resolve.** The OWNER-FINAL scope line below CUTS "Obscura native automation engine (→ browser-use)." But the owner (2026-07-02) wants the **Obscura *browser* KEPT** ("very useful lightweight Rust browser"). These are plausibly TWO different things — an *automation engine* (cut, → browser-use) vs a *lightweight browser surface* (kept). **Confirm with the owner which "Obscura" each line means before building either way.** Do not delete the Obscura browser on the strength of the automation-engine cut; do not revive an automation engine on the strength of "keep Obscura browser."
> 4. **Native truths still hold** (native Apple views, WKWebView lite Browser tab, browser-use Chromium honesty) — only the "Goose-only / one Goose agent" framing is stale. The mascot-on-capability-button behavior is NEW and wanted.

> 🎨 **OWNER DESIGN DIRECTIVE — 2026-06-30 (CURRENT CANON, identical in EVERY plan):** the look = **HIGH-QUALITY FLAT · MINIMAL · THEME-AWARE · OPTIMIZED.** KEEP the Apple-native **FRAME** — rounded-corner window, vibrancy, traffic-lights, the calibrated springs (the curved-white native window the owner likes STAYS). **SURFACES (panels · buttons · lists · inputs) are FLAT + BORDERLESS:** NO thick outlines, NO hard box borders, NO 1px rules — differentiate by a subtle background tint + spacing + a very soft shadow only. NOT the old thick-outline pixel-art look, and NOT translucent-glass on every surface. **TOTAL THEME-AWARENESS:** every surface (native + editor-web + Goose-web) reads the Epistemos tokens for ALL palettes incl. the user CUSTOM palette; no hardcoded color; two-token-sources in lock-step. Full doctrine: `docs/research/EPISTEMOS_NATIVENESS_DOCTRINE_2026_06_29.md`.

> The capabilities plan, hardened. Thermonuclear-strict, hard gates, FULL clones
> where MAS-safe (EdgeParse/unpdf for App Store). Browser-use/Chromium/Python
> donor material is parked while MAS-only is active.

---

```
[$thermo-nuclear-code-quality-review](/Users/jojo/.codex/skills/thermo-nuclear-code-quality-review/SKILL.md)

★ LOOP MODE — NEVER STOP until I (the owner) type "stop". Continuous loop, not a one-shot. ★ FINALIZATION MODE (owner 2026-06-30, superseded for non-MAS lanes by 2026-07-07) — drive the remaining REAL MAS punch-list (Kokoro TTS to live + deferred App Store-safe capability gates) to actual completion with objective evidence. Browser-use signed-Pro packaging is parked. "Code-complete awaiting proof" is NOT done — build/flip only what really works. Don't invent new scope to stay busy, don't rush, don't fake-flip gates; the owner may test later, so build the real remaining MAS items well and do not block on exhaustive self-proof. ★ WORK ORDER (owner 2026-06-30 — REAL WORK FIRST, HARDENING CAPPED): each cycle build the next REAL MAS capability to LIVE and PROVE it in-app before the next. Remaining reals are the MAS-safe deferred/gated bits (arXiv, voice/Kokoro, meeting-STT, vault-MCP, provenance, Obscura/Browser, extensibility, Apple-native, EdgeParse) — flip gate flags ONLY when real; if a required codepack is missing, write it first. Hardening is SECONDARY + BOUNDED: after a real item, at most ONE focused pass on the code you just touched — do NOT launch open-ended app-wide bound/redact sweeps, and NEVER let hardening preempt an unfinished capability. When the reals are live, pull the next owed REAL item; there is ALWAYS more real work — do NOT declare the plan/capabilities 'done' and do NOT stop to 'report done' (you do NOT decide done; ONLY the owner does, by typing stop). If you think you've run out, pick the next real feature/polish/edge-case and KEEP CODING — never loop on hardening/audits in place of real feature work. Only the owner's "stop" ends the loop. Commit at every clean point.

★ CADENCE — STOP RE-VERIFYING EXHAUSTIVELY EVERY LOOP (owner 2026-06-30, for SPEED): a full `xcodebuild`, the full test suite, deep live in-app re-proofs, and audit sweeps are EXPENSIVE — run them as a PERIODIC CHECKPOINT (after a batch of real items, or right before a commit you want to trust), NOT after every micro-edit. Between checkpoints, edit + do FAST local checks only (targeted reads/grep; for Rust use `cargo test --lib` on the touched crate, not the whole suite). ⚠️ BUILD COLLISION: all 3 agents share ONE tree + DerivedData — two concurrent `xcodebuild`s corrupt build.db → exit 65 (a TOOLING failure, not a code error); assume another agent may be mid-build, so build RARELY and only at your checkpoint. TRUST the last green result for any area you did not touch. Real visible progress > continuous exhaustive re-proof.

★ KOKORO VOICE — ENGINE CHOICE (owner-confirmed + research-verified 2026-06-30): Kokoro-82M IS the on-device voice (Apache-2.0 weights = MAS-legal weights-as-data; already wired in `KokoroVoiceGateStatus.swift`, `isReady=false`). To bring it live, wire a **NO-PYTHON native engine** — **Core ML** (`mattmireles/kokoro-coreml`) OR **sherpa-onnx / ONNX-Runtime (C++/Rust)** OR **MLX-Swift**. ⛔ Do NOT wire the `kokoro-onnx` / `kokoro-mlx` REFERENCE WRAPPERS — those are Python and break the no-subprocess MAS rule. The `.onnx` + `voices` files are downloaded DATA (fine); the engine is bundled native code. ⛔ Do NOT pursue GPT-SoVITS or Style-Bert-VITS2 for MAS (GPT-SoVITS = Python/PyTorch-only; SBV2 = Japanese-only + AGPL-3.0 weights) — anime/character voices are a Developer-ID/Experimental-only lane at most, never v1 MAS. ★ STT/TTS SPLIT (owner-locked 2026-06-30): **speech-to-TEXT = Apple native (SpeechAnalyzer, on-device) — keep it.** **Text-to-SPEECH = Kokoro ONLY** — Kokoro is the SOLE app voice; do NOT ship `AVSpeechSynthesizer` / Apple's basic system voice as the read-aloud/TTS voice (drop it as the shipped voice). Until Kokoro's synthesis path is live (`isReady=false` today), **honest-gate TTS as unavailable — NO silent AVSpeech fallback** (honest-capability-gating; the owner does NOT want the Apple basic voice). AVSpeech may remain in code ONLY if fully unwired from the shipped voice path.

WORK MODE — BUILD/FINISH FEATURES; NO UNREQUESTED REFACTORING (owner 2026-06-30): primary activity each cycle = BUILD or FINISH the next real, user-visible capability and PROVE it in-app. ⛔ DO NOT restructure, split files, move code between files, rename, or "centralize / dedupe / inline / share" WORKING code — the owner did NOT ask for refactors, and that churn keeps breaking the shared build (all agents share one tree). Touch a file ONLY to add/finish a capability or fix a real bug that blocks one. Edge-case hardening = find + fix real BUGS in the code paths you're shipping, NOT restructure them. Write a test ONLY to lock a specific bug you just fixed — no coverage-padding suites. Keep the tree COMPILING at every step. One shipped capability > any refactor.

PRIORITY-0 (verify/fix first): the arXiv "PDF not supported" bug — `URLSession.shared.download` saves an EXTENSION-LESS temp file, and the LiteParse importer rejects any path not ending in `.pdf` (LiteParseImport.swift guard) → arXiv ingest fails on a VALID pdf (ArxivIngestService.swift:77). FIX in `URLSessionArxivPDFDownloader.download`: move the temp file to a `.pdf` path before `importToMarkdown`; THEN harden by sniffing the `%PDF-` magic bytes (survives an HTML error page served as a download). If a prior commit already fixed it, VERIFY live end-to-end (download → parse → note + source_pdf) and move on. Then deep-review edge cases: PDF→md (encrypted/0-byte/scanned/multi-column), provenance honest-gate (no fake "Verified"), vault-MCP read-only enforcement, and App Store capability honesty. Browser-use Pro work is parked by the 2026-07-07 MAS-only lock.

★ THIS PLAN WAS UPGRADED (2026-06-29) — RE-READ the canon (it changed) and reconcile any stale claim you hit (no-contradiction). WHAT CHANGED for Plan 3: (1) WORK MODE = deep code + edge-case hardening, FEWER tests (above). (2) PRIORITY-0 = the arXiv PDF temp-file fix (above). (3) NEW app-wide NATIVENESS + UNIFIED-LOOK mandate (the ★ block below + docs/research/EPISTEMOS_NATIVENESS_DOCTRINE_2026_06_29.md + GOOSE_NATIVE_WEB_RESKIN_2026_06_29.md): native Apple views use real Liquid Glass + the verified Apple tokens/springs; browser-use = its OWN Chromium surface (reskin where feasible; CDP ≠ WKWebView); any hosted web panel (incl. the lite Browser tab) = transparent-over-glass + tokens. (4) CODE-RESEARCH (real openable code) + RESEARCH-BETWEEN-IMPLEMENTATION. Scope unchanged (OWNER-FINAL below); CUT items stay cut.

★ STALE-CANON AUDIT (2026-06-29): read docs/research/PROMPT_PLAN_UPGRADE_AUDIT_2026_06_29.md before acting on old build-order wording. Plan 3 is now mostly shipped/staged; when an item already exists, verify/harden it instead of rebuilding or rewriting its codepack.

You are building PLAN 3 = Epistemos capabilities. Deeply hardened, contradiction-free, nothing lost — and whatever is cloned must be FULLY cloned (settings and all, 100% capability, so no usefulness is lost).

READ FIRST (the PLAN doc wins on conflict):
  - docs/research/PLAN_3_CAPABILITIES_2026_06_28.md  (THE plan — scope, §1–§11, CLONES LEDGER, build order)
  - docs/research/PROMPT_PLAN_UPGRADE_AUDIT_2026_06_29.md (stale contradiction audit after the native-unification upgrade)
  - Codepacks (real code + file:line): PLAN_3_EDGEPARSE_CODEPACK, PLAN_3_PROVENANCE_CODEPACK, PLAN_3_VAULT_MCP_CODEPACK, PLAN_3_OBSCURA_TIER1_CODEPACK (historical filename; product name is Browser), PLAN_3_EXTENSIBILITY_CODEPACK, PLAN_3_APPLE_NATIVE_CODEPACK, PLAN_3_LANDING_BUTTONS_CODEPACK, PLAN_3_ARXIV_CODEPACK, PLAN_3_BROWSER_USE_CODEPACK, PLAN_3_VOICE_CODEPACK, PLAN_3_MEETING_STT_CODEPACK, PLAN_3_WHOLE_APP_LOGOS_CODEPACK (all docs/research/*_2026_06_28.md)
  - Project rules: CLAUDE.md (NON-NEGOTIABLE CONSTRAINTS — esp. NO hidden subprocess/Python on the MAS path; keys in Keychain; honest capability gating; @Observable; never block @MainActor). RESEARCH-FIRST: read before editing, verify code/disk before asserting, tag [VERIFIED-CODE].

OWNER-FINAL scope (2026-06-28, re-anchored 2026-07-07): Fast PDF→MD · Provenance moat · Extensibility (skill/MCP install + best-of preset + vault-as-MCP-server where App Store-safe) · Apple-native maximization · Landing-page buttons (every feature is a one-tap button) · Browser (lite native WKWebView tab for the App Store) · arXiv pull · Meeting/STT note · Voice · Whole-app brand logos. CUT/PARKED (do NOT build/re-add while MAS-only is active): ColBERT, local model-management (HF/BYOM/stack/vision), three-engine/Osaurus, DeerFlow, browser-use Chromium robot, Python/Chromium/Developer-ID automation. [⚠️ "Obscura native automation engine (→ browser-use)" was on this CUT list — but the owner (2026-07-02) wants the Obscura BROWSER kept; confirm whether these are the same thing before cutting anything Obscura — see the header's Obscura flag.] AI = MAS/June + `agent_core`.

★ NATIVENESS + UNIFIED LOOK (BINDING — FULL verified detail in docs/research/EPISTEMOS_NATIVENESS_DOCTRINE_2026_06_29.md + GOOSE_NATIVE_WEB_RESKIN_2026_06_29.md: tokens/springs/glass recipe/code-to-lift. READ THEM — this paste is lean.):
  - PLAN-3 SPLIT: NATIVE = Apple-native shared views (QuickLook/VisionKit/Live-Text/thumbnails) + landing-feature buttons + the lite Browser tab CHROME + arXiv/meeting/voice/provenance UI + the "View original PDF" handoff into Plan 2's PDFKit viewer (don't re-invent it). WEB = the lite Browser tab WKWebView with transparent-over-glass + tokens like any web body. Browser-use's Chromium UI is parked donor material, not an active MAS surface.
  - Non-negotiables (detail in the docs): native views = real Liquid Glass + unified tokens/springs · any web panel = transparent-over-glass + tokens · deeply-fluid ProMotion + MINIMAL · A/B pixel-diff = the bar · GRAPH = DO NOT TOUCH · SF Symbols native-only · CODE-RESEARCH (real openable code) + RESEARCH-BETWEEN-IMPLEMENTATION (read before edit, exhaustive, no-contradiction).

BUILD/HARDENING ORDER (per the plan; if an item is already shipped/staged, verify and harden it instead of rebuilding): (1) Fast PDF→md — EdgeParse + unpdf parser path, LiteParse FFI envelope, MAS flip, and PDF/parsed-.md coexistence (source_pdf frontmatter). (2) Provenance moat — the honest VRMLabel.honestLabel gate (no "Verified" chip without a real active anchored claim) + tightened Settings audit + hover-lineage card. (3) Extensibility — vault-as-MCP-server read-only enforcement → skill/MCP install UI → best-of preset. (4) Apple-native (QuickLook/VisionKit Live Text/thumbnails) · Landing buttons (LandingFeatureButton) · arXiv pull. (5) Browser — shipped lite native WKWebView tab (PLAN_3_OBSCURA_TIER1_CODEPACK, historical filename, product name "Browser") for MAS; browser-use Pro vendor code/payload is parked. (6) Meeting/STT note · Voice · whole-app logos.

★ FULL-CLONE requirement (owner: "100% useful before I App-Store it"):
  - EdgeParse + unpdf: pin the REAL repo URL + a real SHA; vendor the full parser crates with the COMPLETE feature set (tables/headings/reading-order/multi-column config) — no partial graft. ProvenanceGate (Apache-2.0/MIT = direct_import). MAS-safe (pure Rust, no Python/dylib).
  - browser-use (parked Developer-ID/Experimental lane): keep the codepack as donor research only while MAS-only is active. Do not vendor Python, Playwright Chromium, Gradio, MCP stdio, or a subordinate browser agent into the MAS product. If any idea is reused, translate only the MAS-safe motif into June + `agent_core` and the native/WKWebView Browser.

THERMONUCLEAR DISCIPLINE (run the skill above, recurring — each capability + a full pass periodically):
  - Honest findings only: correctness, dead/stale code, honesty-constraint violations (esp. fake "verified" provenance chips), perf, contradictions.
  - DELETION GUARDRAIL: harden/dedupe over delete; never delete new/in-progress/owner-requested code; KEEP+flag when unsure; commit deletions separately.
  - NO CONTRADICTIONS: the plan was heavily edited (Obscura/ColBERT cut). Before each stage, grep the plan + codepacks for any stale claim that contradicts the OWNER-FINAL scope; fix the source.

HARD GATES / FORBIDDEN:
  × Any Python/subprocess/Chromium on the MAS (App Store) path — those are Developer-ID/Experimental-only (browser-use, etc.), honest-gated.
  × A "Verified" provenance chip that doesn't dereference a real active claim (the honest gate must ship WITH any chip).
  × Surfacing a write/exec tool through the read-only vault-as-MCP-server (enforce the read-only allowlist at the core).
  × Building the cut items (Obscura native robot, ColBERT, model-management). Leave the WebKitBrowserEngine stub NotConfigured.
  × Build-green ≠ done. PROVEN-DONE bar: real-state · live in-app · migrates existing data · end-to-end · witnessed (Swift Testing @Test compile-verify + manual run for UI; cargo test --lib for Rust — note xcodebuild/cargo check skip #[cfg(test)], so use cargo test). Zero regressions.
  × Keys in UserDefaults (Keychain only); editing .xcodeproj (xcodegen only); committing model files / .gguf.

PARALLELISM / NO-COLLISION (Plan 1 + Plan 2 agents build concurrently in this repo):
  - You OWN: the PDF→md PARSE engine (agent_core EdgeParse/unpdf + the LiteParse FFI/import controller) + the source_pdf storage contract, provenance moat (AnswerPacket/VRMLabel/provenance Swift), vault-as-MCP (Epistemos/Vault/*), extensibility (MCP registry/install/best-of), Apple-native shared views (Views/Shared/*), landing buttons (Views/Landing/LandingFeatureButtons), the lite Browser tab + browser-use (Pro), arXiv (Epistemos/Arxiv/*), meeting/STT, voice, logos.
  - Do NOT touch: Epistemos/Goose/* + Epistemos/Agent/* (Plan 1) · the Plan-2 editor surfaces (Epdoc/code-editor/Prose/MarkEdit-embed/js-editor/HTML-workspace/wikilinks). The PDF *viewer* (PDFKit PDFView) is PLAN 2's — you provide the resolved source_pdf URL, Plan 2 mounts the viewer. Vault-as-MCP read tools query the existing search/vault dispatcher; don't fork it.

Commit at clean points (main-only). When unsure, RESEARCH-FIRST then act. Stop only when I say stop; after the build order is complete with PROVEN-DONE evidence, keep looping through full-app thermo passes, weakest-area hardening, shipped/staged codepack follow-ups, and re-verification.
```
