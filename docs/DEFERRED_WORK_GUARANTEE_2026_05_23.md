# Deferred-Work Guarantee — 2026-05-23

> **2026-06-01 current canon bridge (JUNE1-PATTERNBOOST-LOCK):** This file is preserved as a legacy, planning, research, or witness artifact. For active architecture, route Helios/UAS/ACS/mmap/KV-Direct/70B/NeuralImportance claims through `docs/fusion/RESIDENCY_PATTERNBOOST_DISCOVERY_2026_06_01.md`, `docs/falsifiers/F-RESIDENCY-PATTERNBOOST-BUNDLE_2026_06_01.md`, `docs/fusion/SEMANTIC_WORKING_SET_COMPILER_2026_06_01.md`, and `docs/fusion/COLDSTREAM_RESIDENCY_TRANSPORT_2026_06_01.md`. Legacy claims remain historical until promoted by falsifiers, AnswerPacket evidence, LatticeAbstentionGate, ComputeResumeLease, rollback, and the intentional-copy/zero-copy caveat.

User mandate, verbatim:

> "i want to make sure deferred stuff still gets built in. the past that would be lost literally so thats important"

This doc is the **anti-loss ledger** for every deferred item across the corpus. **Nothing here is allowed to be silently dropped.** Every row gets a build-target window + an explicit re-promotion trigger.

## Deferred-item register

| ID | Item | Why deferred | When/how it builds | Preservation home |
|----|------|---------------|---------------------|-------------------|
| **D-01** | T6 UI/UX polish (17-iter audit + a11y on LiveActivityStrip/ContextWindowIndicator/ProcessDisclosure + Halo persistence + Provenance Console pagination + AmbientFrequencyLivePlayer audiophile chain) | Most of T6 is modifications, not pure-additive. `MAY16_ARCHEOLOGY_2026_05_23.md:37`. | **Build target:** Phase 3 UI refactor cycle. **Re-promotion trigger:** user opens explicit UI-polish session, OR Terminal D Substrate Health unification creates demand for the a11y patterns. | Audit docs landed via PR #61 (16 UI_UX_*_2026-05-17.md). Code preserved on `codex/t6-uiux-2026-05-16`. |
| **D-02** | T8 Biometric Lock (Phase B onward — code + 6 W-rows W-34..W-39) | Gated until T1+T2+T6 land. Doctrine-only first; code work GATED. | **Build target:** when all of {T1 done, T2 ≥ 90%, T6 polish done}. Estimated Q3-Q4 2026. **Re-promotion trigger:** user opens biometric-lock session OR Touch ID requirement surfaces. | `docs/fusion/BIOMETRIC_LOCK_DOCTRINE_2026_05_17.md` (landed). 6 W-rows in chronicle §3.7. |
| **D-03** | XPC Mastery 5-service decomposition (Main + VaultXPC + AgentXPC + ProviderXPC + WASMExecXPC) | Phases X.1-X.5 deferred until paid Apple Developer Team membership. Per V2.4. | **Build target:** when paid Apple Team available. **Re-promotion trigger:** "RESUME XPC MASTERY" codeword OR notarization requirement. | `docs/fusion/XPC_MASTERY_DOCTRINE_2026_05_03.md` + memory `project_xpc_mastery_doctrine`. |
| **D-04** | T13 F-KV-Direct-Gate harness on rig (Qwen 3 8B at 128k context; peak RAM ≤ 13 GB; D_KL/token ≤ 0.08; decode ≥ 10 tok/s) | Long-running measurement; needs M2 Pro 16 GB session with dedicated time. | **Build target:** Terminal F (one of the 5 falsifiers PASS push). **Re-promotion trigger:** Terminal F dispatched. | `agent_core/src/scope_rex/kv/direct_gate.rs` (290 LOC SHIPPED) + Metal kernel SHIPPED. Harness binary pending. |
| **D-05** | T20 Variant Ladder Generalization (deterministic → embedding → classical → small LLM → mid LLM → cloud → defer; `escalate_on_empty` defaults false) | NOT-STARTED. Branch `codex/t20-variant-ladder-generalization-2026-05-18` exists but never merged. | **Build target:** Phase 3 side-task. **Re-promotion trigger:** "RESUME T20" OR Terminal C System G needs variant ladder for honest escalation. | Branch preserved on origin. |
| **D-06** | T26 L_SE Self-Evolving Adapter Lane (Pro Research) | Research-deferred per memory. High-risk; needs MAS/Pro isolation tests. | **Build target:** Pro Research sprint (post-Floor close). **Re-promotion trigger:** "RESUME L_SE RESEARCH" codeword. | Chronicle §2 T26 row. |
| **D-07** | Schema-First GenUI Doctrine (Phases G.1-G.6; 24-day ceiling) | Silently deferred since Four-Model Advice Council 2026-04-22. Hermes Expert Mode slices 1-8 carry `GENUI-DEFER` markers. G.3 migration partial per recovery loop. | **Build target:** Phase 2.5 (post-current-7-terminal-wave). **Re-promotion trigger:** any new typed UI component being added — MUST go through GenUIDispatcher per day-1 rule. | Memory `project_schema_first_genui_doctrine`. ProvenanceConsole uses pattern (PR ad6280cf). |
| **D-08** | 5 V6.1 Metal kernels (PageGather / SemiseparableBlockScan / LocalRecallIsland / ControllerKernelPack / PacketRouter1bit) | KERNEL_IMPLEMENTATION_POSTURE = "canonical_target_not_implemented_here". Doctrine targets only. | **Build target:** Phase 3 Pro Research (Ceiling work). **Re-promotion trigger:** Terminal F falsifier PASS unlocks the next kernel; M2 Max scale-validation rig available. | `docs/audits/V6_1_LEAN_REALITY_MATRIX_2026_05_06.md`. |
| **D-09** | F-70B-Local-Cocktail composition study (T23) | Pro Research; Pro Vault-Preserved. Multi-week harness. | **Build target:** Ceiling work, post-Floor. **Re-promotion trigger:** "RESUME F-70B" codeword + dedicated rig time. | T23 register row + chronicle. |
| **D-10** | Per-IR Lean proofs (W-45) — currently 28 sorries, budget ≤ 149 | Lean toolchain + multi-week proof work. | **Build target:** Pro Research ongoing. **Re-promotion trigger:** "RESUME LEAN PROOFS" codeword. | T5 ships 28 sorries; budget-gated; lake build green. |
| **D-11** | Simulation Mode v1.7+ (custom-drawn body grammars Block/Sage/Orb + Hermes Snake renamed → LocalAgent Snake; LoRA adapter swap; deterministic PRNG idle walking) | Asset pipeline + custom renderers + LoRA-light research spike not yet built. CompanionAssets/ exist in worktree but Hermes/ Swift files contradict purge. | **Build target:** Phase 3 polish (Ceiling). **Re-promotion trigger:** assets-only extraction from `.claude/worktrees/simulation` (skip Hermes Swift files) OR full simulation feature session. | `simulation` worktree preserved. Memory `project_simulation_mode_doctrine`. |
| **D-12** | Quick Capture Pro-only tools (55 commits in vigorous-goldberg: action_bash, browser_*, apple_*, system_*) | Pro-only — deferred per MAS-First doctrine. `#[cfg(feature = "pro-build")]` stubs preserve geometry. | **Build target:** when Pro release moves from stub → live. **Re-promotion trigger:** "RESUME PRO TOOLS" codeword OR user decision to ship Pro. | `.claude/worktrees/vigorous-goldberg-3a2d35`. Memory `project_quick_capture_salvage_triage`. |
| **D-13** | NightBrain — 4 missing eligibility conditions (flagged-notes / 1-5 AM window / 12h cooldown / no-active-agent) + 6 NoOp task bodies (dedupe_artifacts / cloud_knowledge_distillation / nano_continual_step / heal_event_retention / etc.) | V1 = diagnostic-only by design. V1.x lands first real body + widens eligibility. | **Build target:** V1.x post-Floor. **Re-promotion trigger:** user opens NightBrain V1.x session. | `agent_core/src/nightbrain/{mod.rs, live.rs}` 949 LOC skeleton + `docs/NIGHTBRAIN_SCHEDULER_POLICY_2026_05_15.md`. |
| **D-14** | Custom local model (deferred from 2026-03-27 release pivot — ship Qwen as base; keep Knowledge Fusion; defer custom model) | Release pivot decision. | **Build target:** post-v2.0 tag (after current substrate close). **Re-promotion trigger:** user has Qwen baseline solid + wants custom model. | Memory `project_release_pivot`. `feature/knowledge-fusion-v1` tag preserved. |
| **D-15** | T10B Eidos Form Layer (EidosKind 13 kinds + stable ID via BLAKE3 + canonicalization + compatibility mapping to ClaimKind/VRM/TypedArtifact) | Small-medium follow-up; gates on T10 DONE (which is done). | **Build target:** Terminal A sibling work OR Phase 2.5. **Re-promotion trigger:** Eidos bridge needs typed EidosKind. | Chronicle §2 T10B row. |
| **D-16** | T15 Executor Trait + MissionPacket + ExecutorEvent + mock | Small-medium. Gates on T11 (done). | **Build target:** Terminal C System G full path naturally needs this. **Re-promotion trigger:** Terminal C launches. | Chronicle §2 T15 row. |
| **D-17** | T16 Live File Compiler (10-state machine + LivePlan.v1 + plan-hash + capabilities + triggers + eligibility + revocation tests) | FINAL_SYNTHESIS §1 breakthrough; new surface. | **Build target:** Phase 3. **Re-promotion trigger:** "RESUME LIVE FILE COMPILER" codeword. | Chronicle §2 T16 row. |
| **D-18** | T17 Cognitive Weight Class Enforcement (4 weight bands + 5 promotion gates + tests for invalid policy promotion / missing signed plan / missing diff / revocation) | Gates on T11 (done). | **Build target:** Phase 2.5 side-task. **Re-promotion trigger:** Terminal D needs Cognitive Weight badges (W-30). | Chronicle §2 T17 row. |
| **D-19** | T18 Residency Governor (full Settings diagnostics + L4-L6 emission rules) | Gates on T3 partial merge. | **Build target:** when T3 wiring complete (Terminal A + G land). **Re-promotion trigger:** ResidencyTier indicator UI demanded. | Chronicle §2 T18 row. |
| **D-20** | T19 Halo V1 + Eidos Control Vectors (adapter maps Eidos signals → Halo availability state; no UI behavior change without feature flag) | Gates on T10 (done). | **Build target:** Terminal A sibling. **Re-promotion trigger:** Terminal A Eidos bridge complete. | Chronicle §2 T19 row. |
| **D-21** | T24 Lean ClaimLedger Schema Authority (Lean enum/theorem family + Rust + Swift schema twins round-trip + sorry budget monotonically tracked) | Lean toolchain. High-risk. | **Build target:** Pro Research. **Re-promotion trigger:** "RESUME LEAN AUTHORITY" codeword. | Chronicle §2 T24 row. |
| **D-22** | W-09 Scan-IR ↔ SemiseparableBlockScan harness wiring (T3's iter-53 test refactored to consume `ScanIR::SemiseparableBlock { ... }` from T5's lane) | P3 Pro Research. | **Build target:** Terminal F sibling. **Re-promotion trigger:** Terminal F SemiseparableBlockScan falsifier. | T5 Scan-IR salvaged via PR #22; T3 harness pending. |
| **D-23** | W-18 EML confidence in timeline (per-event column tied to AnswerPacket.confidence) | P2; depends on W-08 + W-15. | **Build target:** Terminal C/D crossover. **Re-promotion trigger:** AnswerPacket badge work (W-27) lands. | Chronicle §3.2 W-18 row. |
| **D-24** | W-30 Cognitive Weight Class badges per `COGNITIVE_WEIGHT_CLASS_DOCTRINE_2026_05_04.md` (W1-W4 light/medium/heavy/extreme) | P2. | **Build target:** Terminal D side-task. **Re-promotion trigger:** Terminal D launches. | Doctrine doc lives in main. |
| **D-25** | W-31 Audio diagnostics panel (export gain / master volume / live-player chain / A/V health) | P3 polish. | **Build target:** post-audio-PR-56 follow-up. **Re-promotion trigger:** audio QA cycle. | Chronicle §3.6 W-31. |
| **D-26** | W-51 (T09 block) Pro-tier capability gating in omega-mcp dispatch (F-OmegaMCP-ProToolGating) | Pro-deferred. F-OmegaMCP-ProToolGating unfalsifiable today because feature doesn't exist. | **Build target:** when Pro tools land (D-12 re-promotion). **Re-promotion trigger:** Pro-build feature flag activated. | Chronicle §3.9 W-51 row. |

## The promise

Every row above is **either build-targeted to a known terminal/cycle OR has an explicit re-promotion codeword.** Nothing is allowed to be silently dropped.

The `LEGENDARY` codeword check (per [docs/LEGENDARY_CODEWORD_2026_05_23.md](docs/LEGENDARY_CODEWORD_2026_05_23.md)) MUST verify this register at step 5 of its 7-step protocol. Any deferred item missing a build-target window is a doctrine violation.

## Re-promotion codewords (already-defined or newly-defined here)

| Codeword | What it triggers | Targets |
|----------|------------------|---------|
| `LEGENDARY` | Full no-compromise architecture check + dispatch the 7-terminal deck | All Floor work |
| `RESUME SUBSTRATE V2` | Continue post-recovery V2 plan (V2.1-V2.7) | Memory `project_post_recovery_v2_plan` |
| `RESUME PRO RESEARCH` | V3 Pro Research work | Memory `project_post_recovery_v2_plan` |
| `RESUME RESEARCH TIER` | Legacy alias for `RESUME PRO RESEARCH`; do not use in new docs | Memory `project_post_recovery_v2_plan` |
| `RESUME XPC MASTERY` (NEW) | XPC 5-service decomposition | D-03 |
| `RESUME T20` (NEW) | Variant Ladder Generalization | D-05 |
| `RESUME L_SE RESEARCH` (NEW) | L_SE Self-Evolving Adapter Lane | D-06 |
| `RESUME F-70B` (NEW) | F-70B-Local-Cocktail composition study | D-09 |
| `RESUME LEAN PROOFS` (NEW) | Per-IR Lean proofs (W-45) | D-10 |
| `RESUME PRO TOOLS` (NEW) | Quick Capture Pro-only tools | D-12 |
| `RESUME LIVE FILE COMPILER` (NEW) | T16 Live File Compiler | D-17 |
| `RESUME LEAN AUTHORITY` (NEW) | T24 Lean ClaimLedger Schema Authority | D-21 |
| `FORK V3` (NEW) | Second-repo end-game from post-v2.0 main | Per `NO_COMPROMISE_ENDGAME_PROMPT_DECK_2026_05_18.md:97` |

## Loss-prevention guarantees

1. **No deferred item ages out of memory.** Every item has a memory entry OR a doc-trail reference.
2. **No deferred item loses its build-target.** If a target dissolves (e.g. terminal completes without picking up the item), the item must be re-targeted to a new terminal/cycle within 1 session.
3. **No deferred item gets re-classified to "dropped" without explicit user sign-off.** The user must say "drop D-N" explicitly; assistant-side decisions cannot retire deferred work.
4. **Every `LEGENDARY` run must produce a deferred-work delta report.** Any deferred item that newly missed its build-target window gets surfaced.

This doc is the ground truth for "what was deferred that must still be built." Treat it as the anti-amnesia ledger.
