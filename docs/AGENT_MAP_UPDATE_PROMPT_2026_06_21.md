# Map-Update PROMPT (2026-06-21)

Paste to the build agent so it ingests the latest directives, updates its living implementation map, and
slots each into the plan where it belongs. GROUND IN FILES; no fake-done; never delete the chat.

---

You are updating the Epistemos build's living MAP + plan to absorb the latest 2026-06-21 owner directives
(cwd /Users/jojo/Downloads/Epistemos). Read files before claiming; cite file:line. This is a MAP/PLAN
update pass (re-slot + record); do real work where it's the current phase, otherwise record as ordered plan
items — never as a stash, never labeled "deferred"/droppable.

## STEP 1 — Read the latest directives (they may be newer than your last map)
- docs/OSAURUS_P3_IMPORT_PLAN_2026_06_21_addendum.md — READ ALL OF IT, especially the newest sections:
  ACT reskin = current-chat-UI discipline (fonts/palette/composer); PRESERVE model picker + command palette
  + 38-tool agent panel; Tamagotchi agent-creation (keep + render-fix); per-clone SETTINGS as executive tabs
  (Epistemos|act|work|beyond); landing BLUR transitions; MODE-ENTRY animations (act=native blur-reveal;
  work=ASCII/pixel typewriter + full-page reveal); MOTION LANGUAGE TRIAD (Apple blur + ASCII typewriter +
  micro-motions on titles + display-only, noticeable-not-bloated, never in editors); EPDOC MD-V2;
  per-clone settings; substrate+IP = certain/lower-not-deferred.
- docs/AGENT_DIRECTIVE_CHECK_PROMPT_2026_06_21.md (the 15 directive areas)
- docs/CHAT_BACKEND_QUARANTINE_NEVER_DELETE_2026_06_21.md
- memory: project_osaurus_full_clone_directive_2026_06_21
- your current map: docs/OSAURUS_BUILD_PROGRESS_2026_06_21.md

## STEP 2 — Update the living MAP (docs/OSAURUS_BUILD_PROGRESS_2026_06_21.md)
For each directive/feature, add/refresh a row: item | phase | status (✅/🟡/🔴) | file evidence | next action.
Make sure EVERY new directive above appears in the map (UI discipline, picker/palette/agent-tools preserve,
Tamagotchi keep+fix, per-clone settings tabs, landing blur, mode-entry animations, motion-language triad,
EPDOC MD-V2). If something isn't started, it's 🔴 with a placed plan slot — not omitted.

## STEP 3 — SLOT each into the plan/build order (record where it belongs)
Place each in the correct phase (do NOT pull UI ahead of the engine; do NOT drop anything):
- Current phase = ACT engine: finish linking OsaurusCore (Osaurus-first). Keep going.
- ACT surface phase: current-chat-UI reskin discipline + preserve picker/palette/agent-tools + Tamagotchi
  keep+render-fix + act landing/mode-entry blur animations.
- MOTION LANGUAGE TRIAD = CROSS-CUTTING: apply to titles + display-only surfaces AS each surface is built
  (a standing rule on every new view, not a one-time task) — noticeable, not bloated, never in editors.
- WORK phase: OpenCode shell (real terminal UI palette-matched, named "work") + Goose/Hermes/OpenClaw fuse
  + RustLSP; work landing/mode-entry ASCII typewriter animation; per-clone settings tab for work.
- PER-CLONE SETTINGS tabs: add Epistemos|act|work|beyond as each clone lands.
- EPDOC MD-V2 + substrate-health + IP-repair = sequenced LOWER but CERTAIN (record as ordered ledger items,
  not "deferred"/droppable).
Add anything new to docs/OWNER_REQUESTS_LEDGER_2026_06_18.md as ordered items if not already tracked.

## STEP 4 — Report
Return: the updated map (or its diff), where each new directive was slotted in the build order, anything that
needs an owner decision, and confirm nothing was dropped, stashed, or fake-marked done. Then continue the
current phase (link OsaurusCore) — no input needed.
