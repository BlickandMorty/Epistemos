# SUPERSESSION-GAP concrete plans (S16, 2026-06-19)

Read-only research (subagent), code-grounded. Feeds DEEP_PLAN_AUDIT_HUB. Turns the 4 UNMAPPED
gaps (S5/S7 G-5) into buildable specs. All confirmed absent from the ledger.

## Shared constraints (verified)
MAS entitlements (`Epistemos-AppStore.entitlements`): sandbox ON, app-group `group.com.epistemos.shared`,
user-selected file RW + app-scope bookmarks, **`network.client` only — NO `network.server`** (no inbound
TCP listener on MAS), no subprocess. **Vault `.md` files are DERIVED export; SwiftData is the source-of-truth**
(`VaultSyncService.swift:8-15`) — the biggest sync constraint. **No HTML→Markdown converter exists** (liteparse
is PDF-only; `web_fetch.rs:130 html_to_text` is a lossy stripper). **MCP today is in-process FFI dispatch, NOT
a hosted server** (`omega-mcp StdioServer` is test-only, no `[[bin]]`/main; "Swift owns MCP hosting" has no code).

## GAP 1 — WEB CLIPPER → `R-WEBCLIP`
Two-tier, one HTML→md core. **MAS tier (ship first):** a macOS **Share Extension** xcodegen target `EpistemosClipper` (`type: app-extension`, share-services, accepts url+html) — mirror `EpistemosWidgets` (`project.yml:251`); writes the clip into the app-group container; the app's file-watcher (`VaultSyncService.startFileWatcher:3573`) drains it into an `SDPage`+`.md` (reuse the `LiteParsePDFImportController.importPage` precedent → new `WebClipImportController`). **In-app tier (Pro):** clip the current page — downstream of the (nonexistent) browser host. **HTML→md (real new work):** vendor a pure-Rust readability + html2md crate into `agent_core`, `#[uniffi::export] html_to_markdown(html,base_url)` beside liteparse (ProvenanceGate); OR inject Mozilla-Readability JS in the extension's WKWebView → clean HTML → Rust md. Frontmatter: source_url/clipped_at/title/author. Honesty: real source_url+timestamp; on readability-fail fall back to html_to_text labeled "raw extraction", never fabricate author/date. Effort: medium. MAS core (share-ext) + Pro (full-page/SPA/auth capture).

## GAP 2 — EXPOSE VAULT OVER MCP + AGENTS.md (anti-Tolaria) → `R-VAULT-MCP-SERVER`
Server side does NOT exist (in-process dispatcher + outbound URL directory only). **Add `omega-mcp/src/bin/epistemos_mcp_server.rs` (`[[bin]]`)** wrapping the existing `StdioServer` (`transport.rs:84`) around the ALREADY-BUILT `MCPDispatcher` + `VaultExecutor` (`vault.rs:16`, path-traversal-hardened :33). Expose read_file/write_file/list/create_note/vault_search — **no new tool logic, just route stdio JSON-RPC into existing executors.** **Transport = stdio not TCP** → dodges the missing `network.server` entitlement (external CLI spawns the binary as a child, speaks JSON-RPC over stdio, the Claude Desktop/Codex convention; binary runs OUTSIDE the sandbox, app stays in-process). **AGENTS.md auto-gen:** extend `AgentBackend.fileName(forBackendID:)`/`RuntimeBootstrapWriter` to drop `AGENTS.md`+`.mcp.json` at vault root on open (Tolaria's killer move, pointed at Epistemos's vault). In-process brain stays default (additional door, not replacement). Gating: **Pro** (subprocess-spawned server). Honesty: external writes flow through the SAME `AgentToolProvenanceRecorder` + path guard + approval gate — no privilege escalation. Effort: **medium-LOW** (dispatcher+executors+stdio server already exist — wiring a binary + a generator). **One of the 2 highest-value/lowest-risk wins; directly counters Tolaria.**

## GAP 3 — MULTI-DEVICE SYNC → `R-SYNC`
The `.md` files are DERIVED, so you can't just sync the folder (diverges from SwiftData). **Option A (recommended, local-first):** vault inside an **iCloud Drive** ubiquity container + **invert the source-of-truth so `.md` files become SoT** (SwiftData/`.epcache` become rebuilt-from-disk derived indexes) — aligns with Obsidian + reuses the existing `syncFromVault()→[VaultSyncConflict]` reconciler (`VaultSyncService:3133`); observe via `NSMetadataQuery`; **`.epcache`/SQLite must NEVER sync** (per-device, rebuild-on-open). **Option B (CloudKit):** `NSPersistentCloudKitContainer` (models already "CloudKit-compatible") syncs the true store but needs the entitlement + loses the open-file-vault differentiator — avoid as primary. **Option C (Pro git lane):** `vault_git.rs` (git2) already exists as an agent tool → "commit/push vault to git remote" Obsidian-Git parity. **Recommend A default + C Pro lane; avoid CloudKit primary; CRDT overkill.** Honesty: `.epcache` never syncs (show in health row); conflicts via `VaultSyncConflict` UI never silent-LWW; "eventual not real-time", no Logseq-RTC claim. Effort: **HIGH** (SoT inversion touches `VaultSyncService` 176KB, highest blast radius) — **sequence AFTER the engine-toggle/tools work**.

## GAP 4 — LIVE ARTIFACTS / dashboards → `R-LIVE-ARTIFACTS`
`ArtifactHostView` is an honest stub (every route → "Deferred in v1", 0 refs); `ArtifactRoute` has 6 cases, no htmlWorkspace/dashboard. The REAL extensible surface exists: **HTMLWorkspace** (`HTMLWorkspacePackage` w/ manifest+index.html+**data.json**; `HTMLWorkspaceDocument` + `HTMLWorkspacePatchRouter` 24KB; `HTMLWorkspacePreviewView` WKWebView). json-render/htmlstream/Streamdown are docs-only PATTERNS (no code to lift). **Revive don't rebuild:** add `ArtifactRoute.htmlWorkspace(id)` + render `HTMLWorkspacePreviewView` instead of the deferred panel; subscribe `data.json` to a vault/query source (saved RRF/`fusedSearch` query, or DAG/provenance feed) → on change write new data.json → `HTMLWorkspacePatchRouter` patches the live WKWebView (no reload) = the Claude-Artifacts connected-view behavior on existing infra. Seed table/dashboard/chart templates (json-render idea as Swift/WebKit data-binding, NOT lifting Vercel React). Gating: local package + local data.json = **MAS core**; external feeds = **Pro**. Honesty: show data provenance + last-refresh; failed/stale source shown explicitly (no fake-live numbers); only flip a route live when its resolver renders real data (T4). Effort: **medium-LOW** (renderer/package/patch-router exist). Risk: WKWebView teardown leak on a 2nd surface (reuse shared pool + `dismantleNSView`). **The other highest-value/lowest-risk win; counters Claude Artifacts.**

## Sequencing
`VaultSyncService` + `ChatCoordinator`/`InferenceState` are convergence points. **R-SYNC = highest blast radius, do AFTER engine-toggle/tools.** R-WEBCLIP / R-VAULT-MCP-SERVER / R-LIVE-ARTIFACTS are independently shippable on existing seams. **Two lowest-risk highest-supersession wins: R-VAULT-MCP-SERVER (mostly wiring; counters Tolaria) + R-LIVE-ARTIFACTS (revive orphaned surface; counters Claude Artifacts).**

Key files: `Epistemos-AppStore.entitlements` · `project.yml:251` (widgets target pattern) · `Engine/VaultSyncService.swift` · `Engine/AppGroupContainer.swift` · `Engine/LiteParsePDFImportController.swift` · `agent_core/src/{liteparse.rs,web_fetch.rs:130,storage/vault.rs:646,storage/vault_git.rs}` · `omega-mcp/src/transport.rs:84` + `bin/` · `Bridge/MCPBridge.swift` · `Models/{ArtifactRoute,HTMLWorkspacePackage}.swift` · `Engine/HTMLWorkspaceDocument.swift` + `HTMLWorkspacePatchRouter.swift` · `Views/HTMLWorkspace/HTMLWorkspacePreviewView.swift`.
