# SS-A — Simplify + robustify + connect the cloned-app settings (2026-06-19)

Read-only research (subagent). Feeds SETTINGS_SIMPLIFICATION_HUB. Honors the owner OVERRIDE+BALANCE
(surface ALL, reskin, never hide/delete; simple auto-defaulted front + full under "Advanced";
progressive-disclosure ≠ hiding).

## The machinery already ships (wiring, not invention)
- **Triad:** `<Engine>GateStatus` (pure, always-compiled, MAS "Pro only") + `<Engine>HealthRow` (read-only, value only `if isActive` under `#if !EPISTEMOS_APP_STORE`) + mount in `SubstrateHealthPanel`→"Agent Runtime". Live: ActOsaurus/WorkBackend rows.
- **Progressive-disclosure primitive EXISTS:** `SettingsDisclosureSection` (`SettingsSurfaceComponents.swift:136-189`, collapsed-but-reachable) + `Section(isExpanded:)` (SubstrateHealthPanel already defaults its floor COLLAPSED) — THIS is the literal "Advanced" container the BALANCE note asks for.
- **Pixel chrome:** `SettingsFeaturedPixelPanel`/`.pixelPanel`/`VerifiedFloorChipStrip`. **Native absorbers (simple-front destinations):** `ModelStackSettingsView` (advertise-toggle), `AuthoritySettingsView` (allow/ask/deny), `SkillsSettingsView` (install→quarantine→promote), `ChannelsSettingsView`. **WebKit reskin injector** for OpenClaw's form: `EpistemosWebTheme.applyScript(for:namespace:)`.
- **HIGHEST-LEVERAGE cross-cutting (all 5 clones need it):** consolidate the scattered MCP-install (today across `AgentControlSettingsView`/`AgentBlueprint`/`AgentToolTogglePanel`) into ONE native panel — OpenClaw mcp/Osaurus /mcp/Goose extensions/DeerFlow MCP all register there.

## Per-clone (full surface → auto-default plumbing → simple front → Advanced → robustness → integration)
- **Osaurus (Act, MIT, :1337):** auto-default port `:1337`(loopback,read-only)/models-dir/SQLCipher/VM config/privacy-filter. SIMPLE front: engine on/off + model(→stack) + sandbox allow/ask/deny(→Authority). ADVANCED: server status, VM/Containerization (Pro "not live", not deleted), plugins, raw MLX knobs under `Osaurus · Advanced`. Robust: `isLive=false` until linked; `runTurn` throws not-GPT. Integration: model→stack, MCP→panel, plugins→Skills, sandbox→Authority+Sandbox-seam, status→ActOsaurusHealthRow.
- **OpenClaw (THE complex case, TS/Node, MIT, 33 SECTION_META):** sections = env/update/agents/auth/channels/messages/commands/hooks/skills/tools/gateway/wizard/meta/logging/browser/ui/models/bindings/broadcast/audio/session/cron/web/discovery/canvasHost/talk/plugins/diagnostics/cli/secrets/acp/mcp + per-platform channels + sandbox; ports gateway/18790/18791/18793/CDP 18800-18899. AUTO-DEFAULT/HARDCODE: ALL ports + gateway mode/bind/token (transport→in-process agent_core, no Node) + auth→Keychain + theme→pixel CSS + secrets→Keychain + sandbox→Sandbox-seam (owner never touches a port/key). SIMPLE front (~5): model(→stack)·channels(→Channels)·perms(→Authority)·MCP(→panel)·skills(→Skills). **ADVANCED (the key move): the full schema-driven config-form RESKINNED via CSS injection (`html[data-epistemos-skin="pixel"]`) in a WKWebView under `OpenClaw · Advanced` — reachable, never the raw 33-section wall up front, never deleted** (explicit reversal of S3 "hide config-form"). Robust: Node-dependent panes "Pro · Developer build only" (never fake), transport→in-process so chat works on MAS with no Node, persistent websiteDataStore, dismantleNSView teardown, new `OpenClawGatewayHealthRow`.
- **Goose/Open Code (Work, Apache-2.0):** auto-default provider/model(→stack, hardcode off Goose's)/telemetry-off/auto-update-off/secrets→Keychain. SIMPLE: engine on/off + model + permission_mode(→Authority). ADVANCED: extensions, recipes, repo-index/git/test-loop under `Goose · Advanced`; shell-exec Pro+hardened, shown. Robust: WorkBackendGateStatus already honest (inert until core, "Pro only", no fake). Integration: permission_mode→AuthoritySettingsView, recipes→SkillsSettingsView, extensions→MCP panel, status→WorkBackendHealthRow.
- **DeerFlow (Deep Research, wired E2E, no sidecar):** auto-default max_concurrency=3/keys→Keychain/Coder→Sandbox-seam/RAG→local-vault-auto-on-MAS. SIMPLE: Effort (folds Flash/Std/Pro/Ultra) + toggles (deep-thinking/background-investigation/plan-mode) + HITL-approval + web-vs-local-vault. ADVANCED: per-role model, search providers, raw concurrency, sub-agent depth under `Deep Research · Advanced`. Robust: fix `[id]`→`[source]` citations; web=Pro, local-vault=MAS; bounded spawn. Integration: models→stack, search/MCP→panel, skills→Skills, Coder→Sandbox-seam, status→DeepResearchHealthRow, modes→shared Effort.
- **Hermes (4 algorithm lifts, NOT a hosted app):** no foreign settings surface to preserve — 4 clean-room lifts into agent_runtime/LocalAgentLoop. Surface only as honest status + the existing Skills/memory rows. Robust: deterministic Swift compactor (model-call mid-turn deadlocks the MainActor client); session.search must crawl `<vault>/chats/` not `/sessions/` (OQ-1); folded-skills tier = CONTEXT not stable.

## Ordered plan
1. **Consolidate MCP-install into ONE native panel** (mirror Skills quarantine→promote) — highest leverage, all 5 clones register there. [L]
2. **PixelSkinTokens + EpistemosWebTheme.applyScript** (generalized injector) — prereq to reskin OpenClaw's form without hiding. [M]
3. **Define the reusable `EngineSettingsSection`** = curated native rows (model→stack, perms→Authority, skills→Skills, MCP→panel) + a `SettingsDisclosureSection("… · Advanced")` with the full surface — the literal BALANCE container (SettingsDisclosureSection already exists → composition). [M]
4. **Osaurus + Goose**: simple front (3 knobs each) + Advanced disclosures; status rows live; auto-default ports/dirs/keys. [M, lowest risk]
5. **DeerFlow**: Effort control + toggles + web/local-vault + `Deep Research · Advanced` + the `[id]`→`[source]` provenance fix. [M]
6. **OpenClaw (marquee)**: build-openclaw-ui-bundle.sh + scheme handler + WKWebView host; curated native front (5 knobs) + **full config-form reskinned (CSS) + reachable under `OpenClaw · Advanced`** + OpenClawGatewayHealthRow + Node panes "Pro · Developer build only". [L]
7. **Hermes**: the 4 lifts; surface via existing Skills/memory rows only. [L]
8. **Honesty/witness sweep**: VerifiedFloorChipStrip per engine setting; no green without a witness; value only when live; persistence survives relaunch. [M]

Governing (MASTER_SYNTHESIS §3): no cross-engine import; flag-OFF≠done; MAS-honesty firewall; never-delete/Pro-gate (show subprocess/port/VM knobs); WKWebView teardown; **progressive-disclosure ≠ hiding**.

Key files: `Views/Settings/{SettingsView,SettingsSurfaceComponents:136-189,SubstrateHealthPanel,ModelStackSettingsView,AuthoritySettingsView,SkillsSettingsView,ChannelsSettingsView,ActOsaurusHealthRow,WorkBackendHealthRow,AgentControlSettingsView}.swift` · `ActOsaurus/ActOsaurusGateStatus.swift` · `Work/WorkBackendGateStatus.swift` · `Theme/EpistemosTheme.swift` · `Views/Epdoc/EpdocEditorChromeView.swift` · upstream `~/Downloads/openclaw-main/src/config/*` + `ui/src/ui/views/config-form.render.ts:277-322`.
