# HARDENING_AUDIT.md — running tracker (enterprise hardening run, non-Goose scope)

> Started 2026-07-02 by the /loop hardening session (Claude, Fable). Scope: everything EXCEPT the Goose lane (owner instruction). Directive: EPISTEMOS_HARDENING_DIRECTIVE.md (canonical copy of "EPISTEMOS_HARDENING_DIRECTIVE (2).md").
> Rules: evidence or it didn't happen; every finding gets an ID + severity + boundary ID (from THREAT_MODEL.md once written); findings appear here even when fixed on sight.

## Session ground truth (Phase 0, captured 2026-07-02 evening)

- Branch: `feat/goose-surface` @ `e00edcbd9` ("fix: harden non-goose editor surfaces", 2026-07-02 17:05).
- Worktrees: main repo /Users/jojo/Downloads/Epistemos; sibling /Users/jojo/Downloads/Epistemos-cursor @ dc30f84b1 [cursor-work] (foreign lane — do not touch).
- Dirty files: 9 — 8 modified (Epistemos/App/RootView.swift + 5 Epistemos/Goose/*.swift + 2 EpistemosTests/Goose*Tests.swift) + 1 untracked doc (docs/handoffs/NOTE_GOOSE_MARKEDIT_AUDIT_PROGRESS_2026_07_02.md). All Goose-lane or shared; NEVER stage or revert these. NEVER `git add -A`.
- Toolchain: Xcode 26.4.1 (17E202), Swift 6.3.1, macOS 26.3.1. Directive targets Swift 6.2+ semantics — toolchain exceeds it; fine.
- Build system: xcodegen (`project.yml` is source of truth — NEVER edit Epistemos.xcodeproj directly). Schemes: `Epistemos`, `Epistemos-AppStore`. JS bundles: js-editor (tiptap, build-tiptap-bundle.sh), CoreEditor (build-coreeditor-bundle.sh), bundle-app-runtime-assets.sh. Rust: 13 crate roots (agent_core, bench, epistemos-code-index, epistemos-core, epistemos-research, epistemos-shadow, epistemos-vault, graph-engine, omega-ax, omega-mcp, substrate-core, substrate-rt, syntax-core) — no top-level workspace; build-rust.sh + build-*.sh wrappers.
- Entitlements: Epistemos/Epistemos.entitlements, Epistemos-Debug.entitlements, Epistemos-AppStore.entitlements (audit in flight).
- Prior related work on this branch (do not redo): e00edcbd9 non-goose editor-surface hardening + UI deep clean (handoff: docs/handoffs/NON_GOOSE_UI_DEEP_CLEAN_HANDOFF_2026_07_02.md — targeted suites green, FULL suite NOT rerun); f57e59417 earlier thermo-nuclear pass "complete"; c7def6a06/89329f7cd SAFETY-comment rounds; ee99e67ae prose scrollToCharacterOffset crash fix; c3cb25177 NSRange UTF-16 fix.
- Known open reliability issues inherited (from docs/APP_ISSUES_AUTO_FIX.md + docs/handoffs/GOOSE_HANG_AND_NOTE_FREEZE_HANDOFF_2026_07_01.md): startup main-thread hangs 969ms + 3182ms (ISSUE-2026-05-12-011, Instruments pending); note-open/create freeze (non-Goose, unsolved — MEASURE, don't guess; empty-seed blanks notes so load-before-mount); ~2GB idle RSS (partially fixed); graph pauseEngine doesn't free memory (deferred to graph plan).

## SCOPE RE-CLASSIFICATION (2026-07-02, D-009 owner pivot: remove Omega + local models)

Findings split by the keep/delete boundary. **Do NOT spend effort hardening MOOT (removal-target) findings — that code is being deleted.**

- **MOOT — removal targets (Omega / local-model; will be deleted, not fixed):** CONC-1 (Screen2AXFusion, Omega/Vision double-resume), CONC-3 (ReasoningLoopService, Omega — already reverted), the local-model gating issues (docs/APP_ISSUES_AUTO_FIX ISSUE-2026-05-16-015 RAM gate / canActAsAgent / MLX structured-tool), MLX idle-unload half of PERF-7, any computer-use/exec tool-surface items that are Omega-only. RUST-6 unhardened spawns are in agent_core/src/uas + falsifier (local-model research lanes) → likely deleted too.
- **STILL RELEVANT — kept surfaces (harden these):** MEET-1/2[done]/4/5/6/7/8 (meetings, Apple SpeechAnalyzer), CONC-5[done] (meeting mic), CONC-15 (EmbeddingService data race — uses Apple NLContextualEmbedding = KEPT), CONC-13 (SyntaxCoreService — code editor = kept), CONC-4 (approval continuation — cloud agent), CONC-2 (AgentQueryEngine — KEEP ONLY IF it's the cloud-agent harness, not Omega; reverted for now, confirm via scoping map), WEB-1..5 (editor/browser webviews = kept), BRW-1..4 + BUP-1/2 (browser = kept), ARX-1/2 (arXiv = kept), SEC-2/3[cloud]/4[minus SSMStateService which is removed], RUST-4[done]/5[done]/8[done] (cloud transport + vault = kept), F-0003[done] (Kokoro = kept).
- **CHANGES with removal:** SEC-1 (dangerous entitlements) — allow-unsigned-executable-memory becomes REMOVABLE once MLX is gone (a removal-plan deliverable, not a separate fix); allow-jit re-evaluate. SEC-4's SSMStateService:354 log site disappears with the file. AX/ScreenCapture/AppleEvents entitlements + privacy strings (direct lane) go with Omega.
- Scoping agent (dispatched 2026-07-02) is mapping the exact removal surface + phased plan → present to owner for sign-off before any deletion.

## Findings

| ID | Sev | Boundary | Area | File | Description | Status |
|----|-----|----------|------|------|-------------|--------|
| F-0001 | LOW | — | repo hygiene | EPISTEMOS_HARDENING_DIRECTIVE.md | Directive referenced at canonical root path did not exist; only "EPISTEMOS_HARDENING_DIRECTIVE (2).md" (browser-duplicate name) was committed. Loop prompts + Appendix A verifier would fail to find it. | FIXED on sight: canonical file created = verbatim (2) content + scope note (this session). UNVERIFIED: not yet committed. |
| F-0002 | LOW | — | repo hygiene | .claude/launch.json | Committed launch config points at a session-scratchpad path under /private/tmp/claude-501/.../june-audit-harness/serve.mjs (port 8931) that will not exist after that session; dead config in repo. Goose/june-lane artifact — flag only, do not edit (out-of-scope lane). | DOCUMENTED (owner/goose lane to clean) |
| F-0003 | CRITICAL | — | MAS release gate | LocalPackages/KokoroPipeline (KokoroSynthesisExecutor.swift:654, MLMultiArrayHelpers.swift:84-85) | Release/AppStore build FAILS: Swift `Float16` is unavailable on x86_64 macOS; the AppStore Release config builds universal, so the x86_64 slice of KokoroPipeline (Plan 3 voice) cannot compile → App Store submission impossible. Debug hid it (ONLY_ACTIVE_ARCH=true → arm64 only). Pre-existing app defect (not session regression). Failure scenario: any archive/submit attempt dies; Intel users could never receive the app. | FIX APPLIED this session: fp16 fast path gated `#if arch(arm64)` (generic NSNumber path already correct on Intel); `MemoryLayout<Float16>.size` → `MemoryLayout<UInt16>.size` (same 2-byte width). VERIFIED (implied, strong): the AppStore Release rebuild (bz5tykyb3) compiled PAST the previously-failing KokoroPipeline x86_64 slice into the main app target (the x86_64 Float16 errors were IN KokoroPipeline, a SwiftPM dep compiled before the app target; it did not re-fail there) — 0 errors across the build so far. Definitive `BUILD SUCCEEDED` stamp still pending (build in final Swift universal-compile phase, 33min elapsed). Note: Epistemos/Engine/Mamba2ForwardPass.swift Float16 uses verified already guarded `#if arch(arm64)` (lines 86-310). |

| SEC-1 | HIGH | B6 | entitlements (direct-dist lane) | Epistemos/Epistemos.entitlements:11-26 | Direct-distribution Release stacks: no app-sandbox + cs.disable-library-validation + cs.allow-unsigned-executable-memory + network.server + apple-events. Any same-user process could inject an unsigned dylib into the hardened-runtime app and reach Keychain-resident credentials/automation grants. NOT the MAS lane (MAS entitlements are clean of both). | OPEN (Phase 4). Caution before fixing: MAS lane scrubs llama.framework/omega_ax but the direct lane keeps them — verify llama.framework signing before dropping disable-library-validation; MAS proves MLX itself needs neither. |
| SEC-2 = MEET-3 | HIGH | — | MAS meetings ship-blocker | Epistemos/Epistemos-AppStore.entitlements (+ Epistemos.entitlements) | MAS build declares NSMicrophoneUsageDescription and ships UN-gated mic features (LiveVoiceInputService, MeetingNoteCaptureService, EpistemosSpeechAnalyzer — 0 EPISTEMOS_APP_STORE gates) but lacked com.apple.security.device.audio-input → sandbox denies mic; dictation/meeting capture silently dead in the MAS build. | **FIX APPLIED** 2026-07-02: added `com.apple.security.device.audio-input`=true to Epistemos-AppStore.entitlements (sandboxed MAS, the blocker) AND Epistemos.entitlements (direct Hardened-Runtime Release). Debug lane left as-is (non-sandboxed, mic already works via TCC). Verify PENDING: `codesign -d --entitlements :- Epistemos.app` on an AppStore-config build shows the key + mic TCC prompt on record. (Info.plist NSMicrophone/NSSpeechRecognition strings already present.) |
| SEC-3 | MED | B5 | network hygiene | Epistemos/Engine/LLMService.swift:1822,1893,1977 | Cloud LLM requests use URLSession.shared with no explicit timeouts (Arxiv/BrowserUse/MCPRegistry do set them); a hung POST/trickling SSE can occupy a turn up to the 7-day default resource timeout. | **FIX APPLIED 2026-07-03 (pending build sec3-build.log):** LLMService's `urlSession` default was `.shared` (no timeouts, 7-day resource default). Added `static let defaultCloudSession` = a dedicated `URLSession` with `timeoutIntervalForRequest = 120` (idle-between-bytes cap — a healthy SSE sends tokens/keepalives well within it) + `timeoutIntervalForResource = 1800` (hard 30-min cap per turn, generous for long agent turns), and changed the init default to it. A hung POST / stalled SSE now fails after ≤120s idle instead of occupying a turn indefinitely. (Note: the audit's line refs 1822/1893/1977 were stale — the transport moved to `URLSessionTransportSupport.streamSSE(using: urlSession)`; the session default at CloudLLMClient init was the real source — `LLMService.swift:514`, class `CloudLLMClient` not `LLMService`; static made `nonisolated` so it's usable as a default arg.) **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| SEC-4 | LOW | B6 | log privacy | EidosBridge.swift:145; EidosWiring.swift:235-239; VaultRecallWiring.swift:597,647-651; QueryRuntime.swift:408,429,449; SSMStateService.swift:354,113,140 | User search queries + vault note filenames logged with privacy: .public → user content in unified log/sysdiagnose. (Counter-verified: no keys/transcript content logged anywhere.) | **FIX APPLIED 2026-07-03 (pending build sec4-build.log):** flipped all 12 user-`query`/`trace.query` interpolations from `.public`→`.private` across EidosBridge(1), EidosWiring(3), VaultRecallWiring(5), QueryRuntime(3) — verified 0 `(query, privacy: .public)` remain; diagnostic counts/latency/manifest/backend/tier kept `.public` (not user content); no note-filename `.public` sites in these files. SSMStateService sites moot (file deleted in cloud-only removal). Verify: build green. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| SEC-5 | LOW | — | entitlement hygiene | Epistemos/Epistemos.entitlements:27-30 | temporary-exception.mach-lookup (com.apple.accessibility.api) + sandbox-scoped keys in a file with NO app-sandbox — inert, misstates profile. | **LARGELY RESOLVED (verified 2026-07-03):** the risky/misleading `temporary-exception.mach-lookup (accessibility.api)` + apple-events were REMOVED during the Omega removal — current Epistemos.entitlements has NONE of them. Residual: `files.bookmarks.*`/`files.user-selected.read-write` are inert in this non-sandbox Developer-ID file (MAS review uses Epistemos-AppStore.entitlements, not this one). Left as-is — removing signed entitlements is risky for a cosmetic, non-shipping-lane cleanup; documented not-worth-the-risk. |
| SEC-6 | LOW | — | widgets target | project.yml:331-370 | EpistemosWidgets app-extension has no entitlements file and is NOT embedded in either app — harmless today; blocks shipping widgets later. | OPEN (Phase 7 dead/half-wired inventory). |
| SEC-7 | LOW | — | debug leftovers | WorkOpenGUISupervisor.swift:692; EditorBundleHealthRow.swift:577,592 | Hardcoded dev paths (~/Downloads/Epistemos/.research-clones fallback; /Users/jojo in #Preview) compiled into production; username leakage in binary. | **LARGELY MITIGATED (verified 2026-07-03):** the `EditorBundleHealthRow`/`BackgroundIndexingHealthRow` `/Users/jojo/...` paths (577/592-593) are already inside a `#if DEBUG` `#Preview` block → NOT in release. The only other app-code hit is a source COMMENT in EpdocQuery.swift:7 (stripped from binary). `WorkOpenGUISupervisor.swift:692` is Work-owned (protected — left as-is; noted). No ungated user-path string literals ship in release. |
| SEC-8 | LOW | — | MAS app group | Epistemos-AppStore.entitlements:7-10 | group.com.epistemos.shared without TEAMID prefix — may fail MAS provisioning/validation depending on registration. | OPEN (Phase 4; needs App Store Connect check — owner-side). |

Verified-safe baseline from SEC sweep (evidence in agent report, 2026-07-02): ATS untouched/0 exceptions; 0 real cleartext endpoints (25 http:// hits = loopback/validators, incl. DNS-rebinding defense WorkNativeMCPServer.swift:236); 0 real secrets repo-wide (strict-shape sweep = fake redaction fixtures only; PEM=0); Keychain = kSecUseDataProtectionKeychain + AfterFirstUnlockThisDeviceOnly, non-synchronizable; no telemetry SDKs/endpoints (only a tracker BLOCKlist); NSLog=0, production print≈0 (#Preview only); ITSAppUsesNonExemptEncryption=false both plists; PrivacyInfo.xcprivacy tracking=false + 4 valid required-reason codes; MAS lane compiles out ScreenCapture/AX/AppleEvents (#if !EPISTEMOS_APP_STORE + Scrub Pro Frameworks phase); Rust cloud calls have 300s timeouts + bounded retry; arXiv timeout 15s; Kokoro downloads SHA256-verified + cancellable; security-scoped start/stop 8/12 disciplined-paired. Known loopback listeners (B8 candidates for THREAT_MODEL): VaultMCPServer.swift:117, WorkNativeMCPServer.swift:106, WorkSPAServer.swift, CloudProviderAuthService.swift:514 (OAuth callback), BrowserUseSettingsStore.swift:38 (localhost:11434 default endpoint).

| WEB-1 | MED | B1 | Epdoc bridge webview | Epistemos/Views/Epdoc/EpdocEditorChromeView.swift:742 (Coordinator ~:797) | Epdoc editor webview exposes the `epdoc` bridge but has NO decidePolicyFor (default-allow navigation) — a main-frame nav to a remote origin could post contentDidChange (overwrite note) / storeImageAsset (bounded file writes). Defense-in-depth: openOnClick=false today. | **FIX APPLIED 2026-07-03 (pending build web1-build.log):** added `decidePolicyFor navigationAction` to the Epdoc Coordinator (mirrors WebKitCodeEditorView): `guard !isDetached` → cancel; allow only `scheme == "about" || scheme == epdocEditorURLScheme`; cancel everything else. A main-frame nav to a remote origin can no longer reach the epdoc bridge (contentDidChange/storeImageAsset). **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| WEB-2 | MED | B1 | release inspector | LocalPackages/MarkEdit/.../EditorViewController.swift:178 via MarkEditCoreEditorView.swift:114 (default md-notes path) | Vendored MarkEdit sets isInspectable=true UNCONDITIONALLY → Web Inspector enabled in release on the notes editor (reads/edits notes, probes native bridge). Our own webviews correctly default false. | **FIX APPLIED 2026-07-03 (pending build web-build.log):** `#if DEBUG`-gated the vendored line `EditorViewController.swift:178 webView.isInspectable = true` with a re-vendor justification comment → Web Inspector OFF in release, ON in Debug (dev unaffected). Grep confirms no other ungated `isInspectable = true` in app/LocalPackages. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| WEB-3 | MED | B1 | CORS off on notes host | LocalPackages/MarkEdit AppRuntimeConfig.swift:253 (disableCorsRestrictions default true; our embedding passes no settings.json) | Default markdown editor webview built with _setCORSDisablingPatterns ["*://*/*"] → editor-context JS could read cross-origin responses; exfiltration amplifier compounding WEB-2/WEB-4. | **FIX APPLIED 2026-07-03 (pending build web3-build.log):** flipped MarkEdit `AppRuntimeConfig.disableCorsRestrictions` from default-`true` to default-`false` in BOTH the getter (`?? false`) and the default definition — the notes editor now ENFORCES CORS instead of calling `_setCORSDisablingPatterns ["*://*/*"]`. Local-content editor doesn't need cross-origin reads; this removes the exfiltration amplifier (compounds WEB-2/WEB-4). Vendored-MarkEdit edit w/ re-vendor justification comment. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| WEB-4 | MED | B1 | CSP missing | Resources/Editor/editor.html, Resources/Editor/code-editor.html, Resources/CoreEditor/index.html | Three bundled UI documents rendering user note/code content have NO CSP (HTML Workspace by contrast has a strong per-package CSP). No script-src/connect-src cap if any injection foothold appears. | **INVESTIGATED 2026-07-03 — deferred (needs validated rebuild + CSP tuning, not a blind edit).** Sources: `js-editor/` (→ editor.html + code-editor.html via build-tiptap-bundle.sh, npm/esbuild) + a CoreEditor source (→ index.html via build-coreeditor-bundle.sh). A CORRECT fix must: (1) add `<meta http-equiv="Content-Security-Policy">` to the SOURCE html (editing built artifacts is overwritten on rebuild), (2) tune script-src/style-src/connect-src to the editor's ACTUAL loads (custom scheme, inline bootstrap, KaTeX, bridge) — too strict silently breaks the editor, (3) rebuild via npm + load-test the editor. Can't load/test the editor in this headless env, so a blind CSP would risk breaking notes/code editing for users. Deferred as a validated-rebuild task. Defense-in-depth only — no known injection foothold; JS→native handlers already fail-closed (WEB baseline). |
| WEB-5 | LOW | B1 | JS literal escaping | Epistemos/Views/Epdoc/EpdocKaTeXPreview.swift:92-105 | Hand-rolled LaTeX escaping misses \r + U+2028/U+2029 (worst case: syntax error blanks preview; webview isolated, no handlers/nav/network). Aside: preview registers no scheme handler and only katex.min.css.br is bundled — KaTeX may never actually render (Phase 9 verify). | **FIX APPLIED 2026-07-03 (pending build web-build.log):** replaced the hand-rolled escaping in `EpdocKaTeXPreview.updateNSView` with the shared `jsStringLiteral(_:)` (EpdocEditorBridge.swift:768) which encodes `\ " \n \r \t U+2028 U+2029` — closes the \r + JS-line-separator gap. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |

Verified-safe baseline from WEB sweep (2026-07-02): 10 webview surfaces cataloged; NO loadFileURL/allowingReadAccessTo anywhere (path-guarded custom scheme handlers instead, traversal-rejecting); every JS→native handler fail-closed (typed enums, allowlists, bounded strings, isMainFrame checks; epdoc storeImageAsset content-hash-renames + ext-whitelists); Swift→JS structured/escaped everywhere except WEB-5; no shared process pools/data stores/handlers across trust domains; browser (BrowserView) + BrowserUse webviews have ZERO handlers and scheme/origin gates (BrowserURLGuard blocks data:/file:/javascript:; BrowserUse pinned to one loopback origin, isolated named store); HTML Workspace strongly CSP-sandboxed (default-src 'none', per-package opt-ins); no remote CDNs in bundled web assets; no web-storage secrets; A2UI + Arxiv have no webviews. Goose boundary fact: goose webviews use disjoint handler names + own stores; no sharing crosses the boundary (WorkSPASchemeHandler is shared stateless source only).

| MEET-1 | CRITICAL | B7 | meetings data loss | MeetingNoteCaptureService.swift:49,361-365 + TextCapturePipeline.swift:278 | Transcript hard-bounded to 10,000 chars PREFIX-kept; everything after ~12-15 min silently dropped while UI shows "Recording" + counting duration. Silent second-half loss of any real meeting. | **FIX APPLIED 2026-07-03 (deep-hardening wave, pending build meet1-build.log):** decoupled `MeetingNoteCaptureService.maxTranscriptCharacters` from the 10k `TextCapturePipeline.maxCleanedTextCharacters` (the per-capture cleaned-SUMMARY bound) → dedicated 2,000,000-char safety cap (~30+ hrs of speech; real meetings never hit it); `boundedTranscript` now appends a VISIBLE "[Transcript truncated …]" marker at the cap instead of silently prefix-dropping. Replaced the old test `transcriptBufferIsCappedToCapturePipelineEnvelope` (which ASSERTED the bug) with 2 tests: `longTranscriptIsNotSilentlyTruncated` (24k chars > old cap → full preserved) + `transcriptTruncationAtCapIsVisible` (>2M → visible marker). **VERIFIED 2026-07-03**: TEST BUILD SUCCEEDED; `xcodebuild test-without-building -only-testing:EpistemosTests/MeetingNoteCaptureServiceTests` → both new MEET-1 tests (`longTranscriptIsNotSilentlyTruncated`, `transcriptTruncationAtCapIsVisible`) PASSED; the only suite failure is the pre-existing documented F-STALE-1 stale-source-mirror test (unrelated — refactored button label + removed nav route, part of the 27-test stale cluster). No regression. |
| MEET-2 | CRITICAL | B7 | meetings data loss | TextCapturePipeline.swift:355-358 + MeetingNoteCaptureService.swift:280-284 | Note-persistence failure swallowed; finalize still sets .saved(pageID:"") → UI says "Saved note", disables Save/Discard, transcript gone on any SwiftData save error. | FIX APPLIED (UNVERIFIED — pending build): MeetingNoteCaptureService.finalize now guards `let noteID = result.createdNoteID, !noteID.isEmpty` else → .error state + throw persistenceFailed, transcript retained (finalSegments not reset → canSave true → retry works). Added regression test finalizeSurfacesPersistenceFailureWithoutLosingTranscript (fake runner returns nil createdNoteID → expects .error + transcript survives). Confirmed no existing test expects nil→saved (all use real in-memory contexts producing real page.id; discard/idempotent paths short-circuit before the guard). **VERIFIED 2026-07-02**: `xcodebuild test -only-testing:EpistemosTests/MeetingNoteCaptureServiceTests` → my regression test "finalize surfaces persistence failure and keeps the transcript recoverable" ✔ PASSED (0.003s); all finalize tests (drains/idempotent/discard) ✔ green; 19 of 20 suite tests pass. The 1 failure is unrelated (F-STALE-1 below). |
| F-STALE-1 | LOW | — | stale source-mirror test | EpistemosTests/MeetingNoteCaptureServiceTests.swift:619,625 (`uiRoutesStayInPlan3Surfaces`) | PRE-EXISTING failure (NOT a session regression): asserts source contains `Button("Discard Transcript", role: .destructive)` (refactored to `Button(discardConfirmationActionTitle, ...)` computed title) and `landing.contains("UtilityWindowManager.shared.show(.meetingNote)")` (removed by the home-embed migration — meeting is now a HomeEmbeddedPage; the .meetingNote window is now dead code per PLAN debt #3). Test name "hosted by Plan 3 window" itself describes the removed regime. | CLASSIFIED (Phase 4/7 batch): reconcile with home-embed reality + coordinate with dead `.meetingNote` window removal. Do NOT weaken; update assertions to current nav truth with justification. Feature behavior is fine (behavioral tests pass). |
| MEET-3 | HIGH | — | mic entitlement (extends SEC-2) | Epistemos.entitlements + Epistemos-AppStore.entitlements (both lack com.apple.security.device.audio-input) | Hardened-runtime Release (direct) AND sandboxed MAS build cannot capture mic — meetings records only in Debug. Flagship feature dead in every shipped build. | FIX THIS WAVE (add audio-input to both; verify codesign -d --entitlements + record smoke). Applying after in-flight build completes. |
| MEET-4 | HIGH | — | meetings UX data loss | HomeEmbeddedPage.swift:29-52 + MeetingNoteView.swift:50-52 | "← Home" chip mid-recording destroys the embedded view + @State service + unsaved transcript, NO confirmation (explicit Discard does confirm). | **FIX APPLIED 2026-07-03 (pending build meet4-build.log):** added `HomeEmbeddedLeaveGuardKey` PreferenceKey (HomeEmbeddedPage.swift) — child surfaces report unsaved work upward; the back-to-home chip now reads it via `overlayPreferenceValue` (MainActor body-eval, no `onPreferenceChange` @Sendable concurrency pitfall) and shows a "Leave \(title)? / Leave and Discard / Stay" confirmationDialog when set, calling `goHome()` only on confirm. MeetingNoteView reports `isRecording || canDiscard` (active recording OR non-empty unsaved transcript) via `.preference`. arXiv/browser default false (nothing to lose). Harmless where MeetingNoteView is used outside HomeEmbeddedPage (no reader). Verify: build green; manual/UI confirmation flow (unit-testing a confirmationDialog is not practical — logic `isRecording||canDiscard` is self-evident). **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| MEET-5 | HIGH | — | recording indicator + teardown | StatusBar.swift (absent) + UtilityWindowManager.swift:230,316-331 | No app-wide always-visible recording indicator; capture teardown relies solely on SwiftUI onDisappear inside a retained NSPanel (isReleasedWhenClosed=false, no windowWillClose hook). If onDisappear doesn't fire on orderOut → mic keeps recording after panel close (would be CRITICAL; needs runtime verification). | **RE-CLASSIFIED 2026-07-03 — the NSPanel teardown concern is MOOT (meeting is now the home PAGE, not the panel; panel unreachable — see MEET-6).** The home page tears down via `MeetingNoteView.onDisappear { service.tearDownCapture() }`, which fires reliably on a `homeContent` swap (view removed from hierarchy), so the mic stops on leave. MEET-4's leave-guard now also confirms before the back-chip discards a recording. **Residual (LOWER, documented):** (a) no app-wide always-visible recording badge (UX nice-to-have); (b) switching to another feature (sets `homeContent` directly, bypassing the back-chip guard) still tears down + discards an unsaved transcript WITHOUT confirmation — narrower than the fixed back-chip path. Follow-up: extend the leave-guard to feature-switch transitions + add a status badge. |
| MEET-6 | HIGH | — | dual-surface race | LandingView.swift:173-176 + UtilityWindowManager.swift:417-418 | Meeting page + meeting panel can run simultaneously; both poll LiveVoiceInputService.shared with destructive consumeTranscript() → final segments split nondeterministically between two transcripts. | **MOOT — re-verified 2026-07-03: the meeting PANEL is unreachable.** Grep confirms ZERO `show(.meetingNote)` / `(.meetingNote)` invocation callers in app code (the home-embed migration removed the trigger; `.meetingNote` is now dead code in UtilityWindowManager). Only the home PAGE (`HomeEmbeddedPage("Meeting"){MeetingNoteView()}` via `ui.homeContent = .meeting`) ever mounts a MeetingNoteView, so two capture services cannot poll `LiveVoiceInputService.shared` simultaneously. No dual-surface race. Residual: remove the dead `.meetingNote` UtilityPanel case (LOW cleanup, coordinate w/ F-STALE-1 test). |
| MEET-7 | MED | B6 | meeting content at rest | TextCapturePipeline.swift:333-336 + TraceCollector.swift:204-255 | Content-derived note title (first transcript line) persisted to traces/production/*.jsonl, no retention limit, no user delete. | **FIX APPLIED 2026-07-03 (pending build meet7-build.log):** `TextCapturePipeline.structureGenerated` embedded `title=\(title.prefix(80))` (meeting content) into the persisted trace `content`. Now hashes it: `content: "title_sha256=\(redactedTitleHash(title)) …"` (16-hex SHA256 prefix, or `(empty)`), preserving correlation without the user's words. Only trace event carrying capture content; the note itself legitimately keeps its title. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors** (+ regression test `structureGeneratedHashesTitleForTracePrivacy` added: asserts trace `content` has `title_sha256=` and NOT the raw title). **TEST PASSED 2026-07-03: TextCapturePipelineTests 52/52 green incl. the new privacy test.** |
| MEET-8 | MED | — | audio route change | EpistemosSpeechAnalyzer.swift:291-301 | observeRouteChanges is dead code (0 callers); AirPods connect/disconnect mid-meeting can silently stall transcription. | OPEN (Phase 9). |
| BUP-1 | HIGH | B8 | browser-use lifecycle | BrowserUseRuntimeSupervisor.swift:650-672 + applicationWillTerminate (absent hook) | Teardown = SIGTERM on python parent only (no waitUntilExit/SIGKILL/process group); Chromium children unmanaged; NO app-quit hook → quitting Epistemos orphans python+Chromium; 0600 env file with Keychain-sourced API keys survives crash/force-quit (cleanup only in stopLocked). | **FIX APPLIED 2026-07-03 (pending build bup1-build.log):** (1) **SIGKILL escalation** — `defaultLaunchProcess` handle now SIGTERMs then, off-thread (non-blocking, so onDisappear/quit don't hang), SIGKILLs after a 4s grace if `runtime.isRunning`. (2) **App-quit hook** — new weak `terminationRegistry` (NSLock-guarded; `registerForAppTermination()` on successful start) + `stopAllForAppTermination()` called from `EpistemosAppDelegate.performTeardown` (applicationWillTerminate) → SIGTERMs every live runtime + removes the env file on quit. (3) **Stale env cleanup** — `BrowserUseRuntimePaths.cleanupStaleEnvironmentFiles()` removes `.env` + orphaned `.env.*.tmp`; `purgeStaleEnvironmentFilesAtLaunch()` runs in `applicationDidFinishLaunching` so a crash-leftover 0600 keyed file never persists. **CAVEAT (honest):** macOS `Process` exposes no safe process-group kill (no public setpgid; `kill(-pid)` unsafe without a confirmed group-leader), so Chromium children are closed via browser-use's python SIGTERM handler (playwright shutdown) + the SIGKILL backstop, NOT a direct group kill — documented, not silently skipped. Lock order safe (registry lock released before `stop()` takes lifecycleLock). MAS-safe no-ops (empty registry / nil defaultPaths). **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED (0 errors); BrowserUseRuntimeSupervisorTests 25/25 passed incl. new `cleanupStaleEnvironmentFilesRemovesSecrets` (proves .env + .env.*.tmp removed, unrelated files kept).** Process-lifecycle parts (app-quit SIGTERM/SIGKILL) are code-inspected + non-blocking-by-design; runtime teardown is a documented manual check. |
| BUP-2 | MED | B8 | duplicate supervisors | BrowserUseWebUIView.swift:73 | New supervisor per view instance; page+panel = two supervisors on port 7788 — Stop in one can't stop the other; second Start health-probes green against foreign server. | **FIX APPLIED 2026-07-03 (pending build bup2-build.log):** added `BrowserUseRuntimeSupervisor.shared` singleton; `BrowserUseWebUIView.init`'s `supervisor` default is now `.shared` instead of a fresh `BrowserUseRuntimeSupervisor()` per view → page + panel drive ONE runtime/server on :7788, so Stop reaches the live server and a second Start no longer probes a foreign one. Tests still inject their own supervisor via the init param (unchanged). `.shared` works as a default arg because the class is `nonisolated` (not @MainActor). My BUP-1 app-quit registry now also has a single shared instance to tear down. Caveat: per-view UI state (readiness/loadedURL) stays view-local and converges via readiness polling; the underlying server is now singular. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| BRW-1 | MED | B2 | browser downloads | Views/Browser/BrowserView.swift:545-556 | No WKDownloadDelegate + no canShowMIMEType check → download links silently dead-end. | **FIX APPLIED 2026-07-03 (pending build brw1-build.log):** `decidePolicyFor navigationResponse` now routes `!canShowMIMEType` responses to `.download`; Coordinator adopts `WKDownloadDelegate` (both `didBecome download` overloads set `download.delegate = self`); `decideDestinationUsing` shows an NSSavePanel (sandbox-safe security-scoped URL), removes any pre-existing dest (WKDownload requires it), and reports progress/finish/failure via `tab.lastError`. Conformance forces the required-method signature to match (no silent non-conformance). **VERIFIED 2026-07-03: app target compiled 0 errors incl. BrowserView + the WKDownloadDelegate conformance (a signature mismatch would fail conformance) — the download save-panel UX is a documented manual check.** **REGRESSION CAUGHT + FIXED 2026-07-03 (regression pass):** my `download(_:didFailWithError:)` used `error.localizedDescription`, which BrowserPlan3Tests:79 forbids (browser uses privacy-safe `domain=…code=…` error formats, not raw descriptions that leak paths). Changed to `"Download failed (domain=\(nsError.domain) code=\(nsError.code))"` — AND reworded the explanatory comment (the source-guard does naive substring matching, so even the comment's literal `error.localizedDescription` tripped it). **RE-VERIFIED 2026-07-03: BrowserPlan3Tests now 6/7 — only the pre-existing stale-mirror :201 fails (home-embed regime change), my :79 regression is gone.** |
| BRW-2 | MED | B2 | external handoff | BrowserView.swift:558-573 | No open-in-default-browser anywhere; mailto/tel just "Blocked"; popups navigate in-place. | **FIX APPLIED 2026-07-03 (pending build brw2-build.log):** added a tight `BrowserURLGuard.externalHandoffSchemes = {mailto, tel}` + `isExternalHandoff(url:)`; `decidePolicyFor navigationAction` now hands those to `NSWorkspace.shared.open` (system Mail/Phone) before cancelling, while data:/file:/javascript: stay hard-blocked (NOT handed off). **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** (Handoff UX is a documented manual check.) |
| BRW-3 | LOW | — | private KVC | BrowserView.swift:421 + BrowserUseWebUIView.swift:450 | setValue(forKey:"drawsBackground") private KVC on WKWebView — App Review/OS-update risk. | OPEN (Phase 4 MAS check). |
| BRW-4 | LOW | — | cookie session copy | BrowserView (panel-cached webview) | Ephemeral store actually lives until app quit (webview retained in cached panel); no in-session clear affordance (browser-use has one). | OPEN (Phase 9). |
| ARX-1 | MED | — | arXiv dedup | ArxivSearchView.swift:55,239-246 | Dedup is session-only @State; later-session re-ingest duplicates note+PDF. | **INVESTIGATED 2026-07-03 — deferred (moderate refactor, not a quick fix).** `ArxivIngestService.ingest` HAS `modelContext`+`vaultURL` (could dedup), but: (a) `materializeImportedFiles` deliberately RESERVES a unique filename per ingest (`Plan3ImportFileIO.writeData(toReservedFile:)`) — anti-dedup by design; (b) `arxiv_id` is stored inside the serialized front-matter dict (SDPage.saveBody), NOT a queryable SwiftData column, so there is no cheap `FetchDescriptor` on it. A correct fix needs either a deterministic per-`shortID` note path + existence check, or an indexed `arxiv_id` field (schema change) — scoped, but larger than a point fix. Not started; documented so it isn't mistaken for done. |
| ARX-2 | LOW | B5 | arXiv etiquette | ArxivClient.swift:228 + ingest concurrency | start=0 fixed (no pagination), no retry/backoff/cache; up to ~12 parallel 128MiB PDF ingests, no spacing (arXiv etiquette ~1 req/3s). | OPEN (Phase 9): serial ingest queue + load-more. |
| A11Y-1 | MED | — | graph VoiceOver dead-end | Views/Graph/MetalGraphView.swift (~2400 lines; a11y only :2206-2222 icons) | Zero accessibility elements for nodes/edges; node selection mouse-only. Partial mitigation: HologramSearchSidebar list access. | OPEN (Phase 8): a11y element tree over top-N nodes or accessible list mode; document as follow-up if large. |

FEAT sweep verified-safe baseline (2026-07-02): mic permission requested only at record-start with real denial UX; speech teardown thorough on all error paths; no transcript/query content in os_log (bounded sanitized diagnostics); arXiv XML parsing defensive (XXE off, 5MiB/64KiB/32/50 caps, typed errors); arXiv PDF pipeline validates magic/symlinks/size, vault-confined, rollback on failure/cancel; browser-use secrets Keychain-backed, env file 0600/0700 removed on normal stop, loopback-origin-pinned webview, compiled out of MAS; feature gates honest both directions (no background work when off; nothing auto-starts: boot constructs only the passive TextCapturePipeline); meeting Save=Cmd+S, Discard confirms. Feature classifications: MEETINGS = HALF-WIRED (real pipeline, mic-dead outside Debug, 2 data-loss criticals); ARXIV = WORKING-WIRED (gaps: pagination/dedup/backoff); BROWSER = WORKING-WIRED (both surfaces; lifecycle risks). Framework reality: SwiftUI-lifecycle hybrid (287 SwiftUI vs 51 AppKit-only files), NOT an AppKit shell — directive assumption corrected. Shell cohesion: 5 nav patterns, 118 @Observable vs 93 ObservableObject (rule violation debt), 96 singletons, ~84 hand-rolled empty/error states, ~85% theme-tokenized. Meetings uses mic-only capture (SpeechAnalyzer, macOS 26) — no system-audio/ScreenCaptureKit (other participants only via room mic): product-truth note for Phase 9 docs.

RUST findings (non-goose crates, 2026-07-02): the CRITICAL local-server question is ANSWERED — no listening server in the shipped default/MAS build (single axum channel-relay is pro-build+channel-relay-tools gated, manual-launch only, loopback; zero unix sockets in all 12 crates; epistemos-core binds are #[cfg(test)]). RUST-4 MED B5 epistemos-core/src/agent_runtime/provider_client.rs:40-52 — ProviderHttpClient had NO timeout → app-hang candidate. **FIXED+VERIFIED 2026-07-02**: .connect_timeout(10s)+.read_timeout(120s) (reqwest 0.12.28; read_timeout caps mid-SSE idle without truncating long turns); `cargo check epistemos-core --lib` exit 0. RUST-5 LOW agent_core/src/providers/claude.rs:37 (ClaudeAuth) + credential_pool.rs:12 (ProviderCredentialPool) — #[derive(Debug)] on secret-bearing types (latent leak, no current sink). **FIX APPLIED + VERIFIED 2026-07-02** (`cargo check agent_core --lib` exit 0, no errors): replaced derived Debug with manual redacting impls (ApiKey(***)/OAuthAccessToken(***); pool shows key_count only), kept Clone/PartialEq/Eq. RUST-8 LOW B6 agent_core/src/storage/vault.rs:936-955 — resolve_path filters `..` but never canonicalizes → in-vault symlink escapes vault prefix (omega-mcp/vault.rs + agent_core file_ops.rs:184-199 show the correct nearest-existing-ancestor canonicalize pattern). DEFERRED to a careful pass (security-sensitive path; needs canonicalize-nearest-ancestor + a symlink test; LOW sev — requires attacker write-access inside vault). RUST supply-chain gate BLOCKED: cargo-audit + cargo-deny NOT installed in this env (Phase 6/12 — document, owner installs). RUST-1/2/2b/3 (relay auth/hygiene, pro-only, deferred). RUST-6 LOW 3 research-lane unhardened spawns (falsifier_artifacts/mod.rs:380, uas/*:475,522) not-MAS (Phase 7). RUST-7 LOW 41 uncovered unsafe blocks vs the //SAFETY rule (graph-engine 15 incl. knowledge_core/ring.rs:166-318 + engine.rs:547 unchecked unsafe impl Send; agent_core 10; shadow 7; code-index 7; syntax-core 2) (Phase 6 non-goose). RUST-10 INFO omega-mcp dispatcher ~20 .lock().unwrap() in panic=abort cdylib. RUST-11 INFO no deny.toml/cargo-audit for 12 crates + vendor/liteparse ~673 unsafe. RUST-9 INFO relay/OpenWork port 8787 collision (pro). Verified-safe: 90-tool surface deny-by-default above read-only (all subprocess/exec compiled OUT of MAS); 11 hardened production spawn sites (osascript escaped); all sampled FFI null-checks+UTF-8-validates+catches; no crate UB-unwinds into Swift; keys never cross FFI/never logged by macros; zero todo!/unimplemented!/non-test panic!; epistemos-vault + epistemos-research can #![forbid(unsafe_code)]; 10/11 HTTP clients have timeouts; all Cargo.lock committed; 3 build.rs = uniffi only. Goose boundary: no goose Rust crate; agent_core/src/goose_mas.rs = static catalog (no listener/secret/subprocess); commit-mentioned "socket-bind unsafe" is Swift (GooseRuntimeSupervisor.swift:735).

PERF findings (2026-07-02, static): PERF-2 MED Epistemos/Views/Notes/ProseEditorRepresentable2.swift:81-122 — synchronous full-doc markdown parse + textStorage replace + fold/transclusion at editor MOUNT on main; prime suspect for the unsolved note-open freeze (create-freeze needs spindump — recipe recorded). Fix: threshold-gated deferred full mount, NEVER empty seed (blank-note regression came from empty initialValue). OPEN (Phase 11; coordinate w/ note-freeze — MEASURE first). PERF-3 MED App/AppBootstrap.swift:1490-1516 (+1287-1318 store backup) — SwiftData store repair + container open + ~10 synchronous service/DB opens (EventStore, PaperclipStateStore, Rust resource/permission/Eidos) BEFORE first frame → the 969ms+3182ms launch hangs (ISSUE-2026-05-12-011). Fix: move to startDeferredRuntimeServicesIfNeeded w/ readiness gates. OPEN (Phase 11). PERF-1 MED graph-engine/src/engine.rs:1205-1209 + MetalGraphView.swift:1104-1109 — pauseEngine frees NO gpu/cpu graph memory; overlay keeps NSView alive → hundreds of MB idle RSS after visiting graph once (ISSUE-005 verified). Fix: graph_engine_release_working_set FFI called after idle. OPEN (Phase 11; owner deferred this to canonical graph plan — respect that; document). PERF-4 MED MetalGraphView.swift:1605-1606 — inFlightSemaphore acquires+releases same-thread same-call (decorative); real backpressure is next_drawable() blocking main up to ~1s under GPU saturation. OPEN (Phase 11). PERF-5/6/7 LOW dead code: graph-engine/src/pipeline_cache.rs (~570 lines, not in lib.rs module tree — shaders compile from source per engine create), MetalGraphView.swift:649-703 shared-position-buffer scaffolding (0 call sites, misleading "default-on" comment), MetalRuntimeManager.deepUnload()/releaseWorkingSet() (0 production callers), PerformanceSettingsSection idleUnloadDelay unconsumed, AppBootstrap.swift:2517 mlxUnloaded=false hardcoded (Phase 7 dead-code + Phase 11). PERF-8 LOW HologramOverlay.swift:1475-1489 30Hz timer runs whenever overlay open even with 0 pinned panels. PERF-9 LOW no quit-time stop for AVAudioEngines/paperclip clock/memory-pressure source/Rust drain (App/EpistemosApp.swift:1300-1330). PERF-10 LOW stale CLAUDE.md/doc perf claims (processPool gone, MLXInferenceService.swift no longer exists, CVDisplayLink comments stale) → doc refresh. Verified-safe: display link demand-driven + stops on occlusion/miniaturize/hide/route/pause; no frame-state race (Arc<Mutex<Simulation>> + double-buffered GPU N-body + atomic cross-thread flags + main-thread-only render FFI); all 8 Metal pipelines created once never per-frame (regression-tested buffer reuse); 25/25 timers lifecycle-managed (Settings health rows on one shared 5s clock); theme changes never reload webviews (CSS-var evaluateJavaScript); note body disk I/O off-main both hydrate paths; editor keystroke buffers flush synchronously before teardown. IMPORTANT stale-doc corrections for this run: MLXInferenceService.swift, EpdocWebViewShared.processPool, and MetalRuntimeManager wiring named in CLAUDE.md/memories NO LONGER EXIST as described — verify before citing.

CONC findings (Swift 6 strict-concurrency sweep, 2026-07-02 — all 7 sweeps now in):
CONFIG GROUND TRUTH (Phase 3 baseline): project.yml global SWIFT_VERSION=6.0, SWIFT_DEFAULT_ACTOR_ISOLATION=MainActor, complete strict concurrency (debug+release both compile clean = already at Swift 6 mode). MISSING for the directive's 6.2 target: SWIFT_APPROACHABLE_CONCURRENCY absent, no upcoming-feature flags, no per-target overrides (all 4 targets inherit). UniFFI already handled by patch-uniffi-bindings.py (rewrites generated bindings nonisolated under default-MainActor — deliberate seam, documented). Phase 3 = add approachable-concurrency per target + re-verify zero diagnostics + TSan.
| CONC-1 | HIGH | B4 | double-resume crash | Omega/Vision/Screen2AXFusion.swift:153-193 | VN completion resume + catch resume, no latch → VNRequest failure mid-processing double-resumes continuation → fatal trap (computer-use; #if !EPISTEMOS_APP_STORE so direct-dist lane). | FIX (Phase 3): AtomicResumed/NSLock latch. |
| CONC-2 | HIGH | B4 | unbounded stream + producer leak | Engine/AgentHarness/AgentQueryEngine.swift:159 | Implicit-unbounded AsyncThrowingStream, no onTermination on agent-event hot path → consumer cancel leaves SSE+tools running + unbounded buffer. | **FIX APPLIED 2026-07-03 (pending build conc2-build.log):** `submitMessage` now uses `StreamingBufferPolicy.throwingStream(limit: .textLimit)` (bounded `.bufferingNewest(16_384)`, never `.unbounded`) + captures the producer `Task` and sets `continuation.onTermination = { _ in task.cancel() }` — mirrors the canonical TriageService pattern. Consumer cancel now cancels the turn's Task (propagates to the SSE/tool awaits, which are cancellation-aware) and the buffer is bounded. Caveat: full teardown depends on `runTurn`'s inner awaits honoring cancellation (URLSession + structured concurrency do); deep audit of every tool await is a follow-up. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| CONC-3 | HIGH | B4 | unbounded stream + leak | Omega/Inference/ReasoningLoopService.swift:104 | Same shape on per-token reasoning relay; cancelled consumer burns inference; isReasoning stuck. | FIX (Phase 3): same as CONC-2. |
| CONC-4 | HIGH | B3 | approval continuation leak | Views/Approval/ApprovalModalView.swift:446,186-190 | Deadline enforced only by rendered TimelineView → approval enqueued while modal unrendered/occluded hangs the agent bridge FOREVER (tool-approval hot path). | **FIX APPLIED 2026-07-03 (pending build conc4-build.log):** `ChatApprovalQueue` (@MainActor) now schedules a view-independent `deadlineTimeoutTask` when an approval is enqueued: `Task { try? await Task.sleep(for: .seconds(interval)); resolve(approval, decision: .timedOut) }`. Fires the timeout regardless of whether the modal ever renders. `resolve` is idempotent (removeValue guard), so a user resolution first makes the timeout a no-op; the task is cancelled + nilled on every resolve. The TimelineView-driven `.task(id:)` timeout stays as the on-screen countdown UI. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors** (+ regression test `chatApprovalQueueTimesOutWithoutModalRender` added: enqueue with a 0.3s deadline, NO resolve() + NO modal render → must resolve `.deny` via the view-independent task, not hang). **TEST PASSED 2026-07-03: `chatApprovalQueueTimesOutWithoutModalRender` green in the AuditFixRegressionTests run (the 2 failures in that run were the separate removal-casualty tests, now delegated for reconciliation).** |
| CONC-5 | HIGH | B7 | mic stays on | Engine/EpistemosSpeechAnalyzer.swift:193 | Results AsyncStream lacks onTermination despite documented cancel contract → consumer-cancel leaves mic tap+AVAudioEngine+analyzer running (mic indicator stuck). COMPOUNDS MEET-5. | FIX (Phase 3/9): resultsCont.onTermination→stopInternal(). |
| CONC-6..12 | MED | B6 | main-thread blocking | SessionBrowser.swift:106-134 (transcript reads in SwiftUI body per keystroke), EpistemosDocumentController.swift:408-420 (full-vault scan on doc open), HTMLWorkspacePreviewView.swift:74-86 (~12MB pyodide on main per boot), HexViewerView.swift:152-165, EmbeddingService.swift:254 (unbounded DispatchGroup.wait in NSView deinit), AppBootstrap.swift:1780 (Task(priority:.utility) inherits MainActor → "background" eviction on main), TimeoutUtility Process.terminate() before run() race + pipe-drain block | Beachballs/stalls; CONC-11+CONC-6/7 tie to PERF-2/PERF-3 note-freeze+startup; CONC-12 = terminate-unlaunched ObjC crash. | OPEN (Phase 3/11). |
| CONC-13 | MED | B4 | unlocked FFI ptr | Engine/SyntaxCoreService.swift:14,22 | @unchecked Sendable + mutable OpaquePointer, contract-only "single-threaded" confinement, no lock → 2nd concurrent edit() = Rust UB. | **FIX APPLIED 2026-07-03 (pending build conc13-build.log):** added `private let lock = NSLock()`; serialized all 7 FFI accessors (isValid, generation, handle, stats, edit, kindName, tokensForViewport) with `lock.lock(); defer { lock.unlock() }` so no two `syntax_document_*` calls run on the same `document` concurrently. init is pre-escape and deinit runs at refcount 0 (no concurrent access), so neither locks. Confinement is now enforced, not contract-only. **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| CONC-15 | MED | B4 | existential data race | Graph/EmbeddingService.swift:308-310 | nonisolated(unsafe) activeEmbeddingLookup written on MainActor, read from background embedding tasks, NO sync (SAFETY comment describes intent not mechanism) → torn existential read, TSan-visible. | **FIX APPLIED 2026-07-03 (pending build conc15-build.log):** replaced the bare `nonisolated(unsafe) var activeEmbeddingLookup` with a `nonisolated(unsafe) _activeEmbeddingLookup` backing store guarded by an `NSLock` (`embeddingLookupLock`) behind a `nonisolated` computed `activeEmbeddingLookup` (locked get/set) — matches the file's existing lock convention (ContextualEmbeddingStorage). All 5 non-init access sites go through the lock; init writes the backing store directly (pre-escape). No more torn existential read. (Sibling Bools `swiftEmbeddingFallbackActive`/`preparedQueryEmbeddingActive` are word-atomic — no tear/crash; left as documented lower-risk.) **VERIFIED 2026-07-03: TEST BUILD SUCCEEDED, 0 errors.** |
| CONC-14/16..20 | LOW | — | hygiene | HologramOverlay observer re-install w/o remove; InferenceState.swift:3145 closure-Sendable laundering; AmbientFrequencyLivePlayer.swift:399 Float hardware-atomicity (accepted RT-audio); CloudProviderAuthService OAuth listener leaks; orphanable timers (CodeEditorView:1051, AIPartnerService:441); WorkSPASchemeHandler unbounded 2-yield stream | Leaks/UB-on-ARM64-benign/copy-paste bait. | OPEN (Phase 3/7). |

CONC verified-safe: ZERO DispatchQueue.main.sync in scope (UniFFI .sync rule holds); zero withUnsafeContinuation (all checked); zero raw os_unfair_lock/NSRecursiveLock; SSE/token lane uniformly disciplined (all 24 wrapper sites via StreamingBufferPolicy .bufferingNewest(256)/16384-limit, finish+onTermination verified incl. stall watchdog); TimeoutUtility continuation machinery race-correct (exactly-one-winner NSLock take-and-nil, 5 consumers); NSDocument subclasses safe (canAsynchronouslyWrite=false + assumeIsolated snapshots); EventStore SQLite fully serialized; ~24/75 @unchecked Sendable + ~14/68 nonisolated(unsafe) carry SAFETY comments (most uncommented are sound-by-lock or sound-MainActor-default, but comment coverage is below the CLAUDE.md rule → Phase 3 comment pass); 42/49 observer registrations have verified teardown; NLContextualEmbedding lock is crash-driven+correct. Counts: 85 @unchecked Sendable / 72 nonisolated(unsafe) / 142 Task.detached / ~429 Task{} / 21 DispatchQueue.main.async / 18 DispatchSemaphore / 28 checked-continuations / 55 AsyncStream / 59 assumeIsolated (non-test).

(All 7 Phase-0 sweeps folded in. PLAN- findings live in PLAN_AUDIT.md.)

## Commands run (evidence log)

- `git status` / `git log --oneline -10` / `git worktree list` — captured above.
- `xcodebuild -version; swift --version; sw_vers` — captured above.
- `xcodebuild -list` — schemes captured.
- Debug build IN FLIGHT (background bc2jwaaq0): `xcodebuild -scheme Epistemos -destination 'platform=macOS' -configuration Debug build` → log at scratchpad `xcodebuild-debug.log`. Warning inventory + exit code pending.
- 6 read-only audit subagents dispatched (security/entitlements, Swift concurrency, Rust non-goose crates incl. local-server discovery, WebView/bridge, features+UX, performance/Metal). Reports pending.

## Gates (Phase 0 exit)

- [x] Debug build (`Epistemos`) — GREEN, 10 warnings (3 non-goose app: MarkEditCoreEditorView.swift:287 async-alt, OpLogProjectionHealthRow.swift:148 allowedFileTypes deprecated, KokoroVoiceGateStatus.swift:136 unused `manifest`; 2 vendored MarkEdit concurrency; 2 Goose; +agent_core unused fn).
- [x] Release (`Epistemos-AppStore`) build — **GREEN (BUILD SUCCEEDED, exit 0, ~36min universal -O)** after Float16 fix → **F-0003 VERIFIED**. Warning inventory: same 3 non-goose app warnings as Debug (the 16k raw "warning:" count is bundled JS/Rust output noise). Benign: 4 rollup sourcemap "Error when using sourcemap" notices from a bundled .tsx web-ui (non-fatal, build passed; likely browser-use/june bundle — not owned Swift).
- [ ] Meeting test suite (MeetingNoteCaptureServiceTests) — RUNNING to verify MEET-2.
- [x] Full Swift test suite (EpistemosTests, `xcodebuild test-without-building` after `build-for-testing`, detached via nohup to beat the ~5-6min task kill limit) — **2044 passed / 27 failed** on the non-Goose portion (run then stalled in out-of-scope live-Goose app-hosted suites — iso-DD degradation, killed as expected). FIRST-PARTY CLASSIFICATION (all 27 + F-STALE-1 meeting): **100% stale source-mirror / doc-parity tests + 2 out-of-scope Goose. ZERO behavioral regressions. ZERO in files this session touched (no session regressions).** Forensic's "real suspects" NOW PASS (no HTML-Workspace failures, no NoteSavingStress failures — deep-clean lane held). Breakdown: **Stale source-mirror/doc-parity (Cat A, fix-with-justification in Phase 4/7 batch):** BestOfPresetPlan3Tests:47/117/133 (doc-parity), AppleNativeCodepackPlan3Tests:82/83 (doc-parity), BrowserUseCodepackPlan3Tests:6/46/247 (doc/source mirror), BrowserUseAdapterPlan3Tests:272 (vendor-boundary mirror), BrowserPlan3Tests:201 (browser reachable via utility WINDOW — home-embed regime change), CompanionAvatarGrammarSourceGuardTests:165/172/196/197/199 (landing-cleanup source guards ×5), DarkLightCrashTests:81 (asserts exact multi-line indented block in WebKitCodeEditorView.swift — reformatted; runtime teardown behavior fine), FSRSDecayStateTests:117 (source.contains a label comment string), MeetingNoteCaptureServiceTests uiRoutesStayInPlan3Surfaces:619/625 (=F-STALE-1, refactored button label + removed .meetingNote window route). **Out of scope (Cat B):** GooseACPClientTests:820, GooseLiveIntegrationTests:109 (Goose). CONCLUSION per Rule-6: red state is brittle-test debt, NOT stop-the-line — no regression I introduced, no real app defect surfaced. Total reduced from forensic's ~62 → 27 (deep-clean fixed the HTML-Workspace batch since).
- [ ] Rust baseline (fmt/clippy/test) for non-goose crates — toolchain now free.
- [x] ARCHITECTURE_BASELINE.md complete; THREAT_MODEL.md (B1–B8) complete; PLAN_AUDIT.md complete; APP_REVIEW_NOTES.md complete.
- [ ] Manual smoke pass — deferred (needs app launch).

## F-REMOVAL-CASUALTY-TESTS (discovered 2026-07-03, regression sweep) — TEST DEBT, not app breakage
The cloud-only + Omega removal deleted local models / computer-use / omega-ax. The test-reconciliation subagent fixed COMPILE errors, but **source-guard tests that assert removed STRINGS compile fine and only fail at RUNTIME** — so a cluster was missed. The APP is correct + verified cloud-only; these are STALE TESTS asserting removed features (owner-approved removal), to be reconciled with justification per Rule-5.
**Found (via AuditFixRegressionTests run + rg sweep):**
- **FIXED 2026-07-03:** AuditFixRegressionTests `modelProfileCreationSheetAvoidsRetiredHermesLabels` (asserted local display names Qwen/Ternary Bonsai exist → all local labels now removed; flipped to assert all absent) + `releaseArchiveKeepsLinkedAgentDylibsAndServices`:269 (asserted `build-omega-ax.sh` in project.yml → removed; dropped that assertion, kept omega-mcp/agent-core/agentsEnabled which ARE valid).
- **REMAINING (to reconcile):** RuntimeValidationTests:4667/4670/4672 (local route display-name resolution — LocalTextModelID/`Qwen 3`), :4686 (`.localMLX(preferredLocalTextModelID)` persist — old fallback; now cloud/appleIntelligence), :4801/4802 (`executeComputerAction`/`ComputerUseBridge.shared.execute` — Swift computer-use removed; NOTE :4803/4804 are agent_core Rust FFI = scope-ambiguous "Rust core"); RuntimeCapabilityAndPerformancePolicyTests:476-479 (`_screenCapture: ScreenCaptureService`/`_screen2AXFusion: Screen2AXFusion` — removed Omega); AppStoreHardeningTests:387 (`GGUFRuntimeBridge` in pro target — removed); HELIOSInvariantSourceGuardTests:1541 (crate-list string includes `omega-ax` — removed). Each needs per-assertion classify (dead→flip/remove w/ justification; kept→keep). Some cross Swift↔Rust — the Rust-side execute_computer_action assertions may still be valid (agent_core not in my removal scope). Reconcile carefully; do NOT weaken a KEPT-feature assertion.

### F-REMOVAL-CASUALTY-TESTS — RECONCILED + VERIFIED 2026-07-03
Subagent a6cce091 reconciled the removal-casualty source-guards across **10 test files** (source-authoritative: verified every needle against current app/Rust source before touching). Result: 563 run / 527 pass / 36 fail in the affected suites; **NONE of its ~34 fixed assertions are in the failure list.** Suites now GREEN: AppStoreHardeningTests, ProductionHardeningTests, CargoReleaseProfileTests, HELIOSInvariantSourceGuardTests, AuditFixRegressionTests (+ the earlier AuditFixRegression 2 I fixed + RuntimeValidation/RuntimeCapability/ThemePair/PipelineHookRegistry/ReleaseScriptAudit/NonAgentPruning casualties).
**Coordinator VERIFICATION (spot-checked, not trusted):** GGUFRuntimeBridge + omega-ax genuinely gone from project.yml (flips correct); LocalTextModelID type + localRouteDisplayName genuinely gone (the "2 files" are leftover UserDefaults key STRINGS, not the type — test removal correct); Rust `execute_computer_action` (agent_core bridge.rs + agent_loop.rs, 2 refs) genuinely present (KEEP correct); omega-ax still in .github/workflows/ci.yml (8 refs) so subagent correctly LEFT HELIOS:1541 (flipping would break a green test). Subagent also caught + corrected an error in MY symbol list (IMessageDriverService — I wrongly said kept; it's genuinely gone, 0 files → correctly flipped).
**36 residual failures = OUT OF SCOPE (correctly left):** ~28 F-STALE-1 refactored-kept-feature source-mirrors (RootView/SettingsView cloud identifiers, code-editor, vault-sync, HookRegistry method renames), 4 pre-existing iMessage/Channels (deleted in a PRIOR commit, separate cluster), 2 script-behavioral (ci_test/run_swift_tests entrypoints), 1 force-unwrap code-quality test. These are the documented brittle-test debt, not app breakage.
**REAL app-level removal GAP found + FIXED by coordinator:** `scripts/xcodebuild_epistemos.sh` still called `patch_mlx_package_checkouts()` → deleted `scripts/patch_mlx_metal_warnings.sh` (exit 127 → why ci_test/run_swift_tests failed). REMOVED the dead function + call (bash -n OK). Also cleaned the stale `AppBootstrap.swift:1700` "AmbientCapture is lazy" comment. **FINAL FULL-STATE BUILD GREEN 2026-07-03: TEST BUILD SUCCEEDED, 0 errors** — the entire session (18 hardening fixes + 34-assertion removal reconciliation + build-script fix + comment cleanup) compiles clean together.

## UI / FEATURE OVERHAUL — owner-directed, 2026-07-03 (all build-verified unless noted)

Owner requested a rapid product overhaul alongside hardening. Each item below was landed via `xcodebuild build-for-testing` GREEN (relaunch to see). Committed baseline: a6927ac03 (pre-overhaul).

**Deletions**
- Removed 4 landing feature buttons (vaultMCP, extensions, provenance, voice) from `LandingFeatureButton` enum + all LandingView refs. Kept pdfImport/arxiv/browser/meetingNote.
- Deleted the entire Browser-Use Pro lane (20 files: 7 BrowserUsePro/ + 2 views + 6 tests + 5 scripts) + unwound UIState/UtilityWindowManager/EpistemosApp/AppBootstrap/ExtensionsDetailView + the bundle-app-runtime-assets.sh browser_use_pro block. Lite BrowserView KEPT + intact.

**UI polish**
- arXiv + meeting: `.frame(maxWidth/maxHeight:.infinity)` (fixed half-page glitch), Home button in the top toolbar (meeting's carries the unsaved-transcript confirm), liquid-glass search bar / URL bubble, retired the shared HomeEmbeddedPage floating chip.
- Browser: no Home button; URL bar → floating liquid-glass capsule.

**Themes**
- All non-custom themes share Ember's font faces (EpistemosTheme instance getters + AppDisplayTypography static getters); Platinum keeps its OWN landing-greeting hero font (`LandingCommandTypography.heroFontName`), Classic+Ember use Ember's.
- Platinum heading-size root-cause fix: `headingSizeMultiplier` was 0.72/0.82 at H1/H2 vs Ember 1.0 → now 1.0 all levels (headers line up; no shift on theme switch).
- Custom theme → EXPERIMENTAL + OFF by default: `AppCustomTheme.isActive`/`activeThemePair()` gate on `AppCustomTheme.experimentalDefaultsKey`; Settings toggle hides the custom card until enabled + falls back on disable.

**Browser↔notes + browser theming (owner's #1 want)**
- `BrowserThemeInjection` forces the Epistemos palette (bg/fg/accent, !important) + a pixel `@font-face` (ChonkyPixels) on headings onto EVERY webpage via WKUserScript; re-applied live on theme change.
- "Save to notes" button (VaultSyncService.createPage). Browser opens as a TAB sharing the notes window via `NoteWindowManager.openBrowserTab(url:)` (noteTabbingIdentifier), like HTMLWorkspace/code-editor.

**arXiv**
- Auto-featured feed of recent AI/ML papers on open (was empty state). "View paper" button opens the abs page in the themed in-app browser tab.

**#9 custom-theme web hardening**
- Audit (subagent a77e278d) found KaTeX + HTMLWorkspace-preview gaps; the rest (code editors, MarkEdit, Epdoc, Work) already propagate `theme.resolved`.
- Gap 1 FIXED: EpdocKaTeXPreview now injects `resolved.foreground` + re-applies on theme change.
- Gap 2 FIXED: HTMLWorkspaceArtifactHost passes light/dark (was `previewTheme: nil` → always default).
- Gap 3 DOCUMENTED (known experimental limitation): full custom-PALETTE theming of HTMLWorkspace *previews* needs threading a resolved-color guardCSS through `themeGuardCSSOverride`; deferred (experimental-custom edge case, off by default).

**REMAINING — need owner input or a bundle rebuild (documented, not silently dropped)**
- #12 links auto-open in browser: broad app-wide interception; link-open sites not in obvious places — needs investigation, deferred.
- #15 note-preview top padding + solid title background: VISUAL (NotePreviewSurfaceView chrome) — needs owner verification vs blind guessing (owner frustrated prior agents "just added padding").
- Epdoc adaptive-header-sizing (longer header → smaller, to match prose `MarkdownTextView.fontSize`): needs a js-editor/ bundle rebuild (npm) — WEB-4-class constraint.
- Epdoc↔prose color match in the embedded graph: VISUAL — needs owner verification.
- Platinum greeting hero size: font-family shared; if it reads small, needs a precise per-theme size multiplier (owner to confirm the feel).

## OVERHAUL HARDENING AUDIT — 2026-07-03 (subagent a1fe4148, adversarial, read-only)
Audited the full UI/feature overhaul (a6927ac03..HEAD) for security/concurrency/MAS/robustness. VERDICT: no CRITICAL/HIGH defects, no new MAS blockers. High-risk surfaces PROVEN clean with defense-in-depth: BrowserThemeInjection (theme-only numeric CSS, textContent not innerHTML, no page-content read), BrowserHomePage (encodeURIComponent search, static HTML), BrowserURLGuard (http/https-only, blocks user:pass@ + data:/file:/javascript: at input+nav+response+popup), DataDetection links (http/https→guarded browser only), ArxivClient (HTTPS+host+query validation, XXE off, size caps), custom-theme gating (flag+pair required, clamped hex), openBrowserTab window lifecycle (weak-self observer, no leak/cycle).
Findings + resolution:
- #1 MED (PRE-EXISTING, MAS gray-area): BrowserView `setValue(false, forKey:"drawsBackground")` private KVC. ACCEPTED — not introduced by the overhaul, mitigated (setDrawsBackground: is a public selector on NSScrollView/NSTextField/NSBox so a binary scan can't attribute it to WKWebView; ships in many MAS apps), and the theme injection now forces page backgrounds anyway. Converting to underPageBackgroundColor risks an unverifiable transparency/white-flash regression; left as a conscious accepted call (also tracked as BRW-3).
- #2 LOW (perf): FIXED — theme re-injection in updateNSView now gated on a themeKey (was ~30-50 redundant evals/page-load).
- #3 LOW: FIXED — arXiv feed catch now handles CancellationError (no spurious error on navigate-away).
- #4 LOW: FIXED — browser/version tab close no longer emits a synthetic-key recordNoteClosed activity event.

## DEEP RECURSIVE HARDENING — 2026-07-03 (3 parallel adversarial audits: data / meeting / concurrency)
Owner asked for the deepest possible hardening beneath UI/backend/engines. Ran 3 read-only audits; fixed all CRITICAL + HIGH; MED/LOW documented below with status so NOTHING is undocumented.

### DATA / VAULT INTEGRITY (agent a0090d778) — verdict: well-hardened, NO CRITICAL/HIGH
- MED-1 external-edit conflict is last-mtime-wins on the auto-import path (one side's edits lost). DEFERRED — mitigated by 50-version history; needs conflict-UI routing.
- MED-2 movePage disk-move + SwiftData filePath save not transactional → duplicate on save-failure. FIXED (commit: movePage rollback — move the file back on save failure).
- MED-3 persistent save failure (vault on a disconnected volume) swallowed to log; edits lost only on quit/crash-while-disconnected. DEFERRED — mitigated by dirty-tracking retry-on-reconnect; RootView off-limits for a banner, Settings diagnostic-row surface TBD.
- LOW-4 exportPage empty-file pre-creation (0-byte .md on kill). DEFERRED — mitigated by dedup + empty-never-wins.
- LOW-5 AIPartner log non-atomic write. DEFERRED — rebuildable telemetry.
- LOW-6 no VersionedSchema/MigrationPlan. DEFERRED — future breaking migration is an availability outage, NOT data loss (store + .md preserved).
- LOW-7 SDPage.saveBody swallows the write Bool. DEFERRED — authoritative path is checked; defensive smell.

### MEETING CAPTURE (agent a22feed32) — the #1 trust surface — ALL CRITICAL+HIGH FIXED
- CRITICAL-1 transcript silently truncated to 10k on save. FIXED (#27, commit bca8a13b4).
- HIGH-2 no durable persistence / crash loses transcript. FIXED (#30, commit 9cb82b2b1 — MeetingDraftStore + recovery banner).
- HIGH-3 AirPods/route-change silently stalls transcription. FIXED (#28, commit bca8a13b4 — config-change re-arm).
- HIGH-4 Cmd-W bypasses teardown → hidden mic recording. FIXED (#29, commit ec3c5e9d3 — willClose stop).
- MED-5 mic-permission revoke mid-meeting undetected. DEFERRED — same silent-stall signature as HIGH-3.
- MED-6 Siri/interruption unhandled. DEFERRED — same engine-re-arm remedy as HIGH-3.
- MED-7 input AsyncStream .bufferingNewest(64) silently drops audio under backpressure. DEFERRED — inherent bounded-buffer tradeoff; flag as gap-marker later.
- LOW-8 format conversion on the render thread (NSLock). DEFERRED — quality, not loss.

### ENGINE CONCURRENCY (agent a7bec819) — verdict: well-hardened (main.sync=0, unbounded AsyncStream=0)
- HIGH-1 ModelContext saved across await (BlockMirror hot path + prewarm) → CoreData thread-confinement → store corruption. FIXED (#31, commit e6555e31b).
- HIGH-2 EmbeddingService outer FFI push not under teardown drain gate → engine use-after-free. FIXED (#32, commit e6555e31b).
- MED-3 off-main unsetenv/setenv races getenv. DEFERRED — agent-core env setup (Goose-adjacent), out of non-Goose scope.
- MED-4 VaultIndexActor import/migration reentrancy across await. DEFERRED — self-heals (both migrations synchronous), one-time UserDefaults-gated.
- LOW-5 observeRouteChanges leaks its observer token. MITIGATED — #28 wired an internal re-arm; the leaky public API is now unused (no repeat caller).
- LOW-6 EpdocInsertLinkPicker per-keystroke task not cancelled. FIXED (commit f36e30e97).
- LOW-7 SDFLabelInstanceBuilder static scratch races parallel tests. FIXED (commit 1e10b75da — local var).
- LOW-8 MetalRuntimeManager dead @unchecked Sendable. DEFERRED — delete with the dead MLX stack.
- LOW-9 EmbeddingService nonisolated(unsafe) Bools. DEFERRED — byte-sized, no torn value.

BUILD OPTIMIZATION: Release now sets explicit whole-module optimization (project.yml SWIFT_COMPILATION_MODE: wholemodule, commit 1e10b75da). Effective on next xcodegen + Release build.

## MAS-READINESS AUDIT — 2026-07-03 (agent a43abdc2, read-only)
VERDICT: NO guaranteed-rejection BLOCKER in the current MAS config (Epistemos-AppStore, sandbox ON). Usage strings complete (mic/speech/Documents/Desktop/Downloads present), no forbidden entitlements, no active private APIs, dangerous subprocess/computer-use surfaces correctly #if !EPISTEMOS_APP_STORE-gated. PrivacyInfo.xcprivacy present + well-formed. No ATS exceptions. No runtime npm/node.
OWNER-ACTION items (I did NOT change these — Goose-entangled Work lane and/or entitlement/feature decisions that are the owner's call):
- HIGH: 3 Work runtime supervisors compile Process().run() into the MAS binary UN-gated (WorkOpenWorkSupervisor:79/97, WorkOpenGUISupervisor:161/178, WorkRuntimeSupervisor:106/122). INERT today (binaries not staged in MAS + sandbox blocks exec), so not a guaranteed reject — BUT WorkOpenGUISupervisor.resolveBun (:676-678) probes /opt/homebrew/bin/bun OUTSIDE #if DEBUG, so a MAS build attempts a (denied, reviewer-observable) exec when the Work surface opens. FIX (owner): wrap all three supervisors' Process paths in #if !EPISTEMOS_APP_STORE (mirrors WorkTerminalView/HarnessLab/VaultChatMutator) + drop the homebrew bun fallback in MAS. Goose-entangled Work lane → owner applies.
- MED: com.apple.security.network.server entitlement — confirm a real MAS feature binds a socket (MCP host?) or remove.
- MED: com.apple.security.cs.allow-jit — confirm a real in-process JIT consumer (Kokoro/Metal?) or remove (MLX stack removed).
- MED: setValue(false, forKey:"drawsBackground") private KVC (6 webview sites) — gray-area, usually survives; convert to underPageBackgroundColor if you want zero doubt (deferred: unverifiable transparency regression risk).
- LOW: "Scrub Pro Frameworks" build step is a no-op stub; Widgets/XPC not wired into MAS app (need app-sandbox+app-groups if shipped); CoreLocation import is data-type-only; pin Pyodide indexURL to the bundled copy; AppStore Release code-sign identity is a submission detail.

- MEET MED-5 (permission revoke) + MED-6 (interruption without config-change) FIXED 2026-07-03: EpistemosSpeechAnalyzer now runs a permission/engine monitor while capturing — stops on mic-permission revoke (no more silent "recording") and re-arms if an interruption stopped the engine without an AVAudioEngineConfigurationChange.

## DATA MED-1 — deeper assessment 2026-07-03 (DEFERRED to owner, not fixed — reasoning)
The auto-import path (VaultIndexActor ~1505-1575) resolves a concurrent external-edit + in-app-edit by mtime-wins (`noteBodyIsNewer` / `preserveBody`). When the vault file is the more recent side, an in-app edit is reloaded away.
- Mitigation is PARTIAL: previously-SAVED bodies are recoverable via 50-version history, and the MANUAL "Sync from Vault" (VaultSyncService.syncFromVault → [VaultSyncConflict]) DOES detect + surface these. But an UNSAVED in-app edit discarded by the auto-path is not guaranteed version-captured (auto-import does not call captureVersionIfNeeded before overwrite).
- Why NOT auto-fixed: the correct fix (route the auto-path to a conflict prompt) requires plumbing conflicts from the VaultIndexActor up to a live conflict UI AND is a UX decision (interrupt-on-every-concurrent-edit vs. silent mtime-wins). It is a change to the app's MOST critical data path where a bug loses/corrupts notes — the risk/reward does not favor an autonomous change for a MED. OWNER decision: (a) accept mtime-wins + rely on manual Sync-from-Vault, (b) add auto conflict prompts, or (c) minimally capture a version of the in-app body before the auto-overwrite (lowest-risk hardening, if desired).

- DATA MED-3 (vault-disconnect silent save-fail) ADDRESSED 2026-07-03: VaultSyncService now tracks a save-failure streak (all-failed export batch = likely disconnected volume) + last error; surfaced read-only as a "Vault save" health row in Settings > Diagnostics (RootView is off-limits for a global banner). Additive detection — does NOT change the save/retry logic (edits stay dirty + retry on reconnect).

- MEET MED-7 (silent audio-backpressure drops) FIXED 2026-07-03: EpistemosSpeechAnalyzer counts input-buffer drops lock-free (Atomic, no render-thread lock) in the tap and the 4s monitor drains+logs them off-thread ("N buffers dropped — possible transcript gap"), so backpressure gaps are no longer invisible. Completes the meeting audit: CRITICAL + all HIGH + MED-5/6/7 fixed; only LOW-8 (render-thread conversion lock, quality) remains.

- ARX-2 (arXiv request etiquette) FIXED 2026-07-03: added a shared serialized ArxivRequestThrottle (1.2s min-interval, slot reserved before sleeping to avoid actor-reentrancy double-booking) wired into ArxivClient default fetch. Rapid category-chip / repeated searches no longer hammer the arXiv API (which rate-limits/blocks unpaced callers); a single interactive search sees no delay. Test-injected fetches bypass it.

- BRW-4 (in-session browsing-data clear) FIXED 2026-07-03: BrowserTab.clearBrowsingData() wipes the (non-persistent) cookies/cache/site-storage via WKWebsiteDataStore.removeData(allTypes, .distantPast) then reloads; surfaced as a "Clear browsing data now" destructive button in the browser "Browser limits" privacy popover. Makes the browser data isolation user-controllable (enterprise-trust). Build-verified.

## NOTE DATA-INTEGRITY AUDIT (subagent, 2026-07-03) — 8 findings on the note editing/persistence path
Safe (verified): atomic durable write (NoteFileStorage.atomicWriteUTF8 temp→fsync→rename→fsync→readback + BLAKE3/xattr + quarantine), no body truncation cap, robust JS-bridge escaping, Epdoc .epdoc save pipeline (flushAllForShutdown).
- NOTE-1 (HIGH) — FIXED 2026-07-03: Cmd-Q lost up to a typing burst — note bodies save via async detached Task (scheduleWriteBody), killed by process exit; performTeardown drained only Epdoc. Fix: ProseEditorView willTerminate now persists its body SYNCHRONOUSLY (NoteFileStorage.writeBody) + added NoteFileStorage.flushAllPendingBodiesForShutdown() drained from performTeardown as a backstop (for editors without the observer, e.g. graph-embedded). Build-verified.
- NOTE-2 (HIGH) — OPEN: same note in two editors (wikilink nav → NoteNavigationState.push has no cross-tab dedup) → silent last-writer-wins; each tab's 5s debouncedSave doesn't post pageBodyDidChange so the sibling never reloads. Fix needs a per-pageId lock / shared editor model or cross-tab dedup.
- NOTE-3 (HIGH) — OPEN (deeper MED-1): external/synced vault edit clobbers unsaved in-memory edits; preserveBody mtime guard is defeated by the 5s debounce window (managed-body file lags the editor) + content-blind 3s self-suppression. Fix needs a dirty-flag/hash conflict guard before the import-reload + version-snapshot-before-overwrite.
- NOTE-4 (HIGH) — FIXED 2026-07-03: added NoteDraftStore (mirrors MeetingDraftStore) — ProseEditor writes a .draft on a 1.5s debounce, deletes it on durable save/quit, and AppBootstrap.reconcileOrphanedDrafts() recovers any draft newer than its .md at launch (crash orphan) into the durable body. Build-verified; crash path not runtime-verifiable without a real crash. [was] no crash-recovery draft store for notes (meetings have MeetingDraftStore). Hard crash/kill-9/panic loses everything since the last completed async write; nothing offered on relaunch. Fix: a NoteDraftStore mirroring MeetingDraftStore.
- NOTE-5 (MED) — OPEN: version-history recovery (SDPageVersion) doesn't cover pure-Prose editing — autoSaveInterval=0 by default + Prose autosave writes the .md without calling savePage → zero snapshots for prose-only users.
- NOTE-6 (MED) — OPEN: flushIfNeeded marks lastPersistedBody before the async write confirms (scheduleWriteBody returns a Task immediately); a failed write leaves in-memory state believing it saved. (NOTE-1's added synchronous writeBody mitigates the quit case.)
- NOTE-7 (LOW) — OPEN: undo history resets across NoteWorkspaceMode switches (Coordinator destroyed). Committed text preserved. Expected of most editors.
- NOTE-8 (LOW) — OPEN: write failures are logged, never surfaced to the user (fail-closed, no corruption).

- GAP-1 (imports skip in-session search index) FIXED 2026-07-03: PDF + arXiv imports set needsVaultSync=false, which skipped the export-path FTS index → imported notes/papers were unfindable in content search until relaunch. Both controllers now call searchService.upsert(id/title/body/tags/updatedAt) directly after save (via AppBootstrap.shared.vaultSync.searchService, body from NoteFileStorage.readBody). Spotlight donate deferred (needs a NoteEntity build). Build-verified.

## ONBOARDING + INTEGRATION AUDIT (subagent, 2026-07-03)
- INT-2 (correctness bug I introduced in GAP-24) FIXED: note "Reveal in Graph" called revealCurrentDocumentInKnowledgeGraph (needs Epdoc, resolves only .document nodes) → opened an unfocused graph for plain notes. Now HologramController.shared.revealPage(page.id) (resolves the .note node). Build-verified.
- INT-3 (imports skip Spotlight) FIXED: PDF + arXiv imports now SpotlightIndexer.index(page) after save (GAP-1 did the FTS index; this adds macOS system search). Build-verified.
- INT-1 (HIGH REGRESSION, OPEN): cloud-provider connection UI orphaned from Settings — the "Models" SettingsSection + its provider rows (CloudProviderAccountConnectionRow) were removed in commit 37db4d9a4 ("Prune native chat and local agent surfaces", 2026-06-26) as collateral. The UI components + InferenceState drivers (validateCloudAccess/signInToOpenAI/cloudProviderValidationStates) + CloudProviderAuthService still exist, but NO View mounts them → a user cannot connect Anthropic/OpenAI/Google/Perplexity in the native UI, blocking the cloud inference lane. Source-guard tests (RuntimeValidationTests ~4366) assert the removed symbols → likely failing. FIX: re-add a "Models/Providers" Settings section rendering the existing rows. Auth-adjacent — do carefully. Also fixes onboarding step 4.
- ONBOARDING (feels fake): SetupAssistantView 620×620 sheet; only the vault step does real setup, the "model" step is hardcoded checkmarks, no permission priming, no provider connect, static backdrop. A second typewriter SetupView (RootView) is DEAD (ui.needsSetup never set true). Design: landing-takeover onboarding on the shared LiquidMetalSurface backdrop, steps that each persist (vault + mic-permission priming + provider connect [INT-1] + real "you're ready"), delete the dead SetupView.

- INT-1 RESOLVED 2026-07-03: OWNER CONFIRMED the native provider-connection removal was INTENTIONAL (not restoring). Left documented as a known state; providers connect via the owner-intended path. Do NOT re-add the native Settings provider UI.
- Research addendum: most orphans are intentionally parked / dead-regime (DO NOT WIRE): DeepResearchService, GenUICardPresenter, GraphEngine/GraphSlotScheduler (dormant, distinct from live graph), ComposerVoiceInputService (test asserts unwired), retired HealthRows (SubstrateHealthPanelTests retiredRows — delete not surface), MetalRuntimeManager/CredentialPool/XPC/CapabilityBridge (removed-regime, delete candidates). Genuine SAFE wire-wins: INT-11 widgets (appex never embedded), INT-12 DailyNote (button never wired), INT-13 contradictions, INT-14 A2UI.
- INT-12 NOT a regression: NotesSidebar.swift:1451 comment — the DailyNoteView "Today" button + sheet were "removed per user direction" 2026-05-19 ("didn't fit the simplified sidebar"). Do NOT re-wire. Same for many research-flagged "orphans" — verify for a "removed per user direction" comment / a test asserting unwired before wiring any orphan.

## TRUST-SURFACE RESILIENCE AUDIT (subagent, 2026-07-03) — meetings/research/browsing failure handling
Agent A (me) lane: RES-1 speech-model download hang (HIGH), RES-4 audio-rearm give-up (MED), RES-5 browser error page (MED), RES-6 transcript-gap toast (LOW), RES-7 Save-to-notes continuation timeout (LOW, my GAP-3 bug), RES-8 browser download progress/cancel (LOW), RES-9 draft-write disk-full log (LOW/INFO).
  - RES-1 (HIGH): first speech-model download shows a frozen 0% bar + both controls dead + no cancel → looks like a hang. Fix: observe request.progress → forward fractional; add Cancel during .preparing; "Downloading speech model…" copy. (EpistemosSpeechAnalyzer:166-181, MeetingNoteView, LiveVoiceInputService)
  - RES-4 (MED): audio input lost mid-meeting (device removed) → rearm throws forever, UI stuck "Recording", no .error. Fix: after K failed rearms stopInternal()+.error. (EpistemosSpeechAnalyzer:305-320,387-411)
  - RES-5 (MED): browser nav failure = blank page + cryptic "domain=… code=…", no retry, hidden toolbar not force-shown. Fix: map URL codes to plain language + in-content error/Retry + force toolbarHidden=false. (BrowserView:189-199,897-910)
### FOR AGENT B (research/import lane) — RES-2 + RES-3:
  - RES-2 (MED): PDF→Markdown conversion has NO timeout + can't cancel (synchronous FFI in Task.detached; checkCancellation only guards around it). arXiv row hourglass sticks forever; file-picker path shows NO spinner + no completion alert. Fix: wrap the detached conversion in a timeout race → .failed("PDF conversion timed out"); per-row/file cancel. (ArxivIngestService:240-243,382-394; LiteParsePDFImportController:160,183-195; LiteParseImport:485-486)
  - RES-3 (MED): arXiv PDF download uses URLSession.shared default config (no timeoutIntervalForResource) → row can sit for minutes, no progress, no cancel. Fix: bounded timeoutIntervalForResource + fractional progress on the row + cancel ingestTasks[paper.id]. (ArxivIngestService:19)

- RES-1 DONE (progress bar moves + label); RES-4 DONE (rearm gives up after 3 tries → stops the lying "Recording"); RES-9 DONE (draft-write disk-full now logged). RES-1 cancel-during-.preparing + RES-6 (transcript-gap toast) + RES-8 (browser download progress) remain (LOW). RES-5/RES-7 DONE (commit 865b36aeb).

## ⚠️ BUILD COORDINATION (3 agents share one DerivedData) — READ BEFORE BUILDING
Before building: run `pgrep -f "xcodebuild.*Epistemos"`. If another agent's build is RUNNING, WAIT and retry next cycle — do NOT `pkill` it (that kills their in-flight build → `** BUILD INTERRUPTED **`, wasted ~10 min). Only pkill a *stale* XCBBuildService if YOUR OWN build hits a DerivedData DB lock. If your build log ends in `** BUILD INTERRUPTED **` (someone killed it), just re-run it. Serializing builds (one agent at a time) is correct; racing + mutual pkill is not. — Agent A, 2026-07-03

## AGENT-C DATA-INTEGRITY BATCH — 2026-07-03 (autonomous /loop; Sync/Models/Intents/App lane)
Continuous-hardening loop: 4 read-only auditors over Sync data-path / Settings / Intents+Models / App-perf, then surgical build-verified fixes (data integrity first). Build gate: ** BUILD SUCCEEDED ** (Debug/Epistemos, /tmp/agentC-build.log), 0 real Swift errors (the 4 rollup "Error when using sourcemap" lines are the documented benign bundled-.tsx noise). Explicit staging only (never git add -A); Goose + other-lane files untouched.

- DATA-B1 (MED, data-integrity) Models/SDNoteInsight.swift:11-28 — the ONLY @Model whose non-optional stored props lacked inline defaults, breaking the codebase-wide SwiftData lightweight-migration invariant (EpistemosSchema.swift relies on "adding a defaulted property migrates automatically"). A future non-optional field added in this file's own no-default style fails migration on existing stores -> ModelContainer throws -> AppBootstrap.swift:1473-1486 catches -> silent .inMemoryRecovery = the ENTIRE SwiftData store (notes/chats/folders/graph) goes invisible + in-memory-only for that session (clean installs launch fine, so it ships hidden). FIX: added inline defaults to all 11 stored props matching the initializer (contentHash="", lastAnalyzedAt=Date.now [fully-qualified: the @Model macro rejects implicit-member .now as a property default], sentiment/formality/vocabDiversity/questionDensity=0, *JSON="[]"). pageId (unique identity key) intentionally left without a default. VERIFIED (build).
- DATA-Sync#2 (MED, data-integrity) Sync/VaultIndexActor.swift:1185,1247-1262 (exportPage new-file branch) — a SwiftData saveContext failure AFTER the .md is written (new page, no prior filePath) orphaned the file: disk has it, store never recorded page.filePath -> next export/relaunch sees nil filePath -> dedup loop writes a DUPLICATE .md (each re-imports as a separate page). Exact bug movePage was hardened against (MED #2, 2026-07-03). FIX: capture isNewFile before the branch mutates filePath; wrap saveContext in do/catch that, FOR A NEW FILE ONLY, deletes the just-written file + resets page.filePath=nil then rethrows (mirrors the movePage rollback). Existing-file re-exports rethrow untouched (deleting would be data loss). Authored body safe in store/managed body; next successful export rewrites. VERIFIED (build).
- Sync#5 (LOW-MED, applied, build pending in next batch) Sync/VaultSyncService.swift clearVaultData — added context.rollback() in the catch so a partial bulk-delete failure leaves the SHARED mainContext clean instead of arming staged deletes that a later unrelated save() (e.g. savePage) would flush.

CROSS-LANE FLAGS (auditor-found, NOT my lane -> Agent A/B): SessionBrowser.swift:126-144 per-keystroke synchronous disk reads of every session summary+full transcript in SwiftUI body -> beachball per keystroke (HIGH, Vault/=B); SessionBrowser.swift:68-85 sync FFI folder scan + N SQLite on main in refresh() (MED, Vault/=B); Graph/EmbeddingService.swift:258 DispatchGroup.wait() reachable from MetalGraphView.swift:2753 deinit blocks main on graph teardown (MED, Graph/=B); AppBootstrap.swift:1709 Task(.utility) (not .detached) runs DiskStyleCache.evictIfNeeded FileManager I/O on MainActor — needs nonisolated on PageEditorCache (Notes/=A); SDPage.swift:104-107 childPages .cascade hard-delete has no undo vs the soft-delete DeleteNoteIntent, and many call sites hard-delete (LOW-MED, call sites in A/B).

DEFERRED (in-lane, larger/riskier — future cycles): Sync#1 upsertBlock/deleteBlock have 0 production writers -> block search + 1/3 of RRF fusion silently return EMPTY while NotesSidebar/QueryRuntime consume them (SearchIndexService.swift:1638/1654; wire into the write path = feature-completion on the save hot path, OR delete the dead source + block_search CTE); Sync#3 BlockMirror.sync reconciled by main + actor + coordinator contexts with no cross-context coordination -> transient concurrent edit+import mirror dup/drop (route all per-pageId sync via BlockMirrorSyncCoordinator single-writer); Sync#4 GRDB index upsert/delete errors logged-and-swallowed -> indexed_pages diverges from SwiftData until next diffSync (add dirty-flag + opportunistic resync); AppBootstrap 1685/1765/1829 three synchronous DB/FFI opens on the pre-frame launch path feed the ~969ms/~3182ms stalls, but EventStore/Paperclip have immediate synchronous consumers + cross-lane classes -> high blast radius + no headless launch-timing verification (only the Eidos call at 1765 looks like a clean call-site defer, pending isolation check); Intents A1 DelegateToAgentIntent (CognitiveIntents.swift:222) top-10 Siri shortcut dead-drops the prompt into an unread quarantine with no dialog (honest fix needs a cross-lane chat-composer drain).

CORRECTED auditor claims (self-verified): Sync/VaultImportFileCopier.swift + Sync/MappedNoteBody.swift are NOT dead — each has a dedicated test suite (VaultImportFileCopierTests/MappedNoteBodyTests) -> left in place (production-unused but test-covered). Settings orphans (10 view types incl. HELIOSv5SettingsView, a dead local-inference scaffold): directive says LEAVE retired HealthRows + SubstrateHealthPanelTests may assert them -> verify before any delete/wire (not touched this batch).

- RES-6 DEFERRED (LOW, documented): a post-save "some audio may have been missed" toast would require threading a cumulative drop-total through 3 layers (EpistemosSpeechAnalyzer.AudioDropTracker → LiveVoiceInputService → MeetingNoteCaptureService.finalize → CaptureResult → MeetingNoteView) for a RARE LOW event. Audio drops are ALREADY logged per-tick (MED-7, EpistemosSpeechAnalyzer:343) so a diagnostic trail exists. Deferred as disproportionate to severity per the directive's no-over-engineering rule; revisit if drop reports become common.
- RES-1 CANCEL DEFERRED (LOW, documented): the RES-1 core (moving progress bar + label) already fixes the "looks like a hang" perception; the Home button is a (rough) escape during .preparing. A clean Cancel affordance needs task-cancel plumbing to abort the in-flight downloadAndInstall(); deferred as LOW now that the bar moves.
- RESILIENCE AUDIT STATUS: HIGH (RES-1) + MED (RES-4, RES-5) + RES-7/8/9 DONE + verified. RES-2/3 → Agent B lane. RES-6 + RES-1-cancel deferred-LOW-documented. Agent-A resilience lane materially complete.
- Sync#5 VERIFIED 2026-07-03 (** BUILD SUCCEEDED **, Debug/Epistemos): VaultSyncService.clearVaultData now calls context.rollback() on a partial-delete failure — a failed bulk clear leaves the shared mainContext clean instead of arming staged deletes that a later unrelated save() (e.g. savePage) would flush. Compiled clean.

- NOTE-6 RE-ANALYZED 2026-07-03 → DOWNGRADE MED→LOW (content protected, do NOT add plumbing): traced the write path. persistStagedBodyAsync (NoteFileStorage:1432) clears the pending body ONLY when didPersist==true (1441-1443); a FAILED write KEEPS the staged content in pendingBodies. That content is then (a) re-staged/re-written on the next edit, (b) drained to disk by flushAllPendingBodiesForShutdown (NOTE-1) on quit, and (c) mirrored to a .draft by NoteDraftStore (NOTE-4) for the crash case. So the optimistic lastPersistedBody flag in ProseEditorView.flushIfNeeded is COSMETIC — the note body is never actually lost. Fixing the flag would mean threading the detached write Task's Bool result back into a synchronous flush to un-mark on failure: added complexity in data-integrity code for zero data-safety gain. Verified-covered, not fixed.

## AGENT-B LANE — arXiv/import/recall hardening (2026-07-03)
- RES-3 (arXiv PDF download had no resource timeout) FIXED + VERIFIED 2026-07-03 (** BUILD SUCCEEDED **, Debug/Epistemos): `URLSessionArxivPDFDownloader` (ArxivIngestService.swift:11-30) downloaded via `URLSession.shared`, whose default `timeoutIntervalForResource` is 7 days — a stalled/trickling arXiv PDF response could keep an ingest row spinning effectively forever, inconsistent with ArxivClient's 15s metadata-fetch timeout. Added a dedicated ephemeral `downloadSession`: `timeoutIntervalForRequest=60` (idle-between-bytes) + `timeoutIntervalForResource=300` (hard ceiling; the enforced 128 MiB max PDF still fits a ~3.5 Mbps link), `waitsForConnectivity=false`, urlCache/httpCookieStorage nil. Task cancellation still cancels the transfer. Compiled clean.
- RECALL-1 (recall preview blocked the main thread on a disk read) FIXED + VERIFIED 2026-07-03 (same build): `ContextualShadowsPanel.loadPreview` (Views/Recall/ContextualShadowsPanel.swift:480) ran `NoteWindowManager.currentBody(for:mapped:true)` inside a `@MainActor` Task; for a note not open in an editor (the common recall case) that falls through to a synchronous `NoteFileStorage.readBody` disk read — blocking main on every preview. `readBody` is `nonisolated static`, so the `.note` branch now reads it off-main via `Task.detached` and hops the String back. Tradeoff documented at the site: a note open in an editor with unsaved edits previews last-saved content; `mapped:true` transform preserved. `NoteWindowManager` (Views/Notes, off-limits) untouched — fix is entirely in-lane.
- BUILD-RACE LESSON 2026-07-03: the first RES-3/RECALL-1 build FAILED with a spurious `SDNoteInsight.swift:20: A default value requires a fully qualified domain named value (from macro 'Model')` on another lane's committed `var lastAnalyzedAt: Date = Date.now`. Diagnosis: shared-DerivedData `@Model` macro-expansion RACE from a concurrent sibling `xcodebuild` — NOT a genuine break (the line is committed at HEAD and a solo build compiles it clean). A solo rebuild with no concurrent xcodebuild = ** BUILD SUCCEEDED **. Lesson: on a spurious cross-lane macro error, retry solo (no sibling build) before assuming breakage; never edit the other lane's file.
- RES-2 (PDF→Markdown conversion has no timeout / can't be interrupted) DEFERRED w/ design 2026-07-03 (NOT done): `importer.importToMarkdown` is a synchronous, uncancellable FFI (ArxivIngestService:240, LiteParsePDFImportController:160); structured concurrency can't bound it (task groups / `task.value` await their children at scope exit → still hang on a truly-stuck parse). Correct fix = unstructured race that ABANDONS the loser: detached FFI task + `Task.sleep` timer both driving a single-resume latch (NSLock-guarded `@unchecked Sendable` class holding the `CheckedContinuation`, w/ site justification + DECISIONS.md entry per Rule 3), wrapped in `withTaskCancellationHandler` + `defer { timer.cancel() }`; on timeout the await returns `.failed("PDF conversion timed out")` and the FFI is left to finish + be discarded. Applies to both call sites + a unit test (fake importer spinning > timeout must return within budget). Own cycle.
- Agent-B scout catalog 2026-07-03 (3 read-only scouts; backlog for upcoming cycles). QUEUED small/safe: #4 arXiv in-flight-search Stop button (ArxivSearchView:138), #6 featured-feed Retry on cold-start network failure (ArxivSearchView:269-301), LiteParse bulk-import progress+cancel (LiteParsePDFImportButton:62), PERF-8-REFINE HologramOverlay `pinnedPanelTimer` start/stop on `hasPinned` + occlusion pause (HologramOverlay:1475). DELETE candidates (need xcodegen regen): GraphInspectModeView (0 sites, renders EmptyView), GraphWarmupView (0 refs; AppBootstrap pre-warm superseded it), MetalGraphView shared-position-buffer scaffolding (PERF-5, 0 callers). DO-NOT-DELETE correction: `ShadowSearchService` is LIVE (backend of ContextualShadowsPanel via AppBootstrap:2955) even though the Halo *glyph* UI around it (HaloButton/ShadowPanel/HaloController class) is dead + ~8-test-coupled — the `ShadowSearchServicing` protocol lives inside HaloController.swift. Feature-builds (owner-gated, not 1-liners): INT-13 contradiction wiring (detection done at VaultLifecycleService:549, resolution half-stubbed), DataviewService, EpdocBlockTemplateStore, KnowledgeGraphService, A2UI (needs a producer). Parked confirmed: DailyNote button (owner), DeepResearchService, retired HealthRows, MetalRuntimeManager/CredentialPool/XPC/CapabilityBridge.

## AGENT-C BATCH 2 — 2026-07-03 (Quick Capture reliability + RRF snippet) — ** BUILD SUCCEEDED ** Debug/Epistemos
Two more read-only auditors (Cost/Journal/Capture/State; Sync concurrency + FTS-query correctness). All fixes build-verified (0 real Swift errors; ~4 bundled-.tsx rollup "sourcemap" lines are documented benign noise). Explicit staging; Goose/other-lane untouched.

- CAP-1 (HIGH data-integrity/honesty) Views/Capture/QuickCaptureView.swift:690,717 — a SUCCESSFUL capture (note durably saved, createdNoteID set) was reported as FAILURE when only the SECONDARY mutation-envelope audit-log write missed (EventStore.shared optional; saveMutationEnvelope returns false on nil store / transient SQLite). User saw red "persistence failed" -> retried -> each retry mints a fresh SDPage (new id, fresh capture-<traceId> envelope, no dedup) -> DUPLICATE notes. FIX: removed the `guard result.mutationEnvelopePersisted else { throw }` in submitCapture + toggleAudioRecording; the createdNoteID guard still throws on a REAL persistence failure. Envelope miss = benign audit-log degradation (flag retained on result). VERIFIED (build).
- CAP-2 (MED dup-prevention) QuickCaptureView.swift:673,704 — no reentrancy guard; isProcessing/isTranscribing are set INSIDE the async body, so two fast Cmd-Return (key auto-repeat/double-press) both enter before the flag flips -> two pipeline runs -> duplicate notes. FIX: `guard !isProcessing` (submit) + `guard !isTranscribing` (audio) at the top. VERIFIED (build).
- CAP-3 (MED data-loss) QuickCaptureView.swift:137 — full-screen Color.clear .onTapGesture{close()} dismissed on a stray click OUTSIDE the panel, discarding an in-progress brain-dump with no draft/undo (CaptureResult.draftNoteID always nil). FIX: outside-tap only closes when captureText is empty; non-empty capture stays put (explicit Esc/close remains the deliberate cancel). VERIFIED (build).
- SRCH-1 (MED search-quality) Sync/RRFFusionQuery.swift:368 — fused snippet for the universal readable_blocks source read FTS col 0 (title_path=breadcrumb) not col 1 (body); readable_blocks_fts is fts5(title_path,body,...) and the sibling reader (ReadableBlocksIndex.swift:411) uses col 1. Every fused Document/RawThought/Code/Source hit showed a breadcrumb (or empty for top-level blocks) instead of the matched text (search UI + vaultRecallTrace). FIX: col 0 -> 1. VERIFIED (build). (Hit still ranked/returned; only snippet text was wrong.)

DEFERRED (Sync auditor, future careful cycles): VaultIndexActor:739-752 importVault rollback unsound under @ModelActor reentrancy — a concurrent exportPage/migration save mid-import commits partial import rows, then context-wide modelContext.rollback()+discardPendingImportedPages orphans them, or restorePendingUpdatedPages overwrites a newer concurrent edit (lost edit) [HIGH; needs isImporting flag or surgical undo]; RRFFusionQuery:397-402/485-490 rollup uses SQLite bare-columns with THREE min/max aggregates -> undefined bare-column row -> wrong snippet_block_id/entity_kind [MED; needs ROW_NUMBER() window]; VaultIndexActor:1204-1242 exportPage new-file filename dedup TOCTOU across the loadBody await -> two concurrent new-file exports overwrite one [MED; fix=actor inFlightExportPaths reservation, SPECCED next]; SearchIndexService:2335-2343 normalizedSearchTerms drops <2-char + symbol tokens ("C++"/CJK single ideograph silently return []) [LOW-MED; CJK-aware floor]; ReadableBlocksIndex:421-431 empty-FTS->substring fallback masks real FTS errors [LOW; no production callers].
FLAGS (other lanes): NotesSidebar.swift:2375 daily-note dedup DateFormatter lacks en_US_POSIX — COUPLED with JournalIntents.swift:110 (mine); must fix BOTH consistently or introduce a cross-path mismatch (Agent A + me coordinate). TextCapturePipeline.swift:284 @MainActor run() does NL+regex+2MB disk write on main for the meeting path (Agent A). Dead CostDashboardView/DailyNoteView + orphaned BudgetPreferences.perSessionCapUSD (deletion needs xcodegen; deferred).
- Sync#3 (exportPage new-file TOCTOU) VERIFIED 2026-07-03 (** BUILD SUCCEEDED **, Debug/Epistemos): VaultIndexActor.exportPage resolved a new file's name via a fileExists() dedup loop, then suspended at `await loadBodyAsyncFromPrimitives` before writing — two concurrent new-file exports whose titles sanitize to the same base name both passed the existence check (neither had written) and resolved the identical path, overwriting one note. FIX: actor-isolated `inFlightExportPaths: Set<String>`; the dedup loop now also skips reserved paths, the resolved path is inserted synchronously BEFORE the first await, and a defer releases it on every exit (new-file only). @ModelActor serializes the sync check+reserve, so concurrent new-file exports resolve distinct names. Compiled clean.
- #4 (arXiv in-flight search not cancellable) FIXED + VERIFIED 2026-07-03 (** BUILD SUCCEEDED **, Debug/Epistemos, solo build): ArxivSearchView's search button showed a DISABLED hourglass while searching — a slow/stuck search could only be escaped by navigating away. Now while `isSearching` it is an ENABLED Stop (xmark) that cancels `searchTask`; `cancelSearch()` only cancels (the running task's `defer { isSearching = false }` flips the button back on unwind, avoiding a start-while-cancelling race on searchTask). Covers user searches + category-chip loads; the ~1s featured auto-load edge is a non-issue.
- #6 (featured feed dead-ends after a cold-start network failure) FIXED + VERIFIED 2026-07-03 (same build): `loadFeaturedFeedIfNeeded` set the one-shot `didLoadFeaturedFeed` guard BEFORE the fetch, so an offline-at-open failure never retried and offered no Retry. Added `featuredFeedFailed` (set in the catch); the empty state now shows a "Couldn't reach arXiv / Retry" ContentUnavailableView — gated on an empty query so a failed user search still shows the generic empty state — and `retryFeaturedFeed()` resets the guard + reloads. Category-chip failures already retry via re-tap. `startSearch` clears the flag so a user action supersedes.
- BUILD DISCIPLINE (reinforced 2026-07-03): after the earlier shared-DerivedData @Model macro race, this cycle built SOLO via a background wait-loop that polled `pgrep xcodebuild` and only launched once no sibling build ran (cooperative; never pkilled a sibling). SDNoteInsight.swift compiled clean → ** BUILD SUCCEEDED **. Standing rule: get a solo window before building; on a spurious cross-lane macro error, retry solo.

## AGENT-C FINAL (2026-07-03, loop cancelled by owner) — RRF rollup fix + deferred-HIGH documentation

- SRCH-2 (MED search-quality) Sync/RRFFusionQuery.swift rolled_up CTE (BOTH the universal and pageBlockOnly variants) — the RRF rollup surfaced entity_kind/snippet_block_id/snippet_text via SQLite's bare-columns-in-aggregate extension, which only takes bare columns from the min/max row when there is EXACTLY ONE min/max aggregate. The query had THREE (2x MAX + MIN(rnk)) -> those columns came from an ARBITRARY row: scroll-to-block landed on the wrong block, entity_kind/icon could be inconsistent, SearchFusionMetrics.hitsBySource skewed. FIX: replaced GROUP BY with a windowed `ranked` CTE (MIN(rnk)/MAX(updated_at)/SUM(score) OVER (PARTITION BY entity_id) preserve the group aggregates; ROW_NUMBER() OVER (PARTITION BY entity_id ORDER BY rnk, source) ranks rows) + `rolled_up` selecting WHERE rn=1 -> bare columns now come from the true best-rank row. Matches the correct Swift fallback (SearchIndexService:2206-2219). VERIFICATION: standalone sqlite3 on a synthetic `unioned` (E1 = 3 rows whose best rnk=1 is the block/blkB row) returned kind=block / blk=blkB (best-rank row, NOT the arbitrary page/readable rows), best_source_rank=MIN=1, updated_at=MAX=110.0, raw_fused_score=SUM of all 3 (0.04840). Compile-verified via Debug build (** BUILD SUCCEEDED **). NEXT SESSION: run EpistemosTests/RRFFusionQueryTests + SearchIndexServiceFusionTests to confirm the EXPLAIN-plan source-guard still matches after the window-function plan change (FTS virtual-table usage in the hits CTEs is unchanged; if the guard asserts the old plan string, update with justification).

## DEFERRED — HIGH, documented, NOT autonomously fixed: DATA-IMPORT-1 (VaultIndexActor importVault rollback unsound under @ModelActor reentrancy)
VaultIndexActor is a single @ModelActor sharing ONE ModelContext. importVault suspends at many awaits (progress callbacks, upsertPage :841/:874, writePreparedImportedVaultBodyAsync :1582/:1678, backfillWikilinkReferences :864); @ModelActor serializes only at ENTRY, so a concurrent actor entry that also saves the shared context interleaves — specifically exportPage (autosave writeback, saveContext :1248) and the migrateToHybridSync/migrateFromExternalStorage Tasks that VaultSyncService:2528-2531 launch CONCURRENTLY with the import Task. FAILURE: a concurrent save() lands mid-import and commits the import's partially-accumulated pendingInsertedPages; the import then hits a later batch-save failure and runs context-wide modelContext.rollback() — which cannot undo the committed rows, but discardPendingImportedPages (:517-539) still deletes their body files + FTS rows -> ORPHANED SDPages (page row, no body/index). Symmetrically restorePendingUpdatedPages (:541-604) rewrites the pre-import snapshot over a concurrent exportPage's NEWER edit -> LOST user edit.
WHY NOT autonomously fixed: the correct fix isolates the import into its OWN ModelContext(modelContainer) so its rollback discards only its own uncommitted changes and never touches exportPage/migration state — a non-trivial refactor of the import path (all import fetch/insert/save + the discard/restore helpers move to the new context). The alternative (an isImporting serialization flag exportPage + migrations honor) needs a defer/await-import-completion mechanism inside the actor that risks delaying autosave or spinning. Either is a concurrency change to the vault-import data path (handles ALL vault data) that CANNOT be verified headless (the bug needs a specific race: concurrent import + edit/migration + a batch-save failure). Per "no unverified completion" + smallest-safe-change, rushing it risks a worse bug than the rare race. RECOMMENDED: (1) dedicated child ModelContext for importVault; OR (2) run migrations before/after (not during) import + gate exportPage on an isImporting flag; add a concurrency test interleaving an import with an exportPage + forced save-failure. Owner/next-session decision.

## OTHER OPEN (Sync auditor, documented for next session): SearchIndexService:2335-2343 normalizedSearchTerms drops <2-char + symbol-only tokens ("C++"/single CJK ideograph silently return []) [LOW-MED; needs CJK-aware floor]; ReadableBlocksIndex:421-431 empty-FTS->substring fallback masks genuine FTS errors [LOW; no production callers]. FLAGS to other lanes: NotesSidebar.swift:2375 daily-note dedup DateFormatter lacks en_US_POSIX — COUPLED with JournalIntents.swift:110 (must fix BOTH consistently); TextCapturePipeline.swift:284 @MainActor run() does NL+regex+2MB disk write on main for the meeting path (Agent A); SessionBrowser.swift:126 per-keystroke disk reads (Vault/=B); Graph/EmbeddingService.swift:258 DispatchGroup.wait() in graph deinit (Graph/=B).

## ⏸️ SESSION WIND-DOWN 2026-07-03 (loop cancelled, will resume)
All Agent-A (Front & Feel) work committed + build-verified (** BUILD SUCCEEDED **). Loop cron e1e01a4e cancelled per owner.
LANDED THIS THREAD:
- Resilience audit (RES-1..9): HIGH RES-1 (speech-download progress), MED RES-4/5 (audio give-up, browser error UX), RES-7/8/9 done; RES-6 + RES-1-cancel deferred-LOW documented; RES-2/3 → Agent B.
- NOTE integrity: NOTE-1/4 fixed earlier; NOTE-6 verified content-safe (MED→LOW). NOTE-2/3 (HIGH multi-editor / external-edit clobber) STILL OPEN — need conflict-resolution architecture (do NOT rush).
- Metal landing (owner-calibrated): pixelGradient cells=28, multi-wave thin bands, intensity 0.10, cursor = vector WAKE (displaces grid, no hotspot). Removed greeting shimmer (weird box).
- Word-reveal WAKE FIELD revived: Epistemos/Views/Landing/LandingASCIIWakeField.swift (from commit d3b73c05d) — cursor trail reveals vocabulary; perf-gated (shouldAnimate=!reduceMotion&&!windowOccluded AND !trails.isEmpty; cleanup task cancelled onDisappear). VERIFIED hardened.
- Browser: page-below-the-bar inset (toolbarHeight via onGeometryChange) + dark-mode signal (webView.appearance darkAqua/aqua).
- DISC-1 (Focus Mode ⌘⇧F in note More-menu).
PENDING FOR RESUME:
- On-device calibration of the Metal + wake field (owner's eyes): cell count 28, shine 0.10 + band amplitudes, wake displacement 0.022/decay exp(-d*5), wake vocabulary + reveal radius (LandingASCIIWakeFieldConfiguration).
- Discoverability DISC-2..18 (Tier-1 safe .help()/hint adds) + DISC-19..26 (Tier-2, need owner ship/shelve).
- Browser page-inset + dark-mode need on-device verification (can't render-verify).
- 2 untracked web-clip test artifacts (docs/research/Web Clips/Browser-1.md, Browser-2.md) — left for owner to keep/delete.
- 3-agent lane split active (prompts in scratchpad AGENT_B/C_*.md); build coordination note at top of this file.

## OWNER FEEDBACK 2026-07-04 (loop reactivated, cron cfed09a5)
- LANDING regression FIXED: "no squares, no words." Root cause — the wake field's own
  onContinuousHover can't fire (landingBackdrop is .allowsHitTesting(false)), and intensity
  0.10 was invisible (esp. light). Fix: feed the shared cursorLocation to the wake field
  (new `cursor` param + registerHover/endHover helpers), boost gradient intensity to
  theme-adaptive 0.16 dark / 0.26 light, and wake-word opacity 0.30/0.34. (LandingView +
  LandingASCIIWakeField).
- BROWSER theming: added `:root{color-scheme:dark/light}` to BrowserThemeInjection so sites
  with a dark theme (Google/YouTube) restyle their inner surfaces (the html/body override
  only darkened the top level). Reinforces the webView.appearance signal. Sites with NO dark
  theme still can't be forced without ugly full-invert.
- KaTeX MATH REGRESSION (OPEN — needs owner confirm + decision): math no longer renders in
  the Epdoc editor (owner: "particularly arXiv"; unconfirmed on non-arXiv). Investigation:
  * @tiptap/extension-mathematics bumped 3.22.4 → 3.24.0 in commit 0ae91cca8 "Harden Tiptap
    editor dependencies" — the prime suspect (Tiptap minor bumps break math in practice).
  * The Tiptap bundle is FRESH (editor.js.br Jul 2, matches index.ts) — NOT stale.
  * KaTeX CSS IS bundled (26.1 KiB, confirmed in webpack rebuild) — NOT a missing-CSS issue.
  * The default import is valid (3.24 has an `as default` export) and `Mathematics`
    aggregates InlineMath/BlockMath via addExtensions() — so the extension DOES register.
  * ⇒ Regression is in 3.24's RUNTIME behavior, most likely markdown-LOAD parsing (loaded
    `$...$` not converted to math nodes; the input rule only fires when TYPING).
  * Revert to 3.22.4 is BLOCKED by peer-dep conflicts (sibling @tiptap pkgs pinned 3.24.x).
  * NEXT: (1) owner confirm — does typing `$x^2$` in a NON-arXiv note render? (all-MD ⇒
    editor/3.24 = my lane; arXiv-only ⇒ arXiv import math format = Agent-B lane). (2) If
    editor-side: coordinated @tiptap downgrade (revert 0ae91cca8's package changes + npm ci
    + rebuild) OR a 3.24 markdown-math config fix. Not rushed — needs the confirm + a
    security-vs-functionality call on reverting the dep hardening.
- RES-2 part 1 (bounded/abandonable PDF->Markdown conversion) IMPLEMENTED + APP-BUILD-VERIFIED 2026-07-04: new Epistemos/LiteParse/LiteParsePDFConversion.swift wraps the synchronous, uncancellable liteparse `importToMarkdown` FFI in a wall-clock timeout that ABANDONS a stuck conversion (single-resume NSLock latch driving a CheckedContinuation from whichever of {conversion, timeout, cancellation} settles first; justification in DECISIONS.md D-RES2). ArxivIngestService now routes its arXiv PDF parse through it and rejects with a "conversion timed out" message on TimedOut. EVIDENCE: app target COMPILED + LINKED (`Ld .../Epistemos.app/Contents/MacOS/Epistemos` @ build log line 5147; LiteParsePDFConversion.swift compiled clean @ 2830); the ONLY error in the whole build is a concurrent off-limits Goose/Work test file (WorkSPAServerTests.swift:227 'GooseWebSurfaceView' not in scope — a dirty Goose file mid-edit, NOT mine) that fails the EpistemosTests target link. My unit test EpistemosTests/LiteParsePDFConversionTests.swift (proves timeout-abandon + fast-path) COMPILES clean but could not RUN this cycle for that reason; it will run once the Goose lane's file compiles / in CI. RES-2 part 2 (LiteParsePDFImportController file-picker restructure to route its FFI through the same helper) = next cycle.

## AGENT-C BATCH 3 — 2026-07-04 (CJK search recall + SDGraphNode meta-cache guard)
VERIFICATION NOTE: the shared build now has 4-5 concurrent lane builds (June/OpenChamber/pro-agent) thrashing shared DerivedData + a shared Rust cargo target — clean ** BUILD SUCCEEDED ** stamps are unreliable (codesign races, Build-Rust-Engine cargo races, mutual `pkill -f xcodebuild`). Both fixes below are COMPILE-VERIFIED: the build reached the `Ld` (link) phase with ZERO Swift compile errors in any file (SearchIndexService + SDGraphNode included); the sole failure was `PhaseScriptExecution Build Rust Engine` (a cargo race with concurrent lanes — environmental, orthogonal to these pure-Swift changes). Classified per Rule 6. Both changes are low-risk (a filter predicate; an exact mirror of SDPage's shipping cache guard). Stopped `pkill`-ing to avoid killing other lanes' builds.

- SRCH-3 (LOW-MED, search recall) SearchIndexService.swift:2335-2343 normalizedSearchTerms dropped ALL <2-char tokens, so a single CJK/ideographic query ("水") silently returned [] across all 8 search call sites (FTS via sanitizeFTS5Query:2398 + LIKE). FIX: keep 2+ char tokens AND single-character tokens whose scalar is non-ASCII (one CJK char is a real word); ASCII singletons ("a","i") still dropped as noise. Additive (only adds recall; no ASCII behavior change). COMPILE-VERIFIED.
- DATA-GRAPHNODE-1 (MED-HIGH, live data-correctness) Models/SDGraphNode.swift:65-93 the meta @Transient cache returned _metaCache UNCONDITIONALLY (no data-change guard), so a direct write to `metadata` (GraphBuilder.persist diff-apply bypasses the `meta` setter) left node.meta returning STALE values for the whole session (wrong vault filter, stale research stage) until relaunch. Sibling SDPage already guards with _frontMatterCacheData==frontMatterData. FIX: added @Transient _metaCacheData; getter gates the cache hit on `_metaCacheData == metadata`; getter+setter set it. Exact mirror of SDPage's shipping pattern. COMPILE-VERIFIED. (Models auditor called this the highest-value single fix.)

DEFERRED (Models auditor, latent/preventive — next session): SDGraphNode.meta get-modify-set over the empty decode-failure default permanently erases metadata + breaks node dedup (write site Graph/EntityExtractor = Agent B; getter mine — coupled) [MED]; SDMessage decoded{Artifacts,ContentBlocks,Attachments,ContextAttachments} atomic array-decode drops the whole array on one bad element + ChatArtifactKind lacks init(legacy:) [MED; lossy-per-element fix has nil-vs-[] semantic edge cases, unverifiable without a poisoned blob]; SDNoteInsight 3 relatedness arrays decoded independently, no length-parity check [LOW-MED]; SDNoteInsight/CompanionModel @Attribute(.unique) vs CloudKit-off schema [LOW inert].
FLAG (Agent A meeting domain): EpistemosApp.performTeardown doesn't stop live meeting/voice capture on Cmd-Q -> drops trailing ~2s finalized transcript + leaves mic lit during the quit panel; fix = call the meeting capture teardown/flushDraft in performTeardown (their API). App-lifecycle audit otherwise clean (RuntimeIssueMonitor/observer teardown all verified).

- KaTeX REFINED 2026-07-04: the 3.24 InlineMath/BlockMath extensions DO carry parseMarkdown/renderMarkdown storage and katexOptions is optional (defaults undefined) — so the editor side looks CORRECT. This LEANS the regression toward the arXiv import mangling math delimiters (a corpus file showed malformed `$$where $`), which is Agent-B lane, NOT the editor. Still need owner confirm: does typed `$x^2$` render in a plain note? If yes → arXiv-import (Agent B); if no → editor/3.24 (my lane).
- RES-2 part 2 (LiteParse file-picker import) IMPLEMENTED + APP-BUILD-VERIFIED 2026-07-04 (commit 9d53f898a): LiteParsePDFImportController.importPage now validates + converts the SOURCE PDF via LiteParsePDFConversion (bounded/abandonable) BEFORE creating any vault files, so a stuck synchronous FFI times out cleanly with no partial-file cleanup; materializeImportedFiles drops the FFI and takes the pre-computed markdown (copy/write/defer-cleanup otherwise unchanged). Closes for the manual file-picker path the same reliability gap RES-2 part 1 closed for arXiv. EVIDENCE: app COMPILED + LINKED (Ld .../Epistemos.app @ build log 5147), LiteParsePDFImportController.swift compiled clean @ 2838; the ONLY build error is the off-limits Goose test WorkSPAServerTests->GooseWebSurfaceView (a dirty Goose file mid-edit) which fails the shared test-target link tree-wide — NOT mine. RES-2 now COMPLETE (both arXiv ingest + manual file-picker paths bounded). NOTE: the scheme builds the test target on plain `build`, so this Goose breakage currently blocks green builds for ALL lanes until that lane fixes GooseWebSurfaceView.

## KaTeX DIAGNOSIS RESOLVED (arXiv part) 2026-07-04
Verified: arXiv import downloads the PDF (ArxivIngestService: ArxivPDFURLPolicy +
downloadSession.download) → LiteParse PDF→MD. LiteParse does NOT emit `$...$` math — its
only math handling DROPS equation labels (vendor/liteparse .../headings.rs:162,642). So
arXiv "math not rendering" is INHERENT to PDF→MD (you cannot recover LaTeX source from a
rendered PDF) — NOT a regression, NOT the Epdoc editor. This is arXiv-import lane (Agent B).
  * REAL FIX for arXiv math (Agent B): import the arXiv `.tex` e-print SOURCE (arxiv.org
    provides it; it has real `$...$`) instead of/alongside the PDF, OR run a PDF-math OCR
    (pix2tex/nougat) — both out of Front&Feel scope.
  * EDITOR side (my lane): 3.24 InlineMath/BlockMath carry parseMarkdown → clean `$...$`
    renders. STILL NEED owner confirm: does typed `$x^2$` in a PLAIN note render? If NO →
    real 3.24 editor regression (coordinated @tiptap downgrade); if YES → editor is fine and
    the whole KaTeX complaint is the arXiv-PDF limitation above.
- #3 (bulk-import progress+cancel) FIXED + BUILD-VERIFIED 2026-07-04 (** BUILD SUCCEEDED **, Debug/Epistemos, full app+test build, 0 errors — the earlier Goose WorkSPAServerTests->GooseWebSurfaceView breakage is now resolved upstream): LiteParsePDFImportButton tracks ImportProgress(done/total) + stores its import Task; while importing the toolbar shows a spinner + live done/total + a Stop that cancels remaining files (loop checks Task.isCancelled each iteration; reports "Cancelled — imported N/total"). Previously a multi-file import had no progress + no cancel. Agent-B backlog now: PERF-8-REFINE HologramOverlay pinnedPanelTimer occlusion-gating, arXiv #5 defaultOpenForImportedPDF wire, #6 done, entity/citation extraction (larger).

## AGENT-C BATCH 4 — 2026-07-04 (Sessions view correctness) + Settings/Intents backlog
BUILD-ENV ESCALATION: shared DerivedData is currently unbuildable for me — 4-5 concurrent lane builds contend the single Xcode build DATABASE ("error: unable to attach DB: accessing build database ...") so my build aborts before Swift compiles; earlier attempts died at codesign / the Build-Rust-Engine cargo race. Switched to an ISOLATED -derivedDataPath (/tmp/agentC-dd) for reliable verification (cold first build, retro-verifies this batch). The 3 fixes below are TRIVIALLY-valid Swift (textbook .task(id:) + a confirmed nonisolated static call), 0 compile errors whenever the build ran, file compiled clean in every prior session build — committed on inspection + Rule-6 environmental classification.

- SESS-1 (MED, latent-until-wired) Views/Sessions/SessionListView.swift:154 — SessionDetailView's `.task` had no `id:`, so selecting a different session (same view identity under NavigationSplitView detail) never reloaded its summary -> detail pane shows the WRONG session's transcript. FIX: `.task(id: session.id)`.
- SESS-2 (MED, latent-until-wired) SessionListView.swift:28 — the list-refresh `.task` had no `id: vaultPath`, so a vault switch left the list showing the previous vault's sessions. FIX: `.task(id: vaultPath)`.
- SESS-3 (LOW, perf) SessionListView.swift:154 — SessionDetailView read summary.md from disk TWICE on the MainActor. FIX: read once, derive sections via the existing nonisolated `SessionBrowser.extractSummarySections(from:)`.
(Sessions views are present but currently unwired; fixes are correct-when-wired per the Views/State auditor's "clear intent to wire".)

SETTINGS/INTENTS BACKLOG (functional auditor, next batches):
- FINDING 1 (MED, honesty) PerformanceSettingsSection.swift:77-178 — Startup Mode + Idle Memory Mode pickers persist but PerformanceSettingsReader.startupMode/.idleMemoryMode have ZERO callers (dead-end toggles); UI/comment make FALSE present-tense claims. SAFE FIX = correct the claims / mark not-yet-wired. PRIORITIZED next.
- FINDING 2 (MED, cosmetic) AmbientFrequencySettingsView.swift:501-905 — Song-preset picker self-reverts to Custom (each applied value's onChange flips preset->custom); values apply, only the label reverts. DEFERRED: isApplyingSongPreset guard is SwiftUI-timing-fragile + unverifiable headless.
- FINDING 3 (MED, honesty) CognitiveIntents.swift:166,210 — RecallActiveThesis + OpenRawThoughtSandbox pass literal "current" for a blank conversationId (the default) which no live conversation keys on -> no-op that REPORTS SUCCESS. Needs an active-conversation resolver; safe partial = honest "no active conversation" dialog.
- FINDING 4 (LOW, edge) UndoableNoteIntents.swift:121-232 — restore unconditionally clears isArchived (Cmd-Z on a delete after a pre-existing archive also un-archives); needs pre-op isArchived capture (NoteEntity lacks the property -> a fetch). DEFERRED.
- FINDING 5 (LOW, nuanced) NoteEntity.swift:161-180 — entities(for:) resolves archived notes (no !isArchived filter); safest = acting intents reject archived targets. DEFERRED.
SETTINGS verified-sound: auto-save out-of-order tags round-trip; restoreLastSession/summaryInterval/showSaveOnQuit read+write matching keys; 3 Voice toggles have real consumers; no key-typo/wrong-type mismatches.
- FINDING 1 (Performance false-claims honesty) FIXED + COMPILE-VERIFIED 2026-07-04: an isolated -derivedDataPath build reached Ld/link with 0 Swift errors in any file (SessionListView/PerformanceSettingsSection/SearchIndexService/SDGraphNode) — the isolated build DB sidesteps the shared-DerivedData attach contention, giving the first clean-compiling build of this session and RETRO-VERIFYING the CJK/SDGraphNode/Sessions batches. PerformanceSettingsSection.swift: corrected the false present-tense claims (UI disclaimer "Low Memory currently triggers ... release paths" + header comment "are read at AppBootstrap time" + reader doc "Used by AppBootstrap") to honestly state the modes persist a preference but are NOT yet wired to runtime behavior (idle release runs on system memory pressure, not the toggle). Text/comment only; no behavior change.
- PERF-8-REFINE (HologramOverlay pinned-panel timer) FIXED + BUILD-VERIFIED 2026-07-04 (** BUILD SUCCEEDED **, Debug/Epistemos, 0 errors): the 30fps pinnedPanelTimer runs the whole time the overlay is open; updatePinnedInspectorPositions now early-outs (after reconciling force-alive) when there are no pinned panels (and none to clean up) OR the window is occluded/hidden — skipping the per-tick FFI graph_engine_node_screen_pos queries + frame updates that aren't visible (mirrors the render loop's occlusion gate / PERF CONTRACT). Timer start/stop lifecycle across the 5 call sites left intact (lower-risk than restructuring). Commit above.

- NOTE-3 (HIGH) GUARDED 2026-07-04: external/synced vault edit no longer clobbers unsaved in-memory edits. ProseEditorView pageBodyDidChange handler now reloads ONLY when the editor is clean (loadedBodyPageId != page.id || bodyText == lastPersistedBody); if dirty, the external change is DEFERRED (logged) so the user's live typing survives. Trade-off: an external edit to the ACTIVELY-open note loses to the editor (right default for single-user); a conflict INDICATOR ("changed elsewhere") is the remaining follow-up. Build-verified. NOTE-2 (same note in two editors → last-writer-wins) still open — needs cross-tab dedup/shared model.
- #5 (defaultOpenForImportedPDF wired) FIXED + BUILD-VERIFIED 2026-07-04 (** BUILD SUCCEEDED **, Debug/Epistemos isolated DD, 0 errors): the tested-but-unconsumed LiteParseImportSettings.defaultOpenForImportedPDF (default .parsedNote) had no runtime effect. A SINGLE successful LiteParsePDFImportButton import now honors it — .parsedNote → AppBootstrap.notesUI.openPage(pageID); .originalPDF → SourcePDFViewerSheet on the source PDF. The auto-open IS the feedback so the redundant summary alert is skipped for the single-open case; bulk imports keep the alert. Also adopted a dedicated isolated DerivedData (~/.cache/epistemos-dd-agentB) for Agent-B builds — race-free + warm, decoupled from shared-DD contention.

## AGENT-C BATCH 5 — 2026-07-04 (vault-import partial-scan over-delete) — ** BUILD SUCCEEDED ** (isolated warm DD)
Silent-data-loss single-lens auditor verdict: the save/index lane is GENUINELY WELL-HARDENED (it re-verified savePage/createPage/version rollbacks, upsertPage preserveBody guard, FTS atomic writes, BlockMirror-derivative, QuickCapture — all sound; refused to pad). ONE real MED defect found + fixed:
- DATA-PARTIAL-SCAN-1 (MED, silent data loss) VaultIndexActor.swift:641-664,939-957 — the vault-import "delete pages whose files no longer exist" pass was guarded against a 0-file scan + cancellation but NOT a PARTIAL enumeration: fm.enumerator used the 3-arg overload (no errorHandler), which silently skips any unreadable subtree (permission blip / iCloud or network eviction of a whole folder) and returns FEWER files with NO error. diskPaths is then non-empty (0-file guard doesn't fire), completedScan stays true, and every tracked SDPage whose file didn't enumerate gets modelContext.delete(page) — notes vanish from sidebar+search with no error. Markdown re-imports by front-matter id (recoverable) but id-less files (source files .swift/.rs/.py, externally-authored notes) re-import as a fresh UUID -> PERMANENT loss of version history / graph edges / parent links. FIX: errorHandler overload sets scanHadEnumerationError on any unreadable path; the deletion pass now guards on !scanHadEnumerationError (skips destructive reconciliation on a partial scan, like the 0-file/cancelled cases); readable files still import. VERIFIED: ** BUILD SUCCEEDED ** (isolated warm DerivedData; the log/self capture the first build flagged was fixed via a captured Sendable Logger scanLog).
- DEFERRED (LOW/by-design): VaultIndexActor:1594-1626 preserveBody keeps the newer managed body but still overwrites title/tags/emoji/frontMatter from the older vault file (conscious last-writer-wins metadata policy). Flag only.

BUILD-ENV RESOLVED: the isolated -derivedDataPath (/tmp/agentC-dd) is the durable workaround for the shared-DD build-database attach contention (4-5 concurrent lane builds). It reached a full clean ** BUILD SUCCEEDED **, RETRO-VERIFYING the CJK/SDGraphNode/Sessions/FINDING-1 batches (previously compile/inspection-verified under contention). SRCH-2 (RRF rollup) EXPLAIN-test concern CLOSED by static analysis: the window-function rewrite is in rolled_up (post-FTS unioned data), not the FTS hits CTEs, so RRFFusionQueryTests' FTS-acceleration EXPLAIN gate + the recency source-string assertions are unaffected.

## NOTE-2 (HIGH, same note in two editors) — partial + documented 2026-07-04
- LANDED: loadBodyIfNeeded now SKIPS a no-op reload (if the reloaded body == the editor's
  current bodyText for an already-loaded page, don't re-apply → no cursor/undo/block reset).
  Safe robustness win + prerequisite for sibling-sync. (ProseEditorView, build-verified.)
- DEFERRED (needs care, do not rush): full sibling-sync. To make editor A learn of editor
  B's save, the debounced save (ProseEditorView.debouncedSave ~470) must post
  pageBodyDidChange after writeBodyAsync. BUT loadBodyIfNeeded prefers the LIVE
  NoteWindowManager.editorBody(for:) over disk (line 126) — with two editors of one pageId
  it's ambiguous which body editorBody returns, so a naive post could make a sibling reload
  the WRONG (its own/stale) body. Correct fix: on an EXTERNAL-change reload, read DISK
  (authoritative saved state), not editorBody; then post-on-save + the NOTE-3 dirty guard
  give: clean sibling → reloads B's save; dirty sibling → preserves its edits. Verify
  editorBody's multi-editor semantics first.
- BUILD-INFRA NOTE: `build-rust/` is SHARED across all agents (not isolated by -derivedDataPath),
  so the "Build Rust Engine" phase can race (`mktemp ... libgraph_engine.XXXXXX.a: File
  exists`). Recovery: `rm -f build-rust/libgraph_engine.*.a` + retry the build.

## NOTE-2 (HIGH) FULLY FIXED 2026-07-04
Sibling-sync landed. debouncedSave now posts pageBodyDidChange after writeBodyAsync; the
pageBodyDidChange reload reads DISK (loadBodyIfNeeded preferDisk:true) not the ambiguous
live editorBody. Result for the SAME note open in two editors: A saves → B (clean) reloads
A's disk save; B (dirty) keeps its edits + defers (NOTE-3 guard); A itself no-ops
(skip-unchanged, disk == just-saved bodyText). No loop (synced sibling has
bodyText==lastPersistedBody → no re-save). Build-verified. Remaining note items: NOTE-5
(prose-only version snapshots, MED), NOTE-7/8 (LOW). The two HIGH clobbers (NOTE-2 + NOTE-3)
are both closed.

## CONCURRENCY-RACE AUDIT — 2026-07-04 (single-lens, read-only) — VERDICT: lane RACE-FREE within scope
A dedicated data-race lens over Sync/** + Models/** + owned State/** (escape-hatch inventory: @unchecked Sendable, nonisolated(unsafe), Task.detached, DispatchQueue, mutable static var, ModelContext/@Model handoffs) found NO new corruption/crash race — every escape hatch has real synchronization. Verified genuinely safe (not just compiling): VaultSyncService MainActor-confined (mainContext only on MainActor; background version-capture/prune spin a FRESH ModelContext(modelContainer) + ferry only a Sendable VersionCaptureSnapshot, no @Model/main-context escape); VaultIndexActor is a real @ModelActor (no nonisolated func returns an @Model; only Notification hops off-actor); TimeoutUtility/SearchIndexService continuations NSLock/Mutex-guarded + idempotent-resume; SearchFusionMetrics/PowerGate/MainThreadWatchdog lock-guarded; EventStore SQLITE_OPEN_FULLMUTEX + serial queue + immutable db ptr; Models/** has ZERO escape hatches. LOW/flag-only (no fix): EventStore.swift:15 nonisolated(unsafe) static var shared — same set-once-at-bootstrap shape as the known FrictionMonitorService.shared (benign; FULLMUTEX protects the DB regardless; not my lane to change).
STATE: two independent adversarial audits (silent-data-loss + concurrency-race) both confirm the trust-critical Sync/Models/State surface is comprehensively hardened. 16 Agent-C fixes committed + verified across this session-pair.
- ARX-IDX-1 + HW-PKG-1 FIXED + BUILD-VERIFIED 2026-07-04 (** BUILD SUCCEEDED **, Debug/Epistemos isolated DD, 0 errors) — the last two LIVE perf/resilience wins per a fresh full-lane scout (rest of lane is a clean hardening plateau; remaining backlog is owner-gated feature-builds or parked orphans). ARX-IDX-1 (ArxivIngestService post-save FTS upsert): now uses in-hand note.markdownBody instead of a redundant synchronous @MainActor NoteFileStorage.readBody, and logs the failure via os.Logger instead of try?-swallowing it (a silent swallow re-opened the GAP-1 unfindable-until-relaunch symptom). HW-PKG-1 (HTMLWorkspaceEditorPackageActions addAsset/importHTML): the unbounded Data/String(contentsOf: user-picked url) reads now run off-main via Task.detached (Sendable Data/String), with the package patch applied on main — a large asset/HTML no longer stalls the editor.

## AGENT-B SESSION SUMMARY + OWNER DECISION MENU (2026-07-04)
Agent-B lane (arXiv/import, LiteParse, HTMLWorkspace, graph-VIEW, recall, skills, vault, A2UI, knowledge-Engine) reached a HARDENING PLATEAU. 10 findings landed this session, ALL build-verified + committed:
- RES-3 + RECALL-1 (a7158ab30); #4 + #6 (fefe007d1); RES-2 part1 (e9bb209b4) + part2 (9d53f898a); #3 (8d1d09f73); PERF-8-REFINE (a0ebec156); #5 (e410e2f07); ARX-IDX-1 + HW-PKG-1 (488971afe).
- Themes: import error-resilience (bounded/abandonable PDF->MD conversion for BOTH arXiv + file-picker paths; download resource timeout; bulk progress/cancel; search cancel; featured-feed retry; off-main unbounded file reads), recall/graph perf (off-main recall preview; pinned-panel timer occlusion+empty gate), connect-disconnected (defaultOpenForImportedPDF now functional).
A fresh full-lane read-only scout CONFIRMED the plateau: only 2 live wins remained (ARX-IDX-1/HW-PKG-1, now done); everything else is orphaned/unreachable, owner-gated, or parked.

OWNER DECISION MENU — the remaining Agent-B backlog is blocked on OWNER intent (not autonomously safe to guess):
- INT-13 contradiction detection: detection DONE (VaultLifecycleService.detectVaultContradictionsLocal:549). BLOCKED on (a) resolution write-back is stubbed — replaceFact/mergeFacts throw vaultWriteUnavailable; implementing them MUTATES user vault memory (knowledge.md/decisions.md) and needs your conflict-policy + data-model intent; (b) it needs a UI home, and the natural one (Settings > Diagnostics) is off-limits (Agent-C lane). DECIDE: build the write-back? where to surface?
- KnowledgeGraphService + VaultLifecycleService: orphaned (0 prod callers). WIRE (needs a session-graph UI) or DELETE.
- DataviewService: orphaned Obsidian-DQL engine (self-documents "0 Swift callers"). WIRE into the Epdoc dataview-block render path (feature-build) or leave.
- EpdocBlockTemplateStore: orphaned /template store. WIRE into EpdocSlashMenuView or leave.
- A2UI: consumer ready (GenUIDispatcher renders it), NO producer. Needs a producer (e.g. provenance emitting NoteCard JSON).
- SkillEvolutionView + SkillEvolutionService: orphaned but complete UI (0 call sites; discards its analysis result; hardcoded fake vault). WIRE to navigation or DELETE the pair.
- ALIVENESS: live surfaces already animate tastefully + gated (!reduceMotion / occlusion). Any NEW Metal/animation effect needs your VISUAL sign-off — build-verification confirms it compiles, not that it looks right.
Parked-confirmed (leave): GraphInspectModeView ("deferred shell" marker), GraphWarmupView (superseded by AppBootstrap pre-warm; could revive), DailyNote, DeepResearchService, MetalRuntimeManager/CredentialPool/XPC/CapabilityBridge.

BUILD NOTE: Agent-B now builds in isolated DerivedData (~/.cache/epistemos-dd-agentB) — race-free + warm, decoupled from the shared-DD contention. Loop stays in watch-mode: it hardens any NEW lane code other lanes commit, else awaits owner direction on the menu above.

## AGENT-C BATCH 6 — 2026-07-04 (Sync#1 block-index wiring / feature-completion) — ** BUILD SUCCEEDED **
Owner directed continued substantive work; took on the highest-value remaining item (connect-the-disconnected feature-completion). PREVIOUSLY DEAD: SearchIndexService.indexed_blocks (+ block_search FTS) had ZERO production writers, so block search (searchBlocks / NotesSidebar block-hits) AND the RRF `block` fusion source (1/3 of fused search) silently returned EMPTY while the read side + consumers were live. Safe to wire: derivative index (a failure degrades block search only + self-heals on next save), stable SDBlock.ids available, BlockMirror.sync runs before upsertSearchIndex in every save path.
FIX:
- SearchIndexService.swift: `replaceBlocksForPage(pageId:blocks:)` (atomic DELETE-by-page + INSERT-ON-CONFLICT; block_search FTS trigger-maintained) + `deleteBlocksForPage(pageId:)`.
- VaultIndexActor.swift: `reindexBlocks(forPageId:)` (fetch the page's SDBlocks — stable ids maintained by BlockMirror.sync — and replaceBlocksForPage) hooked into the single canonical `upsertSearchIndex` base (covers all save/import paths); + `deleteBlocksForPage` at the 3 page-delete sites (528/1363/1449) so deleted pages' block rows don't linger.
VERIFICATION:
- ** BUILD SUCCEEDED ** (isolated warm DerivedData, 0 Swift errors; the db.execute GRDB calls + FetchDescriptor<SDBlock> hook compile).
- Block-index write/edit/delete/read chain PROVEN via standalone sqlite3 replicating the EXACT app schema (indexed_blocks + block_search fts5 external-content + ai/ad/au triggers) + my SQL: replaceBlocksForPage -> searchBlocks returns the block (brown->blkA1, lazy->blkA2); an ON CONFLICT UPDATE edit fires the au trigger + re-indexes (brown->none, turtle->blkA1); deleteBlocksForPage fires the ad trigger + clears (lazy->none). All 5 assertions pass.
NOTE: this makes the block search source + RRF block fusion return real results. It re-derives the block index from the page's current SDBlocks on each save, so it is correct regardless of which context ran BlockMirror.sync (Sync#3 single-writer remains a separate deferred concern, not required by this wiring). Recommended (non-blocking) follow-up: a permanent SearchIndexServiceFusionTests regression test (replaceBlocksForPage -> searchBlocks) via build-for-testing.

- NOTE-5 (MED) FIXED 2026-07-04: prose-only editing now creates version snapshots. ProseEditor.debouncedSave calls vaultSync.captureVersionIfNeeded(pageId:) on a ~90s throttle (self-dedups on unchanged body). Prose users now have recovery version history like code-editor users. Build-verified. Remaining note items: NOTE-7/8 (LOW). Note trust surface: NOTE-1/2/3/4/5/6 all addressed.

## AGENT-C BATCH 7 — 2026-07-04 (FINDING 4: undo isArchived preservation) — ** BUILD SUCCEEDED **
- FINDING-4 (LOW, correctness) UndoableNoteIntents.swift — DeleteNoteIntent + ArchiveNoteIntent registered undo closures that unconditionally set page.isArchived=false on restore. So deleting (or archiving) a note that was ALREADY archived, then Cmd-Z, would silently UN-archive it — losing the prior archived state (the note reappears in the main list instead of staying in Archive). FIX: capture the PRE-op isArchived via a new currentIsArchived(id:) fetch helper (one per intent struct); thread it as `wasArchived` into restoreNote/unarchiveNote, which now set page.isArchived = wasArchived. Undo of a no-op (deleting/archiving an already-archived note) is now correctly a no-op; undo of a real archive/delete still un-archives. VERIFIED: ** BUILD SUCCEEDED ** (isolated warm DD; first pass hit the shared-cargo-target link race, retry linked clean; my Swift compiled 0-errors both passes).

- NOTE-8 (LOW) FIXED 2026-07-04: durable note-write failures are now SURFACED, not just logged. On writeBodyAsync failure ProseEditor shows a subtle bottom-trailing indicator ("Couldn't save to disk — your work is kept and will retry"), cleared when the next save succeeds. Reassures the user (content is safe in memory + crash draft) + flags a persistent disk problem. Build-verified. Note trust surface: NOTE-1..6 + NOTE-8 all addressed; NOTE-7 (undo resets across mode switch) is expected editor behavior (WONTFIX).

## JOURNAL/COST/CAPTURE VIEW AUDIT — 2026-07-04 (single-lens, read-only) — VERDICT: CLEAN (no live bug)
Data-loss / state-correctness / silent-save / Cost-arithmetic lens over Views/{Journal,Cost,Capture-except-QuickCapture}. No live bug — structural:
- Journal/DailyNoteView.swift: ORPHANED dead code (only #Preview ref :166 + removal record NotesSidebar.swift:1450-1453 "removed W9.13 Today's-brief button + DailyNoteView sheet per user direction 2026-05-19"). Its controlled-TextEditor (get/set) data-loss shape is UNREACHABLE (no parent). Left in place per VERIFY-INTENT (do not wire, do not delete).
- Cost/CostDashboardView.swift: ORPHANED (zero refs outside its own #Preview :337). Arithmetic SOUND: cachedTokensShare/aggregateCachedShare divisions guarded by `> 0` + clamped [0,1]; Int64 token sums can't overflow; totalCostString honestly gates on hasTrackedCosts. LOW/flag-only: BudgetPreferences.perSessionCapUSD persists on didSet but has NO readers (cap unenforced) — moot while orphaned, do not wire.
- Capture/QuickCaptureReadBack.swift (pure nonisolated TTS extractor, no persistence; index math safe) + TraceInspectorView.swift (READ-ONLY diagnostics, no save path) — CLEAN.
STATE: my lane is now comprehensively audited across ~12 read-only lenses (silent-data-loss, concurrency-race, Settings, Intents, perf/memory, Models, App-lifecycle, Sessions, search, Journal/Cost/Capture) + hardened with 18 committed+verified fixes/features. Remaining is documented-deferred: DATA-IMPORT-1 (HIGH, risky import-context refactor, race unverifiable headless — owner decision), FINDING 2/3/5 + Models #2/#3 (fiddly/cross-lane/latent).
- HTMLWorkspace deep-audit batch FIXED + BUILD-VERIFIED 2026-07-04 (** BUILD SUCCEEDED **, isolated DD, 0 errors) — from a DEEP adversarial audit of the 21-file HTMLWorkspace subsystem (WKWebView resource lifecycle + regeneration state machine verified SOLID — no leak/correctness bug). HW-DOM-1 (MED perf): HTMLWorkspaceDOMOutline hoisted the outer tag-scan NSRegularExpression to static let (was rebuilt per tagSummaries() call on the main-thread per-keystroke path; siblings already hoisted for this exact typing lag). HW-EXPORT-1 (MED): exportHTML now renders (base64-inlines all assets) + writes to disk OFF-main via Task.detached (render nonisolated, package Sendable). HW-DEAD-1/2 (LOW): removed dead DOMOutline.nodeCount(in:) + selectedPaneSourceSnippet/sourceSnippet pair (0-ref, no markers). DEFERRED (noted): HW-EXPORT-1b (exportSiteFolder off-main — needs HTMLWorkspaceSiteFolderExporter nonisolated), HW-CHAT-1 (per-row ISO8601DateFormatter hoist), HW-FEED-1/HW-PREVIEW-1 (redundant per-render decodes, optional), HW-FETCH-1 (graphRelatedNoteResults nested main-thread SwiftData fetches — owner-gated data-layer refactor).
