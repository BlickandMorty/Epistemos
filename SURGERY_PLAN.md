# Phase 2 Cloud-Only Surgery Plan (local-model TYPE/ROUTING layer)

## 🏁 COMPLETE + FULLY VERIFIED 2026-07-03 01:11
Cloud-only migration DONE for the app + test targets. Verification at every level:
- **App target: BUILD SUCCEEDED** (0 errors).
- **Test target: TEST BUILD SUCCEEDED** (0 errors — 17 dead test files deleted + ~14 fixed by subagent, + runtime gate added, + AdaptationExecutor deleted).
- **SOURCE GATE passes**: 0 `.localMLX` producers in InferenceState/TriageService.
- **RUNTIME GATE PASSES** (CloudOnlyRuntimeGateTests, 3/3): "configured cloud provider recognized" ✓, "effectiveChatSurfaceSelection never resolves to a local runtime" ✓, "explicit cloud pin resolves to .cloud" ✓. (log noise = Metal/WebContent sandbox warnings, NOT test failures.)
- **KEEP set intact**: FoundationModels/SpeechAnalyzer/NL-embeddings/CloudModelProvider/MCPBridge/arXiv/WebView all present; kept-feature test suites preserved.
- **Artifacts clean**: no stale omega_ax/mlx dylibs; only kept crates ship. **Copy fixed**: no false "local models available" claims.
⇒ Epistemos is genuinely cloud-only for inference; cloud can't silent-fail to no-model. Reader-facing summary: CLOUD_ONLY_REMOVAL_VERIFICATION.md.
Remaining (minor, non-blocking): confirmatory kept-feature test run in flight; optional TriageService dead-code (appleIntelligenceOnlyStream/CloudRoutingError); doc staleness (APP_REVIEW_NOTES.md). DEFERRED (separate): Work/OpenCode lane removal.


Assembled from 5 read-only mapper agents, ALL DONE. **AUTHORITATIVE per-file plan = `scratchpad/map-inferencestate-core-MASTER.txt`** (keystone a46fdca, cross-verified ALL ~18 files incl. the smaller ones — supersedes the others where they conflict). Supporting detail:
- `scratchpad/map-inferencestate-core-MASTER.txt` — **MASTER** — InferenceState + TriageService + LLMService + LocalModelResolution + LocalModelInfrastructure + AppBootstrap + ACC + AnswerPacket + Overseer + Pipeline + NodeInspector + TimeoutUtility + RuntimeExecutor + BackendRuntimeContract + BrandedTypes + EngineTypes + EpistemosFoundationLineup + AdvertisedModelStore + AdaptationExecutor + tests. Exact line ranges + the riskiest-edit verification protocol.
- `scratchpad/map-3files-runtime-pipeline-infra.txt`, `scratchpad/map-appbootstrap.txt` — corroborating detail.

(scratchpad = /private/tmp/claude-501/-Users-jojo/6594a750-07ca-4662-a2b6-cf70567a762e/scratchpad)

## ⚠️ KEYSTONE CORRECTIONS (override earlier assumptions — verified by master map)
- **KEEP `LocalReasoningMode` (InferenceState 2724-2734).** NOT deletable — shared cloud infra (param of cloudOperatingMode, field of InferenceRequestProfile/InferenceRouteDecision, on cloud stream/generate overloads). Optional rename→ReasoningMode only.
- **KEEP `LocalModelSelectionSurface` (2915-2919).** NOT deletable — public param of triage/stream/generateGeneral (.mainChat from PipelineService/CodeEditorView/VaultOrganizerView), drives cloud auto-route contextTier. Optional rename→RequestSurface.
- **D-B REVISED: `LocalInferenceRoutingError` → reuse EXISTING `CloudLLMError` (LLMService:3280)** (already has .modelRequired/.runtimeUnavailable). NOT a new InferenceRoutingError type. Delete-with-func at local/dead throw sites; replace→CloudLLMError at shared/cloud/Apple sites (TriageService 1579/1607, PipelineService 63-64/610, LLMService 343/477, NodeInspector 380 delete-catch).
- **DELETE WHOLE FILES: `State/LocalModelResolution.swift` (62 lines), `Engine/EpistemosFoundationLineup.swift`** (no surviving consumer).
- **RISKIEST EDIT: `InferenceState.effectiveChatSurfaceSelection(for:)` (4714-4839) + `TriageService.decide()` (173-304)** — the two local-vs-cloud brains. Silent-fail: any surviving `.localMLX` PRODUCER → cloud requests die as "no model" (no crash/compile error). VERIFY after: `grep -n 'return \.localMLX' InferenceState.swift` + `grep '\.localMLX' TriageService.swift` show NO producer (only tombstone case LABELS); runtime gate effectiveChatSurfaceSelection(for:.fast)→.cloud with configured provider; build+swift test green.

## CROSS-CUTTING DECISIONS (locked)

**D-A. KEEP `BackendRuntimeKind` at 3 cases (.gguf/.mlx/.remote).** It is a hand-written Swift MIRROR of the UniFFI-generated Rust `RuntimeKind{Gguf,Mlx,Remote}` (epistemos-core/src/runtime_contract.rs:10, udl:466, generated epistemos_core.swift:6465). Dropping cases breaks the FFI bridge `init(_ runtimeKind:)` (BackendRuntimeContract.swift:767-776) at compile time (non-exhaustive over 3-case Rust enum). Local is ALREADY neutralized at policy layer (AppBootstrap:1519-1535: availableRuntimeKinds:[], primaryGenerationRuntimeKind:.remote, allowMLXGenerationFallback:false). A true cut = coordinated Rust+UDL+regen+all-consumers (Branch B) — DEFERRED (DECISIONS.md). Only delete the dangling `init(localReasoningMode:)` (22-29).

**D-B. RENAME `LocalInferenceRoutingError` → `InferenceRoutingError` (provider-neutral), do NOT delete.** Defined TriageService.swift:925 (7 cases). Thrown in ~11 CLOUD/shared sites: TriageService 1345,1579,1607,2069,2204,2227,2230,2276,2284 + LLMService 343,477. TriageService:2204 uses it as generic cloud fallback (`lastCloudError ?? lastLocalError ?? .modelRequired`). Keep cases used by cloud (modelRequired, runtimeUnavailable); audit/drop local-only cases (fastModeUnsupported, modelLoaderUnavailable, modelLoadStalled, insufficientMemory) IF unused after strip.

**D-C. KEEP `ChatModelSelection.localMLX(String)` (InferenceState:2599).** NOT in removal list; constructed ~30 sites + tests. Vestigial `.localMLX` switch arms stay for exhaustiveness (return .unavailable / "local" label). Removing it = massive cascade — DEFERRED.

**D-D. OverseerProtocol = Option A (behavior-preserving).** Replace the local-runtime guard (554-562) with unconditional `return .managedAgentSession`; delete now-unreachable 564-658. Matches CURRENT runtime (local stubs already nil ⇒ always managed-agent). Option B (re-tier cloud) = owner decision, NOT taken.

## PER-FILE EDITS (from 4 done maps; see map files for exact before/after)

**AppBootstrap.swift:** DELETE `syncLocalRuntimeConditions`(2857-2862) + `wireLocalRuntimeLifecycle`(2793-2855, orphaned — retrieval refresh preserved via 2253/2371). STRIP line 2384 + 2395 (`setPreparedLocalTextModelIDs([])`), keep retrieval-config apply. KEEP 2624 preferredChatModelSelection key + 1519/1522-1535 remote control plane. Optional: LocalRuntimeObserverToken(928-931)+tokens(933-934)+teardownRuntimeObservers(3539-3543); localModelDirectories vestigial [URL] group.

**OverseerProtocol.swift:** Option A — 554-658 → `return .managedAgentSession`.

**AnswerPacketEmitter.swift:** STRIP `.localMLX` arm (467-470) → `case .localMLX: return .unavailable`. Keep .cloud/.appleIntelligence.

**AgentCommandCenterState.swift:** DELETE local-brain loop in refreshBrainCatalog (319-331, keep cloud fallback 333-352); foundationRecommendArmed+preferredLocalBrainID (628-658); localBrain (660-677); matches(localModel:) (1311-1316). STRIP recommendedBrain (591-626, per-case drop localBrain; `.todo` 617-618 MUST get cloud fallback: `cloudBrain(...) ?? availableBrains.first`); supportedOperatingModes .local arm (1257-1281, keep bool-based).

**NodeInspectorState.swift:** DELETE local-typed catch (380-383); generic catch (384-389) handles all.

**PipelineService.swift:** DELETE runToolLoop (559-611). STRIP classify (62-64, drop LocalInferenceRoutingError patterns→InferenceRoutingError after rename); run() local-vs-cloud dispatch (239-306, delete local branch 255-286, promote else-body cloud path 287-306); property/param/init localModelClient (148,165,175). KEEP observedToolExecutor(613-799 — source-presence test), generateDirectStream, .localMLX arms.

**LocalModelInfrastructure.swift:** DELETE local-catalog cluster lines 8-139 (LocalModelKind, ModelCapabilityRole, LocalModelDescriptor, GemmaQAT{Stage,Candidate,Ladder}, LocalModelCatalog, extension LocalHardwareCapabilitySnapshot) — has LIVE consumers in InferenceState/TriageService/EpistemosFoundationLineup/AdvertisedModelStore (delete lockstep). DELETE PreparedGenerationRuntimeConfiguration (317-405). STRIP PreparedModelDescriptor (mlxOutputPath 160/178/195, runtimeKind resolver 201-218→delete, resolvedMLXOutputPath 228-230, matchesSidecarModelID 232-241); Snapshot(261-267,276-282) + State(677-683,692-698) generator accessors; Registry Entry.mlxOutputPath(719,736,785). KEEP all PreparedRetrieval* + PreparedModelDescriptor retrieval fields (graph/search).

**RuntimeExecutor.swift:** STRIP RuntimeLane `.mlx`(47)/`.gguf`(48) + stableID(56-57)/displayName(66-67)/knownLanes(79-80). Zero external consumers — safe. KEEP .appleIntelligence/.cloud/.stub.

**TimeoutUtility.swift:** STRIP BreakerDomain `.mlx`(258) + breakerConfig arm(305-313) + BreakerRegistry mlx(642,650,657,665). KEEP cloud/foundationModels/vault.

**BackendRuntimeContract.swift:** DELETE only `init(localReasoningMode:)` (22-29). KEEP enum 3 cases (D-A).

## KEYSTONE — InferenceState.swift: DONE → see MASTER map for exact ranges.
DELETE type blocks: 6-1255 (LocalTextModelID + LocalRuntimeHealthSnapshot + LocalModelUseCase + LocalAgentToolTier + exts), 2703-2722 (LocalRoutingMode — STOP at 2722, KEEP LocalReasoningMode 2724-2734), 2846-2913 (LocalModelInstallStateSummary + LocalRuntimeThermalState + LocalRuntimeConditions — KEEP LocalModelSelectionSurface 2915-2919), 2921-2932 (LocalModelSelection), 2975-3140 (LocalHardwareCapabilitySnapshot). DELETE class fields 3254,3264,3265,3278,3279,3280,3281-3286,3287 (KEEP policyEngine 3288). DELETE local-member block 3809-3886 + 3904-4566 (KEEP-STRIP policyContext 3888-3901). DELETE pure-local methods 5509-5560,5631,5733-5736,5972-6008,4044. KEEP-STRIP shared: policyContext(3888-3901), effectivePolicyContext(5470-5497), effectiveChatSurfaceSelection(4714-4839 RISKIEST), .localMLX-arm collapses (4580-4606,4621-4623,4638-4639,4847-4848,5038-5039,5056-5073), setPreferredChatModelSelection(5759-5783), setActiveAIProvider(5683-5710), persistPreferredChatModelSelection(5241-5249), normalizedChatModelSelection(5863-5872 KEEP verbatim — load-bearing cloud fallback), init(3369-3430). KEEP all Cloud* + ChatModelSelection(.localMLX tombstone arm→const) + EpistemosOperatingMode + LocalReasoningMode + LocalModelSelectionSurface.

## TEST BLAST RADIUS (fix in same change)
- ProductionHardeningTests.swift:849 (wireLocalRuntimeLifecycle presence), :25,88,268,289,461,468 (BreakerDomain.mlx)
- AnswerPacketEmitterTests.swift:265-283 (delete 2 SSM tests; keep 285-314)
- FoundationRecommendPolicyTests.swift (preferredLocalBrainID/foundationRecommendArmed — delete/rework)
- RuntimeValidationTests.swift:1747(keep primaryRetriever),4810,5382-5384(observedToolExecutor presence — KEEP that code),5421,5425
- BackendRuntimeContractTests.swift, AdaptationExecutorTests.swift:16,45 (AdaptationExecutor is local-MLX LoRA — likely delete whole path)
- QueryRuntimeTests.swift:1204, BlockEmbeddingTests.swift:234-706, GraphPhysicsSettingsAuditTests.swift:372 (mlxOutputPath: nil call sites)
- LocalForAllModesAgentRouteGuardTests.swift:35,37,59; CoworkChatModeTests.swift:66; ReasoningLoopAgentEventTests

## ⚙️ EXECUTION PROGRESS (surgery in flight — build RED until sweep complete, expected)
DONE: (1) deleted whole files State/LocalModelResolution.swift + Engine/EpistemosFoundationLineup.swift (consumers confirmed only in being-deleted code). (2) InferenceState.swift type-block deletion via content-markers (robust to map's ~1-line drift): removed LocalTextModelID cluster + LocalRoutingMode + LocalModelInstallStateSummary/Thermal/RuntimeConditions + LocalModelSelection + LocalHardwareCapabilitySnapshot = 1520 lines (6022→4502). KEEP verified intact: CloudModelProvider/CloudTextModelID/LocalReasoningMode/LocalModelSelectionSurface/ChatModelSelection/InferenceState class.
⚠️ **InferenceState line numbers now SHIFTED ~-1520 below old line 3140.** MASTER-map line numbers for the InferenceState CLASS (fields 3254+, local-member block 3809-4566, methods 5509+, effectiveChatSurfaceSelection 4714-4839, decide-adjacent) are STALE — LOCATE BY CONTENT/NAME (grep the member name), verify, then edit. Other files (TriageService/LLMService/etc) unchanged so their map line#s still valid until edited.
PROGRESS 2: InferenceState local FIELDS deleted (routingMode, memoryGateForcedModelIDs, preferredLocalTextModelID, installedLocalTextModelIDs, preparedLocalTextModelIDs, availableLocalGenerationRuntimeKinds, localRuntimeConditions, latestLocalRuntimeHealth, hardwareCapabilitySnapshot). KEPT policyEngine + cloud fields. NOTE: local members are INTERLEAVED with KEEP methods (not the map's contiguous 3809-4566 block) — effectiveChatSurfaceSelection now @3194, policyContext @2368, routeDecision @3979, normalizedChatModelSelection @4343 — so delete per-member by name, not range.
IN-FLIGHT SUBAGENT ad259e0016cc1a874 (background): executing 7 mechanical periphery files (LocalModelInfrastructure, RuntimeExecutor, TimeoutUtility, BackendRuntimeContract init, AnswerPacketEmitter, NodeInspectorState, AdvertisedModelStore) — no-build, precise edits per master map. DO NOT edit those 7; check its report next cycle.
PROGRESS 3: ⚠️RISKIEST EDIT DONE + verified — effectiveChatSurfaceSelection rewritten cloud-only (brace-counted replace); 0 `.localMLX` producers (resolves stale local pin → .appleIntelligence if available else fallbackChatModelSelection() = configured cloud; preserves autoModel .cloud returns + hasConfiguredCloudAccess guards + .cloud pin). usesAutomaticCloudRouteForChatSurfaces .localMLX arm → true. Helpers verified KEEP: normalizedChatModelSelection(.localMLX→.cloud(.openAIGPT54)), preferredAutoRouteCloudModel, fallbackChatModelSelection (all pure-cloud).
PROGRESS 6: AppBootstrap DONE+verified (4 strips + wireLocalRuntimeLifecycle/syncLocalRuntimeConditions deleted + LocalRuntimeObserverToken struct/fields/teardown-loop removed; braces balanced; only remaining "ref" = benign legacy-key string "epistemos.preferredLocalTextModelID" in resetAllData purge — KEEP). ACC DONE+verified (refreshBrainCatalog local loop removed, recommendedBrain→cloud-only, foundationRecommendArmed+preferredLocalBrainID+localBrain+matches(localModel:) deleted, supportedOperatingModes .local arm stripped to bool-based, orphaned nonisolated/doc-comments cleaned; 0 deleted-symbol code refs, braces balanced). NOTE: brace-count func deletion leaves orphaned leading doc-comments/`nonisolated` — grep+clean after.
## POST-BUILD CLEANUP — STALE STRINGS FIXED 2026-07-03 (safe: string/comment-only, app target green, subagent on test target):
✅ InferenceState provider summaries (openAI/anthropic/google/zai/kimi) — removed false "while keeping local models available" clauses. ✅ ACC image-gen ".image" strings (MLX Flux→"Fal image generation") + ".todo" (dropped "LocalAgent-compatible") + isExecutableInCurrentBuild comment (dropped "MLX Flux on-device"). ✅ PipelineService noLLMService + modelNotReady messages (dropped "Work/OpenCode", → "Open Settings → AI to connect a cloud provider").
✅ RUST-ARTIFACT CLEANUP 2026-07-03 (safe, non-app-code): deleted stale build-rust/libomega_ax.dylib (dead — not built/linked anymore); fixed build-epistemos-shadow.sh omega-ax comment; removed dead libomega_ax_mb_max budget + comment from docs/perf-budgets.toml. Remaining omega-ax refs: (a) internal docs (GROUND_TRUTH_SYNTHESIS/ARCHITECTURE_BASELINE/DECISIONS/HARDENING_AUDIT/etc — mostly historical records, low-priority; **APP_REVIEW_NOTES.md worth updating for Apple submission accuracy**), (b) agent_core/src/{falsifier_artifacts,uas,bin} `omega_ax_cargo_test` release-audit evidence-axis string constants — EXCLUDED "Rust core" scope, stale-but-non-breaking, left as-is.
✅ RUNTIME GATE test DRAFTED (scratchpad/CloudOnlyRuntimeGateTests.swift) — ready to `cp` into EpistemosTests/ once test target green. Uses makeInferenceState(TestKeychainStore + CloudModelProvider.openAI.apiKeyKeychainKey pattern from CloudProviderAuthServiceTests). Asserts: configuredCloudProviders.contains(.openAI); effectiveChatSurfaceSelection(mode) never .localMLX ∀ modes; explicit cloud pin → .cloud.
## ✅✅✅ TEST TARGET GREEN 2026-07-03 01:06 — TEST BUILD SUCCEEDED, 0 errors. Subagent a4fb71f5 DONE (370K tok, 174 tools): reconciled full 16-file blast radius — 17 whole-dead test files DELETED (SSM/local-model/Phase4-5 bridges/computer-use/Omega orchestrator-research-hybridrouter-graphmemory/ambient), ~14 files surgically fixed (removed local/computer-use refs, KEPT cloud/kept assertions incl dead-code-guards asserting removed-stays-removed). KEEP-feature test suites verified intact (meetings/arXiv/browser/cloud/editor/graph/vault) — no over-deletion.
POST-SUBAGENT SEQUENCE — EXECUTED: (1) ✅ runtime-gate test deployed (EpistemosTests/CloudOnlyRuntimeGateTests.swift); (2) ✅ deleted AdaptationExecutor.swift + AdaptationExecutorTests (0 app consumers verified — dead local LoRA); (3) final-testbuild.log RUNNING (build-for-testing w/ runtime gate + AdaptationExecutor gone); (4) NEXT: test-without-building → run suite incl runtime gate; (5) report pass/fail.
STILL TODO (post-subagent, needs build): delete AdaptationExecutor.swift (0 consumers verified — but AdaptationExecutorTests handled by subagent; delete file after subagent done to avoid racing that logical unit) + BootstrapPacketBuilder/AgentAuthority review + TriageService dead-code (appleIntelligenceOnlyStream/CloudRoutingError/steeringHintsJSON) + RUNTIME GATE test.

## POST-BUILD CLEANUP FINDINGS (audit 2026-07-03 — stale content compiles but misleads; fix AFTER test-subagent done, don't edit app code during its builds)
- **InferenceState.swift:219,221,223,225** — cloud-provider descriptions (Settings→AI) say "...while keeping local models available" / "local models remain available" — NOW FALSE, user-facing, App-Store/enterprise-trust risk. FIX: strip the stale local-models clause.
- **AgentCommandCenterState.swift:816,899** — image-gen strings "Generate an image on-device via MLX Flux" / "MLX Flux preferred (Fal when explicit)" — MLX Flux is local (removed). Image gen is cloud/Fal now. FIX strings to cloud-only (or verify image path).
- **AdaptationExecutor.swift** — DEAD local-MLX LoRA (line 70 "non-MLX runtime"). Map flagged optional-delete; per no-dead-code → DELETE whole file (verify 0 kept consumers first: rg AdaptationExecutor).
- **PipelineService.swift:23** — error msg "...Use Work/OpenCode or Goose for model-backed chat" — Work/OpenCode being removed; update copy.
- **BootstrapPacketBuilder.swift:227** ("No local model available — cloud inference only") + **AgentAuthority.swift:45,61** ("Download models & assets"/"model weights") — review: former is accurate-ish, latter may be KEEP (agent artifact perm). LOW priority.
- Also (subagent-flagged) TriageService dead-but-compiling: appleIntelligenceOnlyStream, CloudRoutingError enum, steeringHintsJSON param, localLLMService write-only prop.

## KEEP-SET VERIFIED SURGICAL (audit 2026-07-03): removal preserved ALL mandated KEEP surfaces — FoundationModels/AppleIntelligence (18 files), SpeechAnalyzer/Transcriber voice (5), NLEmbedding/NLContextualEmbedding (3), CloudModelProvider (7), MCPBridge cloud-agent tool bus (4), arXiv (10), WKWebView browser/editor (32). AdaptationExecutor confirmed 0 app-code consumers (safe delete). No dead Omega menu/command entry points. ⇒ removal excised ONLY local-model/Omega machinery. Verification triad complete: build green + source gate + KEEP-set intact.

## ✅✅ PROGRESS 12 — APP TARGET GREEN + SOURCE GATE PASS (2026-07-03 00:03)
surgery-build5 = **BUILD SUCCEEDED, 0 errors.** The entire cloud-only surgery COMPILES. Residual iteration: build3(20 InferenceState errs)→build4(1 AppSupervisor.mlx breaker err)→build5 GREEN. AppSupervisor.checkInference rewritten cloud-only (breakers.cloud not .mlx).
SOURCE GATE **PASSES**: 0 `.localMLX` producers in InferenceState (grep `return .localMLX|= .localMLX(`), 0 InferenceRouteKind.localMLX route producers in TriageService, effectiveChatSurfaceSelection 0 localMLX returns; ChatModelSelection.localMLX tombstone case KEPT (exhaustiveness). ⇒ app can NEVER route local; cloud can't silent-fail to "no model".
REMAINING: (1) TEST target reconcile — test-build.log enumerating blast radius (map lists: AnswerPacketEmitterTests[del 2 SSM tests], ProductionHardeningTests[wireLocalRuntimeLifecycle/registry.mlx/breakerConfig(.mlx) asserts], FoundationRecommendPolicyTests[preferredLocalBrainID], RuntimeValidationTests[GemmaQATRuntimeLadder/resolvedRuntimeKind/observedToolExecutor source-presence], BackendRuntimeContractTests, AdaptationExecutorTests, LocalForAllModesAgentRouteGuardTests, CoworkChatModeTests[effectiveLocalAgentTextModelID], MockLLMClient/ReasoningLoopAgentEventTests[LLMSnapshot.reasoningMode], QueryRuntimeTests/BlockEmbeddingTests/GraphPhysicsSettingsAuditTests[mlxOutputPath: nil]). (2) RUNTIME GATE — add+run test: effectiveChatSurfaceSelection(.fast)→.cloud + routeDecision.selectedRoute==.cloud with configured provider. (3) optional cleanup: TriageService dead-code flags (appleIntelligenceOnlyStream/CloudRoutingError/steeringHintsJSON), stale comments.

PROGRESS 11 (residual pass): surgery-build3 FAILED w/ 20 InferenceState errors (all contained). FIXED: (a) STRUCTURAL — batch delete swallowed class-closing `}` + `extension CloudModelProvider {` wrapper, orphaning preferredOrder/fallbackPriority inside class → restored wrapper after anthropicUsesClaudeCodeAccountRuntime. (b) operatingModeCapabilities was WRONGLY deleted (KEEP — availableOperatingModes/supportsThinkingOperatingMode call it) → restored as `baseOperatingModeCapabilities(for: preferredChatModelSelection)`. (c) init strips: purgePersistedLocalModelSelection call + memoryGateForcedModelIDs load. (d) deleted 6 missed local survivors (qwen3UnifiedPickerPairAvailable, memoryGateModelID, localRuntimeFallbackMode/StatusDetail/LastRunSummary, currentLocalRuntimeHealth — all 0 external callers). InferenceState 3516→3433, braces 0. **surgery-build4 RUNNING.** LESSON: batch member-delete can swallow class-close/extension-open boundary lines when the last deleted member abuts an extension — verify structural boundaries after. NEXT: read surgery-build4; iterate residuals to green, then tests + gates.

PROGRESS 10 (CODE-COMPLETE): InferenceState 700-line BATCH DELETE done — 42 pure-local members removed via member-boundary detection (caught brace-count over-extension bug on stored props; member-block approach = [decl−leading-comments, next-decl-start−1]; brace balance 0; 4220→3516 lines). EXCLUDED false-positives KEEP: chatReasoningTier(ChatReasoningTier cloud enum, 7 ext callers), legacyRemoteDefaultsKeys(string array), setChatReasoningTier. Stripped init fastEffortOverride load + AppBootstrap setAvailableLocalGenerationRuntimeKinds call. PipelineService GAP filled (runToolLoop del, run() cloud-promote, streamGeneralLocally block del, classify→CloudLLMError, localModelClient del). OverseerProtocol GAP filled (Option A: selectedRoute→return .managedAgentSession). App-wide deleted-symbol refs 78→2 (both BENIGN legacy-key STRINGS: AppBootstrap:2614 + InferenceState:1653 legacyRemoteDefaultsKeys — KEEP). **surgery-build3.log = DEFINITIVE BUILD RUNNING.** NEXT: read surgery-build3; if green → tests reconcile + source gate(grep no `return .localMLX` producer / no InferenceRouteKind.localMLX producer) + runtime gate(effectiveChatSurfaceSelection(.fast)→.cloud). If errors → fix survivor-callers compiler names.

PROGRESS 9: BOTH SUBAGENTS DONE+verified. TriageService (2529→1437, all local funcs/enums/LocalInferenceRoutingError gone, cloud spine kept; note appleIntelligenceStreamWithFallback rewritten Apple-only, guards removed not swapped — 1 intentional deviation). LLMService (3567→3398, LocalConfigurableLLMClient+ext+localProvider+sharedLocalGenerate deleted, strips→CloudLLMError, LLMSnapshot.reasoningMode removed; localLLMClient prop left write-only). PARENT did PipelineService (the GAP subagent flagged: 1200→1077, deleted runToolLoop + promoted cloud else-branch in run() + deleted shouldForceDirectLocalStream/streamGeneralLocally block + classify→CloudLLMError + localModelClient prop/param/assign; 0 local refs, braces 0). Deleted 14 InferenceState def-only local methods.
**COORDINATED BUILD RUNNING** (scratchpad/surgery-build.log). 78 app-wide deleted-symbol refs remain (mostly InferenceState inter-dependent local methods refing deleted LocalModelCatalog/GemmaQAT/LocalTextModelID: effectiveLocalTextModelID*/sizedFast*/memoryGate*/localModelSelection/sanitizeStoredLocalChatSelectionIfNeeded/localRouteDisplayName/sanitizedInteractiveLocalTextModelID/releaseSelectableInstalled*/normalizedReleaseSelectable* + callers). NEXT: read surgery-build.log errors → delete flagged local methods/strip refs (compiler-guided) → iterate to green → tests → source+runtime gates.
SUBAGENT DEAD-CODE FLAGS (TriageService, compile-clean, clean later): appleIntelligenceOnlyStream, CloudRoutingError enum, shouldRetryWithLocalModel/isRefusalResponse/isTruncatedResponse (tests), steeringHintsJSON, localLLMService write-only.

PROGRESS 8: 2nd subagent a5829cab did TriageService (DONE — 2598→1437 lines, ~1160 removed, 0 local-func defs) + now on LLMService (still has LocalConfigurableLLMClient). Parent proactively deleted 11 def-only (0-caller-verified) InferenceState local members:  (brace balance 0). NOTE: verify caller-count via `rg -c name` (total=1 → def-only safe); autoSubstitute* is a `nonisolated var` not func.
REMAINING: InferenceState local methods WITH callers (effectiveLocalTextModelID*/effectiveLocalAgentTextModelID/hasUsableLocalTextModel/canAutomaticallyRouteToLocalMLX/localModelSelection/sanitizeStoredLocalChatSelectionIfNeeded/localRouteDisplayName/sanitizedInteractiveLocalTextModelID/sizedFast*/memoryGate*/setAvailableLocalGenerationRuntimeKinds[+AppBootstrap:1519 caller]) — BUILD-GUIDED (delete callers→them). Once LLMService done → coordinated build → fix residuals → tests → gates.

PROGRESS 7: InferenceState display-label residuals DONE — deleted activeLocalTextModelID + activeLocalTextModelDisplayName; collapsed .localMLX arms in activeChatModelDisplayName (→"Auto Route" via usesAutomaticCloudRouteForChatSurfaces), baseOperatingModeCapabilities (→[.fast]), isChatSurfaceRuntimeReady (→false), effectiveModelLabel (→"Cloud Model"), chatSurfaceRouteDescription (→cloud text). All exhaustive (ChatModelSelection.localMLX tombstone kept, arms unreachable since effectiveChatSurfaceSelection never produces .localMLX).
REMAINING: 2nd subagent a5829cab (TriageService/LLMService, ~14min) finishing — check next cycle. InferenceState = DELETE pure-local methods (localModelSelection/canRoute*/setRoutingMode/setPreferredLocalTextModelID/setMemoryGateForced/set*Local*/sanitized*/autoSubstitute*/effectiveLocalTextModelID*/effectiveLocalAgentTextModelID/sizedFast*/memoryGate*/localRouteDisplayName/normalizedReleaseSelectable*/releaseSelectableInstalled*/purgePersistedLocalModelSelection?/etc) — BUILD-GUIDED (ref deleted types). THEN coordinated build → fix residuals → tests → source+runtime gates (grep no `return .localMLX` producer in InferenceState; effectiveChatSurfaceSelection(.fast)→.cloud with configured provider).

PROGRESS 5: PERIPHERY SUBAGENT ad259e DONE+verified (7 files: LocalModelInfrastructure 829→534, RuntimeExecutor, TimeoutUtility, BackendRuntimeContract, AnswerPacketEmitter, NodeInspectorState, AdvertisedModelStore — all removed-symbol counts 0, kept anchors present; 2 cosmetic stale comments flagged). TriageService BRAINS DONE (parent): InferencePolicyContext struct→4 fields, InferenceRouteDecision struct (dropped localSelection/reuseWarmModel/selectedLocalModelID), decide() cloud-only (0 local producers, terminal→.cloud), explicitRoute (dropped localSelection param, .localMLX→nil), shouldAutoRouteToCloud (dropped param+conjunct). InferenceState init stripped (hardwareCapabilitySnapshot param+assign, preferredLocalTextModelID, localRuntimeConditions, routingMode load block). SUBAGENT a5829cab RUNNING: TriageService-remaining (InferenceRouteKind/TriageDecision .localMLX, ~22 local funcs, 4 dispatchers, LocalInferenceRoutingError→CloudLLMError) + LLMService (LocalConfigurableLLMClient del + strips). DON'T edit TriageService/LLMService (subagent owns). 
REMAINING (parent): InferenceState display-label .localMLX-arm collapses + delete pure-local methods (BUILD-GUIDED — they ref deleted types); AppBootstrap ref-fixes (wireLocalRuntimeLifecycle+syncLocalRuntimeConditions del, 2 setPreparedLocalTextModelIDs strips, setRoutingMode(.auto)@2638 del); ACC (local-brain per map File 8). THEN coordinated build → fix residuals → tests → source+runtime gates.

PROGRESS 4: CRITICAL InferenceState shared strips DONE — policyContext + effectivePolicyContext → 4 cloud fields (appleIntelligenceAvailable/cloudAutoRouteEnabled/hasConfiguredCloudModels/preferredChatModelSelection); setPreferredChatModelSelection + setActiveAIProvider (routingMode/setRoutingMode blocks removed); persistPreferredChatModelSelection (preferredLocalTextModelID writes removed). All cloud logic + exhaustiveness tombstone arms preserved. The 3 routing-brain edits (effectiveChatSurfaceSelection + 2 policy builders) — where silent cloud-break lives — are complete.
REMAINING (parent, build-guided): InferenceState residuals = display-label .localMLX-arm collapses (effectiveModelLabel/isChatSurfaceRuntimeReady/activeChatModelDisplayName/chatSurfaceRouteDescription/baseOperatingModeCapabilities) + init strip (hardwareCapabilitySnapshot param+assign, routingMode/preferredLocalTextModelID/localRuntimeConditions) + delete pure-local methods (localModelSelection/canRoute*/setRoutingMode/setPreferred*/set*Local*/sanitized*/autoSubstitute*/effectiveLocalTextModelID*/effectiveLocalAgentTextModelID/sizedFast*/etc) — LET COMPILER FLAG THESE (they ref deleted types). TriageService(InferencePolicyContext struct strip to 4 fields + decide() cloud-only + delete local funcs + LocalInferenceRoutingError→CloudLLMError per map throw-table); LLMService(LocalConfigurableLLMClient del + strips + CloudLLMError). AppBootstrap/ACC ref-fixes. THEN build compiler-guided + residuals + tests + gates. Periphery subagent ad259e still finishing 7 files (check next cycle).

## EXECUTION STRATEGY
Coordinated single sweep (build fails until ALL consumers fixed — cannot do piecemeal). Order: (1) rename LocalInferenceRoutingError→InferenceRoutingError globally; (2) delete local-catalog cluster + LocalTextModelID enum + local-only funcs; (3) strip shared-function local branches (keep cloud fallbacks per maps); (4) strip RuntimeLane/BreakerDomain .mlx; (5) delete BackendRuntimeContract init(localReasoningMode:); (6) fix tests; (7) build compiler-guided, iterate. VERIFY cloud inference: after green, confirm resolvedModel(for:)→CloudTextModelID path intact + a cloud triage smoke.
