# HELIOS V5 W25 — Falsifier protocol for PCF-10 Interpretability-to-Runtime Transfer.

id: PCF-10
title: "Interpretability-to-Runtime Transfer"
class: candidate
lane: L5
severity: WARN
state: candidate

statement: |
  Per docs/HELIOS_V5_DOC_6_THEOREM_CANON.md §3 PCF-10.
  See substrate at: epistemos-vault/src/runtime/transfer.rs

acceptance:
  metric: substrate_compiles_and_round_trips
  threshold: 1.0
  units: fraction
  caveat: "Stage-0 substrate-presence proxy; real M2 Max falsifier on Goodfire toy model lands per W25.b"

stage_0_proxy:
  cargo_test_filter: "epistemos_vault::runtime::transfer"
  expected_pass: true

m2_max_protocol:
  hardware: M2 Max
  procedure: |
    1. Substrate-only stage-0: cargo test --lib --features research/vault as appropriate.
    2. M2 Max-specific run lands per W25.b — verifies on Goodfire toy
       model (see PCF-1 falsifier in DOC 6 §3) that the substrate
       actually decomposes / reconstructs / transfers per the claim.
  notes: |
    PCF-N entries are state:candidate per HELIOS V5 v5.2 — they don't
    promote above CANDIDATE until per-falsifier passes on M2 Max OR
    Codex independently verifies the Goodfire May 5 2026 page.

literature:
  - "HELIOS V5 v5.2 PART 5 T28"

insertion_site: "epistemos-vault/src/runtime/transfer.rs"
